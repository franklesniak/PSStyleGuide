<!-- markdownlint-disable-file MD013 -->
<!-- markdownlint-configure-file { "MD024": { "siblings_only": true } } -->

# Chronological PSStyleGuide-first cross-repository convergence plan

## Purpose

Complete the remaining PSStyleGuide and TerraformStyleGuide convergence work in one verified execution order. PS P1, P1A, infrastructure-conformance issue #160, PR #165 / issue #166, and the first pinned Terraform comparison are completed history. The first incomplete action is to create the focused Terraform cycle-1 authoring-conformance repair issue required by the completed comparison. Issue creation does not commence implementation.

This plan contains only numbered atomic leaf tasks. Each task has one executor, one primary action or lifecycle gate, and one objectively testable result. Conditional leaves record `SKIPPED — NO_REPAIR` only when their named incomplete comparison predecessor emits `NO_REPAIR`. A reciprocal repair loop repeats only the explicitly named issue-through-handoff leaf range as a new instance.

No task depends on a shared execution contract, common lifecycle, common prompt, or common-foundation section. Every operative requirement appears in each task that needs it.

## Execution classes

- `Coding agent executable`: A coding agent can complete the task with the stated tools and authority. A task-local human approval remains a required input when stated.
- `Human execution required`: A repository owner or administrator must make or approve the decision. A coding agent can prepare evidence but cannot supply approval.
- `Not executable; tracking/control only`: The item records a control state and cannot itself authorize implementation. No current task uses this class.

## Execution-order map

| Task range | Ordered result |
| --- | --- |
| 1–19 | Create, implement, review, merge, hand off, reverse-compare, and close the cycle-1 Terraform authoring-conformance repair. |
| 20–46 | Implement, review, merge, and reciprocally close PS #161. |
| 47–74 | Implement, review, merge, and reciprocally close PS #162; post the interim handoff. |
| 75–103 | Update, implement, review, merge, and reciprocally close PS #158; close PS #159. |
| 104–131 | Implement, review, merge, and reciprocally close PS #163; close Terraform #31. |
| 132–158 | Implement, review, merge, and reciprocally close Terraform #21. |
| 159–171 | Inventory and close all untracked cross-repository blockers. |
| 172–209 | Prepare, review, decide, prove, land, and reciprocally close P1B/T1B. |
| 210–236 | Implement, review, merge, and close the PS #148 applicability cycle. |
| 237–254 | Implement, review, merge, and close Terraform #23 and its PS applicability result. |
| 255–272 | Prepare and review PS #149, make the fixed-date decision, process any approved fallback, and land P3. |
| 273–300 | Implement, review, merge, and reciprocally close Terraform #24. |
| 301–327 | Implement, review, merge, and close the PS #151 applicability cycle. |
| 328–350 | Check residual triggers, close triggered work, and run later paired cycles when applicable. |
| 351–351 | Publish the final completion audit. |

The only calendar override is Task 260, which runs at `2026-08-21T23:59:59Z`. Its task-local controls define the incomplete-predecessor branch. Task 175 has an `SS` relationship with Task 172; every other declared graph edge is `FS` or a task-local conditional repeat.

## Dependency relationship reference

| Code | Relationship | Meaning |
| --- | --- | --- |
| `FS` | Finish-to-start | The predecessor must finish before the successor starts. |
| `FF` | Finish-to-finish | The successor cannot finish before the predecessor finishes. |
| `SS` | Start-to-start | The successor cannot start before the predecessor starts. |
| `SF` | Start-to-finish | The predecessor cannot finish before the successor starts. |

The graph contains no legitimate `SF` relationship. Former finish-to-finish tracker conditions are represented as separate closure tasks.

## Verified current state

This state was re-queried with the authenticated GitHub connector, paginated REST reads, GraphQL dependency and review-thread reads, and local Git object verification at `2026-08-14T13:28:18Z`. Every state-changing task must re-query its task-local identities before acting.

### Completed history retained as downstream evidence

- PS P1 issue #145 is closed through PR #150 at `3b611fd47a8eb9b24248715be7df97b0f3115e6b`.
- PS P1A issue #146 is closed through PR #153 at `2d56357d9f52c76734027174bf62278e6f3d4cd6`, tree `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- PS infrastructure-conformance PR #164 is merged and issue #160 is closed. The merge commit is `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc`; reviewed head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f` passed its review and independent quality gates before merge.
- PR #164 landed generator blob `f8f011eecaae4d9c4bf8541fcab723621256eb63`, exact-path verifier blob `6c0547cb1d28533943258019c1d9a285cbdfc6d5`, policy-validator blob `24e47314a62bb31cc6332cba6e56116efc7602bd`, build-workflow blob `284b08257a48aacc0fe7e474b315f9eb482e5c62`, policy-contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`, negative-case blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`, and candidate-case blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1`. Its immutable governance source is `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8`.
- PS PR #165 is merged. Its reviewed head is `81926eff7a0692574bbd97c571ce910385344eb0`, reviewed tree `7e8c91d9892239105350ac6e20bda912930350b2`, merge commit `74488bd1b23dcc845449bf0be328202947c3102f`, and landed tree `7e8c91d9892239105350ac6e20bda912930350b2`. It changed only `CLAUDE.md`, landed blob `8d94ca91036ecd4619661896fea137b30293d458`, and used a two-parent merge commit. The recorded Claude loop is current-head clean; the run-specific input identifies the independent final quality check as complete; both head checks and both landed checks succeeded. Landed handoff comment `5289229250` records the merge and validation. Issue #166 closed as completed at `2026-08-14T03:45:24Z` through the PR’s only closing reference.
- The later PR #165 governance blob controls subsequent review loops but does not replace immutable PR #164 as the cycle-1 implementation source.
- Terraform T1 issue #20 is closed through PR #26 at `143f54e52075a1ae1e999a6e242073e3d8d4a46b`. Terraform PR #27 landed at `aae05282b57f093cec8b63e59138db72c982f10e`; PR #30 landed at `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268`.

### Completed cycle-1 comparison

- The completed source Task 8 disposition is `REPAIR_REQUIRED`, target `franklesniak/TerraformStyleGuide`. Its retained decision file SHA-256 is `26aaa6fed33ec59dc5084a7c51c4d28b7bb9a0d410cb4799247024c7a8122db5`.
- `GF-HOSTS` is a blocker: the PS source has 32 functions, 32 complete help records, zero parser errors, and zero PSScriptAnalyzer Warning/Error findings on PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; the Terraform generator and embedded verifier have incomplete help and mapped warnings.
- `GF-GIT` is a blocker: Terraform’s embedded verifier has zero help records, three plural helper nouns, and one assigned-but-unused variable.
- The Terraform generator has eight basic help records but zero examples, zero `.INPUTS`, zero private-helper `.NOTES`, one missing `.DESCRIPTION`, and nine warnings. The embedded exact-path block has five functions, zero help records, and five warnings. One additional mapped `Write-Host` warning makes 15 mapped Terraform warnings.
- Runtime behavior remained green: PS policy `46/46`; Terraform policy `286/286`, expected version `1.0.20260811.0`, run digest `0ad7d187e215c988758483cda2677748d1774c1dcd5d6e5c892d42342daf492c`; reviewed Node `v24.18.1`, npm `11.16.0`, archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0`.
- The required successor is one focused Terraform issue and one focused PR that correct authoring conformance without runtime, serialization, Git-status, atomic-write, credential, graph, or generated-artifact behavior change. It may refresh only coupled validator digests, guard/name identities, expected version, and append-only negative fixtures. The landed repair must be followed by the reverse PS comparison.

### Current open dependency slate

- No issue or PR was created in either repository after the source-plan state. No implementation PR is open in either repository.
- PS #159 is open with child #160 closed and children #161, #162, and #158 open. Native edges remain #160 → #161 → #162. PS #158 remains a nonblocking child.
- PS #147 remains blocked by open administrator issue #152 and closed #146, then blocks #148; the native chain continues #148 → #149 → #151.
- PS #163 remains open and independent. PS #155 and #156 remain open and independent residual items.
- Terraform #31 remains open, has no native dependencies, and contains only pre-landing cycle-1 comment `5269136547`; no cycle-1 comparison, repair issue, or disposition is posted there.
- Terraform #21 is open with only closed blockers #20 and #28; the native chain continues #21 → #22 → #23 → #24 → #25.
- Current PS `main` is `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`. Current Terraform `main` is `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268`.

### Immutable implementation sources

| Role | Commit | Tree or relevant identity |
| --- | --- | --- |
| PS P1 implementation, PR #150 | `3b611fd47a8eb9b24248715be7df97b0f3115e6b` | Landed PS #145 implementation |
| PS P1A implementation, PR #153 | `2d56357d9f52c76734027174bf62278e6f3d4cd6` | `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50` |
| PS infrastructure conformance, PR #164 | `18f577174196627c3c12f8ed8b67336bd17d1009` | `ec41501c2abeddb282df581bb2534177900564cc`; immutable cycle-1 source |
| PS review-protocol update, PR #165 | `74488bd1b23dcc845449bf0be328202947c3102f` | `7e8c91d9892239105350ac6e20bda912930350b2`; governance for future loops only |
| Terraform T1 implementation, PR #26 | `143f54e52075a1ae1e999a6e242073e3d8d4a46b` | Landed Terraform #20 implementation |
| Terraform supply-freeze method, PR #27 | `aae05282b57f093cec8b63e59138db72c982f10e` | Recorder blob `05778c0eda0273a9217f7dc953795c2240473a14`; method blob `36010d2dac98631845d8e880689f7c315ccbcdb7` |
| Terraform comparison baseline, PR #30 | `fbfc3aca874e235cace92f506377f5c9e0704160` | `3c6e54be9d722b8f61aa225b1414f228f7531268` |

## Chronological task slate

## Task 1 — create the focused repair issue for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed cycle-1 comparison | `FS` | The supplied and hash-verified disposition is `REPAIR_REQUIRED` for one focused Terraform authoring-conformance repair. |

### Objective

Create exactly one focused Terraform issue for the validated cycle-1 repair. Do not commence implementation, create a branch, edit repository bytes, or create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Re-query both repositories’ `main` refs, Terraform issue #31, all open Terraform issues and PRs, and all items created or updated since the completed comparison. Paginate every relevant connection.
2. Confirm that no existing issue has the exact validated repair scope. If one now exists, stop and reconcile identity and scope instead of creating a duplicate.
3. Create one focused Terraform issue. Include the problem, completed GF matrix, exact pinned commits/trees/blobs, all affected roles and paths, the required help/naming/analyzer scope, prohibited behavior changes, allowed coupled validator updates, exact validation, security and failure-preservation requirements, real dependencies, trigger/reopen condition, and expected terminal state: one reviewed and independently quality-checked Terraform PR, landed evidence, then reverse PS comparison.
4. Read the new issue, its timeline, and all native dependency connections back with pagination. Correct any false, missing, reversed, or tracker-only dependency only within this issue-creation action. Record its URL as `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]` for output Task 2.
5. Verify that no branch, repository byte, PR, review request, setting, or implementation-commencement record changed.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained Terraform issue with correct native dependencies exists, its URL is recorded, and implementation has not commenced.

### Complete when

One focused, self-contained Terraform issue with correct native dependencies exists, its URL is recorded, and authenticated read-back proves that implementation has not commenced and no other state changed.

## Task 2 — commence the focused repair for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 1 | `FS` | A focused issue exists. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]` from output Task 1, then re-query the issue and confirm that it retains the exact validated scope before recording commencement.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot.

## Task 3 — implement the focused repair for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 2 | `FS` | The focused issue is commenced. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]` from output Task 1 and the commencement record from output Task 2. Re-query both and stop unless the focused issue owns the free implementation slot.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed.

## Task 4 — create or update the focused repair PR for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 3 | `FS` | Use the committed repair candidate. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read the committed candidate and evidence from output Task 3 and `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]` from output Task 1. Re-query both repository refs and the implementation slot before any push or PR write.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks.

## Task 5 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 1 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 4 | `FS` | The focused repair PR is review-ready. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read `[TERRAFORM_CYCLE1_REPAIR_PR_URL]` from output Task 4. Re-query its current head/tree, every feedback surface, all checks, linked issues, dependencies, and review baselines before requesting the first review round.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 4's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 5 for the new head before Task 6. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree.

## Task 6 — run the independent final quality check on the focused repair PR for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 5 | `FS` | A terminal-clean record applies to the current repair head/tree. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read `[TERRAFORM_CYCLE1_REPAIR_PR_URL]` and the terminal review-loop record from output Task 5. Start a fresh coding-agent session and re-query the same current head/tree before the independent audit.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 4's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 5 for the new head, and then repeat Task 6 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record.

## Task 7 — merge the focused repair PR for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 6 | `FS` | Both review gates apply to the same current head/tree. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`, `[TERRAFORM_CYCLE1_REVIEWED_HEAD]`, and `[TERRAFORM_CYCLE1_REVIEWED_TREE]` from output Tasks 5 and 6. Re-query all merge-gate evidence before merge.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded.

## Task 8 — publish the landed repair handoff for cycle 1 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 7 | `FS` | The repair PR is merged with an actual landed identity. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Validated cycle-1 repair authority and boundaries

This task is on the `REPAIR_REQUIRED` branch selected by the completed cycle-1 comparison. The target is only [`franklesniak/TerraformStyleGuide`](https://github.com/franklesniak/TerraformStyleGuide). The task must re-query both `main` refs and all item state before its allowed transition.

Immutable comparison inputs:

| Role | Pinned identity |
| --- | --- |
| PS reviewed source | PR #164 head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS landed source | Merge commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc` |
| PS planning-time `main` context | `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`; do not substitute its later `CLAUDE.md` for PR #164’s immutable source |
| Terraform destination baseline | PR #30 merge and `main` commit `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Reciprocal tracker | Terraform issue #31, open at planning time with only pre-landing cycle-1 comment `5269136547` and no earlier cycle-1 disposition |

Pinned role identities:

| Role | PS PR #164 path — blob / SHA-256 | Terraform baseline path — blob / SHA-256 |
| --- | --- | --- |
| Generator | `.github/workflows/Generate-StyleGuideArtifacts.ps1` — `f8f011eecaae4d9c4bf8541fcab723621256eb63` / `179398728a8b8a04f40201f8fa7a98a09f1f45b3157cbe08607acf659415dd62` | Same path — `e5bdd8f64569541eb3c724387dfdff1847c18793` / `8a7f71b38171ff07003d913d2e55c9945c79fe10c15f651ecb47c28c170ed746` |
| Exact-path verifier | `.github/workflows/Test-ExactGitPathSet.ps1` — `6c0547cb1d28533943258019c1d9a285cbdfc6d5` / `e7b48ba94917c7440a4e084b559a931fcb480e331e33230cff543fbed3f1c46a` | Embedded in `.github/workflows/build.yml` — `f68dfe2da4842d2086b079e09328e3298856123e` / `14a374749edaa88f9db0200933d0c540c6baea630afb3b2cb94f6a6aa8bd85a2` |
| Policy validator | `.github/workflows/Validate-WorkflowPolicy.mjs` — `24e47314a62bb31cc6332cba6e56116efc7602bd` / `6be3ea50b872fdbc4a3b7754c01b681a8cb21dbe846df20925252493015711d2` | Same path — `fb17e06f2f456a0d7df510b0bb34c52d88b7893b` / `4228cc2fe7a43e485168e8e0742216564efe8c273ba45866574ca127991e7bc6` |
| PS policy authority and cases | Contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`; negative cases blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`; candidate cases blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1` | Corresponding authority and 286 fixtures are embedded in the Terraform validator |
| Governance boundary | PR #164 `CLAUDE.md` blob `6075d43c208482a7697a36287c10691fce310fb8` | `CLAUDE.md` blob `c4100bfa36526643d04f6a5571d1ea2dd807b2d3`, SHA-256 `28e77152391d51aed5ba93c59ed79af7f5c516d5ee0f1af2ce13cd4842e26387` |

The completed matrix found blockers only in `GF-HOSTS` and `GF-GIT`. Preserve the recorded `same` or proved `intentional difference` disposition for every other row unless new pinned evidence requires a new finding-specific decision.

| Matrix row | Completed disposition |
| --- | --- |
| `GF-PARAMETERS`, `GF-DESTINATION`, `GF-CONTENT`, `GF-WRITE`, `GF-FAILURE`, `GF-VERSION`, `GF-NODE-LOCK`, `GF-ACTION-INPUTS`, `GF-GRAPH`, `GF-EVIDENCE` | Intentional differences with equal-strength repository-specific evidence |
| `GF-SERIALIZATION`, `GF-YAML`, `GF-ACTION-PINS`, `GF-CREDENTIALS` | Same |
| `GF-HOSTS` | Blocker: incomplete Terraform helper help and mapped PSScriptAnalyzer warnings |
| `GF-GIT` | Blocker: embedded verifier has no help records, three plural helper nouns, and one assigned-but-unused variable |

The focused repair must:

- add complete comment-based help to every applicable private helper in the Terraform generator and embedded exact-path verifier, including synopsis, description, parameter/input/output truth, examples, and private-helper notes as required by the PS authoring standard;
- use approved singular function names and truthful collection- or type-prefixed variable names;
- remove each applicable parser or PSScriptAnalyzer Warning/Error finding, or add a narrowly justified suppression only where the rule does not apply;
- preserve generator output, serialization, Git raw-NUL/native-status semantics, atomic publication, failure truth, credentials, workflow graph, and generated artifacts;
- change only the affected Terraform authoring surface and the necessarily coupled policy-validator digests, guard/name identities, expected version, and append-only negative fixtures; and
- avoid a PS repository-byte change in this destination repair.

Required validation is: PowerShell `7.6.4` and Windows PowerShell `5.1.26100.8972`; zero parser errors and zero applicable PSScriptAnalyzer Warning/Error findings; generator byte-idempotence; embedded Git negative cases; the reviewed Linux Node `v24.18.1` / npm `11.16.0` policy run from archive SHA-256 `d6c664df3f3f61458e8c277585571328522d705166723a7c7823a9253a4d15a0` with all 286 existing fixtures plus any append-only cases; both repository Markdown lint surfaces; `git diff --check`; and successful current-head CI.

Lifecycle placeholders are permitted only after their named predecessor creates them:

- `[TERRAFORM_CYCLE1_REPAIR_ISSUE_URL]`: obtain from output Task 1’s created issue.
- `[TERRAFORM_CYCLE1_REPAIR_PR_URL]`: obtain from output Task 4’s created PR.
- `[TERRAFORM_CYCLE1_REVIEWED_HEAD]` and `[TERRAFORM_CYCLE1_REVIEWED_TREE]`: obtain from the final unchanged head accepted by output Tasks 5 and 6.
- `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and affected blob identities: obtain from output Task 7 after merge and verify again in output Task 8.

### Procedure

1. Read the actual merge output from output Task 7, including `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, merge parents, affected paths, and blobs. Re-query the landed ref and checks before posting the handoff.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit.

## Task 9 — reverse-compare cycle 1 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 8 | `FS` | Use the immutable landed PR #164 / issue #160 source and the verified Terraform landed handoff from Task 8. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 9 | Read both repositories only from the two landed commits. Record all compared blobs. |
| Task 9 | Repeat the same role map and reciprocal matrix in the PS direction. |
| Task 10 | If Terraform added or corrected common behavior, create a focused PS sync-back issue. Pin both landed commits and blobs. |
| Task 11 | Start the PS sync-back only after issue setup and a free implementation slot. |
| Task 13 | If a PS sync-back is required, prepare one focused PR and run all four task-local conditional repair PR lifecycle actions before it merges. |
| Task 18 | If a PS PR lands, compare the new PS commit in Terraform again. For a required Terraform repair, run the same four task-local conditional repair PR lifecycle actions before merge. |
| Task 9 | Stop and run a new decision process if a matrix row changes direction twice. |
| Task 9 | Update PS #159 and Terraform #31 with the permanent cycle 1 record. |
| Task 9 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 9 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 10 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 11 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 13 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 18 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 12 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 19 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 19 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Validated cycle-1 reverse-comparison inputs

This task runs only after the focused Terraform repair handoff exists. Re-query both repositories and every cited item. Compare the immutable PS PR #164 landed source commit `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc`, against `[TERRAFORM_CYCLE1_LANDED_COMMIT]`, `[TERRAFORM_CYCLE1_LANDED_TREE]`, and the final affected Terraform blobs obtained from output Task 8. Do not substitute the later PS PR #165 `CLAUDE.md` for PR #164’s immutable source.

Verify that the focused repair changed only authoring conformance and coupled policy evidence; that runtime, serialization, raw-NUL Git status handling, atomic write, failure truth, credentials, graph, and generated outputs remain equivalent; that both hosts have complete help and zero applicable parser/PSScriptAnalyzer Warning/Error findings; that generator idempotence, embedded Git negative cases, the Node `v24.18.1` / npm `11.16.0` policy suite with 286 or more fixtures, both Markdown lint surfaces, `git diff --check`, and landed CI pass; and that every matrix row is `same` or a fully supported intentional difference. If Terraform added or corrected common behavior that PS lacks, emit `REPAIR_REQUIRED` for PS with exact pinned identities. Otherwise emit `NO_REPAIR`. Post only the comparison evidence record; do not edit issue bodies or dependencies and do not commence implementation.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 10 — create or update the focused repair issue for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 9 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 9's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 11 — commence the focused repair for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 10 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 10 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 12 — implement the focused repair for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 11 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 11 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 13 — create or update the focused repair PR for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 12 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 12 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 14 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 1 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 13 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 13 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 13's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 14 for the new head before Task 15. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 15 — run the independent final quality check on the focused repair PR for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 14 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 14 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 13's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 14 for the new head, and then repeat Task 15 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 16 — merge the focused repair PR for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 15 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 15 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 17 — publish the landed repair handoff for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 16 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 16 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 18 — recheck the reciprocal result for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 9 | `FS` | Use the original pinned comparison and branch result. |
| Task 17 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 10–17 as a new numbered-range instance. After its new handoff, repeat Task 18.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 19 — publish the fixed-point closure for cycle 1 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 18 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 18 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 20 — commence PS generator and exact-path convergence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 19 | `FS` | Cycle 1 has a permanent fixed-point record. |

### Objective

Explicitly commence only the implementation governed by implement PS generator and exact-path convergence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-conformance PS generator blob: `c54012e549aedef827ae3ccb669b512a1f14c644`.
- Pre-conformance PS path-verifier blob: `dc786aed1bd8c9d3bdcb25c6ea79207fc1d63c1b`.
- Terraform generator blob at `fbfc3aca874e235cace92f506377f5c9e0704160`: `e5bdd8f64569541eb3c724387dfdff1847c18793`.
- Terraform build-workflow blob that contains embedded Git/worktree checks: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform policy-validator blob that contains related fixtures: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The landed the completed PR #164 and issue #160 handoff commit is the editing baseline. Replace `18f577174196627c3c12f8ed8b67336bd17d1009` in PS #161 before implementation. Use the older PS blobs only to identify pre-convergence behavior.

### Required generated-destination behavior

1. Use the fixed repository map as the only path authority. Require the exact destination to be index-tracked.
2. Permit only an absent filesystem entry or one ordinary non-link file under the validated repository root.
3. Precompute and validate all complete payload bytes before destination mutation.
4. Create a fresh, unpredictable, same-directory ordinary candidate. Use bounded retries for real collisions.
5. Write all bytes, durably flush, close, reopen, and verify length, digest, BOM, carriage-return, and final-newline rules.
6. Revalidate the parent, destination state, and candidate identity immediately before publication.
7. For an existing destination, call `File.Replace(candidate, destination, null)` once. For an absent destination, call non-overwriting, same-directory `File.Move(candidate, destination)` once.
8. Before publication returns, preserve the old destination or proved absence on failure. Remove only the proved candidate.
9. After publication returns, do not attempt rollback and do not claim cross-file atomicity.
10. Return success only when bounded final evidence proves destination bytes and state. Otherwise, return `ReplacementStateUncertain` with bounded recovery evidence.
11. Preserve fixed order and truthful partial-success records when a later artifact fails.

### Reusable Git/path contract

Keep `Test-ExactGitPathSet.ps1` as the reusable implementation. Add all applicable Terraform invariants for fixed executable identity, a constructed child environment, disabled ambient Git controls, raw NUL framing, hostile-byte handling, duplicate and cardinality rejection, exact native-status classification, staged, working, and untracked surfaces, hooks and configuration control-surface detection, and bounded ordinary worktree inspection. Do not duplicate the normative algorithm in workflow YAML. A thin caller can contain job-specific orchestration only.

### Scope, validation, and handoff

1. Freeze the affected-path set before coding. Assign mere pin or fixture changes in `build.yml` or policy files to Task 49.
2. Complete the full foundation catalog and a file-operation matrix for an existing destination, absent tracked destination, unexpected destination, link/reparse substitution, candidate collision, write/flush/verify failure, publication failure, post-publication uncertainty, cleanup failure, and multi-artifact partial success.
3. Keep the issue read-only for workflow publication. P1B/T1B introduces the first writer.
4. Run fault injection outside the real repository. Test Windows PowerShell 5.1, PowerShell 7 on Windows, and PowerShell 7 on Ubuntu.
5. Prove byte-identical, idempotent output; exact result schemas; truthful failure states; no generated drift; hostile-path handling; script-version progression; and clean staged and working path sets.
6. Create or update one focused PS #161 PR. Run initial local validation and required checks.
7. Record the base, candidate head/tree, source and candidate blobs, versions, SHA-256 values, result schema, fixture identities, runtime matrix, and intentional differences. Stop before merge.

- Verified landed PR #164 generator blob: `f8f011eecaae4d9c4bf8541fcab723621256eb63`.
- Verified landed PR #164 exact-path-verifier blob: `6c0547cb1d28533943258019c1d9a285cbdfc6d5`.
- Verified landed PR #164 commit/tree: `18f577174196627c3c12f8ed8b67336bd17d1009` / `ec41501c2abeddb282df581bb2534177900564cc`.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 21 — implement PS generator and exact-path convergence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 20 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS generator and exact-path convergence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-conformance PS generator blob: `c54012e549aedef827ae3ccb669b512a1f14c644`.
- Pre-conformance PS path-verifier blob: `dc786aed1bd8c9d3bdcb25c6ea79207fc1d63c1b`.
- Terraform generator blob at `fbfc3aca874e235cace92f506377f5c9e0704160`: `e5bdd8f64569541eb3c724387dfdff1847c18793`.
- Terraform build-workflow blob that contains embedded Git/worktree checks: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform policy-validator blob that contains related fixtures: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The landed the completed PR #164 and issue #160 handoff commit is the editing baseline. Replace `18f577174196627c3c12f8ed8b67336bd17d1009` in PS #161 before implementation. Use the older PS blobs only to identify pre-convergence behavior.

### Required generated-destination behavior

1. Use the fixed repository map as the only path authority. Require the exact destination to be index-tracked.
2. Permit only an absent filesystem entry or one ordinary non-link file under the validated repository root.
3. Precompute and validate all complete payload bytes before destination mutation.
4. Create a fresh, unpredictable, same-directory ordinary candidate. Use bounded retries for real collisions.
5. Write all bytes, durably flush, close, reopen, and verify length, digest, BOM, carriage-return, and final-newline rules.
6. Revalidate the parent, destination state, and candidate identity immediately before publication.
7. For an existing destination, call `File.Replace(candidate, destination, null)` once. For an absent destination, call non-overwriting, same-directory `File.Move(candidate, destination)` once.
8. Before publication returns, preserve the old destination or proved absence on failure. Remove only the proved candidate.
9. After publication returns, do not attempt rollback and do not claim cross-file atomicity.
10. Return success only when bounded final evidence proves destination bytes and state. Otherwise, return `ReplacementStateUncertain` with bounded recovery evidence.
11. Preserve fixed order and truthful partial-success records when a later artifact fails.

### Reusable Git/path contract

Keep `Test-ExactGitPathSet.ps1` as the reusable implementation. Add all applicable Terraform invariants for fixed executable identity, a constructed child environment, disabled ambient Git controls, raw NUL framing, hostile-byte handling, duplicate and cardinality rejection, exact native-status classification, staged, working, and untracked surfaces, hooks and configuration control-surface detection, and bounded ordinary worktree inspection. Do not duplicate the normative algorithm in workflow YAML. A thin caller can contain job-specific orchestration only.

### Scope, validation, and handoff

1. Freeze the affected-path set before coding. Assign mere pin or fixture changes in `build.yml` or policy files to Task 49.
2. Complete the full foundation catalog and a file-operation matrix for an existing destination, absent tracked destination, unexpected destination, link/reparse substitution, candidate collision, write/flush/verify failure, publication failure, post-publication uncertainty, cleanup failure, and multi-artifact partial success.
3. Keep the issue read-only for workflow publication. P1B/T1B introduces the first writer.
4. Run fault injection outside the real repository. Test Windows PowerShell 5.1, PowerShell 7 on Windows, and PowerShell 7 on Ubuntu.
5. Prove byte-identical, idempotent output; exact result schemas; truthful failure states; no generated drift; hostile-path handling; script-version progression; and clean staged and working path sets.
6. Create or update one focused PS #161 PR. Run initial local validation and required checks.
7. Record the base, candidate head/tree, source and candidate blobs, versions, SHA-256 values, result schema, fixture identities, runtime matrix, and intentional differences. Stop before merge.

- Verified landed PR #164 generator blob: `f8f011eecaae4d9c4bf8541fcab723621256eb63`.
- Verified landed PR #164 exact-path-verifier blob: `6c0547cb1d28533943258019c1d9a285cbdfc6d5`.
- Verified landed PR #164 commit/tree: `18f577174196627c3c12f8ed8b67336bd17d1009` / `ec41501c2abeddb282df581bb2534177900564cc`.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 22 — create or update the candidate PR for PS generator and exact-path convergence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 21 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS generator and exact-path convergence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-conformance PS generator blob: `c54012e549aedef827ae3ccb669b512a1f14c644`.
- Pre-conformance PS path-verifier blob: `dc786aed1bd8c9d3bdcb25c6ea79207fc1d63c1b`.
- Terraform generator blob at `fbfc3aca874e235cace92f506377f5c9e0704160`: `e5bdd8f64569541eb3c724387dfdff1847c18793`.
- Terraform build-workflow blob that contains embedded Git/worktree checks: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform policy-validator blob that contains related fixtures: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The landed the completed PR #164 and issue #160 handoff commit is the editing baseline. Replace `18f577174196627c3c12f8ed8b67336bd17d1009` in PS #161 before implementation. Use the older PS blobs only to identify pre-convergence behavior.

### Required generated-destination behavior

1. Use the fixed repository map as the only path authority. Require the exact destination to be index-tracked.
2. Permit only an absent filesystem entry or one ordinary non-link file under the validated repository root.
3. Precompute and validate all complete payload bytes before destination mutation.
4. Create a fresh, unpredictable, same-directory ordinary candidate. Use bounded retries for real collisions.
5. Write all bytes, durably flush, close, reopen, and verify length, digest, BOM, carriage-return, and final-newline rules.
6. Revalidate the parent, destination state, and candidate identity immediately before publication.
7. For an existing destination, call `File.Replace(candidate, destination, null)` once. For an absent destination, call non-overwriting, same-directory `File.Move(candidate, destination)` once.
8. Before publication returns, preserve the old destination or proved absence on failure. Remove only the proved candidate.
9. After publication returns, do not attempt rollback and do not claim cross-file atomicity.
10. Return success only when bounded final evidence proves destination bytes and state. Otherwise, return `ReplacementStateUncertain` with bounded recovery evidence.
11. Preserve fixed order and truthful partial-success records when a later artifact fails.

### Reusable Git/path contract

Keep `Test-ExactGitPathSet.ps1` as the reusable implementation. Add all applicable Terraform invariants for fixed executable identity, a constructed child environment, disabled ambient Git controls, raw NUL framing, hostile-byte handling, duplicate and cardinality rejection, exact native-status classification, staged, working, and untracked surfaces, hooks and configuration control-surface detection, and bounded ordinary worktree inspection. Do not duplicate the normative algorithm in workflow YAML. A thin caller can contain job-specific orchestration only.

### Scope, validation, and handoff

1. Freeze the affected-path set before coding. Assign mere pin or fixture changes in `build.yml` or policy files to Task 49.
2. Complete the full foundation catalog and a file-operation matrix for an existing destination, absent tracked destination, unexpected destination, link/reparse substitution, candidate collision, write/flush/verify failure, publication failure, post-publication uncertainty, cleanup failure, and multi-artifact partial success.
3. Keep the issue read-only for workflow publication. P1B/T1B introduces the first writer.
4. Run fault injection outside the real repository. Test Windows PowerShell 5.1, PowerShell 7 on Windows, and PowerShell 7 on Ubuntu.
5. Prove byte-identical, idempotent output; exact result schemas; truthful failure states; no generated drift; hostile-path handling; script-version progression; and clean staged and working path sets.
6. Create or update one focused PS #161 PR. Run initial local validation and required checks.
7. Record the base, candidate head/tree, source and candidate blobs, versions, SHA-256 values, result schema, fixture identities, runtime matrix, and intentional differences. Stop before merge.

- Verified landed PR #164 generator blob: `f8f011eecaae4d9c4bf8541fcab723621256eb63`.
- Verified landed PR #164 exact-path-verifier blob: `6c0547cb1d28533943258019c1d9a285cbdfc6d5`.
- Verified landed PR #164 commit/tree: `18f577174196627c3c12f8ed8b67336bd17d1009` / `ec41501c2abeddb282df581bb2534177900564cc`.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #161 candidate PR exists with complete initial validation and an accurate PR body. It is ready for the Claude Code review loop.

### Complete when

The PS #161 candidate PR exists with complete initial validation and an accurate PR body. It is ready for the Claude Code review loop.

## Task 23 — run the Anthropic Claude Code review loop on the PS #161 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 22 | `FS` | The PS #161 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #161 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #161 PR. Do not merge. If the loop changes bytes, rerun the Task 22 validation and continue the loop on the new head.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree with complete current-head reviewer and deferral-sweep evidence.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree with complete current-head reviewer and deferral-sweep evidence.

## Task 24 — run the independent final quality check on the PS #161 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 23 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #161 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #161 PR. Require full PS #161 issue-requirement coverage, comment and deferral audits, PR-body accuracy, final-diff inspection, validation, checks, and mergeability. Return to Task 23 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 23.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 23.

## Task 25 — merge the PS #161 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 24 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 26 — publish the landed handoff for the PS #161 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 25 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #161 is closed by the landed PR and its permanent handoff is ready for the Terraform comparison.

### Complete when

PS #161 is closed by the landed PR and its permanent handoff is ready for the Terraform comparison.

## Task 27 — compare cycle 2 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 26 | `FS` | Use the final landed PS #161 commit. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 27 | Pin the Task 26 PS commit, tree, generator and verifier blobs, result schemas, and fixtures. |
| Task 27 | Compare the complete generated-destination algorithm and reusable Git/path contract with Terraform. |
| Task 27 | Complete the full foundation catalog and the Task 26 file-operation matrix. |
| Task 27 | If all common behavior matches, post a pinned no-change record. |
| Task 28 | If a common difference exists, create or update one focused Terraform issue before implementation. Then run the four task-local conditional repair PR lifecycle actions. |
| Task 27 | Preserve repository-specific filenames and identity only when the intentional-difference record proves equal security and failure strength. |
| Task 27 | Post the result to PS #161, PS #159, and Terraform #31. |
| Task 27 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 27 | Map files by role, not only by filename. |
| Task 27 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 30 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 28 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 29 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 31 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 27 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 28 — create or update the focused repair issue for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 27 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 27's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 29 — commence the focused repair for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 28 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 28 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 30 — implement the focused repair for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 29 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 31 — create or update the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 30 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 30 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 32 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 31 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 31 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 31's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 32 for the new head before Task 33. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 33 — run the independent final quality check on the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 32 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 32 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 31's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 32 for the new head, and then repeat Task 33 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 34 — merge the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 33 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 33 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 35 — publish the landed repair handoff for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 34 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 34 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 36 — reverse-compare cycle 2 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 35 | `FS` | Use only the final landed commits from Tasks 26 and 43. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 36 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 36 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 37 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 38 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 40 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 45 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 39 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 46 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 46 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 37 — create or update the focused repair issue for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 36 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 36's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 38 — commence the focused repair for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 37 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 37 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 39 — implement the focused repair for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 38 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 38 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 40 — create or update the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 39 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 39 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 41 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 40 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 40 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 40's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 41 for the new head before Task 42. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 42 — run the independent final quality check on the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 41 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 41 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 40's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 41 for the new head, and then repeat Task 42 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 43 — merge the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 42 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 42 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 44 — publish the landed repair handoff for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 43 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 43 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 45 — recheck the reciprocal result for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 36 | `FS` | Use the original pinned comparison and branch result. |
| Task 44 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 37–44 as a new numbered-range instance. After its new handoff, repeat Task 45.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 46 — publish the fixed-point closure for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 45 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 45 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 47 — commence PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 46 | `FS` | Cycle 2 has a permanent fixed-point record. |

### Objective

Explicitly commence only the implementation governed by implement PS workflow isolation and policy convergence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final landed Task 26 commit is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 46 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from the future landed PS #161 handoff before editing; that predecessor identity cannot exist yet.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 48 — implement PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 47 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS workflow isolation and policy convergence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final landed Task 26 commit is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 46 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from the future landed PS #161 handoff before editing; that predecessor identity cannot exist yet.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 49 — create or update the candidate PR for PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 48 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS workflow isolation and policy convergence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final landed Task 26 commit is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 46 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from the future landed PS #161 handoff before editing; that predecessor identity cannot exist yet.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

## Task 50 — run the Anthropic Claude Code review loop on the PS #162 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 49 | `FS` | The PS #162 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #162 PR. Do not merge. If the loop changes bytes, rerun the Task 49 validation and continue on the new head.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 51 — run the independent final quality check on the PS #162 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 50 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #162 PR. Require full issue-contract, workflow-topology, policy-case, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 50 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 50.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 50.

## Task 52 — merge the PS #162 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 51 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 53 — publish the landed handoff for the PS #162 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 52 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #162 is closed and the final landed policy contract is ready for Task 62.

### Complete when

PS #162 is closed and the final landed policy contract is ready for Task 62.

## Task 54 — compare cycle 3 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 53 | `FS` | Use the final landed PS #162 commit and policy contract. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 54 | Pin all Task 53 workflow, policy, contract, case, and validation blobs. |
| Task 54 | Complete the full foundation catalog and workflow-topology table in Terraform. |
| Task 54 | Apply all blockers from Task 53's required topology and policy. |
| Task 58 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 54 | Update PS #162, PS #159, and Terraform #31 with full identities and validation. |
| Task 54 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 54 | Map files by role, not only by filename. |
| Task 54 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 57 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 55 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 56 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 58 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 54 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 55 — create or update the focused repair issue for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 54 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 54's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 56 — commence the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 55 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 55 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 57 — implement the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 56 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 56 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 58 — create or update the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 57 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 57 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 59 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 58 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 58 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 58's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 59 for the new head before Task 60. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 60 — run the independent final quality check on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 59 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 59 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 58's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 59 for the new head, and then repeat Task 60 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 61 — merge the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 60 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 60 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 62 — publish the landed repair handoff for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 61 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 61 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 63 — reverse-compare cycle 3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 62 | `FS` | Use only the final landed commits from Tasks 53 and 70. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 63 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 63 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 64 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 65 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 67 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 72 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 66 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 73 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 73 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 64 — create or update the focused repair issue for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 63 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 63's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 65 — commence the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 64 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 64 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 66 — implement the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 65 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 65 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 67 — create or update the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 66 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 66 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 68 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 67 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 67 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 67's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 68 for the new head before Task 69. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 69 — run the independent final quality check on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 68 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 68 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 67's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 68 for the new head, and then repeat Task 69 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 70 — merge the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 69 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 69 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 71 — publish the landed repair handoff for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 70 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 70 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 72 — recheck the reciprocal result for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 63 | `FS` | Use the original pinned comparison and branch result. |
| Task 71 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 64–71 as a new numbered-range instance. After its new handoff, repeat Task 72.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 73 — publish the fixed-point closure for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 72 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 72 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 74 — post the initial three-cycle handoff

> **Execution class: Coding agent executable.** This is an evidence-publication task, not an implementation task.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 73 | `FS` | Cycles 1 through 3 have permanent fixed-point records. |

### Objective

Complete only this leaf action: post the initial three-cycle handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Post one interim handoff to PS #159 and Terraform #31. List cycles 1 through 3, every PS and Terraform issue and PR, bases, reviewed heads/trees, merge methods, landed commits/trees, source and destination blobs, matrices, validation, review outcomes, intentional differences, and fixed-point result. State that PS #158 remains nonblocking and has no native blocking edge to PS #147 or #152. Verify every identifier before posting. Do not close either tracker.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

### Complete when

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

## Task 75 — update PS #158 with the landed workflow-policy starting point

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 74 | `FS` | The final cycle 3 policy contract and interim handoff exist. |

### Objective

Update only the existing PS #158 issue record with the final landed workflow-policy starting point. Do not commence implementation.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #158, its parent #159, the landed workflow-policy PR and commit, and both repository main refs.
2. Replace `<PS_WORKFLOW_POLICY_PR_URL>` and `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` in the task-local text below with the exact predecessor outputs.
3. Post the text as a planning comment without replacing the issue body. Read the comment back and verify both identities and the nonblocking scope.
4. Do not create a feature branch, edit implementation files, open a PR, or occupy the implementation slot.

### Exact issue update

Before implementation, replace both placeholders and post this planning comment to PS #158 without replacing its body:

> This issue is a child of [PS issue #159](https://github.com/franklesniak/PSStyleGuide/issues/159) and starts after `<PS_WORKFLOW_POLICY_PR_URL>` lands. Re-read the final landed `workflow-policy-contract.json` from `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` before adapting the recorder. Preserve this issue's existing nonblocking scope and immutable Terraform PR #27 source pins.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #158 contains the verified starting-point comment, and implementation has not started.

### Complete when

PS #158 contains the verified starting-point comment, and implementation has not started.

## Task 76 — commence the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 75 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement the PS read-only supply-freeze recorder. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 77 — implement the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 76 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS read-only supply-freeze recorder without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-read the final Task 73 policy contract and the immutable Terraform PR #27 inputs.
2. Preserve the issue's existing nonblocking scope.
3. Keep the recorder read-only and capture its output outside the repository.
4. Prove that it does not mutate a manifest, lockfile, installed tree, contract, generated output, staged set, or worktree.
5. Complete a documented reduced reciprocal matrix for the narrow recorder surface. Include every applicable foundation row and prove why omitted rows are inapplicable.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 78 — create or update the candidate PR for the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 77 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS read-only supply-freeze recorder; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #158 PR. Run initial validation and required checks.
2. Record the candidate head/tree, all source and candidate blobs, tool/runtime identities, exact output location, mutation-absence evidence, validation, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

## Task 79 — run the Anthropic Claude Code review loop on the PS #158 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 78 | `FS` | The PS #158 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #158 PR. Do not merge. If the loop changes bytes, rerun the Task 78 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 80 — run the independent final quality check on the PS #158 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 79 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #158 issue coverage, immutable-source verification, read-only and mutation-absence proof, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 79 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 79.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 79.

## Task 81 — merge the PS #158 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 80 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 82 — publish the landed handoff for the PS #158 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 81 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #158 is closed and its permanent handoff is ready for Terraform.

### Complete when

PS #158 is closed and its permanent handoff is ready for Terraform.

## Task 83 — compare cycle 4 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 82 | `FS` | Use the final landed PS #158 recorder and handoff. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 83 | Pin the PS source and landed commits and blobs and the immutable Terraform PR #27 inputs. |
| Task 83 | Compare the read-only recorder roles with a documented reduced matrix. |
| Task 86 | Prove that both implementations preserve repository state and capture output outside the repository. |
| Task 87 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 83 | Update PS #158, PS #159, and Terraform #31 with all identities, validation, and differences. |
| Task 83 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 83 | Map files by role, not only by filename. |
| Task 83 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 86 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 84 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 85 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 87 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 83 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 84 — create or update the focused repair issue for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 83 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 83's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 85 — commence the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 84 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 84 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 86 — implement the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 85 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 85 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 87 — create or update the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 86 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 86 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 88 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 87 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 87 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 87's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 88 for the new head before Task 89. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 89 — run the independent final quality check on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 88 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 88 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 87's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 88 for the new head, and then repeat Task 89 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 90 — merge the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 89 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 89 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 91 — publish the landed repair handoff for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 90 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 90 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 92 — reverse-compare cycle 4 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 91 | `FS` | Use only the final landed commits from Tasks 82 and 99. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 92 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 92 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 93 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 94 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 96 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 101 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 95 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 102 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 102 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 93 — create or update the focused repair issue for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 92 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 92's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 94 — commence the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 93 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 93 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 95 — implement the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 94 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 94 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 96 — create or update the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 95 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 95 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 97 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 96 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 96 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 96's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 97 for the new head before Task 98. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 98 — run the independent final quality check on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 97 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 97 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 96's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 97 for the new head, and then repeat Task 98 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 99 — merge the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 98 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 98 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 100 — publish the landed repair handoff for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 99 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 99 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 101 — recheck the reciprocal result for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 92 | `FS` | Use the original pinned comparison and branch result. |
| Task 100 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 93–100 as a new numbered-range instance. After its new handoff, repeat Task 101.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 102 — publish the fixed-point closure for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 101 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 101 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 103 — close the PS foundation umbrella

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 102 | `FS` | PS #160, #161, #162, and #158 and all four reciprocal cycles are complete. |

### Objective

Complete only this leaf action: close the PS foundation umbrella. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #159 and its four native children.
2. Verify that #160, #161, #162, and #158 are closed and that every cycle has a permanent fixed-point record.
3. Post the foundation handoff. Include every issue and PR URL, base, reviewed head/tree, merge method, landed commit/tree, affected path and blob, complete or reduced matrix, validation command and result, runtime identity, review outcome, intentional difference, and reciprocal closure result.
4. Verify that Terraform #31 links the same four cycle records.
5. Close PS #159. Do not create or merge an aggregate foundation PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #159 is closed with complete, matching evidence in both repositories.

### Complete when

PS #159 is closed with complete, matching evidence in both repositories.

## Task 104 — commence the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 103 | `FS` | The foundation umbrella is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement the PS Claude review-loop command. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 105 — implement the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 104 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS Claude review-loop command without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Reconfirm the existing owner authorization because the new file is an agent entry point.
2. Add a thin, repository-local command wrapper.
3. Accept a pull-request URL. Require the URL when the caller does not supply it.
4. Delegate the authoritative process to the repository-local root `CLAUDE.md`.
5. Treat Codex and Copilot as co-equal reviewers, as `CLAUDE.md` requires.
6. Do not duplicate a step count, gate count, round limit, or other volatile protocol detail.
7. Do not fetch instructions from a moving external branch or add a shared runtime dependency.
8. Record a line-by-line comparison with the pinned Terraform input. Explain each intentional difference.
9. Complete a documented reduced reciprocal matrix. Run Markdown lint and all applicable local agent-instruction checks.
10. Keep the issue and PR separate from foundation, supply-freeze, and infrastructure-conformance work.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 106 — create or update the candidate PR for the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 105 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS Claude review-loop command; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #163 PR. Record the candidate head/tree, pinned input, line comparison, validation, and initial checks. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

## Task 107 — run the Anthropic Claude Code review loop on the PS #163 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 106 | `FS` | The PS #163 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #163 PR. Do not merge. If the loop changes bytes, rerun the Task 106 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 108 — run the independent final quality check on the PS #163 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 107 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #163 issue coverage, protected-entry-point authorization, pinned-input comparison, absence of volatile protocol duplication, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 107 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 107.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 107.

## Task 109 — merge the PS #163 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 108 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 110 — publish the landed handoff for the PS #163 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 109 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

### Complete when

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

## Task 111 — compare cycle 5 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 110 | `FS` | Use the final landed PS #163 command. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 111 | Pin the PS command and authoritative `CLAUDE.md` blobs and the immutable Terraform command blob. |
| Task 111 | Compare both thin wrappers line by line with a documented reduced matrix. |
| Task 111 | Preserve each repository's authoritative local instruction file and repository identity. |
| Task 115 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 111 | Update PS #163 and Terraform #31 with all identities, validation, and intentional differences. |
| Task 111 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 111 | Map files by role, not only by filename. |
| Task 111 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 114 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 112 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 113 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 115 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 111 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 112 — create or update the focused repair issue for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 111 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 111's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 113 — commence the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 112 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 112 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 114 — implement the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 113 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 113 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 115 — create or update the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 114 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 114 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 116 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 115 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 115 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 115's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 116 for the new head before Task 117. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 117 — run the independent final quality check on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 116 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 116 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 115's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 116 for the new head, and then repeat Task 117 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 118 — merge the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 117 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 117 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 119 — publish the landed repair handoff for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 118 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 118 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 120 — reverse-compare cycle 5 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 119 | `FS` | Use only the final landed commits from Tasks 110 and 127. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 120 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 120 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 121 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 122 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 124 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 129 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 123 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 130 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 130 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 121 — create or update the focused repair issue for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 120 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 120's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 122 — commence the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 121 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 121 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 123 — implement the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 122 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 122 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 124 — create or update the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 123 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 123 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 125 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 124 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 124 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 124's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 125 for the new head before Task 126. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 126 — run the independent final quality check on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 125 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 125 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 124's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 125 for the new head, and then repeat Task 126 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 127 — merge the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 126 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 126 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 128 — publish the landed repair handoff for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 127 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 127 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 129 — recheck the reciprocal result for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 120 | `FS` | Use the original pinned comparison and branch result. |
| Task 128 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 121–128 as a new numbered-range instance. After its new handoff, repeat Task 129.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 130 — publish the fixed-point closure for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 129 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 129 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 131 — close the Terraform initial-convergence tracker

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 130 | `FS` | All five initial reciprocal cycles have permanent closure records. |

### Objective

Complete only this leaf action: close the Terraform initial-convergence tracker. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Tracker contract

Terraform #31 retains evidence; it does not authorize an aggregate implementation PR. Its five ordered records are:

1. PS infrastructure conformance from Terraform PR #30.
2. PS generator and path convergence from Terraform PRs #26 and #30.
3. PS workflow and policy convergence from Terraform PRs #26 and #30.
4. PS #158 supply-freeze adaptation from Terraform PR #27.
5. PS Claude-command parity from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`.

### Procedure

1. Re-query Terraform #31.
2. Verify that each cycle record contains the PS issue and landed PR; source and destination commits and blobs; a complete foundation matrix or justified reduced matrix; every focused Terraform sync issue and PR; the reverse PS comparison; validation and intentional differences; and the permanent fixed-point result.
3. Reconcile the tracker against PS #159 and PS #163. Correct a record only with verified evidence.
4. Close Terraform #31. Do not create or merge an aggregate foundation implementation PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

### Complete when

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

## Task 132 — commence Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 131 | `FS` | The five initial cycles are closed and the final Terraform baseline is known. |

Terraform #21 exists, but its creation did not start implementation. Do not start Terraform #22 or #24 in this task.

### Objective

Explicitly commence only the implementation governed by implement Terraform #21 from the landed PS P1A source. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 131 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Verify the five Task 131 closure records and the free implementation slot.
2. Explicitly commence only Terraform #21.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 133 — implement Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 132 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #21 from the landed PS P1A source without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 131 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Re-query Terraform #21. Record its four source blobs, four destination paths, source and destination identities, and expected repository-specific differences.
2. Classify the capability as common.
3. Adapt the landed PS implementation. Do not redesign it independently or introduce a shared dependency.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 134 — create or update the candidate PR for Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 133 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #21 from the landed PS P1A source; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 131 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Create or update one focused Terraform #21 PR. Run initial local validation and required checks.
2. Record its base, candidate head/tree, affected paths and destination blobs, validation, runtime identities, and issue links. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

### Complete when

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

## Task 135 — run the Anthropic Claude Code review loop on the Terraform #21 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 134 | `FS` | The Terraform #21 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide against the Terraform #21 PR. Read that repository's current root `CLAUDE.md`. Do not merge. If the loop changes bytes, rerun the Task 134 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 136 — run the independent final quality check on the Terraform #21 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 135 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require all Terraform #21 requirements, four-path scope, 115/141-row allocation, comments, deferrals, PR-body accuracy, validation, checks, and mergeability. Return to Task 135 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 135.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 135.

## Task 137 — merge the Terraform #21 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 136 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 138 — publish the landed handoff for the Terraform #21 implementation

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 137 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

### Complete when

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

## Task 139 — reverse-compare Terraform #21 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 138 | `FS` | Use the landed Terraform #21 result and immutable PS P1A commit. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 139 | Replace every placeholder in the reverse-comparison record with a verified identity. |
| Task 139 | Read PS only from `2d56357d9f52c76734027174bf62278e6f3d4cd6` and Terraform only from the Task 138 landed commit. Record every compared blob and map files by role. |
| Task 139 | Complete every applicable foundation row and the exact four-file mapping. Preserve the stated 115-row PS and 141-row Terraform allocations. |
| Task 139 | If no common difference remains, make no PS change and post a pinned reverse-comparison record. |
| Task 140 | If Terraform added or corrected common behavior, create or update one focused PS sync-back issue. Pin both landed commits and blobs. |
| Task 141 | Start the sync-back only after issue setup and a free implementation slot. Prepare one focused PS PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 139 | If PS changes, compare its new landed commit in Terraform. For a required Terraform repair, run the same four task-local conditional repair PR lifecycle actions. |
| Task 139 | Stop for a new decision process if a row changes direction twice. |
| Task 139 | Post the current reciprocal evidence on Terraform #21 and the PS P1A handoff. Do not close Terraform #21 until Task 158 confirms the final Terraform state. |
| Task 139 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 139 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 140 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 141 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 143 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 139 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 142 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 139 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 139 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 140 — create or update the focused repair issue for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 139 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 139's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 141 — commence the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 140 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 140 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 142 — implement the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 141 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 141 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 143 — create or update the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 142 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 142 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 144 — run the Anthropic Claude Code review loop on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 143 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 143 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 143's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 144 for the new head before Task 145. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 145 — run the independent final quality check on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 144 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 144 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 143's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 144 for the new head, and then repeat Task 145 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 146 — merge the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 145 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 145 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 147 — publish the landed repair handoff for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 146 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 146 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 148 — perform the final Terraform recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 147 | `FS` | Use the final landed PS state from Task 147. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 148 | Compare the final Task 147 PS landed commit against Terraform. |
| Task 149 | If a common difference remains, create a focused Terraform issue before implementation. Use the task-local execution controls, conditional repair PR lifecycle, and final reciprocal-recheck controls below until the row is `same` or a proved intentional difference. |
| Task 148 | Verify that every common row is `same`, every intentional difference has complete evidence, and no row is a blocker. |
| Task 158 | Post one permanent closure record on Terraform #21 and the PS P1A handoff. Include both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, affected paths, source and destination blobs, final matrix digest, validation, runtime identities, review outcomes, and explicit non-applicability results. |
| Task 148 | Close Terraform #21. |
| Task 148 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 148 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 149 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 151 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 158 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 158 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 149 — create or update the focused repair issue for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 148 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 148's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 150 — commence the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 149 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 149 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 151 — implement the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 150 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 150 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 152 — create or update the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 151 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 151 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 153 — run the Anthropic Claude Code review loop on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 152 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 152 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 152's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 153 for the new head before Task 154. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 154 — run the independent final quality check on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 153 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 153 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 152's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 153 for the new head, and then repeat Task 154 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 155 — merge the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 154 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 154 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 156 — publish the landed repair handoff for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 155 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 155 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 157 — recheck the reciprocal result for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 148 | `FS` | Use the original pinned comparison and branch result. |
| Task 156 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 149–156 as a new numbered-range instance. After its new handoff, repeat Task 157.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 158 — publish the fixed-point closure for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 157 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 157 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 159 — inventory all untracked cross-repository differences

> **Execution class: Coding agent executable.** This is read-only analysis and does not start implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 158 | `FS` | The P1A/Terraform #21 reciprocal cycle is closed. |

### Objective

Complete only this leaf action: inventory all untracked cross-repository differences. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch both current `main` commits and trees in clean checkouts. Pin them as the sweep baselines.
2. Inventory every path that controls generation, validation, workflow policy, Actions permissions and pins, Node/npm acquisition, Markdown lint, Git/path checks, supply-freeze evidence, Dependabot, agent review commands, issue handoffs, and contributor instructions.
3. Map files by role. Do not infer role equality or difference from filenames alone.
4. For every common role, compare bytes and semantics, success and negative cases, failure classification, cleanup, credentials, permissions, native statuses, platform coverage, evidence retention, documentation, and validation commands.
5. Run the full foundation catalog for foundation roles. Use a documented reduced matrix only for a narrow non-foundation role.
6. Classify each row as `same`, `intentional difference`, `tracked work`, or `untracked blocker`.
7. Accept an intentional difference only under the task-local intentional-difference evidence requirements below. Link the owning issue for every `tracked work` row.
8. Treat a missing, duplicate, unknown, renamed, empty, or unexplained row as an `untracked blocker`.
9. For each untracked blocker, validate the finding and complete the required finding-specific decision process before proposing a repair.
10. Publish a read-only inventory that identifies the behind repository, affected role and blobs, evidence, owner/disposition, and proposed focused issue for each blocker. Do not edit implementation files in this task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

### Complete when

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

## Task 160 — assess one untracked-blocker reciprocal cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 159 | `FS` | The complete read-only inventory and ordered blocker list exist. |
| Prior completed instance of Tasks 160–170 | `FS` | Close the prior repair cycle before the next instance starts. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 160 | Select the next untracked blocker in the published order. |
| Task 161 | Create or update one focused issue in the behind repository. Pin the other repository's landed commit and source blobs. |
| Task 162 | Issue creation does not start implementation. Start only after issue setup and a free implementation slot. |
| Task 160 | Keep both repositories self-contained. Do not combine unrelated fixes or change settings without exact authorization. |
| Task 164 | Prepare one focused repair PR. Run the candidate, Claude Code review-loop, independent final quality-check, and merge actions as discrete lifecycle steps. |
| Task 169 | Compare the landed repair in the other repository. Post a pinned no-change record or run the same four discrete steps for one focused reciprocal repair. |
| Task 160 | Run the reverse comparison. Stop for a new decision process if a row changes direction twice. |
| Task 170 | Close the cycle only when every common row is `same`, all intentional differences have complete evidence, and no blocker remains. |
| Task 170 | Post the permanent closure record with both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, matrix digest, validation, runtimes, review outcomes, and non-applicability results. |
| Task 160 | Repeat Task 170 only if another ordered blocker remains. |
| Task 160 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 160 | Map files by role, not only by filename. |
| Task 160 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 163 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 161 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 162 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 164 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 169 | Compare the landed destination result back against the source repository. |
| Task 161 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 164 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 163 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 170 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 170 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 161 — create or update the focused repair issue for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 160 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 160's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 162 — commence the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 161 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 161 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 163 — implement the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 162 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 162 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 164 — create or update the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 163 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 163 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 165 — run the Anthropic Claude Code review loop on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 164 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 164 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 164's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 165 for the new head before Task 166. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 166 — run the independent final quality check on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 165 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 165 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 164's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 165 for the new head, and then repeat Task 166 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 167 — merge the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 166 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 166 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 168 — publish the landed repair handoff for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 167 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 167 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 169 — recheck the reciprocal result for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 160 | `FS` | Use the original pinned comparison and branch result. |
| Task 168 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 161–168 as a new numbered-range instance. After its new handoff, repeat Task 169.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 170 — publish the fixed-point closure for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 169 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 169 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 171 — finalize the global consistency sweep

> **Execution class: Coding agent executable.** This is a verification and evidence-publication task.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 159 | `FS` | The inventory exists. |
| Every applicable Task 170 instance | `FS` | All untracked blocker cycles are closed. |

### Objective

Complete only this leaf action: finalize the global consistency sweep. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch both final `main` commits and trees.
2. Re-run the complete inventory, file-role map, foundation catalog, and every required reduced matrix.
3. Verify that every role has an owner and disposition, each `tracked work` row links its live planned issue, and no `untracked blocker` remains.
4. Post the final sweep record. Include final commits/trees, file/blob map, matrices and digests, validation, runtimes, intentional differences, explicit non-applicability results, and zero untracked blockers.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

### Complete when

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

## Task 172 — commence the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 171 | `FS` | The global sweep proves zero untracked blockers. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P1B writer candidate and decision evidence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify that the implementation slot is free. Explicitly start only #147.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 173 — prepare the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 172 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P1B writer candidate and decision evidence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-read the final PS foundation handoff and every landed contract.
2. Reconcile each PS #147 assumption with those contracts.
3. Implement the writer and terminal check `Build Style Guide Artifacts / approve_candidate`.
4. Prepare an amendment or replacement for `docs/decisions/0001-accept-in-repository-trust-root.md`.
5. Record workflow identity, permissions, required-check name, unique evidence ref, source pins, and rollback inputs.
6. Prepare the exact temporary and persistent settings requests and proof plan for PS #152.
7. Do not give the writer effective `contents: write` access before Task 178 records human approval.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 174 — create or update the candidate PR for the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 173 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P1B writer candidate and decision evidence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one PS #147 PR. Reach one locally validated candidate head. Record its exact head and tree.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

### Complete when

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

## Task 175 — prepare read-only PS #152 settings evidence

> **Execution class: Coding agent executable.** This is read-only preparation and can overlap Task 174.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 174 | `SS` | Begin only after Task 172 starts and its proposed design is identifiable. |

### Objective

Complete only this leaf action: prepare read-only PS #152 settings evidence. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #152 and current rules, classic branch protection, `main`, application identity, and writer permissions.
2. Preserve the Terraform PR #29 bypass-eligibility finding and the limited 2026-08-01 approval scope.
3. Map every requested temporary and persistent setting to the Task 174 candidate.
4. Prepare the non-mutating bypass-eligibility check, field-equivalent temporary-rule request, effective-bypass and rejection drills, evidence-retention plan, cleanup proof, exact-restoration proof, persistent-ruleset request, and rollback plan.
5. Identify every decision that needs owner or administrator approval. Do not infer approval from prior evidence.
6. Do not change a setting, create a rule, or give the writer effective write access.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

### Complete when

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

## Task 176 — run the Anthropic Claude Code review loop on the PS #147 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 174 | `FS` | The PS #147 candidate PR exists and initial checks pass. |
| Task 175 | `FS` | The read-only PS #152 evidence package is complete. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #147 PR. Do not merge or change repository settings. If the loop changes bytes or the writer design, rerun the applicable Task 174 validation and refresh Task 175 settings evidence before the next round.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

## Task 177 — run the independent final quality check on the PS #147 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 176 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require full PS #147 issue coverage; writer, evidence, lease/refspec, credential, terminal-result, rollback, comment, deferral, PR-body, validation, and mergeability checks; and consistency with the PS #152 settings request. Return to Task 176 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 176, and the settings evidence still applies.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 176, and the settings evidence still applies.

## Task 178 — approve or reject the P1B authority decisions

> **Execution class: Human execution required.** The repository owner or administrator must decide each item.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 175 | `FS` | Review the exact settings requests and proof plan. |
| Task 177 | `FS` | Review the exact candidate bytes and independent quality-check result. |

### Objective

Complete only this leaf action: approve or reject the P1B authority decisions. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Human actions:**

1. Review and approve or reject the proposed trust-root decision.
2. Review and approve or reject each temporary and persistent PS #152 settings request.
3. If integration ID `15368` is unavailable, select and approve another writer design.
4. Do not approve a substitute rule without also approving the writer design.
5. If the design permits human bypass, approve or reject that bypass in a separate decision.
6. Record every approval or rejection, condition, owner, canonical location, and expiry.
7. Do not reuse an approval for different bytes or settings.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 174 or Task 175 before work continues. After a candidate-byte change, rerun Tasks 176 and 185.

### Complete when

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 174 or Task 175 before work continues. After a candidate-byte change, rerun Tasks 176 and 185.

## Task 179 — prove and activate the approved PS #152 settings

> **Execution class: Coding agent executable.** Recorded human approval is a required input. Do not merge PS #147 in this task.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 178 | `FS` | Every applicable approval exists and applies to the current candidate and exact settings. |

### Objective

Complete only this leaf action: prove and activate the approved PS #152 settings. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify all approvals. Stop if one is absent, expired, conditional on unmet work, or for different bytes or settings.
2. Revalidate the reviewed PS #147 head.
3. Start the bounded temporary proof under PS #152.
4. Export current rules, classic protection, `main`, application identity, and writer permissions.
5. Perform the non-mutating bypass-eligibility check.
6. If integration ID `15368` is unavailable, stop and return to Task 178.
7. Create the approved temporary field-equivalent rule.
8. Run the effective-bypass and rejection drills. Retain the required evidence.
9. Delete the temporary rule and evidence ref. Prove exact restoration.
10. Revalidate the reviewed PS #147 head.
11. As the final settings action, create the approved persistent ruleset immediately before Task 181.
12. Retain the create response, ruleset ID, normalized JSON, digest, effective rules, exact required check and integration, `main` before and after the setting change, and rollback proof.
13. Close PS #152 only after the persistent rule is active and evidence is complete.
14. Do not copy Terraform ruleset IDs, digests, effective-rule JSON, or audit records.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

### Complete when

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

## Task 180 — merge PS P1B

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 179 | `FS` | PS #152 is closed and the approved persistent rule is active. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Revalidate the reviewed PS #147 head, checks, workflow identity, required check, active ruleset, application identity, and approvals.
2. Stop if any byte, setting, identity, or approval differs from the proved state.
3. Apply the task-local merge gate below. Require the Task 176 terminal-clean record and Task 177 independent `PASS` record to match the current head and tree.
4. Merge the reviewed P1B PR and close PS #147.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 181 — publish the landed handoff for PS P1B

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 180 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Post the permanent P1B handoff for Terraform #22. Include all source and landed identities, paths and blobs, permission and credential boundaries, candidate consumption, ancestry/lease/refspec and native-status behavior, failure and cleanup postconditions, ruleset evidence, rollback proof, validation, review-loop result, final quality-check result, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

### Complete when

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

## Task 182 — update Terraform #22 from the landed PS P1B handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 181 | `FS` | Use the landed PS P1B commit and permanent handoff. |
| Task 158 | `FS` | Terraform #21 is closed. |

### Objective

Complete only this leaf action: update Terraform #22 from the landed PS P1B handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Issue update:**

Replace every placeholder with a verified identity and add or amend a `Starting point — reuse landed PSStyleGuide P1B` section in Terraform #22:

> PSStyleGuide P1B landed through `<PS_P1B_PR_URL>` at `<PS_P1B_LANDED_COMMIT>`, tree `<PS_P1B_LANDED_TREE>`. Use that landed implementation and permanent handoff `<PS_P1B_HANDOFF_URL>` as the T1B implementation source. Do not rebuild the writer, evidence protocol, lease/refspec checks, credential boundary, terminal result, or rollback design.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Record every PS source blob and every adapted Terraform destination blob. Change repository names, generated filenames, schema/type prefixes, check names, ruleset names, and local evidence identifiers. Preserve common permissions, job isolation, credential materialization, candidate consumption, ancestry/lease/refspec checks, native statuses, failure postconditions, cleanup, and audit truth.
>
> Use Terraform's own administrator issue and live settings evidence. Never copy PS ruleset IDs, request digests, application/rule responses, `main` values, or audit records. Refresh the reciprocal matrix at implementation start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

### Complete when

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

## Task 183 — commence Terraform #22

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 182 | `FS` | The issue record is complete, Terraform #21 is closed, and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #22. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #22 from the landed PS P1B commit and final Terraform baseline.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 184 — implement Terraform #22

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 183 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #22 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the writer and evidence protocol. Preserve the common controls named in Task 182.
2. Use only Terraform's authorized settings and live evidence.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 185 — create or update the candidate PR for Terraform #22

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 184 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #22; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation. Treat unexplained weaker behavior as a blocker.
2. Create or update one focused Terraform #22 PR. Run initial validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

### Complete when

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

## Task 186 — run the Anthropic Claude Code review loop on the Terraform #22 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 185 | `FS` | The Terraform #22 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide. Do not merge or change settings without exact authorization. If bytes change, rerun Task 185 validation and refresh the reciprocal matrix.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 187 — run the independent final quality check on the Terraform #22 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 186 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #22 issue, writer, settings, permissions, credentials, evidence, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 186 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 186.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 186.

## Task 188 — merge the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 187 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 189 — publish the landed handoff for the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 188 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

### Complete when

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

## Task 190 — reverse-compare T1B in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 189 | `FS` | Use only the landed P1B and T1B commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 190 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 190 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 191 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 192 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 194 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 190 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 193 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 190 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 190 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 191 — create or update the focused repair issue for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 190 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 190's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 192 — commence the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 191 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 191 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 193 — implement the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 192 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 192 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 194 — create or update the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 193 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 193 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 195 — run the Anthropic Claude Code review loop on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 194 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 194 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 194's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 195 for the new head before Task 196. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 196 — run the independent final quality check on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 195 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 195 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 194's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 195 for the new head, and then repeat Task 196 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 197 — merge the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 196 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 196 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 198 — publish the landed repair handoff for T1B in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 197 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 197 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 199 — perform the final Terraform T1B recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 198 | `FS` | Use the final landed PS state from Task 198. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 199 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 199 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 200 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 202 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 209 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 209 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 200 — create or update the focused repair issue for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 199 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 199's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 201 — commence the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 200 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 200 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 202 — implement the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 201 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 201 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 203 — create or update the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 202 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 202 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 204 — run the Anthropic Claude Code review loop on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 203 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 203 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 203's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 204 for the new head before Task 205. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 205 — run the independent final quality check on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 204 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 204 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 203's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 204 for the new head, and then repeat Task 205 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 206 — merge the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 205 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 205 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 207 — publish the landed repair handoff for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 206 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 206 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 208 — recheck the reciprocal result for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 199 | `FS` | Use the original pinned comparison and branch result. |
| Task 207 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 200–207 as a new numbered-range instance. After its new handoff, repeat Task 208.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 209 — publish the fixed-point closure for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 208 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 208 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 210 — commence PS #148

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 209 | `FS` | The P1B/T1B cycle is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement PS #148. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only PS #148.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 211 — implement PS #148

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 210 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #148 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #148 and its native dependency. Classify the capability and pin all landed inputs and affected paths.
2. Implement the issue contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 212 — create or update the candidate PR for PS #148

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 211 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #148; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused PS #148 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

### Complete when

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

## Task 213 — run the Anthropic Claude Code review loop on the PS #148 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 212 | `FS` | The PS #148 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #148 PR. Do not merge. If bytes change, rerun Task 212 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 214 — run the independent final quality check on the PS #148 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 213 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #148 issue-contract, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 213 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 213.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 213.

## Task 215 — merge the PS #148 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 214 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 216 — publish the landed handoff for the PS #148 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 215 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

### Complete when

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

## Task 217 — compare PS #148 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 216 | `FS` | Use the final landed PS #148 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 217 | Compare the affected PS #148 role with Terraform. |
| Task 218 | If common behavior is absent or weaker, create one focused Terraform issue from the landed PS handoff, then run the four task-local conditional repair PR lifecycle actions. |
| Task 217 | If Terraform is already equivalent, post a pinned no-change record. |
| Task 217 | If the blank-line example is PowerShell-specific, post a pinned non-applicability record. |
| Task 217 | Do not create an issue only for symmetry. |
| Task 217 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 217 | Map files by role, not only by filename. |
| Task 217 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 220 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 218 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 219 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 221 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 217 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 218 — create or update the focused repair issue for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 217 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 217's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 219 — commence the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 218 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 218 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 220 — implement the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 219 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 219 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 221 — create or update the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 220 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 220 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 222 — run the Anthropic Claude Code review loop on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 221 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 221 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 221's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 222 for the new head before Task 223. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 223 — run the independent final quality check on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 222 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 222 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 221's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 222 for the new head, and then repeat Task 223 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 224 — merge the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 223 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 223 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 225 — publish the landed repair handoff for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 224 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 224 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 226 — reverse-compare the PS #148 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 225 | `FS` | Use the final landed or no-change result from Task 225. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 226 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 226 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 227 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 228 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 230 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 235 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 229 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 236 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 236 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 227 — create or update the focused repair issue for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 226 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 226's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 228 — commence the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 227 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 227 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 229 — implement the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 228 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 228 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 230 — create or update the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 229 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 229 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 231 — run the Anthropic Claude Code review loop on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 230 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 230 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 230's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 231 for the new head before Task 232. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 232 — run the independent final quality check on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 231 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 231 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 230's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 231 for the new head, and then repeat Task 232 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 233 — merge the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 232 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 232 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 234 — publish the landed repair handoff for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 233 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 233 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 235 — recheck the reciprocal result for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 226 | `FS` | Use the original pinned comparison and branch result. |
| Task 234 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 227–234 as a new numbered-range instance. After its new handoff, repeat Task 235.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 236 — publish the fixed-point closure for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 235 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 235 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 237 — commence Terraform #23

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 236 | `FS` | The PS #148 cycle is closed and the implementation slot is free. |
| Task 189 | `FS` | Terraform #22, the native predecessor of #23, is closed. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #23. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #23.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 238 — implement Terraform #23

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 237 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #23 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query Terraform #23 and verify its native prerequisites.
2. Classify it as Terraform-specific unless comparison proves a common PS role.
3. Implement its state-version recovery contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 239 — create or update the candidate PR for Terraform #23

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 238 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #23; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused Terraform #23 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

### Complete when

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

## Task 240 — run the Anthropic Claude Code review loop on the Terraform #23 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 239 | `FS` | The Terraform #23 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 239 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 241 — run the independent final quality check on the Terraform #23 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 240 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #23 state-version recovery, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 240 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 240.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 240.

## Task 242 — merge the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 241 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 243 — publish the landed handoff for the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 242 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

### Complete when

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

## Task 244 — evaluate Terraform #23 applicability in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 243 | `FS` | Use the landed Terraform #23 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 244 | Search PS for a role affected by Terraform #23's state-version recovery behavior. |
| Task 245 | If PS has a common role and is absent or weaker, create one focused PS issue, then use the task-local execution controls, conditional repair PR lifecycle, and reciprocal fixed-point controls below. |
| Task 244 | If PS is already equivalent, post a pinned no-change record. |
| Task 244 | If PS has no corresponding role, post a pinned non-applicability record. |
| Task 244 | Do not invent a PS issue only for symmetry. |
| Task 244 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 244 | Map files by role, not only by filename. |
| Task 244 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 247 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 245 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 246 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 248 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 253 | Compare the landed destination result back against the source repository. |
| Task 245 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 248 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 247 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 254 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 254 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 245 — create or update the focused repair issue for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 244 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 244's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 246 — commence the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 245 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 245 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 247 — implement the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 246 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 246 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 248 — create or update the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 247 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 247 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 249 — run the Anthropic Claude Code review loop on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 248 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 248 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 248's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 249 for the new head before Task 250. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 250 — run the independent final quality check on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 249 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 249 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 248's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 249 for the new head, and then repeat Task 250 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 251 — merge the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 250 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 250 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 252 — publish the landed repair handoff for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 251 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 251 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 253 — recheck the reciprocal result for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 244 | `FS` | Use the original pinned comparison and branch result. |
| Task 252 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 245–252 as a new numbered-range instance. After its new handoff, repeat Task 253.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 254 — publish the fixed-point closure for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 253 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 253 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 255 — commence the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 254 | `FS` | Terraform #23 and its PS applicability cycle are complete. |
| Task 181 | `FS` | The P1B PS handoff exists. |
| Task 216 | `FS` | The P2 PS handoff exists. |
| Completed PS #145 and #146 | `FS` | The P1 and P1A handoffs exist. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P3 candidate for merge. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #149.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 256 — prepare the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 255 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P3 candidate for merge without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the P1, P1A, P1B, and P2 landed handoffs and all native issue dependencies.
2. Re-query PS #149, Terraform #24, and current dependency, package-manager, audit, process-runner, exception, Husky, workflow, and evidence contracts.
3. Pin every source commit and blob. Record intended paths and expected repository-specific differences.
4. Implement the complete PS #149 contract. Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, failure truth, and evidence retention.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 257 — create or update the candidate PR for the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 256 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P3 candidate for merge; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Create or update one PS #149 PR. Reach one locally validated candidate head. Record its base, head/tree, checks, paths and blobs, validation, runtime identities, decision-record changes, and rollback inputs.
2. Stop before review-loop execution or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #149 has one locally validated candidate PR and a verified evidence package.

### Complete when

PS #149 has one locally validated candidate PR and a verified evidence package.

## Task 258 — run the Anthropic Claude Code review loop on the PS #149 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 257 | `FS` | The PS #149 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #149 PR. Do not merge. If bytes change, rerun Task 257 validation and refresh its evidence package.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 259 — run the independent final quality check on the PS #149 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 258 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #149 governance, dependency, audit, Husky, exception, scheduled/manual behavior, issue, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 258 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 258. The PR is eligible for the checkpoint decision but remains unmerged.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 258. The PR is eligible for the checkpoint decision but remains unmerged.

## Task 260 — check the PS #149 advisory-deadline state

> **Execution class: Coding agent executable.** Run this task at exactly `2026-08-21T23:59:59Z`. It is a status check, not implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | Calendar constraint | Run at `2026-08-21T23:59:59Z` even if an earlier numbered task is incomplete. |

The PS P1 advisory decision expires at `2026-08-30T23:59:59Z`. The checkpoint is a coordination gate, not a native issue dependency.

### Objective

Complete only this leaf action: check the PS #149 advisory-deadline state. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #149 and the P1, P1A, P1B, and P2 handoffs.
2. Record each issue and PR state, reviewed head, checks, dependency state, and merge readiness.
3. If PS #149 is merge-ready, record that its current merge path is available for Task 261.
4. If PS #149 is not merge-ready, prepare the fallback evidence package for Task 261.
5. Record incomplete earlier tasks explicitly. Do not wait beyond the checkpoint and do not create a false PS #149 dependency.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

### Complete when

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

## Task 261 — select and approve the PS #149 deadline path

> **Execution class: Human execution required.** The repository owner must select the deadline path.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 260 | `FS` | Use the exact timestamped checkpoint evidence. |

### Objective

Complete only this leaf action: select and approve the PS #149 deadline path. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Human actions:**

1. If PS #149 is merge-ready, approve or reject its existing merge path.
2. If PS #149 is not merge-ready, approve or reject a separate superseding-decision task.
3. If the superseding task is approved, set its start date to `2026-08-22`.
4. Record the approved scope, owner, canonical approval, and expiry.
5. Approve every repository-settings change in the applicable administrator issue.
6. Do not extend the expiry without a new recorded decision.
7. Do not reuse approval for different bytes or settings.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 272.

### Complete when

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 272.

## Task 262 — create or update the approved advisory-fallback issue

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 261 | `FS` | The owner approved a separate superseding-decision task and its exact scope. |

### Objective

Create or update only the focused advisory-fallback issue authorized by the owner. Do not commence implementation. If no fallback was approved, record `SKIPPED — EXISTING MERGE PATH` and do not create an issue.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the Task-local owner decision, PS #149, its current PR, the advisory evidence, and all repository dependencies.
2. If the owner approved the existing PS #149 merge path and did not approve a fallback, record `SKIPPED — EXISTING MERGE PATH` and stop.
3. If a fallback was approved, search for the separately approved focused issue. Update it when it already represents the exact scope; otherwise create it.
4. Record the canonical approval, owner, start date, expiry, exact fallback scope, affected paths, replacement contract, validator/case requirements, compensating controls, validation, rollback, and real native dependencies.
5. Read the issue and dependencies back. Do not create a branch, edit bytes, update a PR, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

## Task 263 — commence an approved advisory fallback

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 262 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement an approved advisory fallback. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 262 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Re-query the approved fallback issue, exact owner authorization, native dependencies, both current main refs, and active implementation PR inventory.
3. Require the approved start date to have arrived, every prerequisite to be complete, and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, approval, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and validation plan. Do not edit or update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 264 — implement an approved advisory fallback

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 263 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement an approved advisory fallback without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 263 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Verify that no feature implementation issue or PR is active. If the PS #149 PR is still active, preserve its exact head and tree. Close it only when the Task 261 approval expressly authorizes closure. Otherwise, do not start fallback implementation.
3. Prepare the complete fallback from the approved scope.
4. Include the current audit, every accepted finding, each reason, and each compensating control.
5. Include the canonical approval and expiry, exact replacement contract bytes, validator and case changes, and every dependent version, digest, and name pin.
6. Include validation and rollback instructions.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 265 — create or update the candidate PR for an approved advisory fallback

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 264 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement an approved advisory fallback; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 264 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Create one focused fallback PR under the separately approved task. Run initial local validation and required checks. Record its head SHA and tree.
3. Prove on the candidate that the validator does not enter `advisory-expired`. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 266 — run the Anthropic Claude Code review loop on the fallback PR

> **Execution class: Coding agent executable by Anthropic Claude Code.** Skip this task when Task 265 is skipped.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 265 | `FS` | The approved fallback candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the fallback PR. Do not merge. If bytes change, rerun Task 265 validation and expiry proof.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 267 — run the independent final quality check on the fallback PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session. Skip this task when Task 265 is skipped.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 266 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require exact approval/scope, advisory-expiry, contract, validator, case, pin, rollback, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 266 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 266.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 266.

## Task 268 — merge the approved fallback PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 267 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If the approved fallback path was not selected and the predecessor review/quality tasks were skipped, record `SKIPPED — EXISTING MERGE PATH` and stop.
2. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
4. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
5. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 269 — publish the landed handoff for the approved fallback PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 268 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If the predecessor recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
4. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 270 — check whether the PS #149 candidate requires a post-fallback refresh

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 259 | `FS` | The PS #149 candidate passed review and the independent quality check. |
| Task 261 | `FS` | The owner approved the current merge path or supplied a later applicable approval. |
| Task 269 | `FS` when applicable | The approved fallback is landed. |

### Objective

Decide whether the reviewed PS #149 candidate remains merge-ready after the approved fallback path. Do not edit, update a PR, request review, approve, merge, or publish a handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. If Task 269 changed an input or overlapping path, rebase or update the PS #149 candidate. Return to Task 258 for the Claude Code review loop and Task 259 for a new independent quality check. Obtain a new Task 261 approval when the prior approval does not cover the resulting bytes.

### Procedure

1. Re-query the PS #149 candidate, the fallback branch result, all dependencies, decisions, settings, approvals, reviews, checks, and current `main`.
2. Compare the reviewed candidate head/tree and every input or overlapping path with the state after the fallback branch.
3. If no relevant byte, input, setting, approval, or evidence changed, output `MERGE_READY` for the exact current PS #149 head/tree.
4. If refresh is required, output `REFRESH_REQUIRED` with the exact scope. Repeat Tasks 256 and 265 as new update instances, then repeat Tasks 258 and 267. Repeat Task 261 when the prior approval does not cover the resulting bytes or settings. Then repeat this task.
5. Do not perform any refresh action in this task.

### Candidate-refresh gate

Apply this gate only if Procedure step 2 changes the PS #149 PR:

1. Complete the affected issue scope, local validation, PR-body correction, and initial required checks on the new candidate.
2. Record the new base, head SHA and tree, affected paths, issue links, validation, runtime identities, and changed decision or rollback evidence.
3. Stop the merge action. Return to Task 258 for a new Claude Code review loop and Task 259 for a new independent final quality check.
4. Require a new Task 261 approval when the current approval does not cover the new bytes or settings. Do not resume this task until all three records apply to the same current head and tree.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

### Complete when

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

## Task 271 — merge the PS #149 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 270 | `FS` | Require `MERGE_READY` for the exact current PS #149 head/tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Revalidate the approved candidate head, tree, checks, dependencies, decisions, settings, and approval.
2. Stop if any item still differs from the approved evidence.
3. Apply the task-local merge gate below.
4. Merge one PS #149 PR and close the issue.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 272 — publish the landed handoff for the PS #149 P3 implementation

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 271 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Post the permanent P3 handoff. Include source and landed identities, paths and blobs, common mechanism choice, package and tool identities, validation, runtime evidence, decision records, review-loop result, final quality-check result, intentional differences, and rollback instructions.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

### Complete when

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

## Task 273 — update Terraform #24 from the landed PS P3 handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 272 | `FS` | Use the final landed PS #149 commit and handoff. |
| Task 243 | `FS` | Terraform #23, the native predecessor of #24, is closed. |

### Objective

Complete only this leaf action: update Terraform #24 from the landed PS P3 handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Issue update:**

Replace all placeholders with verified identities and update Terraform #24:

> PSStyleGuide dependency and npm governance landed through `<PS_P3_PR_URL>` at `<PS_P3_LANDED_COMMIT>`, tree `<PS_P3_LANDED_TREE>`. Use permanent handoff `<PS_P3_HANDOFF_URL>` as the implementation source.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read every relevant PS source blob from that commit. Do not independently select a package-manager policy, audit parser, process runner, exception schema, Husky installer, workflow topology, or evidence format. Adapt repository identity, issue URLs, package findings, generated filenames, check names, and Terraform-specific dependency graph.
>
> Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, and failure truth. Refresh the reciprocal matrix at start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

### Complete when

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

## Task 274 — commence Terraform #24

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 273 | `FS` | The issue update is complete and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #24. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #24 from the landed PS P3 source.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 275 — implement Terraform #24

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 274 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #24 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the capability; do not independently redesign the named contracts.
2. Preserve all common controls from Task 273 and record every repository-specific adaptation.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 276 — create or update the candidate PR for Terraform #24

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 275 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #24; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation.
2. Create or update one focused Terraform #24 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

### Complete when

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

## Task 277 — run the Anthropic Claude Code review loop on the Terraform #24 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 276 | `FS` | The Terraform #24 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 276 validation and refresh the reciprocal matrix.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 278 — run the independent final quality check on the Terraform #24 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 277 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #24 package, audit, exception, Husky, workflow, issue, comment, deferral, PR-body, validation, checks, and mergeability coverage. Return to Task 277 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 277.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 277.

## Task 279 — merge the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 278 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 280 — publish the landed handoff for the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 279 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

### Complete when

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

## Task 281 — reverse-compare T3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 280 | `FS` | Use only the landed PS #149 and Terraform #24 commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 281 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 281 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 282 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 283 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 285 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 281 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 284 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 281 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 281 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 282 — create or update the focused repair issue for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 281 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 281's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 283 — commence the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 282 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 282 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 284 — implement the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 283 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 283 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 285 — create or update the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 284 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 284 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 286 — run the Anthropic Claude Code review loop on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 285 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 285 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 285's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 286 for the new head before Task 287. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 287 — run the independent final quality check on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 286 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 286 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 285's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 286 for the new head, and then repeat Task 287 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 288 — merge the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 287 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 287 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 289 — publish the landed repair handoff for T3 in PS

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 288 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 288 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 290 — perform the final Terraform T3 recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 289 | `FS` | Use the final landed PS state from Task 289. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 290 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 290 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 291 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 293 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 300 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 300 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 291 — create or update the focused repair issue for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 290 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 290's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 292 — commence the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 291 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 291 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 293 — implement the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 292 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 292 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 294 — create or update the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 293 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 293 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 295 — run the Anthropic Claude Code review loop on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 294 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 294 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 294's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 295 for the new head before Task 296. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 296 — run the independent final quality check on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 295 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 295 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 294's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 295 for the new head, and then repeat Task 296 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 297 — merge the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 296 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 296 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 298 — publish the landed repair handoff for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 297 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 297 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 299 — recheck the reciprocal result for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 290 | `FS` | Use the original pinned comparison and branch result. |
| Task 298 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 291–298 as a new numbered-range instance. After its new handoff, repeat Task 299.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 300 — publish the fixed-point closure for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 299 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 299 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 301 — commence PS #151

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 300 | `FS` | PS #149 and its full reciprocal cycle are closed. |

### Objective

Explicitly commence only the implementation governed by implement PS #151. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #151.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 302 — implement PS #151

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 301 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #151 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #151 and all landed policy and P3 contracts.
2. Implement the issue's Node permission-model confinement and duplicate-key preflight controls without silently attributing them to PS #162.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 303 — create or update the candidate PR for PS #151

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 302 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #151; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused PS #151 PR. Run initial local validation and required checks. Record its head SHA and tree, affected controls, files and blobs, runtimes, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

### Complete when

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

## Task 304 — run the Anthropic Claude Code review loop on the PS #151 PR

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 303 | `FS` | The PS #151 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #151 PR. Do not merge. If bytes change, rerun Task 303 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 305 — run the independent final quality check on the PS #151 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 304 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #151 Node permission-model, duplicate-key preflight, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 304 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 304.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 304.

## Task 306 — merge the PS #151 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 305 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 307 — publish the landed handoff for the PS #151 PR

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 306 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

### Complete when

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

## Task 308 — compare PS #151 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 307 | `FS` | Use the final landed PS #151 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 308 | Compare the landed Node permission-model and duplicate-key controls with the Terraform validator and workflow topology. |
| Task 309 | If an applicable control is absent or weaker, create one focused Terraform issue from the landed PS commit and blobs, then run the four task-local conditional repair PR lifecycle actions. |
| Task 308 | If Terraform is equivalent, post a pinned no-change record. |
| Task 308 | If a control is structurally inapplicable, post a pinned non-applicability record. |
| Task 308 | Do not create a placeholder issue only for symmetry. |
| Task 308 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 308 | Map files by role, not only by filename. |
| Task 308 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 311 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 309 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 310 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 312 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 308 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 309 — create or update the focused repair issue for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 308 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 308's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 310 — commence the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 309 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 309 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 311 — implement the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 310 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 310 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 312 — create or update the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 311 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 311 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 313 — run the Anthropic Claude Code review loop on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 312 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 312 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 312's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 313 for the new head before Task 314. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 314 — run the independent final quality check on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 313 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 313 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 312's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 313 for the new head, and then repeat Task 314 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 315 — merge the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 314 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 314 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 316 — publish the landed repair handoff for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 315 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 315 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 317 — reverse-compare the PS #151 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 316 | `FS` | Use the final Task 316 Terraform result. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 317 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 317 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 318 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 319 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 321 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 326 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 320 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 327 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 327 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 318 — create or update the focused repair issue for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 317 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 317's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 319 — commence the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 318 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 318 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 320 — implement the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 319 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 319 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 321 — create or update the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 320 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 320 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 322 — run the Anthropic Claude Code review loop on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 321 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 321 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 321's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 322 for the new head before Task 323. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 323 — run the independent final quality check on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 322 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 322 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 321's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 322 for the new head, and then repeat Task 323 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 324 — merge the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 323 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 323 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 325 — publish the landed repair handoff for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 324 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 324 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 326 — recheck the reciprocal result for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 317 | `FS` | Use the original pinned comparison and branch result. |
| Task 325 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 318–325 as a new numbered-range instance. After its new handoff, repeat Task 326.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 327 — publish the fixed-point closure for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 326 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 326 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 328 — check the independent residual triggers

> **Execution class: Coding agent executable.** This is a read-only trigger check and does not start implementation.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | — | No technical predecessor. Run here to avoid interrupting the known capability cycles. Re-run when a listed input changes. |

Keep PS #155 and #156 open and independent. Do not add either issue as a blocker for PS #159, #152, #147, Terraform #21, or Terraform #22 without a new material trigger.

### Objective

Complete only this leaf action: check the independent residual triggers. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Trigger check:**

1. Re-query the current issues, repositories, supported hosts, native helpers, candidate files, and external runners.
2. Revisit PS #155 only if the candidate validator permits a competing writer, Windows PowerShell 5.1 support ends and a portable API becomes available, or a later task adopts a suitable native helper.
3. Revisit PS #156 only if the candidate files change or a suitable external runner becomes available.
4. Classify each trigger as `present` or `absent` and link its evidence.
5. If all triggers are absent, post a dated no-trigger record and stop.
6. If a trigger is present, record the affected issue and exact material evidence. Do not start implementation in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 339 must evaluate.

### Complete when

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 339 must evaluate.

## Task 329 — assess one triggered residual cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 328 | `FS` | A material trigger is documented. |
| Task 327 | `FS` | The preceding known fixed-point cycle is closed. |
| Prior completed instance of Tasks 329–339 | `FS` | Close one residual cycle before starting another. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 329 | Validate the trigger and complete the required finding-specific decision process. |
| Task 330 | Create one focused issue with only its real dependencies. Issue creation does not start implementation. |
| Task 331 | Verify the implementation slot is free. Explicitly start only that residual issue. |
| Task 333 | Prepare one focused PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 329 | Record the complete landed identity, validation, review, and rollback evidence. |
| Task 329 | Do not change repository settings without exact administrator authorization. |
| Task 338 | If the other repository has the same role, compare the landed result there. |
| Task 332 | Create a reciprocal issue before implementation only when a common difference exists. |
| Task 331 | Start the reciprocal issue only after issue setup and a free implementation slot. Run the four task-local conditional repair PR lifecycle actions, then run the reverse comparison. |
| Task 339 | Close the cycle only when each common behavior is `same` or a proved intentional difference with equal security and failure strength. |
| Task 329 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 329 | Map files by role, not only by filename. |
| Task 329 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 332 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 330 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 331 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 333 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 338 | Compare the landed destination result back against the source repository. |
| Task 330 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 333 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 332 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 339 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 339 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 330 — create or update the focused repair issue for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 329 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 329's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 331 — commence the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 330 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 330 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 332 — implement the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 331 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 331 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 333 — create or update the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 332 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 332 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 334 — run the Anthropic Claude Code review loop on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 333 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 333 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 333's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 334 for the new head before Task 335. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 335 — run the independent final quality check on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 334 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 334 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 333's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 334 for the new head, and then repeat Task 335 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 336 — merge the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 335 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 335 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 337 — publish the landed repair handoff for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 336 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 336 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 338 — recheck the reciprocal result for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 329 | `FS` | Use the original pinned comparison and branch result. |
| Task 337 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 330–337 as a new numbered-range instance. After its new handoff, repeat Task 338.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 339 — publish the fixed-point closure for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 338 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 338 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 340 — assess one future PS-first paired capability cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 327 | `FS` | All currently known capability cycles are closed. |
| Every applicable Task 339 instance | `FS` | Triggered residual work is closed. |
| Prior completed instance of Tasks 340–350 | `FS` | The prior future cycle has a permanent fixed-point record. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 341 | Create or update the PS issue. Classify it as `common capability`, `repository-specific capability`, or `administrative only`. Issue creation does not start implementation. |
| Task 340 | Record landed inputs and blobs, intended paths, real native dependencies, expected differences, and any mapped Terraform issue. |
| Task 342 | Verify the implementation slot is free. Explicitly start only the PS implementation. |
| Task 344 | Prepare one PS PR and run the four task-local conditional repair PR lifecycle actions. Post a permanent handoff with exact landed identities and matrix rows. |
| Task 341 | Create or update the Terraform issue only from that landed PS handoff. Do not replace placeholders before the PS PR lands, and never use a branch, reviewed head, test-merge ref, or anticipated squash SHA as the landed commit. |
| Task 342 | After issue setup and a free implementation slot, explicitly start only the Terraform adaptation. Do not create a shared runtime dependency. |
| Task 340 | Run the four task-local conditional repair PR lifecycle actions for the Terraform PR. |
| Task 341 | Run the reverse PS comparison. Create a focused PS sync-back issue before any correction and start it only after issue setup and a free implementation slot. |
| Task 340 | If PS changes, compare Terraform again. Run the four task-local conditional repair PR lifecycle actions for any required Terraform repair. |
| Task 342 | Apply the row-direction stop rule. Repeat until the cycle reaches a fixed point. Do not start the next PS capability before the permanent closure record exists. |
| Task 340 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 340 | Map files by role, not only by filename. |
| Task 340 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 343 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 341 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 342 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 344 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 349 | Compare the landed destination result back against the source repository. |
| Task 341 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 344 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 343 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 350 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 350 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Anthropic Claude Code review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 341 — create or update the focused repair issue for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 340 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 340's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 342 — commence the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 341 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 341 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 343 — implement the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 342 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Anthropic Claude Code review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 342 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 344 — create or update the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 343 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 343 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 345 — run the Anthropic Claude Code review loop on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable by Anthropic Claude Code.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 344 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 344 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 344's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 345 for the new head before Task 346. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 346 — run the independent final quality check on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 345 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 345 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 344's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 345 for the new head, and then repeat Task 346 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 347 — merge the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 346 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 346 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 348 — publish the landed repair handoff for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 347 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 347 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 349 — recheck the reciprocal result for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 340 | `FS` | Use the original pinned comparison and branch result. |
| Task 348 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific historical decisions or live settings evidence.

For each intentional difference, name both literals or behaviors. Explain the repository need. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not use convenience, implementation history, separate authorship, or lower effort as a repository-specific reason.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Anthropic Claude Code review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 341–348 as a new numbered-range instance. After its new handoff, repeat Task 349.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 350 — publish the fixed-point closure for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 349 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 349 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 351 — verify final completion

> **Execution class: Coding agent executable.** This is a read-only final audit and evidence-publication task.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Tasks 1–327 | `FS` | Every known task and reciprocal cycle is complete. |
| Every applicable Task 339 and Task 350 instance | `FS` | All triggered residual and in-scope future cycles are complete. |
| Task 328 | `FS` | The latest residual-trigger record is current. |

### Objective

Complete only this leaf action: verify final completion. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Completion audit:**

Verify all of the following:

- Every implementation had a focused issue record and a separate explicit commencement record. No two feature implementation issues or PRs were active at the same time.
- Every cross-repository input was pinned to a landed commit and blob. No work copied from a moving branch or introduced a shared runtime dependency, reusable cross-repository workflow, submodule, package, or third source of truth.
- Every PR completed a discrete Anthropic Claude Code review loop and then a discrete independent fresh-agent quality check against the same final head SHA and tree before merge. Every permanent cycle record contains both gate results and both repositories' issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, validation, runtime identities, and review outcomes.
- Every merged PR passed the whole-PR comment and deferral audit. No unfinished reviewer finding or illegitimate deferral remains. Every legitimate deferral has a cited, self-contained GitHub issue with correct native dependencies.
- Every merged PR title and body accurately describe its final landed diff, issue scope, validation, security effects, intentional differences, deferred-issue links, and rollback information.
- Every required reciprocal matrix contains each applicable row exactly once. Every common row is `same`. Every intentional difference names both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition. No blocker remains.
- Every posted issue body and comment contains verified identities. No placeholder or fabricated SHA, URL, PR number, tree, or blob remains.
- Every native dependency represents a real prerequisite. No tracker falsely blocks implementation, and no setting changed without exact administrator authorization.
- Initial cycles 1 through 5 closed in order with their Terraform comparisons, required repairs, reverse PS comparisons, and permanent fixed-point records.
- PS #159 and its four children are closed with a permanent landed handoff that names both repositories' final commits and blobs.
- PS #163 closed without duplicating volatile protocol text.
- Terraform #31 contains all five initial cycle records and closed without an aggregate foundation implementation PR.
- PR #164 was reviewed and landed through PS #160, and its reciprocal cycle reached a fixed point.
- The generators share the canonical tracked-present/tracked-absent publication and structured-result behavior.
- Both generators use fixed root/destination authority, complete BOM-less UTF-8/LF payloads, same-directory durable candidate verification, one `File.Replace` or non-overwriting `File.Move` publication call, truthful `ReplacementStateUncertain` reporting, no post-publication rollback claim, and reusable raw NUL-delimited Git validation with exact native-status handling.
- Repository-code jobs in both repositories contain no actions and have no repository-token scopes.
- Both repositories use separate action-only publication jobs, verified official Node distributions in action-free Node jobs, strict offline policy parsing and fixtures, machine-readable authority for roles, inputs, defaults, supply identities and case allocation, and the same script-version grammar and PowerShell authoring rules.
- Both repositories have the same common workflow-policy behavior and case coverage, except for recorded repository-specific literals. No workflow writer existed before P1B/T1B introduced and proved it.
- PS #158 is complete, and both repositories have a read-only, reproducible supply-freeze method.
- Terraform #21 closed from landed PS PR #153 after the reverse PS comparison and all sync-back work.
- The global sweep inventoried all common roles, resolved every untracked blocker through focused cycles, and retained final two-repository evidence.
- PS #147 and #152 are complete, Terraform #22 consumed the landed P1B handoff, and the P1B/T1B cycle has no blocker.
- PS #148 and Terraform #23 each have a landed result and the required cross-repository applicability record.
- PS #149 landed after the deadline-path decision. Terraform #24 consumed its handoff after #23. The P3/T3 cycle reached a fixed point.
- PS #151 has an applicable Terraform follow-up or a pinned structural non-applicability record.
- The advisory decision did not expire without completed PS #149 or a separately approved, fully pinned superseding decision.
- PS #155 and #156 remained independent unless a documented trigger created a focused blocker.
- Every later common capability used a landed PS handoff, a Terraform adaptation, a reverse PS comparison, and fixed-point closure before the next PS implementation.
- No shared cross-repository runtime dependency exists.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Post one permanent final audit that identifies the final PS and Terraform commits and trees, links every fixed-point record, lists all validation and runtime evidence, records every intentional difference and non-applicability result, and states that no blocker remains.

### Complete when

Post one permanent final audit that identifies the final PS and Terraform commits and trees, links every fixed-point record, lists all validation and runtime evidence, records every intentional difference and non-applicability result, and states that no blocker remains.

## References

- [PS issue #145](https://github.com/franklesniak/PSStyleGuide/issues/145) and [PS PR #150](https://github.com/franklesniak/PSStyleGuide/pull/150)
- [PS issue #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153)
- PS issues [#147](https://github.com/franklesniak/PSStyleGuide/issues/147), [#148](https://github.com/franklesniak/PSStyleGuide/issues/148), [#149](https://github.com/franklesniak/PSStyleGuide/issues/149), [#151](https://github.com/franklesniak/PSStyleGuide/issues/151), [#152](https://github.com/franklesniak/PSStyleGuide/issues/152), [#155](https://github.com/franklesniak/PSStyleGuide/issues/155), [#156](https://github.com/franklesniak/PSStyleGuide/issues/156), and [#158](https://github.com/franklesniak/PSStyleGuide/issues/158)
- [PS foundation umbrella #159](https://github.com/franklesniak/PSStyleGuide/issues/159), child issues [#160](https://github.com/franklesniak/PSStyleGuide/issues/160), [#161](https://github.com/franklesniak/PSStyleGuide/issues/161), and [#162](https://github.com/franklesniak/PSStyleGuide/issues/162), and [merged PR #164](https://github.com/franklesniak/PSStyleGuide/pull/164)
- [PS Claude-command issue #163](https://github.com/franklesniak/PSStyleGuide/issues/163)
- [Terraform issue #20](https://github.com/franklesniak/TerraformStyleGuide/issues/20), [PR #26](https://github.com/franklesniak/TerraformStyleGuide/pull/26), [PR #27](https://github.com/franklesniak/TerraformStyleGuide/pull/27), and [PR #30](https://github.com/franklesniak/TerraformStyleGuide/pull/30)
- Terraform issues [#21](https://github.com/franklesniak/TerraformStyleGuide/issues/21), [#22](https://github.com/franklesniak/TerraformStyleGuide/issues/22), [#23](https://github.com/franklesniak/TerraformStyleGuide/issues/23), [#24](https://github.com/franklesniak/TerraformStyleGuide/issues/24), and [#31](https://github.com/franklesniak/TerraformStyleGuide/issues/31)
- [GitHub secure-use guidance for Actions](https://docs.github.com/en/actions/reference/security/secure-use)
- [GitHub Actions runner authentication design](https://github.com/actions/runner/blob/main/docs/design/auth.md)
- [GitHub Actions `GITHUB_TOKEN`](https://docs.github.com/en/actions/concepts/security/github_token)
- [GitHub workflow permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions)
- [Node.js permission model](https://nodejs.org/api/permissions.html)
- [.NET `File.Replace`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace)
- [.NET `File.Move`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.move)
- [Git pathname format and `-z`](https://git-scm.com/docs/git-status#_pathname_format_notes_and_z)
- [GitHub issue dependencies](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-issue-dependencies)
