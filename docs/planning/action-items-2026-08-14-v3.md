<!-- markdownlint-disable-file MD013 -->
<!-- markdownlint-configure-file { "MD024": { "siblings_only": true } } -->

# Chronological PSStyleGuide-first cross-repository convergence plan

## Purpose

Complete the remaining PSStyleGuide and TerraformStyleGuide convergence work in one verified execution order. The completed history includes the landed cycle-1 handoff, the no-repair branch, fixed-point closure, PS #161 commencement, and the validated two-script PS #161 candidate. These results remain immutable downstream inputs. Draft PR #167 exists, but its v2 scripts cannot pass the unchanged v1 caller and policy-identity checks. The first incomplete action is to update PS issue #161 so the complete atomic v2 integration can proceed without weakening either check.

This plan contains only numbered atomic leaf tasks. Each task has one executor, one primary action or lifecycle gate, and one objectively testable result. Conditional leaves record `SKIPPED — NO_REPAIR` only when their named comparison predecessor emits `NO_REPAIR`. A reciprocal repair loop repeats only the explicitly named issue-through-handoff leaf range as a new instance.

No task depends on a shared execution contract, common lifecycle, common prompt, replacement contract, or common-foundation section. Every operative requirement appears in each task that needs it.

## Execution classes

- `Coding agent executable`: A coding agent can complete the task with the stated tools and authority. A task-local human approval remains a required input when stated.
- `Human execution required`: A repository owner or administrator must make or approve the decision. A coding agent can prepare evidence but cannot supply approval.
- `Not executable; tracking/control only`: The item records a control state and cannot itself authorize implementation. No current task uses this class.

## Execution-order map

| Task range | Ordered result |
| --- | --- |
| 1–28 | Complete the atomic v2 caller and policy-identity integration, update, review, merge, and reciprocally close PS #161. |
| 29–56 | Implement, review, merge, and reciprocally close the broader PS #162 workflow-isolation and policy-convergence cycle; post the interim handoff. |
| 57–85 | Update, implement, review, merge, and reciprocally close PS #158; close PS #159. |
| 86–113 | Implement, review, merge, and reciprocally close PS #163; close Terraform #31. |
| 114–140 | Implement, review, merge, and reciprocally close Terraform #21. |
| 141–153 | Inventory and close all untracked cross-repository blockers. |
| 154–191 | Prepare, review, decide, prove, land, and reciprocally close P1B/T1B. |
| 192–218 | Implement, review, merge, and close the PS #148 applicability cycle. |
| 219–236 | Implement, review, merge, and close Terraform #23 and its PS applicability result. |
| 237–254 | Prepare and review PS #149, make the fixed-date decision, process any approved fallback, and land P3. |
| 255–282 | Implement, review, merge, and reciprocally close Terraform #24. |
| 283–309 | Implement, review, merge, and close the PS #151 applicability cycle. |
| 310–332 | Check residual triggers, close triggered work, and run later paired cycles when applicable. |
| 333–333 | Publish the final completion audit. |

The only calendar override is Task 242, which runs at `2026-08-21T23:59:59Z`. Its task-local controls define the incomplete-predecessor branch. Every declared task edge is `FS` or a task-local conditional repeat. The completed-plan correction of the contradictory `SS` claim for the read-only PS #152 evidence task remains an `FS` edge from Task 156 because Task 157 requires Task 156's completion record.

## Dependency relationship reference

| Code | Relationship | Meaning |
| --- | --- | --- |
| `FS` | Finish-to-start | The predecessor must finish before the successor starts. |
| `FF` | Finish-to-finish | The successor cannot finish before the predecessor finishes. |
| `SS` | Start-to-start | The successor cannot start before the predecessor starts. |
| `SF` | Start-to-finish | The predecessor cannot finish before the successor starts. |

The graph contains no legitimate `SF` relationship. Former finish-to-finish tracker conditions are represented as separate closure tasks.

## Verified current state

This state was re-queried with authenticated, paginated GitHub REST and GraphQL reads, the GitHub connector, Git object reads, and GitHub Actions failed-step logs through `2026-08-15T03:35:18Z`. Every state-changing task must re-query its task-local identities before acting.

### Completed history retained as downstream evidence

- PS P1 issue #145 is closed through PR #150 at `3b611fd47a8eb9b24248715be7df97b0f3115e6b`.
- PS P1A issue #146 is closed through PR #153 at `2d56357d9f52c76734027174bf62278e6f3d4cd6`, tree `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- PS infrastructure-conformance PR #164 is merged and issue #160 is closed. The merge commit is `18f577174196627c3c12f8ed8b67336bd17d1009`, tree `ec41501c2abeddb282df581bb2534177900564cc`; reviewed head `3c204a5bc45542d2ecc38b30caff80ec3b3e6d7f` passed its review and independent quality gates before merge.
- PR #164 landed generator blob `f8f011eecaae4d9c4bf8541fcab723621256eb63`, exact-path verifier blob `6c0547cb1d28533943258019c1d9a285cbdfc6d5`, policy-validator blob `24e47314a62bb31cc6332cba6e56116efc7602bd`, build-workflow blob `284b08257a48aacc0fe7e474b315f9eb482e5c62`, policy-contract blob `e1b991d3ba5233750cf91d422f1fa844bb7004f0`, negative-case blob `252423934501a1a5ffa7a9d33ea0f001b4068d11`, and candidate-case blob `0e81b9a8f57f6369eec5a47e2ea3310b06e00fd1`.
- PS PR #165 is merged. Its reviewed head is `81926eff7a0692574bbd97c571ce910385344eb0`, merge commit is `74488bd1b23dcc845449bf0be328202947c3102f`, and landed tree is `7e8c91d9892239105350ac6e20bda912930350b2`. Issue #166 is closed.
- Terraform T1 issue #20 is closed through PR #26 at `143f54e52075a1ae1e999a6e242073e3d8d4a46b`. Terraform PR #27 landed at `aae05282b57f093cec8b63e59138db72c982f10e`; PR #30 landed at `fbfc3aca874e235cace92f506377f5c9e0704160`, tree `3c6e54be9d722b8f61aa225b1414f228f7531268`.

### Completed cycle-1 fixed-point history

- The permanent Terraform PR #33 handoff exists on tracker #31 as comment `5298510839`. PR #33 merged at `3b53c7ebf4bca40d641d721eb2455bb510331e4b`, tree `74973d8ed6d0d0e7cec811563cfd42c7ff809ff1`, with all four landed checks successful.
- The reverse comparison emitted `NO_REPAIR`. Byte-identical records exist on PS #159 as comment `5298810750` and Terraform #31 as comment `5298810892`. Their 16-row matrix has six `same`, ten proved `intentional difference`, and zero blocker rows.
- Every conditional repair leaf completed as `SKIPPED — NO_REPAIR`. No issue, branch, PR, review, merge, or repair handoff was created for that no-repair branch.
- The final reciprocal recheck emitted `FIXED_POINT_READY`. Byte-identical closure records exist on PS #159 as comment `5299237436` and Terraform #31 as comment `5299237522`. Both umbrellas correctly remain open for later cycles.

### Completed PS #161 implementation state

- Issue #161 was explicitly commenced through comment `5299338559`. The issue remains open and owns the active implementation slot.
- The two-script implementation produced and validated candidate `4d9506e9181a744a1640bc814a9fd60bf153bdf1`, tree `53aa40670c9233cd3515ec7772072a5fb0757b49`, from base `74488bd1b23dcc845449bf0be328202947c3102f`.
- The generator candidate blob is `9e90fda2d5146b670a234cb509f8cab3bd9d7433`, SHA-256 `b70c6b0043a7b10b672385d7cf8c9cee5c21ac00dd41e1d7b250b628f841cbeb`, schema `PSStyleGuide.GeneratorResult.v2`, version `1.0.20260814.0`.
- The exact-path verifier candidate blob is `566e809d906cb1ac7b41e04cf81636401d8dc86d`, SHA-256 `57e8cb792a31faadbb4765d4282fb5d4f33a4c35a7028ef51d79e864ac6217fd`, schema `PSStyleGuide.ExactGitPathSetResult.v2`, version `1.0.20260814.0`.
- The two scripts passed Windows PowerShell 5.1 and PowerShell 7.6.4 on Windows and Ubuntu, parser and PSScriptAnalyzer validation, generator fault cases, verifier hostile-path and native-failure cases, byte equality, idempotence, result-schema checks, and clean staged and working path-set checks.

### Current PR #167 and validated ordering defect

- Draft PR #167 is the only open PSStyleGuide PR. It closes #161, targets `main`, has head `4d9506e9181a744a1640bc814a9fd60bf153bdf1`, changes exactly the two candidate scripts, is mergeable, and has no review submission, review thread, inline review comment, or PR conversation comment.
- `verify_generated_artifacts` failed because unchanged `build.yml` requires generator schema v1 and version `1.0.20260813.0` after the v2 generator exits 0.
- `markdownlint` failed in its offline policy step because the unchanged contract authenticates the v1 script digests and versions. The validator also authenticates the contract's canonical identity, and the contract authenticates the validator digest.
- The exact minimal closure requires three additional paths: `.github/workflows/build.yml`, `.github/workflows/workflow-policy-contract.json`, and `.github/workflows/Validate-WorkflowPolicy.mjs`. The current case catalog contains no v1 script identity or version literal and is not part of the proved minimal update.
- Issue #162 remains natively blocked by #161 and requires the actual landed #161 commit as its editing baseline. Its broader topology, Node acquisition, policy-case expansion, and workflow-isolation work cannot correctly precede #161.

### Current open dependency slate

- PS #159 is open with child #160 closed and children #161, #162, and #158 open. Native edges remain #160 → #161 → #162. PS #158 remains a nonblocking child.
- PS #147 remains blocked by open administrator issue #152 and closed #146, then blocks #148; the native chain continues #148 → #149 → #151.
- PS #163 remains open and independent. PS #155 and #156 remain open and independent residual items.
- Terraform #31 remains open with completed child #32. The parent/child tracker relationship is not a blocker.
- Terraform #21 is open with only closed blockers #20 and #28; the native chain continues #21 → #22 → #23 → #24 → #25.
- Current PS `main` is `74488bd1b23dcc845449bf0be328202947c3102f`, tree `7e8c91d9892239105350ac6e20bda912930350b2`. Current Terraform `main` is `3b53c7ebf4bca40d641d721eb2455bb510331e4b`, tree `74973d8ed6d0d0e7cec811563cfd42c7ff809ff1`.

### Immutable implementation sources

| Role | Commit | Tree or relevant identity |
| --- | --- | --- |
| PS P1 implementation, PR #150 | `3b611fd47a8eb9b24248715be7df97b0f3115e6b` | Landed PS #145 implementation |
| PS P1A implementation, PR #153 | `2d56357d9f52c76734027174bf62278e6f3d4cd6` | `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50` |
| PS infrastructure conformance, PR #164 | `18f577174196627c3c12f8ed8b67336bd17d1009` | `ec41501c2abeddb282df581bb2534177900564cc` |
| PS review-governance update, PR #165 | `74488bd1b23dcc845449bf0be328202947c3102f` | `7e8c91d9892239105350ac6e20bda912930350b2` |
| PS #161 two-script candidate, PR #167 | `4d9506e9181a744a1640bc814a9fd60bf153bdf1` | `53aa40670c9233cd3515ec7772072a5fb0757b49` |
| Terraform T1 implementation, PR #26 | `143f54e52075a1ae1e999a6e242073e3d8d4a46b` | Landed Terraform #20 implementation |
| Terraform supply-freeze method, PR #27 | `aae05282b57f093cec8b63e59138db72c982f10e` | Recorder blob `05778c0eda0273a9217f7dc953795c2240473a14`; method blob `36010d2dac98631845d8e880689f7c315ccbcdb7` |
| Terraform pre-repair baseline, PR #30 | `fbfc3aca874e235cace92f506377f5c9e0704160` | `3c6e54be9d722b8f61aa225b1414f228f7531268` |
| Terraform cycle-1 repair, PR #33 | `3b53c7ebf4bca40d641d721eb2455bb510331e4b` | `74973d8ed6d0d0e7cec811563cfd42c7ff809ff1` |

## Chronological task slate

## Task 1 — update PS #161 for the atomic v2 caller and policy-identity integration

> **Execution class: Coding agent executable.**

### Task variables

This task has no unresolved variable. Re-query every fixed identity before the issue write.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope |
| PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Verified open issue |
| Existing candidate PR | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified open draft PR |
| Existing candidate base | `74488bd1b23dcc845449bf0be328202947c3102f` | Verified PR #167 base |
| Existing candidate head/tree | `4d9506e9181a744a1640bc814a9fd60bf153bdf1` / `53aa40670c9233cd3515ec7772072a5fb0757b49` | Verified PR #167 head and tree |

### Record inputs

The completed commencement record is comment `5299338559`. The completed candidate contains only the generator and exact-path verifier. The verified state and exact five-file amendment below contain every input needed for this task; revalidate every remote fact independently.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed #161 commencement, two-script implementation, and preserved PR #167 state | `FS` | Issue #161 owns the implementation slot, the two-script candidate is exact and validated, and no formal review has started. |

### Objective

Update only PS issue #161 so it authorizes the complete atomic v2 caller and policy-identity integration. Do not commence the added implementation scope, edit repository bytes, update PR #167, request review, or merge.

### Execution controls

1. Use authenticated structured GitHub reads. Paginate the issue comments, PR comments, reviews, review threads, commits, checks, closing references, dependencies, and sub-issue relationships.
2. Fetch current PS and Terraform `origin/main`. Verify the one-at-a-time implementation slot remains owned by #161 and PR #167 is still the only open implementation PR.
3. Preserve every existing #161 objective, security rule, generated-destination rule, exact-Git-path rule, validation requirement, handoff requirement, parent link, and native dependency.
4. Do not change issue dependencies, parent relationships, labels, assignees, repository settings, or any other issue.
5. Do not weaken, waive, skip, or relabel either failed check.
6. Keep the broader workflow topology, action-free job boundary, anonymous acquisition, Node distribution verification, negative-case expansion, job isolation, and publication-graph work in #162.
7. Record the exact issue body before and after the update, its UTF-8 SHA-256, and the authenticated read-back identity. Replace no value with a placeholder.

### Verified scope amendment

The final #161 affected-path set is exactly:

1. `.github/workflows/Generate-StyleGuideArtifacts.ps1` — completed v2 implementation.
2. `.github/workflows/Test-ExactGitPathSet.ps1` — completed v2 implementation.
3. `.github/workflows/build.yml` — thin v2 caller schema and version integration.
4. `.github/workflows/workflow-policy-contract.json` — v2 script versions, script SHA-256 values, changed build run-step identity, and validator identity.
5. `.github/workflows/Validate-WorkflowPolicy.mjs` — authenticated canonical contract digest needed to validate the changed contract.

The current evidence does not authorize a change to `workflow-policy-cases.json`. A later executor can add that path only after a separate finding-specific decision proves that the unchanged case catalog cannot validate a required v2 semantic rule. Mere convenience, refactoring, or broader #162 coverage is not sufficient.

### Procedure

1. Re-query #161, #162, #159, PR #167, all PR review and comment surfaces, every current check, and native dependencies. Fetch both repository main refs.
2. Verify that PR #167 still has the exact base, head, tree, two paths, one commit, draft state, zero formal review state, and the two documented failure causes.
3. Read the current #161 body. Prepare an exact body edit that preserves all existing text except the stale assignment of mere caller or policy-pin integration to #162.
4. Add an ASD-STE100-compliant atomic integration subsection. Name the five exact paths, the required v2 caller and authenticated identity closure, the unchanged case-catalog presumption, and the work that remains exclusively in #162.
5. Update only issue #161. Read the issue back through the API. Compare every preserved section and the new subsection with the intended body.
6. Record the before/after body hashes, issue update timestamp, exact path set, unchanged dependency state, PR state, and next action. Stop without commencing implementation.

### Validation and evidence

Require all of these results:

- #161 remains open under parent #159, is blocked only by closed #160, and still blocks #162.
- PR #167 remains an open draft with no review action and no repository-byte change in this task.
- The issue body contains all five exact paths once, assigns only the bounded integration to #161, retains the full existing implementation and validation contract, and assigns broader isolation work to #162.
- No placeholder, fabricated identity, dependency change, issue closure, PR update, repository setting, or repository-byte change occurred.

### Stop and escalation conditions

Stop without updating the issue if the candidate head, path set, failure causes, issue dependency, implementation-slot ownership, or #162 boundary changed. Stop after read-back if any existing requirement was lost or the affected-path set is not exact. State the observed mismatch and the smallest required planning decision.

### Exact output

PS issue #161 contains one verified atomic v2 integration amendment with the exact five-file scope. No implementation commencement, repository byte, PR state, dependency, or setting changed.

### Complete when

Authenticated read-back proves the exact five-file #161 scope, every existing issue requirement is preserved, the #162 boundary and native dependencies are unchanged, and this task performed no later lifecycle action.

## Task 2 — commence the added PS #161 v2 integration scope

> **Execution class: Coding agent executable.**

### Task variables

Only the completion record token is unresolved. Replace it with the exact Task 1 output before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope |
| PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Task 1 updated this issue |
| Existing candidate PR | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified draft PR |

### Record inputs

#### Task 1 completion record

~~~markdown
# Task 1 completion record

{{TASK_001_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 1 | `FS` | Authenticated read-back proves the exact five-file #161 scope and unchanged #162 boundary. |

### Objective

Explicitly commence only the added three-file v2 integration scope under issue #161. Publish the commencement record. Do not edit repository bytes, push a commit, update PR #167, request review, or merge.

### Execution controls

1. Re-query #161, #162, #159, PR #167, every PR comment and review surface, commits, checks, closing references, and dependencies. Paginate every connection.
2. Fetch current PS and Terraform `origin/main`. Verify PS `main` remains `74488bd1b23dcc845449bf0be328202947c3102f` and the PR head remains `4d9506e9181a744a1640bc814a9fd60bf153bdf1`. If either changed, stop and reconcile before posting.
3. Verify that #161 still owns the one implementation slot, #162 remains blocked, no other implementation PR is open, and no review gate has started on #167.
4. Keep issue update, commencement, implementation, PR update, review, quality check, and merge as separate leaves. Perform only commencement in this task.
5. Do not change dependencies, issue bodies, PR metadata, repository bytes, branch refs, or settings.

### Verified inputs and commencement boundary

The completed two-script candidate is immutable input. The added implementation owns only:

- `.github/workflows/build.yml`;
- `.github/workflows/workflow-policy-contract.json`; and
- `.github/workflows/Validate-WorkflowPolicy.mjs`.

The final candidate path set will contain these three paths plus the two completed scripts. The implementation must not change `workflow-policy-cases.json` unless a new, separately recorded finding and rubric authorize it. It must not implement #162 topology or policy-coverage expansion.

### Procedure

1. Validate Task 1's completion record and re-read the exact #161 amendment.
2. Revalidate the current PR base, head, tree, two paths, one commit, draft state, zero review state, and both failed-check causes.
3. Prepare one commencement comment with a stable marker. State the exact three added paths, the five-file final boundary, the existing base/head/tree, the two deterministic failure causes, the implementation and validation limits, the prohibition on PR update in this task, and the next task.
4. Post the comment only to #161. Read it back and verify its database ID, node ID, UTF-8 byte length, SHA-256, timestamp, actor, marker multiplicity, path list, and absence of placeholders.
5. Re-query the PR and dependencies. Verify that no other state changed.

### Validation and evidence

Record the exact authenticated queries, page exhaustion, main refs, issue and PR identities, comment URL and IDs, comment-body digest, unchanged PR head and check results, unchanged dependencies, and worktree status. The commencement record must state that no repository implementation byte changed.

### Stop and escalation conditions

Stop before posting if Task 1 is incomplete, the issue amendment is missing, an identity changed, another implementation slot is active, review started, or the exact path boundary is not provable. Stop after posting if read-back differs. Do not start Task 3 until the commencement record is exact.

### Exact output

One verified #161 comment explicitly commences only the added v2 caller and policy-identity integration. No repository byte, branch, PR, dependency, issue body, or setting changed.

### Complete when

The authenticated commencement record is exact and unique, the implementation slot remains #161, PR #167 and all dependencies are unchanged, and Task 3 has a complete five-file boundary and exact starting identities.

## Task 3 — implement the atomic PS #161 v2 caller and policy-identity integration

> **Execution class: Coding agent executable.**

### Task variables

Only the completion record token is unresolved. Replace it with the exact Task 2 output before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope |
| Issue | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Updated and commenced by Tasks 1–2 |
| Candidate PR | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Existing draft PR; do not update it in this task |
| Starting commit/tree | `4d9506e9181a744a1640bc814a9fd60bf153bdf1` / `53aa40670c9233cd3515ec7772072a5fb0757b49` | Exact completed two-script candidate and current PR head |

### Record inputs

#### Task 2 completion record

~~~markdown
# Task 2 completion record

{{TASK_002_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 2 | `FS` | The exact three-file addition is explicitly commenced under the verified five-file #161 boundary. |

### Objective

Implement and locally validate only the v2 caller and authenticated policy-identity closure on top of the exact PR #167 head. Commit the result locally. Do not push, update the PR, request review, or merge.

### Execution controls

1. Re-query #161, PR #167, all current checks and review surfaces, dependencies, and both repository main refs before editing.
2. Work from exact commit `4d9506e9181a744a1640bc814a9fd60bf153bdf1`. Use an isolated branch or worktree. Do not silently rebase or replace either completed script.
3. Change exactly `.github/workflows/build.yml`, `.github/workflows/workflow-policy-contract.json`, and `.github/workflows/Validate-WorkflowPolicy.mjs`. The final diff from PR base must contain exactly those three files plus the two completed scripts.
4. Preserve workflow topology, jobs, permissions, triggers, action pins, Node/npm supply identities, package files, generated outputs, case-catalog bytes, and all unrelated policy requirements.
5. Keep the offline policy validator fail-closed. Do not add an alternate hash, wildcard, transitional allowlist, bypass, skipped check, or compatibility mode.
6. Validate each unexpected required change through a new finding-specific options, rubric, and score record before editing. Stop for a plan correction if a sixth path is technically required.
7. Record every source and candidate blob, SHA-256, script version, result schema, contract canonical digest, validator digest, build run-step digest, command, runtime, exit code, and result.

### Exact integration contract

1. Update the `build.yml` generator caller to require `PSStyleGuide.GeneratorResult.v2` and generator version `1.0.20260814.0`. Preserve the accepted successful `Overall` values and exact native-exit handling unless the v2 contract proves a necessary stricter condition.
2. Verify the exact-path verifier call against `PSStyleGuide.ExactGitPathSetResult.v2` and version `1.0.20260814.0`. Add only the thin caller checks required by the v2 output contract. Do not duplicate the verifier algorithm in YAML.
3. In `workflow-policy-contract.json`, replace the generator version and SHA-256 with `1.0.20260814.0` and `b70c6b0043a7b10b672385d7cf8c9cee5c21ac00dd41e1d7b250b628f841cbeb`.
4. In the same contract, replace the path-verifier version and SHA-256 with `1.0.20260814.0` and `57e8cb792a31faadbb4765d4282fb5d4f33a4c35a7028ef51d79e864ac6217fd`.
5. Recompute every contract-authenticated identity affected by the exact `build.yml` run-step bytes. Do not copy an anticipated digest.
6. Compute the canonical contract identity with the validator's exact `contractIdentityView` and canonical JSON algorithm. This view excludes only `validatorIdentity`. Update `EXPECTED_CONTRACT_CANONICAL_SHA256` in `Validate-WorkflowPolicy.mjs` to the measured digest.
7. Compute the final validator file SHA-256 after the canonical contract constant is exact. Update only `validatorIdentity.sha256` in the contract to that measured validator digest. Because `validatorIdentity` is excluded from the canonical contract view, this closes the identity cycle without a false fixed point.
8. Keep `workflow-policy-cases.json` byte-identical. Run every current case exactly once. A failure caused by an outdated semantic fixture is a stop condition that requires a new recorded decision; it is not permission to weaken or silently edit a case.

### Procedure

1. Create or reuse an isolated local branch at the exact PR head. Verify the starting tree and clean status.
2. Read the three baseline files from the exact starting commit. Record their Git blobs and SHA-256 values.
3. Apply the exact integration contract in dependency order: build caller, contract script identities and run-step identity, canonical contract digest in the validator, final validator digest in the contract.
4. Verify that the completed generator and verifier blobs did not change. Verify that the case catalog, package files, workflows other than `build.yml`, and generated artifacts did not change.
5. Run JSON and YAML parsing, policy preflight, the complete offline policy suite, every current case exactly once, the generator, the exact-path verifier, generated-drift checks, both Markdown surfaces, parser validation in Windows PowerShell 5.1 and PowerShell 7, PSScriptAnalyzer Warning/Error checks, added-line marker sweeps, and `git diff --check`.
6. Re-run the integration validation from a clean index and working tree state. Prove the expected five-path diff from PR base and exact three-path diff from the starting head.
7. Commit the three-file integration locally with one focused commit. Record the new commit, parent, tree, five final paths, file blobs, SHA-256 values, validation matrix, and exact result.
8. Do not push the branch or change any GitHub object.

### Validation and evidence

Require native exit 0 and exact v2 schemas from both scripts, successful policy preflight and full validation, all current policy cases passing exactly once, zero Markdown findings, zero applicable parser or PSScriptAnalyzer findings, no generated drift, a clean post-commit worktree, exact parentage, and the exact three/five-path sets. Record Windows and Ubuntu runtime identities when the repository's existing validation harness supports them; do not weaken the completed two-script cross-host evidence.

### Stop and escalation conditions

Stop without a commit if the starting identity changed, a completed script must change, a sixth path is required, the case catalog must change without a completed decision, a digest cannot be reproduced independently, any validation fails, or the integration would implement broader #162 work. Stop after the local commit if parentage, tree, path set, or worktree cleanliness is not exact. Do not push.

### Exact output

A locally committed, fully validated three-file v2 integration candidate exists on top of `4d9506e9181a744a1640bc814a9fd60bf153bdf1`. Its final diff from PR base contains exactly five authorized paths, and no GitHub state changed.

### Complete when

The local commit, parent, tree, blobs, digests, schemas, versions, five-path set, complete validation results, and clean worktree are recorded and independently reproducible; no branch push or PR, review, issue, dependency, or setting change occurred.

## Task 4 — create or update the candidate PR for PS generator and exact-path convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #161 URL | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Verified existing GitHub issue |
| Task 4 PR URL | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified existing draft PR #167; re-query before execution |

### Record inputs

#### Task 3 completion record

~~~markdown
# Task 3 completion record

{{TASK_003_COMPLETION_RECORD}}
~~~

The completed two-script implementation record supplies the exact candidate and three-host validation. Task 3 supplies the locally committed three-file integration and the exact five-file final diff.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 3 | `FS` | Use the locally committed, fully validated three-file integration on top of the exact existing PR #167 head. |

### Objective

Update only existing draft PR #167 with the complete five-file PS #161 candidate, an exact PR body, and successful initial checks. Do not start the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-conformance PS generator blob: `c54012e549aedef827ae3ccb669b512a1f14c644`.
- Pre-conformance PS path-verifier blob: `dc786aed1bd8c9d3bdcb25c6ea79207fc1d63c1b`.
- Terraform generator blob at `fbfc3aca874e235cace92f506377f5c9e0704160`: `e5bdd8f64569541eb3c724387dfdff1847c18793`.
- Terraform build-workflow blob that contains embedded Git/worktree checks: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform policy-validator blob that contains related fixtures: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The landed the completed PR #164 and issue #160 handoff commit is the editing baseline. Replace `18f577174196627c3c12f8ed8b67336bd17d1009` in PS #161 before implementation. Use the older PS blobs only to identify pre-convergence behavior.

### Required generated-destination behavior

1. Use the fixed repository map as the only path authority. Require the exact destination to be index-tracked.
2. Permit only an absent filesystem entry or one ordinary non-link file under the validated repository root.
3. Precompute and validate all complete payload bytes before destination mutation.
4. Create a fresh, unpredictable, same-directory ordinary candidate. Use bounded retries for real collisions.
5. Write all bytes, durably flush, close, reopen, and verify length, digest, BOM, carriage-return, and final-newline rules.
6. Revalidate the parent, destination state, and candidate identity immediately before publication.
7. For an existing destination, call `File.Replace(candidate, destination, null)` once. For an absent destination, call non-overwriting, same-directory `File.Move(candidate, destination)` once.
8. Before publication returns, preserve the old destination or proved absence on failure. Remove only the proved candidate.
9. After publication returns, do not attempt rollback and do not claim cross-file atomicity.
10. Return success only when bounded final evidence proves destination bytes and state. Otherwise, return `ReplacementStateUncertain` with bounded recovery evidence.
11. Preserve fixed order and truthful partial-success records when a later artifact fails.

### Reusable Git/path contract

Keep `Test-ExactGitPathSet.ps1` as the reusable implementation. Add all applicable Terraform invariants for fixed executable identity, a constructed child environment, disabled ambient Git controls, raw NUL framing, hostile-byte handling, duplicate and cardinality rejection, exact native-status classification, staged, working, and untracked surfaces, hooks and configuration control-surface detection, and bounded ordinary worktree inspection. Do not duplicate the normative algorithm in workflow YAML. A thin caller can contain job-specific orchestration only.

### Scope, validation, and handoff

1. Freeze the final affected-path set at exactly the two completed scripts plus `build.yml`, `workflow-policy-contract.json`, and `Validate-WorkflowPolicy.mjs`. The minimal v2 caller and authenticated identity closure belongs to #161. Keep broader topology, Node acquisition, and policy-coverage expansion in Task 31 and the #162 lifecycle.
2. Complete the full foundation catalog and a file-operation matrix for an existing destination, absent tracked destination, unexpected destination, link/reparse substitution, candidate collision, write/flush/verify failure, publication failure, post-publication uncertainty, cleanup failure, and multi-artifact partial success.
3. Keep the issue read-only for workflow publication. P1B/T1B introduces the first writer.
4. Run fault injection outside the real repository. Test Windows PowerShell 5.1, PowerShell 7 on Windows, and PowerShell 7 on Ubuntu.
5. Prove byte-identical, idempotent output; exact result schemas; truthful failure states; no generated drift; hostile-path handling; script-version progression; and clean staged and working path sets.
6. Update existing draft PR #167 only. Push the Task 3 commit safely, update the PR body to the exact five-file state, and require both initial checks to pass on the resulting head.
7. Record the base, candidate head/tree, source and candidate blobs, versions, SHA-256 values, result schema, fixture identities, runtime matrix, and intentional differences. Stop before merge.

- Verified landed PR #164 generator blob: `f8f011eecaae4d9c4bf8541fcab723621256eb63`.
- Verified landed PR #164 exact-path-verifier blob: `6c0547cb1d28533943258019c1d9a285cbdfc6d5`.
- Verified landed PR #164 commit/tree: `18f577174196627c3c12f8ed8b67336bd17d1009` / `ec41501c2abeddb282df581bb2534177900564cc`.

### Atomic v2 integration scope

The final PR must change exactly these five paths relative to base `74488bd1b23dcc845449bf0be328202947c3102f`:

- `.github/workflows/Generate-StyleGuideArtifacts.ps1`;
- `.github/workflows/Test-ExactGitPathSet.ps1`;
- `.github/workflows/build.yml`;
- `.github/workflows/workflow-policy-contract.json`; and
- `.github/workflows/Validate-WorkflowPolicy.mjs`.

The generator and verifier must retain their completed candidate blobs. The three Task 3 paths must contain only the thin v2 caller and closed policy-identity integration. `workflow-policy-cases.json`, workflow topology, Node/npm acquisition, action pins, permissions, package files, and generated outputs remain unchanged. PR #167's body must remove the stale statement that Task 31 owns the minimal integration and must state the exact work that remains in #162.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query #161, #162, #159, PR #167, every review and comment surface, commits, checks, closing references, dependencies, and both current main refs. Paginate every connection.
2. Verify Task 3's local commit has the exact recorded parent, tree, three-path delta from the old PR head, five-path delta from PR base, clean worktree, and successful validation. Verify that no review started on the old head.
3. Push the Task 3 commit to `codex/issue-161-generator-exact-path-convergence` with a non-force update. Stop if the remote head is not the expected parent.
4. Update only PR #167. Preserve its issue-closing reference. Replace the two-file and Task 31 deferral claims with the exact five-file scope, final head/tree, commits, blobs, versions, schemas, contract and validator digests, validation, security effects, intentional differences, rollback, and bounded #162 remainder.
5. Read the PR back. Verify its base, final head/tree, commit reachability, five paths, body digest, issue link, draft state, and zero review-loop state.
6. Wait for both initial checks on the new head. Require `verify_generated_artifacts` and `markdownlint` to complete successfully. Inspect failed-step logs for any failure. Do not waive or classify a failure as expected.
7. Re-run the task-local candidate preparation gate against the exact final head. Stop before ready-for-review transition, Claude Code review, independent quality check, or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Draft PR #167 contains the exact five-file #161 candidate, has an accurate read-back PR body and identities, and both initial checks pass on its current head. Formal review has not started.

### Complete when

Authenticated read-back proves PR #167's exact base, current head/tree, reachable commits, five authorized paths, issue link, body, validation evidence, and successful `verify_generated_artifacts` and `markdownlint` checks for that same head; no review-loop, independent-quality, merge, or later lifecycle action occurred.

## Task 5 — run the Anthropic Claude Code review loop on the PS #161 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #161 URL | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Verified existing GitHub issue |
| Task 4 PR URL | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified existing PR #167; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 4 completion record

~~~markdown
# Task 4 completion record

{{TASK_004_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 4 | `FS` | The PS #161 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #161 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #161 PR. Do not merge. If the loop changes bytes, rerun the Task 4 validation and continue the loop on the new head.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree with complete current-head reviewer and deferral-sweep evidence.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree with complete current-head reviewer and deferral-sweep evidence.

## Task 6 — run the independent final quality check on the PS #161 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #161 URL | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Verified existing GitHub issue |
| Task 4 PR URL | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified existing PR #167; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 5 completion record

~~~markdown
# Task 5 completion record

{{TASK_005_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 5 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #161 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #161 PR. Require full PS #161 issue-requirement coverage, comment and deferral audits, PR-body accuracy, final-diff inspection, validation, checks, and mergeability. Return to Task 5 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 5.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 5.

## Task 7 — merge the PS #161 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #161 URL | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Verified existing GitHub issue |
| Task 4 PR URL | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified existing PR #167; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 6 completion record

~~~markdown
# Task 6 completion record

{{TASK_006_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 6 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 8 — publish the landed handoff for the PS #161 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #161 URL | `https://github.com/franklesniak/PSStyleGuide/issues/161` | Verified existing GitHub issue |
| Task 4 PR URL | `https://github.com/franklesniak/PSStyleGuide/pull/167` | Verified existing PR #167; re-query before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 7 completion record

~~~markdown
# Task 7 completion record

{{TASK_007_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 7 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #161 is closed by the landed PR and its permanent handoff is ready for the Terraform comparison.

### Complete when

PS #161 is closed by the landed PR and its permanent handoff is ready for the Terraform comparison.

## Task 9 — compare cycle 2 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 8 completion record

~~~markdown
# Task 8 completion record

{{TASK_008_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 8 | `FS` | Use the final landed PS #161 commit. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 9 | Pin the Task 8 PS commit, tree, generator and verifier blobs, result schemas, and fixtures. |
| Task 9 | Compare the complete generated-destination algorithm and reusable Git/path contract with Terraform. |
| Task 9 | Complete the full foundation catalog and the Task 8 file-operation matrix. |
| Task 9 | If all common behavior matches, post a pinned no-change record. |
| Task 10 | If a common difference exists, create or update one focused Terraform issue before implementation. Then run the four task-local conditional repair PR lifecycle actions. |
| Task 9 | Preserve repository-specific filenames and identity only when the intentional-difference record proves equal security and failure strength. |
| Task 9 | Post the result to PS #161, PS #159, and Terraform #31. |
| Task 9 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 9 | Map files by role, not only by filename. |
| Task 9 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 12 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 10 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 11 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 13 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 9 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 10 — create or update the focused repair issue for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by this task; replace {{TASK_010_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 9 completion record

~~~markdown
# Task 9 completion record

{{TASK_009_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 9 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 9's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 11 — commence the focused repair for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by Task 10; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 10 completion record

~~~markdown
# Task 10 completion record

{{TASK_010_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 10 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 10 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 12 — implement the focused repair for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by Task 10; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 11 completion record

~~~markdown
# Task 11 completion record

{{TASK_011_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 11 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 11 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 13 — create or update the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 10 issue URL | `{{TASK_010_ISSUE_URL}}` | Produced by Task 10; resolve globally before execution |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by this task; replace {{TASK_013_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 12 completion record

~~~markdown
# Task 12 completion record

{{TASK_012_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 12 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 12 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 14 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 13 completion record

~~~markdown
# Task 13 completion record

{{TASK_013_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 13 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 13 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 13's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 14 for the new head before Task 15. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 15 — run the independent final quality check on the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 14 completion record

~~~markdown
# Task 14 completion record

{{TASK_014_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 14 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 14 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 13's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 14 for the new head, and then repeat Task 15 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 16 — merge the focused repair PR for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 15 completion record

~~~markdown
# Task 15 completion record

{{TASK_015_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 15 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 15 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 17 — publish the landed repair handoff for cycle 2 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 13 PR URL | `{{TASK_013_PR_URL}}` | Produced by Task 13; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 16 completion record

~~~markdown
# Task 16 completion record

{{TASK_016_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 16 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 16 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 18 — reverse-compare cycle 2 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 17 completion record

~~~markdown
# Task 17 completion record

{{TASK_017_COMPLETION_RECORD}}
~~~

#### Task 8 completion record

~~~markdown
# Task 8 completion record

{{TASK_008_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 17 | `FS` | Use only the final landed commits from Tasks 8 and 43. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 18 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 18 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 19 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 20 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 22 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 27 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 21 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 28 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 28 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 19 — create or update the focused repair issue for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by this task; replace {{TASK_019_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 18 completion record

~~~markdown
# Task 18 completion record

{{TASK_018_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 18 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 18's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 20 — commence the focused repair for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by Task 19; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 19 completion record

~~~markdown
# Task 19 completion record

{{TASK_019_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 19 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 19 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 21 — implement the focused repair for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by Task 19; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 20 completion record

~~~markdown
# Task 20 completion record

{{TASK_020_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 20 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 20 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 22 — create or update the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 19 issue URL | `{{TASK_019_ISSUE_URL}}` | Produced by Task 19; resolve globally before execution |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by this task; replace {{TASK_022_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 21 completion record

~~~markdown
# Task 21 completion record

{{TASK_021_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 21 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 21 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 23 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 22 completion record

~~~markdown
# Task 22 completion record

{{TASK_022_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 22 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 22 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 22's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 23 for the new head before Task 24. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 24 — run the independent final quality check on the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 23 completion record

~~~markdown
# Task 23 completion record

{{TASK_023_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 23 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 23 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 22's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 23 for the new head, and then repeat Task 24 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 25 — merge the focused repair PR for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 24 completion record

~~~markdown
# Task 24 completion record

{{TASK_024_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 24 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 24 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 26 — publish the landed repair handoff for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 22 PR URL | `{{TASK_022_PR_URL}}` | Produced by Task 22; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 25 completion record

~~~markdown
# Task 25 completion record

{{TASK_025_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 25 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 25 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 27 — recheck the reciprocal result for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 18 completion record

~~~markdown
# Task 18 completion record

{{TASK_018_COMPLETION_RECORD}}
~~~

#### Task 26 completion record

~~~markdown
# Task 26 completion record

{{TASK_026_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 18 | `FS` | Use the original pinned comparison and branch result. |
| Task 26 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 12–26 as a new numbered-range instance. After its new handoff, repeat Task 27.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 28 — publish the fixed-point closure for cycle 2 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 27 completion record

~~~markdown
# Task 27 completion record

{{TASK_027_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 27 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 27 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 29 — commence PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 28 completion record

~~~markdown
# Task 28 completion record

{{TASK_028_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 28 | `FS` | Cycle 2 has a permanent fixed-point record. |

### Objective

Explicitly commence only the implementation governed by implement PS workflow isolation and policy convergence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final landed Task 8 commit is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 28 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from the future landed PS #161 handoff before editing; that predecessor identity cannot exist yet.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 30 — implement PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 29 completion record

~~~markdown
# Task 29 completion record

{{TASK_029_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS workflow isolation and policy convergence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final landed Task 8 commit is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 28 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from the future landed PS #161 handoff before editing; that predecessor identity cannot exist yet.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 31 — create or update the candidate PR for PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 31 PR URL | `{{TASK_031_PR_URL}}` | Produced by this task; replace {{TASK_031_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 30 completion record

~~~markdown
# Task 30 completion record

{{TASK_030_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 30 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS workflow isolation and policy convergence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final landed Task 8 commit is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 28 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from the future landed PS #161 handoff before editing; that predecessor identity cannot exist yet.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

## Task 32 — run the Anthropic Claude Code review loop on the PS #162 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 31 PR URL | `{{TASK_031_PR_URL}}` | Produced by Task 31; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 31 completion record

~~~markdown
# Task 31 completion record

{{TASK_031_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 31 | `FS` | The PS #162 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #162 PR. Do not merge. If the loop changes bytes, rerun the Task 31 validation and continue on the new head.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 33 — run the independent final quality check on the PS #162 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 31 PR URL | `{{TASK_031_PR_URL}}` | Produced by Task 31; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 32 completion record

~~~markdown
# Task 32 completion record

{{TASK_032_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 32 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #162 PR. Require full issue-contract, workflow-topology, policy-case, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 32 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 32.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 32.

## Task 34 — merge the PS #162 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 31 PR URL | `{{TASK_031_PR_URL}}` | Produced by Task 31; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 33 completion record

~~~markdown
# Task 33 completion record

{{TASK_033_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 33 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 35 — publish the landed handoff for the PS #162 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 31 PR URL | `{{TASK_031_PR_URL}}` | Produced by Task 31; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 34 completion record

~~~markdown
# Task 34 completion record

{{TASK_034_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 34 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #162 is closed and the final landed policy contract is ready for Task 44.

### Complete when

PS #162 is closed and the final landed policy contract is ready for Task 44.

## Task 36 — compare cycle 3 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 35 completion record

~~~markdown
# Task 35 completion record

{{TASK_035_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 35 | `FS` | Use the final landed PS #162 commit and policy contract. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 36 | Pin all Task 35 workflow, policy, contract, case, and validation blobs. |
| Task 36 | Complete the full foundation catalog and workflow-topology table in Terraform. |
| Task 36 | Apply all blockers from Task 35's required topology and policy. |
| Task 40 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 36 | Update PS #162, PS #159, and Terraform #31 with full identities and validation. |
| Task 36 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 36 | Map files by role, not only by filename. |
| Task 36 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 39 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 37 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 38 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 40 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 36 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 37 — create or update the focused repair issue for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 37 issue URL | `{{TASK_037_ISSUE_URL}}` | Produced by this task; replace {{TASK_037_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 36 completion record

~~~markdown
# Task 36 completion record

{{TASK_036_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 36 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 36's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 38 — commence the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 37 issue URL | `{{TASK_037_ISSUE_URL}}` | Produced by Task 37; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 37 completion record

~~~markdown
# Task 37 completion record

{{TASK_037_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 37 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 37 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 39 — implement the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 37 issue URL | `{{TASK_037_ISSUE_URL}}` | Produced by Task 37; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 38 completion record

~~~markdown
# Task 38 completion record

{{TASK_038_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 38 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 38 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 40 — create or update the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 37 issue URL | `{{TASK_037_ISSUE_URL}}` | Produced by Task 37; resolve globally before execution |
| Task 40 PR URL | `{{TASK_040_PR_URL}}` | Produced by this task; replace {{TASK_040_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 39 completion record

~~~markdown
# Task 39 completion record

{{TASK_039_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 39 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 39 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 41 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 40 PR URL | `{{TASK_040_PR_URL}}` | Produced by Task 40; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 40 completion record

~~~markdown
# Task 40 completion record

{{TASK_040_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 40 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 40 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 40's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 41 for the new head before Task 42. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 42 — run the independent final quality check on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 40 PR URL | `{{TASK_040_PR_URL}}` | Produced by Task 40; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 41 completion record

~~~markdown
# Task 41 completion record

{{TASK_041_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 41 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 41 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 40's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 41 for the new head, and then repeat Task 42 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 43 — merge the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 40 PR URL | `{{TASK_040_PR_URL}}` | Produced by Task 40; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 42 completion record

~~~markdown
# Task 42 completion record

{{TASK_042_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 42 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 42 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 44 — publish the landed repair handoff for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 40 PR URL | `{{TASK_040_PR_URL}}` | Produced by Task 40; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 43 completion record

~~~markdown
# Task 43 completion record

{{TASK_043_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 43 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 43 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 45 — reverse-compare cycle 3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 44 completion record

~~~markdown
# Task 44 completion record

{{TASK_044_COMPLETION_RECORD}}
~~~

#### Task 35 completion record

~~~markdown
# Task 35 completion record

{{TASK_035_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 44 | `FS` | Use only the final landed commits from Tasks 35 and 70. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 45 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 45 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 46 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 47 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 49 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 54 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 48 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 55 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 55 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 46 — create or update the focused repair issue for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 46 issue URL | `{{TASK_046_ISSUE_URL}}` | Produced by this task; replace {{TASK_046_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 45 completion record

~~~markdown
# Task 45 completion record

{{TASK_045_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 45 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 45's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 47 — commence the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 46 issue URL | `{{TASK_046_ISSUE_URL}}` | Produced by Task 46; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 46 completion record

~~~markdown
# Task 46 completion record

{{TASK_046_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 46 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 46 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 48 — implement the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 46 issue URL | `{{TASK_046_ISSUE_URL}}` | Produced by Task 46; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 47 completion record

~~~markdown
# Task 47 completion record

{{TASK_047_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 47 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 47 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 49 — create or update the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 46 issue URL | `{{TASK_046_ISSUE_URL}}` | Produced by Task 46; resolve globally before execution |
| Task 49 PR URL | `{{TASK_049_PR_URL}}` | Produced by this task; replace {{TASK_049_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 48 completion record

~~~markdown
# Task 48 completion record

{{TASK_048_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 48 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 48 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 50 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 49 PR URL | `{{TASK_049_PR_URL}}` | Produced by Task 49; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 49 completion record

~~~markdown
# Task 49 completion record

{{TASK_049_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 49 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 49 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 49's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 50 for the new head before Task 51. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 51 — run the independent final quality check on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 49 PR URL | `{{TASK_049_PR_URL}}` | Produced by Task 49; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 50 completion record

~~~markdown
# Task 50 completion record

{{TASK_050_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 50 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 50 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 49's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 50 for the new head, and then repeat Task 51 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 52 — merge the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 49 PR URL | `{{TASK_049_PR_URL}}` | Produced by Task 49; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 51 completion record

~~~markdown
# Task 51 completion record

{{TASK_051_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 51 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 51 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 53 — publish the landed repair handoff for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 49 PR URL | `{{TASK_049_PR_URL}}` | Produced by Task 49; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 52 completion record

~~~markdown
# Task 52 completion record

{{TASK_052_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 52 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 52 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 54 — recheck the reciprocal result for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 45 completion record

~~~markdown
# Task 45 completion record

{{TASK_045_COMPLETION_RECORD}}
~~~

#### Task 53 completion record

~~~markdown
# Task 53 completion record

{{TASK_053_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 45 | `FS` | Use the original pinned comparison and branch result. |
| Task 53 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 39–53 as a new numbered-range instance. After its new handoff, repeat Task 54.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 55 — publish the fixed-point closure for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 54 completion record

~~~markdown
# Task 54 completion record

{{TASK_054_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 54 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 54 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 56 — post the initial three-cycle handoff

> **Execution class: Coding agent executable.** This is an evidence-publication task, not an implementation task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 55 completion record

~~~markdown
# Task 55 completion record

{{TASK_055_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 55 | `FS` | Cycles 1 through 3 have permanent fixed-point records. |

### Objective

Complete only this leaf action: post the initial three-cycle handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Post one interim handoff to PS #159 and Terraform #31. List cycles 1 through 3, every PS and Terraform issue and PR, bases, reviewed heads/trees, merge methods, landed commits/trees, source and destination blobs, matrices, validation, review outcomes, intentional differences, and fixed-point result. State that PS #158 remains nonblocking and has no native blocking edge to PS #147 or #152. Verify every identifier before posting. Do not close either tracker.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

### Complete when

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

## Task 57 — update PS #158 with the landed workflow-policy starting point

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 56 completion record

~~~markdown
# Task 56 completion record

{{TASK_056_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 56 | `FS` | The final cycle 3 policy contract and interim handoff exist. |

### Objective

Update only the existing PS #158 issue record with the final landed workflow-policy starting point. Do not commence implementation.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #158, its parent #159, the landed workflow-policy PR and commit, and both repository main refs.
2. Replace `<PS_WORKFLOW_POLICY_PR_URL>` and `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` in the task-local text below with the exact predecessor outputs.
3. Post the text as a planning comment without replacing the issue body. Read the comment back and verify both identities and the nonblocking scope.
4. Do not create a feature branch, edit implementation files, open a PR, or occupy the implementation slot.

### Exact issue update

Before implementation, replace both placeholders and post this planning comment to PS #158 without replacing its body:

> This issue is a child of [PS issue #159](https://github.com/franklesniak/PSStyleGuide/issues/159) and starts after `<PS_WORKFLOW_POLICY_PR_URL>` lands. Re-read the final landed `workflow-policy-contract.json` from `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` before adapting the recorder. Preserve this issue's existing nonblocking scope and immutable Terraform PR #27 source pins.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #158 contains the verified starting-point comment, and implementation has not started.

### Complete when

PS #158 contains the verified starting-point comment, and implementation has not started.

## Task 58 — commence the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 57 completion record

~~~markdown
# Task 57 completion record

{{TASK_057_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 57 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement the PS read-only supply-freeze recorder. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 59 — implement the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 58 completion record

~~~markdown
# Task 58 completion record

{{TASK_058_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 58 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS read-only supply-freeze recorder without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-read the final Task 55 policy contract and the immutable Terraform PR #27 inputs.
2. Preserve the issue's existing nonblocking scope.
3. Keep the recorder read-only and capture its output outside the repository.
4. Prove that it does not mutate a manifest, lockfile, installed tree, contract, generated output, staged set, or worktree.
5. Complete a documented reduced reciprocal matrix for the narrow recorder surface. Include every applicable foundation row and prove why omitted rows are inapplicable.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 60 — create or update the candidate PR for the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by this task; replace {{TASK_060_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 59 completion record

~~~markdown
# Task 59 completion record

{{TASK_059_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 59 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS read-only supply-freeze recorder; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #158 PR. Run initial validation and required checks.
2. Record the candidate head/tree, all source and candidate blobs, tool/runtime identities, exact output location, mutation-absence evidence, validation, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

## Task 61 — run the Anthropic Claude Code review loop on the PS #158 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 60 completion record

~~~markdown
# Task 60 completion record

{{TASK_060_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 60 | `FS` | The PS #158 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #158 PR. Do not merge. If the loop changes bytes, rerun the Task 60 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 62 — run the independent final quality check on the PS #158 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 61 completion record

~~~markdown
# Task 61 completion record

{{TASK_061_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 61 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #158 issue coverage, immutable-source verification, read-only and mutation-absence proof, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 61 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 61.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 61.

## Task 63 — merge the PS #158 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 62 completion record

~~~markdown
# Task 62 completion record

{{TASK_062_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 62 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 64 — publish the landed handoff for the PS #158 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 60 PR URL | `{{TASK_060_PR_URL}}` | Produced by Task 60; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 63 completion record

~~~markdown
# Task 63 completion record

{{TASK_063_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 63 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #158 is closed and its permanent handoff is ready for Terraform.

### Complete when

PS #158 is closed and its permanent handoff is ready for Terraform.

## Task 65 — compare cycle 4 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 64 completion record

~~~markdown
# Task 64 completion record

{{TASK_064_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 64 | `FS` | Use the final landed PS #158 recorder and handoff. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 65 | Pin the PS source and landed commits and blobs and the immutable Terraform PR #27 inputs. |
| Task 65 | Compare the read-only recorder roles with a documented reduced matrix. |
| Task 68 | Prove that both implementations preserve repository state and capture output outside the repository. |
| Task 69 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 65 | Update PS #158, PS #159, and Terraform #31 with all identities, validation, and differences. |
| Task 65 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 65 | Map files by role, not only by filename. |
| Task 65 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 68 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 66 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 67 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 69 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 65 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 66 — create or update the focused repair issue for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by this task; replace {{TASK_066_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 65 completion record

~~~markdown
# Task 65 completion record

{{TASK_065_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 65 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 65's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 67 — commence the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by Task 66; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 66 completion record

~~~markdown
# Task 66 completion record

{{TASK_066_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 66 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 66 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 68 — implement the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by Task 66; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 67 completion record

~~~markdown
# Task 67 completion record

{{TASK_067_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 67 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 67 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 69 — create or update the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 66 issue URL | `{{TASK_066_ISSUE_URL}}` | Produced by Task 66; resolve globally before execution |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by this task; replace {{TASK_069_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 68 completion record

~~~markdown
# Task 68 completion record

{{TASK_068_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 68 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 68 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 70 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 69 completion record

~~~markdown
# Task 69 completion record

{{TASK_069_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 69 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 69 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 69's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 70 for the new head before Task 71. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 71 — run the independent final quality check on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 70 completion record

~~~markdown
# Task 70 completion record

{{TASK_070_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 70 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 70 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 69's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 70 for the new head, and then repeat Task 71 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 72 — merge the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 71 completion record

~~~markdown
# Task 71 completion record

{{TASK_071_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 71 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 71 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 73 — publish the landed repair handoff for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 69 PR URL | `{{TASK_069_PR_URL}}` | Produced by Task 69; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 72 completion record

~~~markdown
# Task 72 completion record

{{TASK_072_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 72 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 72 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 74 — reverse-compare cycle 4 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 73 completion record

~~~markdown
# Task 73 completion record

{{TASK_073_COMPLETION_RECORD}}
~~~

#### Task 64 completion record

~~~markdown
# Task 64 completion record

{{TASK_064_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 73 | `FS` | Use only the final landed commits from Tasks 64 and 99. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 74 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 74 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 75 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 76 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 78 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 83 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 77 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 84 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 84 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 75 — create or update the focused repair issue for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by this task; replace {{TASK_075_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 74 completion record

~~~markdown
# Task 74 completion record

{{TASK_074_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 74 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 74's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 76 — commence the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by Task 75; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 75 completion record

~~~markdown
# Task 75 completion record

{{TASK_075_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 75 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 75 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 77 — implement the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by Task 75; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 76 completion record

~~~markdown
# Task 76 completion record

{{TASK_076_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 76 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 76 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 78 — create or update the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 75 issue URL | `{{TASK_075_ISSUE_URL}}` | Produced by Task 75; resolve globally before execution |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by this task; replace {{TASK_078_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 77 completion record

~~~markdown
# Task 77 completion record

{{TASK_077_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 77 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 77 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 79 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 78 completion record

~~~markdown
# Task 78 completion record

{{TASK_078_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 78 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 78 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 78's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 79 for the new head before Task 80. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 80 — run the independent final quality check on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 79 completion record

~~~markdown
# Task 79 completion record

{{TASK_079_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 79 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 79 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 78's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 79 for the new head, and then repeat Task 80 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 81 — merge the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 80 completion record

~~~markdown
# Task 80 completion record

{{TASK_080_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 80 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 80 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 82 — publish the landed repair handoff for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 78 PR URL | `{{TASK_078_PR_URL}}` | Produced by Task 78; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 81 completion record

~~~markdown
# Task 81 completion record

{{TASK_081_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 81 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 81 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 83 — recheck the reciprocal result for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 82 completion record

~~~markdown
# Task 82 completion record

{{TASK_082_COMPLETION_RECORD}}
~~~

#### Task 74 completion record

~~~markdown
# Task 74 completion record

{{TASK_074_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 74 | `FS` | Use the original pinned comparison and branch result. |
| Task 82 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 68–82 as a new numbered-range instance. After its new handoff, repeat Task 83.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 84 — publish the fixed-point closure for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 83 completion record

~~~markdown
# Task 83 completion record

{{TASK_083_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 83 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 83 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 85 — close the PS foundation umbrella

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #159 URL | `https://github.com/franklesniak/PSStyleGuide/issues/159` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 84 completion record

~~~markdown
# Task 84 completion record

{{TASK_084_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 84 | `FS` | PS #160, #161, #162, and #158 and all four reciprocal cycles are complete. |

### Objective

Complete only this leaf action: close the PS foundation umbrella. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #159 and its four native children.
2. Verify that #160, #161, #162, and #158 are closed and that every cycle has a permanent fixed-point record.
3. Post the foundation handoff. Include every issue and PR URL, base, reviewed head/tree, merge method, landed commit/tree, affected path and blob, complete or reduced matrix, validation command and result, runtime identity, review outcome, intentional difference, and reciprocal closure result.
4. Verify that Terraform #31 links the same four cycle records.
5. Close PS #159. Do not create or merge an aggregate foundation PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #159 is closed with complete, matching evidence in both repositories.

### Complete when

PS #159 is closed with complete, matching evidence in both repositories.

## Task 86 — commence the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 85 completion record

~~~markdown
# Task 85 completion record

{{TASK_085_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 85 | `FS` | The foundation umbrella is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement the PS Claude review-loop command. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 87 — implement the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 86 completion record

~~~markdown
# Task 86 completion record

{{TASK_086_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 86 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS Claude review-loop command without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Reconfirm the existing owner authorization because the new file is an agent entry point.
2. Add a thin, repository-local command wrapper.
3. Accept a pull-request URL. Require the URL when the caller does not supply it.
4. Delegate the authoritative process to the repository-local root `CLAUDE.md`.
5. Treat Codex and Copilot as co-equal reviewers, as `CLAUDE.md` requires.
6. Do not duplicate a step count, gate count, round limit, or other volatile protocol detail.
7. Do not fetch instructions from a moving external branch or add a shared runtime dependency.
8. Record a line-by-line comparison with the pinned Terraform input. Explain each intentional difference.
9. Complete a documented reduced reciprocal matrix. Run Markdown lint and all applicable local agent-instruction checks.
10. Keep the issue and PR separate from foundation, supply-freeze, and infrastructure-conformance work.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 88 — create or update the candidate PR for the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 88 PR URL | `{{TASK_088_PR_URL}}` | Produced by this task; replace {{TASK_088_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 87 completion record

~~~markdown
# Task 87 completion record

{{TASK_087_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 87 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS Claude review-loop command; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #163 PR. Record the candidate head/tree, pinned input, line comparison, validation, and initial checks. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

## Task 89 — run the Anthropic Claude Code review loop on the PS #163 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 88 PR URL | `{{TASK_088_PR_URL}}` | Produced by Task 88; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 88 completion record

~~~markdown
# Task 88 completion record

{{TASK_088_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 88 | `FS` | The PS #163 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #163 PR. Do not merge. If the loop changes bytes, rerun the Task 88 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 90 — run the independent final quality check on the PS #163 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 88 PR URL | `{{TASK_088_PR_URL}}` | Produced by Task 88; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 89 completion record

~~~markdown
# Task 89 completion record

{{TASK_089_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 89 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #163 issue coverage, protected-entry-point authorization, pinned-input comparison, absence of volatile protocol duplication, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 89 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 89.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 89.

## Task 91 — merge the PS #163 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 88 PR URL | `{{TASK_088_PR_URL}}` | Produced by Task 88; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 90 completion record

~~~markdown
# Task 90 completion record

{{TASK_090_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 90 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 92 — publish the landed handoff for the PS #163 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 88 PR URL | `{{TASK_088_PR_URL}}` | Produced by Task 88; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 91 completion record

~~~markdown
# Task 91 completion record

{{TASK_091_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 91 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

### Complete when

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

## Task 93 — compare cycle 5 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 92 completion record

~~~markdown
# Task 92 completion record

{{TASK_092_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 92 | `FS` | Use the final landed PS #163 command. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 93 | Pin the PS command and authoritative `CLAUDE.md` blobs and the immutable Terraform command blob. |
| Task 93 | Compare both thin wrappers line by line with a documented reduced matrix. |
| Task 93 | Preserve each repository's authoritative local instruction file and repository identity. |
| Task 97 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 93 | Update PS #163 and Terraform #31 with all identities, validation, and intentional differences. |
| Task 93 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 93 | Map files by role, not only by filename. |
| Task 93 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 96 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 94 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 95 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 97 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 93 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 94 — create or update the focused repair issue for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 94 issue URL | `{{TASK_094_ISSUE_URL}}` | Produced by this task; replace {{TASK_094_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 93 completion record

~~~markdown
# Task 93 completion record

{{TASK_093_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 93 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 93's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 95 — commence the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 94 issue URL | `{{TASK_094_ISSUE_URL}}` | Produced by Task 94; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 94 completion record

~~~markdown
# Task 94 completion record

{{TASK_094_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 94 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 94 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 96 — implement the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 94 issue URL | `{{TASK_094_ISSUE_URL}}` | Produced by Task 94; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 95 completion record

~~~markdown
# Task 95 completion record

{{TASK_095_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 95 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 95 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 97 — create or update the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 94 issue URL | `{{TASK_094_ISSUE_URL}}` | Produced by Task 94; resolve globally before execution |
| Task 97 PR URL | `{{TASK_097_PR_URL}}` | Produced by this task; replace {{TASK_097_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 96 completion record

~~~markdown
# Task 96 completion record

{{TASK_096_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 96 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 96 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 98 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 97 PR URL | `{{TASK_097_PR_URL}}` | Produced by Task 97; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 97 completion record

~~~markdown
# Task 97 completion record

{{TASK_097_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 97 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 97 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 97's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 98 for the new head before Task 99. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 99 — run the independent final quality check on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 97 PR URL | `{{TASK_097_PR_URL}}` | Produced by Task 97; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 98 completion record

~~~markdown
# Task 98 completion record

{{TASK_098_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 98 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 98 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 97's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 98 for the new head, and then repeat Task 99 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 100 — merge the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 97 PR URL | `{{TASK_097_PR_URL}}` | Produced by Task 97; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 99 completion record

~~~markdown
# Task 99 completion record

{{TASK_099_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 99 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 99 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 101 — publish the landed repair handoff for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 97 PR URL | `{{TASK_097_PR_URL}}` | Produced by Task 97; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 100 completion record

~~~markdown
# Task 100 completion record

{{TASK_100_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 100 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 100 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 102 — reverse-compare cycle 5 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 101 completion record

~~~markdown
# Task 101 completion record

{{TASK_101_COMPLETION_RECORD}}
~~~

#### Task 92 completion record

~~~markdown
# Task 92 completion record

{{TASK_092_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 101 | `FS` | Use only the final landed commits from Tasks 92 and 127. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 102 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 102 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 103 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 104 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 106 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 111 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 105 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 112 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 112 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 103 — create or update the focused repair issue for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 103 issue URL | `{{TASK_103_ISSUE_URL}}` | Produced by this task; replace {{TASK_103_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 102 completion record

~~~markdown
# Task 102 completion record

{{TASK_102_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 102 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 102's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 104 — commence the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 103 issue URL | `{{TASK_103_ISSUE_URL}}` | Produced by Task 103; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 103 completion record

~~~markdown
# Task 103 completion record

{{TASK_103_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 103 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 103 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 105 — implement the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 103 issue URL | `{{TASK_103_ISSUE_URL}}` | Produced by Task 103; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 104 completion record

~~~markdown
# Task 104 completion record

{{TASK_104_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 104 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 104 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 106 — create or update the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 103 issue URL | `{{TASK_103_ISSUE_URL}}` | Produced by Task 103; resolve globally before execution |
| Task 106 PR URL | `{{TASK_106_PR_URL}}` | Produced by this task; replace {{TASK_106_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 105 completion record

~~~markdown
# Task 105 completion record

{{TASK_105_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 105 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 105 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 107 — run the Anthropic Claude Code review loop on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 PR URL | `{{TASK_106_PR_URL}}` | Produced by Task 106; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 106 completion record

~~~markdown
# Task 106 completion record

{{TASK_106_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 106 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 106 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 106's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 107 for the new head before Task 108. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 108 — run the independent final quality check on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 PR URL | `{{TASK_106_PR_URL}}` | Produced by Task 106; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 107 completion record

~~~markdown
# Task 107 completion record

{{TASK_107_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 107 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 107 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 106's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 107 for the new head, and then repeat Task 108 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 109 — merge the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 PR URL | `{{TASK_106_PR_URL}}` | Produced by Task 106; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 108 completion record

~~~markdown
# Task 108 completion record

{{TASK_108_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 108 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 108 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 110 — publish the landed repair handoff for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 PR URL | `{{TASK_106_PR_URL}}` | Produced by Task 106; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 109 completion record

~~~markdown
# Task 109 completion record

{{TASK_109_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 109 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 109 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 111 — recheck the reciprocal result for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 102 completion record

~~~markdown
# Task 102 completion record

{{TASK_102_COMPLETION_RECORD}}
~~~

#### Task 110 completion record

~~~markdown
# Task 110 completion record

{{TASK_110_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 102 | `FS` | Use the original pinned comparison and branch result. |
| Task 110 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 96–110 as a new numbered-range instance. After its new handoff, repeat Task 111.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 112 — publish the fixed-point closure for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 111 completion record

~~~markdown
# Task 111 completion record

{{TASK_111_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 111 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 111 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 113 — close the Terraform initial-convergence tracker

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #31 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/31` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 112 completion record

~~~markdown
# Task 112 completion record

{{TASK_112_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 112 | `FS` | All five initial reciprocal cycles have permanent closure records. |

### Objective

Complete only this leaf action: close the Terraform initial-convergence tracker. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Tracker contract

Terraform #31 retains evidence; it does not authorize an aggregate implementation PR. Its five ordered records are:

1. PS infrastructure conformance from Terraform PR #30.
2. PS generator and path convergence from Terraform PRs #26 and #30.
3. PS workflow and policy convergence from Terraform PRs #26 and #30.
4. PS #158 supply-freeze adaptation from Terraform PR #27.
5. PS Claude-command parity from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`.

### Procedure

1. Re-query Terraform #31.
2. Verify that each cycle record contains the PS issue and landed PR; source and destination commits and blobs; a complete foundation matrix or justified reduced matrix; every focused Terraform sync issue and PR; the reverse PS comparison; validation and intentional differences; and the permanent fixed-point result.
3. Reconcile the tracker against PS #159 and PS #163. Correct a record only with verified evidence.
4. Close Terraform #31. Do not create or merge an aggregate foundation implementation PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

### Complete when

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

## Task 114 — commence Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 113 completion record

~~~markdown
# Task 113 completion record

{{TASK_113_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 113 | `FS` | The five initial cycles are closed and the final Terraform baseline is known. |

Terraform #21 exists, but its creation did not start implementation. Do not start Terraform #22 or #24 in this task.

### Objective

Explicitly commence only the implementation governed by implement Terraform #21 from the landed PS P1A source. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 113 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Verify the five Task 113 closure records and the free implementation slot.
2. Explicitly commence only Terraform #21.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 115 — implement Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 114 completion record

~~~markdown
# Task 114 completion record

{{TASK_114_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 114 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #21 from the landed PS P1A source without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 113 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Re-query Terraform #21. Record its four source blobs, four destination paths, source and destination identities, and expected repository-specific differences.
2. Classify the capability as common.
3. Adapt the landed PS implementation. Do not redesign it independently or introduce a shared dependency.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 116 — create or update the candidate PR for Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 116 PR URL | `{{TASK_116_PR_URL}}` | Produced by this task; replace {{TASK_116_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 115 completion record

~~~markdown
# Task 115 completion record

{{TASK_115_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 115 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #21 from the landed PS P1A source; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 113 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Create or update one focused Terraform #21 PR. Run initial local validation and required checks.
2. Record its base, candidate head/tree, affected paths and destination blobs, validation, runtime identities, and issue links. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

### Complete when

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

## Task 117 — run the Anthropic Claude Code review loop on the Terraform #21 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 116 PR URL | `{{TASK_116_PR_URL}}` | Produced by Task 116; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 116 completion record

~~~markdown
# Task 116 completion record

{{TASK_116_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 116 | `FS` | The Terraform #21 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide against the Terraform #21 PR. Read that repository's current root `CLAUDE.md`. Do not merge. If the loop changes bytes, rerun the Task 116 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 118 — run the independent final quality check on the Terraform #21 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 116 PR URL | `{{TASK_116_PR_URL}}` | Produced by Task 116; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 117 completion record

~~~markdown
# Task 117 completion record

{{TASK_117_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 117 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require all Terraform #21 requirements, four-path scope, 115/141-row allocation, comments, deferrals, PR-body accuracy, validation, checks, and mergeability. Return to Task 117 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 117.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 117.

## Task 119 — merge the Terraform #21 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 116 PR URL | `{{TASK_116_PR_URL}}` | Produced by Task 116; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 118 completion record

~~~markdown
# Task 118 completion record

{{TASK_118_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 118 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 120 — publish the landed handoff for the Terraform #21 implementation

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 116 PR URL | `{{TASK_116_PR_URL}}` | Produced by Task 116; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 119 completion record

~~~markdown
# Task 119 completion record

{{TASK_119_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 119 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

### Complete when

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

## Task 121 — reverse-compare Terraform #21 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 120 completion record

~~~markdown
# Task 120 completion record

{{TASK_120_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 120 | `FS` | Use the landed Terraform #21 result and immutable PS P1A commit. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 121 | Replace every placeholder in the reverse-comparison record with a verified identity. |
| Task 121 | Read PS only from `2d56357d9f52c76734027174bf62278e6f3d4cd6` and Terraform only from the Task 120 landed commit. Record every compared blob and map files by role. |
| Task 121 | Complete every applicable foundation row and the exact four-file mapping. Preserve the stated 115-row PS and 141-row Terraform allocations. |
| Task 121 | If no common difference remains, make no PS change and post a pinned reverse-comparison record. |
| Task 122 | If Terraform added or corrected common behavior, create or update one focused PS sync-back issue. Pin both landed commits and blobs. |
| Task 123 | Start the sync-back only after issue setup and a free implementation slot. Prepare one focused PS PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 121 | If PS changes, compare its new landed commit in Terraform. For a required Terraform repair, run the same four task-local conditional repair PR lifecycle actions. |
| Task 121 | Stop for a new decision process if a row changes direction twice. |
| Task 121 | Post the current reciprocal evidence on Terraform #21 and the PS P1A handoff. Do not close Terraform #21 until Task 140 confirms the final Terraform state. |
| Task 121 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 121 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 122 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 123 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 125 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 121 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 124 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 121 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 121 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 122 — create or update the focused repair issue for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 121 completion record

~~~markdown
# Task 121 completion record

{{TASK_121_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 121 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 121's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 123 — commence the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 122 completion record

~~~markdown
# Task 122 completion record

{{TASK_122_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 122 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 122 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 124 — implement the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 123 completion record

~~~markdown
# Task 123 completion record

{{TASK_123_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 123 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 123 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 125 — create or update the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 125 PR URL | `{{TASK_125_PR_URL}}` | Produced by this task; replace {{TASK_125_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 124 completion record

~~~markdown
# Task 124 completion record

{{TASK_124_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 124 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 124 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 126 — run the Anthropic Claude Code review loop on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 125 PR URL | `{{TASK_125_PR_URL}}` | Produced by Task 125; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 125 completion record

~~~markdown
# Task 125 completion record

{{TASK_125_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 125 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 125 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 125's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 126 for the new head before Task 127. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 127 — run the independent final quality check on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 125 PR URL | `{{TASK_125_PR_URL}}` | Produced by Task 125; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 126 completion record

~~~markdown
# Task 126 completion record

{{TASK_126_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 126 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 126 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 125's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 126 for the new head, and then repeat Task 127 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 128 — merge the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 125 PR URL | `{{TASK_125_PR_URL}}` | Produced by Task 125; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 127 completion record

~~~markdown
# Task 127 completion record

{{TASK_127_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 127 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 127 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 129 — publish the landed repair handoff for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 125 PR URL | `{{TASK_125_PR_URL}}` | Produced by Task 125; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 128 completion record

~~~markdown
# Task 128 completion record

{{TASK_128_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 128 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 128 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 130 — perform the final Terraform recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 129 completion record

~~~markdown
# Task 129 completion record

{{TASK_129_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 129 | `FS` | Use the final landed PS state from Task 129. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 130 | Compare the final Task 129 PS landed commit against Terraform. |
| Task 131 | If a common difference remains, create a focused Terraform issue before implementation. Use the task-local execution controls, conditional repair PR lifecycle, and final reciprocal-recheck controls below until the row is `same` or a proved intentional difference. |
| Task 130 | Verify that every common row is `same`, every intentional difference has complete evidence, and no row is a blocker. |
| Task 140 | Post one permanent closure record on Terraform #21 and the PS P1A handoff. Include both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, affected paths, source and destination blobs, final matrix digest, validation, runtime identities, review outcomes, and explicit non-applicability results. |
| Task 130 | Close Terraform #21. |
| Task 130 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 130 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 131 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 133 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 140 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 140 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 131 — create or update the focused repair issue for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 131 issue URL | `{{TASK_131_ISSUE_URL}}` | Produced by this task; replace {{TASK_131_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 130 completion record

~~~markdown
# Task 130 completion record

{{TASK_130_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 130 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 130's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 132 — commence the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 131 issue URL | `{{TASK_131_ISSUE_URL}}` | Produced by Task 131; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 131 completion record

~~~markdown
# Task 131 completion record

{{TASK_131_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 131 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 131 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 133 — implement the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 131 issue URL | `{{TASK_131_ISSUE_URL}}` | Produced by Task 131; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 132 completion record

~~~markdown
# Task 132 completion record

{{TASK_132_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 132 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 132 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 134 — create or update the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 131 issue URL | `{{TASK_131_ISSUE_URL}}` | Produced by Task 131; resolve globally before execution |
| Task 134 PR URL | `{{TASK_134_PR_URL}}` | Produced by this task; replace {{TASK_134_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 133 completion record

~~~markdown
# Task 133 completion record

{{TASK_133_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 133 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 133 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 135 — run the Anthropic Claude Code review loop on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 134 PR URL | `{{TASK_134_PR_URL}}` | Produced by Task 134; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 134 completion record

~~~markdown
# Task 134 completion record

{{TASK_134_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 134 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 134 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 134's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 135 for the new head before Task 136. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 136 — run the independent final quality check on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 134 PR URL | `{{TASK_134_PR_URL}}` | Produced by Task 134; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 135 completion record

~~~markdown
# Task 135 completion record

{{TASK_135_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 135 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 135 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 134's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 135 for the new head, and then repeat Task 136 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 137 — merge the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 134 PR URL | `{{TASK_134_PR_URL}}` | Produced by Task 134; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 136 completion record

~~~markdown
# Task 136 completion record

{{TASK_136_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 136 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 136 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 138 — publish the landed repair handoff for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 134 PR URL | `{{TASK_134_PR_URL}}` | Produced by Task 134; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 137 completion record

~~~markdown
# Task 137 completion record

{{TASK_137_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 137 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 137 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 139 — recheck the reciprocal result for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 130 completion record

~~~markdown
# Task 130 completion record

{{TASK_130_COMPLETION_RECORD}}
~~~

#### Task 138 completion record

~~~markdown
# Task 138 completion record

{{TASK_138_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 130 | `FS` | Use the original pinned comparison and branch result. |
| Task 138 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 124–138 as a new numbered-range instance. After its new handoff, repeat Task 139.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 140 — publish the fixed-point closure for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 139 completion record

~~~markdown
# Task 139 completion record

{{TASK_139_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 139 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 139 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 141 — inventory all untracked cross-repository differences

> **Execution class: Coding agent executable.** This is read-only analysis and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 140 completion record

~~~markdown
# Task 140 completion record

{{TASK_140_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 140 | `FS` | The P1A/Terraform #21 reciprocal cycle is closed. |

### Objective

Complete only this leaf action: inventory all untracked cross-repository differences. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch both current `main` commits and trees in clean checkouts. Pin them as the sweep baselines.
2. Inventory every path that controls generation, validation, workflow policy, Actions permissions and pins, Node/npm acquisition, Markdown lint, Git/path checks, supply-freeze evidence, Dependabot, agent review commands, issue handoffs, and contributor instructions.
3. Map files by role. Do not infer role equality or difference from filenames alone.
4. For every common role, compare bytes and semantics, success and negative cases, failure classification, cleanup, credentials, permissions, native statuses, platform coverage, evidence retention, documentation, and validation commands.
5. Run the full foundation catalog for foundation roles. Use a documented reduced matrix only for a narrow non-foundation role.
6. Classify each row as `same`, `intentional difference`, `tracked work`, or `untracked blocker`.
7. Accept an intentional difference only under the task-local intentional-difference evidence requirements below. Link the owning issue for every `tracked work` row.
8. Treat a missing, duplicate, unknown, renamed, empty, or unexplained row as an `untracked blocker`.
9. For each untracked blocker, validate the finding and complete the required finding-specific decision process before proposing a repair.
10. Publish a read-only inventory that identifies the behind repository, affected role and blobs, evidence, owner/disposition, and proposed focused issue for each blocker. Do not edit implementation files in this task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

### Complete when

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

## Task 142 — assess one untracked-blocker reciprocal cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 141 completion record

~~~markdown
# Task 141 completion record

{{TASK_141_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 141 | `FS` | The complete read-only inventory and ordered blocker list exist. |
| Prior completed instance of Tasks 135–152 | `FS` | Close the prior repair cycle before the next instance starts. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 142 | Select the next untracked blocker in the published order. |
| Task 143 | Create or update one focused issue in the behind repository. Pin the other repository's landed commit and source blobs. |
| Task 144 | Issue creation does not start implementation. Start only after issue setup and a free implementation slot. |
| Task 142 | Keep both repositories self-contained. Do not combine unrelated fixes or change settings without exact authorization. |
| Task 146 | Prepare one focused repair PR. Run the candidate, Claude Code review-loop, independent final quality-check, and merge actions as discrete lifecycle steps. |
| Task 151 | Compare the landed repair in the other repository. Post a pinned no-change record or run the same four discrete steps for one focused reciprocal repair. |
| Task 142 | Run the reverse comparison. Stop for a new decision process if a row changes direction twice. |
| Task 152 | Close the cycle only when every common row is `same`, all intentional differences have complete evidence, and no blocker remains. |
| Task 152 | Post the permanent closure record with both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, matrix digest, validation, runtimes, review outcomes, and non-applicability results. |
| Task 142 | Repeat Task 152 only if another ordered blocker remains. |
| Task 142 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 142 | Map files by role, not only by filename. |
| Task 142 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 145 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 143 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 144 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 146 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 151 | Compare the landed destination result back against the source repository. |
| Task 143 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 146 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 145 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 152 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 152 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 143 — create or update the focused repair issue for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 143 issue URL | `{{TASK_143_ISSUE_URL}}` | Produced by this task; replace {{TASK_143_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 142 completion record

~~~markdown
# Task 142 completion record

{{TASK_142_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 142 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 142's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 144 — commence the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 143 issue URL | `{{TASK_143_ISSUE_URL}}` | Produced by Task 143; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 143 completion record

~~~markdown
# Task 143 completion record

{{TASK_143_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 143 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 143 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 145 — implement the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 143 issue URL | `{{TASK_143_ISSUE_URL}}` | Produced by Task 143; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 144 completion record

~~~markdown
# Task 144 completion record

{{TASK_144_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 144 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 144 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 146 — create or update the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 143 issue URL | `{{TASK_143_ISSUE_URL}}` | Produced by Task 143; resolve globally before execution |
| Task 146 PR URL | `{{TASK_146_PR_URL}}` | Produced by this task; replace {{TASK_146_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 145 completion record

~~~markdown
# Task 145 completion record

{{TASK_145_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 145 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 145 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 147 — run the Anthropic Claude Code review loop on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 146 PR URL | `{{TASK_146_PR_URL}}` | Produced by Task 146; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 146 completion record

~~~markdown
# Task 146 completion record

{{TASK_146_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 146 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 146 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 146's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 147 for the new head before Task 148. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 148 — run the independent final quality check on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 146 PR URL | `{{TASK_146_PR_URL}}` | Produced by Task 146; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 147 completion record

~~~markdown
# Task 147 completion record

{{TASK_147_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 147 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 147 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 146's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 147 for the new head, and then repeat Task 148 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 149 — merge the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 146 PR URL | `{{TASK_146_PR_URL}}` | Produced by Task 146; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 148 completion record

~~~markdown
# Task 148 completion record

{{TASK_148_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 148 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 148 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 150 — publish the landed repair handoff for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_142_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 142; resolve globally before execution |
| Task 146 PR URL | `{{TASK_146_PR_URL}}` | Produced by Task 146; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 149 completion record

~~~markdown
# Task 149 completion record

{{TASK_149_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 149 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 149 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 151 — recheck the reciprocal result for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 142 completion record

~~~markdown
# Task 142 completion record

{{TASK_142_COMPLETION_RECORD}}
~~~

#### Task 150 completion record

~~~markdown
# Task 150 completion record

{{TASK_150_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 142 | `FS` | Use the original pinned comparison and branch result. |
| Task 150 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 136–150 as a new numbered-range instance. After its new handoff, repeat Task 151.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 152 — publish the fixed-point closure for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 151 completion record

~~~markdown
# Task 151 completion record

{{TASK_151_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 151 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 151 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 153 — finalize the global consistency sweep

> **Execution class: Coding agent executable.** This is a verification and evidence-publication task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 141 completion record

~~~markdown
# Task 141 completion record

{{TASK_141_COMPLETION_RECORD}}
~~~

#### Task 152 completion record

~~~markdown
# Task 152 completion record

{{TASK_152_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 141 | `FS` | The inventory exists. |
| Every applicable Task 152 instance | `FS` | All untracked blocker cycles are closed. |

### Objective

Complete only this leaf action: finalize the global consistency sweep. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch both final `main` commits and trees.
2. Re-run the complete inventory, file-role map, foundation catalog, and every required reduced matrix.
3. Verify that every role has an owner and disposition, each `tracked work` row links its live planned issue, and no `untracked blocker` remains.
4. Post the final sweep record. Include final commits/trees, file/blob map, matrices and digests, validation, runtimes, intentional differences, explicit non-applicability results, and zero untracked blockers.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

### Complete when

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

## Task 154 — commence the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 153 completion record

~~~markdown
# Task 153 completion record

{{TASK_153_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 153 | `FS` | The global sweep proves zero untracked blockers. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P1B writer candidate and decision evidence. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify that the implementation slot is free. Explicitly start only #147.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 155 — prepare the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 154 completion record

~~~markdown
# Task 154 completion record

{{TASK_154_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 154 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P1B writer candidate and decision evidence without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-read the final PS foundation handoff and every landed contract.
2. Reconcile each PS #147 assumption with those contracts.
3. Implement the writer and terminal check `Build Style Guide Artifacts / approve_candidate`.
4. Prepare an amendment or replacement for `docs/decisions/0001-accept-in-repository-trust-root.md`.
5. Record workflow identity, permissions, required-check name, unique evidence ref, source pins, and rollback inputs.
6. Prepare the exact temporary and persistent settings requests and proof plan for PS #152.
7. Do not give the writer effective `contents: write` access before Task 160 records human approval.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 156 — create or update the candidate PR for the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 156 PR URL | `{{TASK_156_PR_URL}}` | Produced by this task; replace {{TASK_156_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 155 completion record

~~~markdown
# Task 155 completion record

{{TASK_155_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 155 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P1B writer candidate and decision evidence; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one PS #147 PR. Reach one locally validated candidate head. Record its exact head and tree.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

### Complete when

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

## Task 157 — prepare read-only PS #152 settings evidence

> **Execution class: Coding agent executable.** This is read-only preparation and starts after Task 156 completes.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 154 completion record

~~~markdown
# Task 154 completion record

{{TASK_154_COMPLETION_RECORD}}
~~~

#### Task 156 completion record

~~~markdown
# Task 156 completion record

{{TASK_156_COMPLETION_RECORD}}
~~~

#### Task 157 authorization record

~~~markdown
# Task 157 authorization record

{{TASK_157_AUTHORIZATION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 156 | `FS` | Begin after the candidate PR is complete and its exact identity is available in the Task 156 completion record. |

### Objective

Complete only this leaf action: prepare read-only PS #152 settings evidence. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #152 and current rules, classic branch protection, `main`, application identity, and writer permissions.
2. Preserve the Terraform PR #29 bypass-eligibility finding and the limited 2026-08-01 approval scope.
3. Map every requested temporary and persistent setting to the Task 156 candidate.
4. Prepare the non-mutating bypass-eligibility check, field-equivalent temporary-rule request, effective-bypass and rejection drills, evidence-retention plan, cleanup proof, exact-restoration proof, persistent-ruleset request, and rollback plan.
5. Identify every decision that needs owner or administrator approval. Do not infer approval from prior evidence.
6. Do not change a setting, create a rule, or give the writer effective write access.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

### Complete when

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

## Task 158 — run the Anthropic Claude Code review loop on the PS #147 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |
| Task 156 PR URL | `{{TASK_156_PR_URL}}` | Produced by Task 156; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 156 completion record

~~~markdown
# Task 156 completion record

{{TASK_156_COMPLETION_RECORD}}
~~~

#### Task 157 completion record

~~~markdown
# Task 157 completion record

{{TASK_157_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 156 | `FS` | The PS #147 candidate PR exists and initial checks pass. |
| Task 157 | `FS` | The read-only PS #152 evidence package is complete. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #147 PR. Do not merge or change repository settings. If the loop changes bytes or the writer design, rerun the applicable Task 156 validation and refresh Task 157 settings evidence before the next round.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

## Task 159 — run the independent final quality check on the PS #147 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |
| Task 156 PR URL | `{{TASK_156_PR_URL}}` | Produced by Task 156; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 158 completion record

~~~markdown
# Task 158 completion record

{{TASK_158_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 158 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require full PS #147 issue coverage; writer, evidence, lease/refspec, credential, terminal-result, rollback, comment, deferral, PR-body, validation, and mergeability checks; and consistency with the PS #152 settings request. Return to Task 158 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 158, and the settings evidence still applies.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 158, and the settings evidence still applies.

## Task 160 — approve or reject the P1B authority decisions

> **Execution class: Human execution required.** The repository owner or administrator must decide each item.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 157 completion record

~~~markdown
# Task 157 completion record

{{TASK_157_COMPLETION_RECORD}}
~~~

#### Task 159 completion record

~~~markdown
# Task 159 completion record

{{TASK_159_COMPLETION_RECORD}}
~~~

#### Task 160 authorization record

~~~markdown
# Task 160 authorization record

{{TASK_160_AUTHORIZATION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 157 | `FS` | Review the exact settings requests and proof plan. |
| Task 159 | `FS` | Review the exact candidate bytes and independent quality-check result. |

### Objective

Complete only this leaf action: approve or reject the P1B authority decisions. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Human actions:**

1. Review and approve or reject the proposed trust-root decision.
2. Review and approve or reject each temporary and persistent PS #152 settings request.
3. If integration ID `15368` is unavailable, select and approve another writer design.
4. Do not approve a substitute rule without also approving the writer design.
5. If the design permits human bypass, approve or reject that bypass in a separate decision.
6. Record every approval or rejection, condition, owner, canonical location, and expiry.
7. Do not reuse an approval for different bytes or settings.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 156 or Task 157 before work continues. After a candidate-byte change, rerun Tasks 158 and 185.

### Complete when

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 156 or Task 157 before work continues. After a candidate-byte change, rerun Tasks 158 and 185.

## Task 161 — prove and activate the approved PS #152 settings

> **Execution class: Coding agent executable.** Recorded human approval is a required input. Do not merge PS #147 in this task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 160 completion record

~~~markdown
# Task 160 completion record

{{TASK_160_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 160 | `FS` | Every applicable approval exists and applies to the current candidate and exact settings. |

### Objective

Complete only this leaf action: prove and activate the approved PS #152 settings. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify all approvals. Stop if one is absent, expired, conditional on unmet work, or for different bytes or settings.
2. Revalidate the reviewed PS #147 head.
3. Start the bounded temporary proof under PS #152.
4. Export current rules, classic protection, `main`, application identity, and writer permissions.
5. Perform the non-mutating bypass-eligibility check.
6. If integration ID `15368` is unavailable, stop and return to Task 160.
7. Create the approved temporary field-equivalent rule.
8. Run the effective-bypass and rejection drills. Retain the required evidence.
9. Delete the temporary rule and evidence ref. Prove exact restoration.
10. Revalidate the reviewed PS #147 head.
11. As the final settings action, create the approved persistent ruleset immediately before Task 163.
12. Retain the create response, ruleset ID, normalized JSON, digest, effective rules, exact required check and integration, `main` before and after the setting change, and rollback proof.
13. Close PS #152 only after the persistent rule is active and evidence is complete.
14. Do not copy Terraform ruleset IDs, digests, effective-rule JSON, or audit records.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

### Complete when

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

## Task 162 — merge PS P1B

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 156 PR URL | `{{TASK_156_PR_URL}}` | Produced by Task 156; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 161 completion record

~~~markdown
# Task 161 completion record

{{TASK_161_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 161 | `FS` | PS #152 is closed and the approved persistent rule is active. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Revalidate the reviewed PS #147 head, checks, workflow identity, required check, active ruleset, application identity, and approvals.
2. Stop if any byte, setting, identity, or approval differs from the proved state.
3. Apply the task-local merge gate below. Require the Task 158 terminal-clean record and Task 159 independent `PASS` record to match the current head and tree.
4. Merge the reviewed P1B PR and close PS #147.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 163 — publish the landed handoff for PS P1B

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 156 PR URL | `{{TASK_156_PR_URL}}` | Produced by Task 156; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 162 completion record

~~~markdown
# Task 162 completion record

{{TASK_162_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 162 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Post the permanent P1B handoff for Terraform #22. Include all source and landed identities, paths and blobs, permission and credential boundaries, candidate consumption, ancestry/lease/refspec and native-status behavior, failure and cleanup postconditions, ruleset evidence, rollback proof, validation, review-loop result, final quality-check result, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

### Complete when

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

## Task 164 — update Terraform #22 from the landed PS P1B handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 140 completion record

~~~markdown
# Task 140 completion record

{{TASK_140_COMPLETION_RECORD}}
~~~

#### Task 163 completion record

~~~markdown
# Task 163 completion record

{{TASK_163_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 163 | `FS` | Use the landed PS P1B commit and permanent handoff. |
| Task 140 | `FS` | Terraform #21 is closed. |

### Objective

Complete only this leaf action: update Terraform #22 from the landed PS P1B handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Issue update:**

Replace every placeholder with a verified identity and add or amend a `Starting point — reuse landed PSStyleGuide P1B` section in Terraform #22:

> PSStyleGuide P1B landed through `<PS_P1B_PR_URL>` at `<PS_P1B_LANDED_COMMIT>`, tree `<PS_P1B_LANDED_TREE>`. Use that landed implementation and permanent handoff `<PS_P1B_HANDOFF_URL>` as the T1B implementation source. Do not rebuild the writer, evidence protocol, lease/refspec checks, credential boundary, terminal result, or rollback design.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Record every PS source blob and every adapted Terraform destination blob. Change repository names, generated filenames, schema/type prefixes, check names, ruleset names, and local evidence identifiers. Preserve common permissions, job isolation, credential materialization, candidate consumption, ancestry/lease/refspec checks, native statuses, failure postconditions, cleanup, and audit truth.
>
> Use Terraform's own administrator issue and live settings evidence. Never copy PS ruleset IDs, request digests, application/rule responses, `main` values, or audit records. Refresh the reciprocal matrix at implementation start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

### Complete when

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

## Task 165 — commence Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 164 completion record

~~~markdown
# Task 164 completion record

{{TASK_164_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 164 | `FS` | The issue record is complete, Terraform #21 is closed, and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #22. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #22 from the landed PS P1B commit and final Terraform baseline.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 166 — implement Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 165 completion record

~~~markdown
# Task 165 completion record

{{TASK_165_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 165 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #22 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the writer and evidence protocol. Preserve the common controls named in Task 164.
2. Use only Terraform's authorized settings and live evidence.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 167 — create or update the candidate PR for Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 167 PR URL | `{{TASK_167_PR_URL}}` | Produced by this task; replace {{TASK_167_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 166 completion record

~~~markdown
# Task 166 completion record

{{TASK_166_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 166 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #22; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation. Treat unexplained weaker behavior as a blocker.
2. Create or update one focused Terraform #22 PR. Run initial validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

### Complete when

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

## Task 168 — run the Anthropic Claude Code review loop on the Terraform #22 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 167 PR URL | `{{TASK_167_PR_URL}}` | Produced by Task 167; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 167 completion record

~~~markdown
# Task 167 completion record

{{TASK_167_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 167 | `FS` | The Terraform #22 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide. Do not merge or change settings without exact authorization. If bytes change, rerun Task 167 validation and refresh the reciprocal matrix.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 169 — run the independent final quality check on the Terraform #22 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 167 PR URL | `{{TASK_167_PR_URL}}` | Produced by Task 167; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 168 completion record

~~~markdown
# Task 168 completion record

{{TASK_168_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 168 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #22 issue, writer, settings, permissions, credentials, evidence, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 168 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 168.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 168.

## Task 170 — merge the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 167 PR URL | `{{TASK_167_PR_URL}}` | Produced by Task 167; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 169 completion record

~~~markdown
# Task 169 completion record

{{TASK_169_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 169 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 171 — publish the landed handoff for the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 167 PR URL | `{{TASK_167_PR_URL}}` | Produced by Task 167; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 170 completion record

~~~markdown
# Task 170 completion record

{{TASK_170_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 170 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

### Complete when

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

## Task 172 — reverse-compare T1B in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 171 completion record

~~~markdown
# Task 171 completion record

{{TASK_171_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 171 | `FS` | Use only the landed P1B and T1B commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 172 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 172 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 173 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 174 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 176 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 172 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 175 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 172 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 172 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 173 — create or update the focused repair issue for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 173 issue URL | `{{TASK_173_ISSUE_URL}}` | Produced by this task; replace {{TASK_173_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 172 completion record

~~~markdown
# Task 172 completion record

{{TASK_172_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 172 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 172's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 174 — commence the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 173 issue URL | `{{TASK_173_ISSUE_URL}}` | Produced by Task 173; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 173 completion record

~~~markdown
# Task 173 completion record

{{TASK_173_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 173 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 173 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 175 — implement the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 173 issue URL | `{{TASK_173_ISSUE_URL}}` | Produced by Task 173; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 174 completion record

~~~markdown
# Task 174 completion record

{{TASK_174_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 174 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 174 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 176 — create or update the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 173 issue URL | `{{TASK_173_ISSUE_URL}}` | Produced by Task 173; resolve globally before execution |
| Task 176 PR URL | `{{TASK_176_PR_URL}}` | Produced by this task; replace {{TASK_176_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 175 completion record

~~~markdown
# Task 175 completion record

{{TASK_175_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 175 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 175 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 177 — run the Anthropic Claude Code review loop on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 176 PR URL | `{{TASK_176_PR_URL}}` | Produced by Task 176; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 176 completion record

~~~markdown
# Task 176 completion record

{{TASK_176_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 176 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 176 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 176's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 177 for the new head before Task 178. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 178 — run the independent final quality check on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 176 PR URL | `{{TASK_176_PR_URL}}` | Produced by Task 176; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 177 completion record

~~~markdown
# Task 177 completion record

{{TASK_177_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 177 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 177 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 176's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 177 for the new head, and then repeat Task 178 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 179 — merge the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 176 PR URL | `{{TASK_176_PR_URL}}` | Produced by Task 176; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 178 completion record

~~~markdown
# Task 178 completion record

{{TASK_178_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 178 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 178 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 180 — publish the landed repair handoff for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 176 PR URL | `{{TASK_176_PR_URL}}` | Produced by Task 176; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 179 completion record

~~~markdown
# Task 179 completion record

{{TASK_179_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 179 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 179 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 181 — perform the final Terraform T1B recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 180 completion record

~~~markdown
# Task 180 completion record

{{TASK_180_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 180 | `FS` | Use the final landed PS state from Task 180. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 181 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 181 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 182 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 184 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 191 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 191 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 182 — create or update the focused repair issue for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 182 issue URL | `{{TASK_182_ISSUE_URL}}` | Produced by this task; replace {{TASK_182_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 181 completion record

~~~markdown
# Task 181 completion record

{{TASK_181_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 181 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 181's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 183 — commence the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 182 issue URL | `{{TASK_182_ISSUE_URL}}` | Produced by Task 182; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 182 completion record

~~~markdown
# Task 182 completion record

{{TASK_182_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 182 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 182 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 184 — implement the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 182 issue URL | `{{TASK_182_ISSUE_URL}}` | Produced by Task 182; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 183 completion record

~~~markdown
# Task 183 completion record

{{TASK_183_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 183 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 183 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 185 — create or update the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 182 issue URL | `{{TASK_182_ISSUE_URL}}` | Produced by Task 182; resolve globally before execution |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by this task; replace {{TASK_185_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 184 completion record

~~~markdown
# Task 184 completion record

{{TASK_184_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 184 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 184 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 186 — run the Anthropic Claude Code review loop on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 185 completion record

~~~markdown
# Task 185 completion record

{{TASK_185_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 185 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 185 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 185's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 186 for the new head before Task 187. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 187 — run the independent final quality check on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 186 completion record

~~~markdown
# Task 186 completion record

{{TASK_186_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 186 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 186 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 185's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 186 for the new head, and then repeat Task 187 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 188 — merge the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 187 completion record

~~~markdown
# Task 187 completion record

{{TASK_187_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 187 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 187 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 189 — publish the landed repair handoff for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 188 completion record

~~~markdown
# Task 188 completion record

{{TASK_188_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 188 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 188 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 190 — recheck the reciprocal result for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 181 completion record

~~~markdown
# Task 181 completion record

{{TASK_181_COMPLETION_RECORD}}
~~~

#### Task 189 completion record

~~~markdown
# Task 189 completion record

{{TASK_189_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 181 | `FS` | Use the original pinned comparison and branch result. |
| Task 189 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 175–189 as a new numbered-range instance. After its new handoff, repeat Task 190.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 191 — publish the fixed-point closure for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 190 completion record

~~~markdown
# Task 190 completion record

{{TASK_190_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 190 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 190 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 192 — commence PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 191 completion record

~~~markdown
# Task 191 completion record

{{TASK_191_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 191 | `FS` | The P1B/T1B cycle is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement PS #148. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only PS #148.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 193 — implement PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 192 completion record

~~~markdown
# Task 192 completion record

{{TASK_192_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 192 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #148 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #148 and its native dependency. Classify the capability and pin all landed inputs and affected paths.
2. Implement the issue contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 194 — create or update the candidate PR for PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by this task; replace {{TASK_194_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 193 completion record

~~~markdown
# Task 193 completion record

{{TASK_193_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 193 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #148; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused PS #148 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

### Complete when

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

## Task 195 — run the Anthropic Claude Code review loop on the PS #148 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 194 completion record

~~~markdown
# Task 194 completion record

{{TASK_194_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 194 | `FS` | The PS #148 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #148 PR. Do not merge. If bytes change, rerun Task 194 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 196 — run the independent final quality check on the PS #148 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 195 completion record

~~~markdown
# Task 195 completion record

{{TASK_195_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 195 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #148 issue-contract, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 195 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 195.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 195.

## Task 197 — merge the PS #148 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 196 completion record

~~~markdown
# Task 196 completion record

{{TASK_196_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 196 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 198 — publish the landed handoff for the PS #148 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 197 completion record

~~~markdown
# Task 197 completion record

{{TASK_197_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 197 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

### Complete when

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

## Task 199 — compare PS #148 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 198 completion record

~~~markdown
# Task 198 completion record

{{TASK_198_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 198 | `FS` | Use the final landed PS #148 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 199 | Compare the affected PS #148 role with Terraform. |
| Task 200 | If common behavior is absent or weaker, create one focused Terraform issue from the landed PS handoff, then run the four task-local conditional repair PR lifecycle actions. |
| Task 199 | If Terraform is already equivalent, post a pinned no-change record. |
| Task 199 | If the blank-line example is PowerShell-specific, post a pinned non-applicability record. |
| Task 199 | Do not create an issue only for symmetry. |
| Task 199 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 199 | Map files by role, not only by filename. |
| Task 199 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 202 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 200 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 201 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 203 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 199 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 200 — create or update the focused repair issue for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 199 completion record

~~~markdown
# Task 199 completion record

{{TASK_199_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 199 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 199's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 201 — commence the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 200 completion record

~~~markdown
# Task 200 completion record

{{TASK_200_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 200 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 200 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 202 — implement the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 201 completion record

~~~markdown
# Task 201 completion record

{{TASK_201_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 201 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 201 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 203 — create or update the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by this task; replace {{TASK_203_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 202 completion record

~~~markdown
# Task 202 completion record

{{TASK_202_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 202 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 202 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 204 — run the Anthropic Claude Code review loop on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 203 completion record

~~~markdown
# Task 203 completion record

{{TASK_203_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 203 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 203 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 203's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 204 for the new head before Task 205. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 205 — run the independent final quality check on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 204 completion record

~~~markdown
# Task 204 completion record

{{TASK_204_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 204 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 204 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 203's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 204 for the new head, and then repeat Task 205 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 206 — merge the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 205 completion record

~~~markdown
# Task 205 completion record

{{TASK_205_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 205 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 205 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 207 — publish the landed repair handoff for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 206 completion record

~~~markdown
# Task 206 completion record

{{TASK_206_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 206 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 206 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 208 — reverse-compare the PS #148 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 207 completion record

~~~markdown
# Task 207 completion record

{{TASK_207_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 207 | `FS` | Use the final landed or no-change result from Task 207. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 208 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 208 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 209 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 210 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 212 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 217 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 211 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 218 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 218 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 209 — create or update the focused repair issue for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 208 completion record

~~~markdown
# Task 208 completion record

{{TASK_208_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 208 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 208's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 210 — commence the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 209 completion record

~~~markdown
# Task 209 completion record

{{TASK_209_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 209 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 209 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 211 — implement the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 210 completion record

~~~markdown
# Task 210 completion record

{{TASK_210_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 210 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 210 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 212 — create or update the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 212 PR URL | `{{TASK_212_PR_URL}}` | Produced by this task; replace {{TASK_212_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 211 completion record

~~~markdown
# Task 211 completion record

{{TASK_211_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 211 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 211 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 213 — run the Anthropic Claude Code review loop on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 212 PR URL | `{{TASK_212_PR_URL}}` | Produced by Task 212; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 212 completion record

~~~markdown
# Task 212 completion record

{{TASK_212_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 212 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 212 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 212's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 213 for the new head before Task 214. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 214 — run the independent final quality check on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 212 PR URL | `{{TASK_212_PR_URL}}` | Produced by Task 212; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 213 completion record

~~~markdown
# Task 213 completion record

{{TASK_213_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 213 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 213 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 212's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 213 for the new head, and then repeat Task 214 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 215 — merge the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 212 PR URL | `{{TASK_212_PR_URL}}` | Produced by Task 212; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 214 completion record

~~~markdown
# Task 214 completion record

{{TASK_214_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 214 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 214 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 216 — publish the landed repair handoff for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 212 PR URL | `{{TASK_212_PR_URL}}` | Produced by Task 212; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 215 completion record

~~~markdown
# Task 215 completion record

{{TASK_215_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 215 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 215 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 217 — recheck the reciprocal result for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 208 completion record

~~~markdown
# Task 208 completion record

{{TASK_208_COMPLETION_RECORD}}
~~~

#### Task 216 completion record

~~~markdown
# Task 216 completion record

{{TASK_216_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 208 | `FS` | Use the original pinned comparison and branch result. |
| Task 216 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 202–216 as a new numbered-range instance. After its new handoff, repeat Task 217.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 218 — publish the fixed-point closure for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 217 completion record

~~~markdown
# Task 217 completion record

{{TASK_217_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 217 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 217 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 219 — commence Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 171 completion record

~~~markdown
# Task 171 completion record

{{TASK_171_COMPLETION_RECORD}}
~~~

#### Task 218 completion record

~~~markdown
# Task 218 completion record

{{TASK_218_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 218 | `FS` | The PS #148 cycle is closed and the implementation slot is free. |
| Task 171 | `FS` | Terraform #22, the native predecessor of #23, is closed. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #23. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #23.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 220 — implement Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 219 completion record

~~~markdown
# Task 219 completion record

{{TASK_219_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 219 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #23 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query Terraform #23 and verify its native prerequisites.
2. Classify it as Terraform-specific unless comparison proves a common PS role.
3. Implement its state-version recovery contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 221 — create or update the candidate PR for Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 221 PR URL | `{{TASK_221_PR_URL}}` | Produced by this task; replace {{TASK_221_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 220 completion record

~~~markdown
# Task 220 completion record

{{TASK_220_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 220 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #23; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused Terraform #23 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

### Complete when

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

## Task 222 — run the Anthropic Claude Code review loop on the Terraform #23 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 221 PR URL | `{{TASK_221_PR_URL}}` | Produced by Task 221; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 221 completion record

~~~markdown
# Task 221 completion record

{{TASK_221_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 221 | `FS` | The Terraform #23 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 221 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 223 — run the independent final quality check on the Terraform #23 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 221 PR URL | `{{TASK_221_PR_URL}}` | Produced by Task 221; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 222 completion record

~~~markdown
# Task 222 completion record

{{TASK_222_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 222 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #23 state-version recovery, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 222 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 222.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 222.

## Task 224 — merge the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 221 PR URL | `{{TASK_221_PR_URL}}` | Produced by Task 221; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 223 completion record

~~~markdown
# Task 223 completion record

{{TASK_223_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 223 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 225 — publish the landed handoff for the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 221 PR URL | `{{TASK_221_PR_URL}}` | Produced by Task 221; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 224 completion record

~~~markdown
# Task 224 completion record

{{TASK_224_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 224 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

### Complete when

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

## Task 226 — evaluate Terraform #23 applicability in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 225 completion record

~~~markdown
# Task 225 completion record

{{TASK_225_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 225 | `FS` | Use the landed Terraform #23 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 226 | Search PS for a role affected by Terraform #23's state-version recovery behavior. |
| Task 227 | If PS has a common role and is absent or weaker, create one focused PS issue, then use the task-local execution controls, conditional repair PR lifecycle, and reciprocal fixed-point controls below. |
| Task 226 | If PS is already equivalent, post a pinned no-change record. |
| Task 226 | If PS has no corresponding role, post a pinned non-applicability record. |
| Task 226 | Do not invent a PS issue only for symmetry. |
| Task 226 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 226 | Map files by role, not only by filename. |
| Task 226 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 229 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 227 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 228 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 230 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 235 | Compare the landed destination result back against the source repository. |
| Task 227 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 230 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 229 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 236 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 236 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 227 — create or update the focused repair issue for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 226 completion record

~~~markdown
# Task 226 completion record

{{TASK_226_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 226 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 226's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 228 — commence the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 227 completion record

~~~markdown
# Task 227 completion record

{{TASK_227_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 227 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 227 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 229 — implement the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 228 completion record

~~~markdown
# Task 228 completion record

{{TASK_228_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 228 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 228 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 230 — create or update the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 230 PR URL | `{{TASK_230_PR_URL}}` | Produced by this task; replace {{TASK_230_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 229 completion record

~~~markdown
# Task 229 completion record

{{TASK_229_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 229 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 229 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 231 — run the Anthropic Claude Code review loop on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 230 PR URL | `{{TASK_230_PR_URL}}` | Produced by Task 230; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 230 completion record

~~~markdown
# Task 230 completion record

{{TASK_230_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 230 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 230 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 230's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 231 for the new head before Task 232. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 232 — run the independent final quality check on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 230 PR URL | `{{TASK_230_PR_URL}}` | Produced by Task 230; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 231 completion record

~~~markdown
# Task 231 completion record

{{TASK_231_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 231 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 231 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 230's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 231 for the new head, and then repeat Task 232 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 233 — merge the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 230 PR URL | `{{TASK_230_PR_URL}}` | Produced by Task 230; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 232 completion record

~~~markdown
# Task 232 completion record

{{TASK_232_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 232 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 232 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 234 — publish the landed repair handoff for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 230 PR URL | `{{TASK_230_PR_URL}}` | Produced by Task 230; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 233 completion record

~~~markdown
# Task 233 completion record

{{TASK_233_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 233 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 233 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 235 — recheck the reciprocal result for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 226 completion record

~~~markdown
# Task 226 completion record

{{TASK_226_COMPLETION_RECORD}}
~~~

#### Task 234 completion record

~~~markdown
# Task 234 completion record

{{TASK_234_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 226 | `FS` | Use the original pinned comparison and branch result. |
| Task 234 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 220–234 as a new numbered-range instance. After its new handoff, repeat Task 235.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 236 — publish the fixed-point closure for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 235 completion record

~~~markdown
# Task 235 completion record

{{TASK_235_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 235 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 235 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 237 — commence the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 163 completion record

~~~markdown
# Task 163 completion record

{{TASK_163_COMPLETION_RECORD}}
~~~

#### Task 198 completion record

~~~markdown
# Task 198 completion record

{{TASK_198_COMPLETION_RECORD}}
~~~

#### Task 236 completion record

~~~markdown
# Task 236 completion record

{{TASK_236_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 236 | `FS` | Terraform #23 and its PS applicability cycle are complete. |
| Task 163 | `FS` | The P1B PS handoff exists. |
| Task 198 | `FS` | The P2 PS handoff exists. |
| Completed PS #145 and #146 | `FS` | The P1 and P1A handoffs exist. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P3 candidate for merge. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #149.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 238 — prepare the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 237 completion record

~~~markdown
# Task 237 completion record

{{TASK_237_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 237 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P3 candidate for merge without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the P1, P1A, P1B, and P2 landed handoffs and all native issue dependencies.
2. Re-query PS #149, Terraform #24, and current dependency, package-manager, audit, process-runner, exception, Husky, workflow, and evidence contracts.
3. Pin every source commit and blob. Record intended paths and expected repository-specific differences.
4. Implement the complete PS #149 contract. Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, failure truth, and evidence retention.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 239 — create or update the candidate PR for the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by this task; replace {{TASK_239_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 238 completion record

~~~markdown
# Task 238 completion record

{{TASK_238_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 238 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P3 candidate for merge; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Create or update one PS #149 PR. Reach one locally validated candidate head. Record its base, head/tree, checks, paths and blobs, validation, runtime identities, decision-record changes, and rollback inputs.
2. Stop before review-loop execution or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #149 has one locally validated candidate PR and a verified evidence package.

### Complete when

PS #149 has one locally validated candidate PR and a verified evidence package.

## Task 240 — run the Anthropic Claude Code review loop on the PS #149 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 239 completion record

~~~markdown
# Task 239 completion record

{{TASK_239_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 239 | `FS` | The PS #149 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #149 PR. Do not merge. If bytes change, rerun Task 239 validation and refresh its evidence package.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 241 — run the independent final quality check on the PS #149 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 240 completion record

~~~markdown
# Task 240 completion record

{{TASK_240_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 240 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #149 governance, dependency, audit, Husky, exception, scheduled/manual behavior, issue, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 240 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 240. The PR is eligible for the checkpoint decision but remains unmerged.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 240. The PR is eligible for the checkpoint decision but remains unmerged.

## Task 242 — check the PS #149 advisory-deadline state

> **Execution class: Coding agent executable.** Run this task at exactly `2026-08-21T23:59:59Z`. It is a status check, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | Calendar constraint | Run at `2026-08-21T23:59:59Z` even if an earlier numbered task is incomplete. |

The PS P1 advisory decision expires at `2026-08-30T23:59:59Z`. The checkpoint is a coordination gate, not a native issue dependency.

### Objective

Complete only this leaf action: check the PS #149 advisory-deadline state. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #149 and the P1, P1A, P1B, and P2 handoffs.
2. Record each issue and PR state, reviewed head, checks, dependency state, and merge readiness.
3. If PS #149 is merge-ready, record that its current merge path is available for Task 243.
4. If PS #149 is not merge-ready, prepare the fallback evidence package for Task 243.
5. Record incomplete earlier tasks explicitly. Do not wait beyond the checkpoint and do not create a false PS #149 dependency.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

### Complete when

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

## Task 243 — select and approve the PS #149 deadline path

> **Execution class: Human execution required.** The repository owner must select the deadline path.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 242 completion record

~~~markdown
# Task 242 completion record

{{TASK_242_COMPLETION_RECORD}}
~~~

#### Task 243 authorization record

~~~markdown
# Task 243 authorization record

{{TASK_243_AUTHORIZATION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 242 | `FS` | Use the exact timestamped checkpoint evidence. |

### Objective

Complete only this leaf action: select and approve the PS #149 deadline path. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Human actions:**

1. If PS #149 is merge-ready, approve or reject its existing merge path.
2. If PS #149 is not merge-ready, approve or reject a separate superseding-decision task.
3. If the superseding task is approved, set its start date to `2026-08-22`.
4. Record the approved scope, owner, canonical approval, and expiry.
5. Approve every repository-settings change in the applicable administrator issue.
6. Do not extend the expiry without a new recorded decision.
7. Do not reuse approval for different bytes or settings.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 254.

### Complete when

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 254.

## Task 244 — create or update the approved advisory-fallback issue

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 243 completion record

~~~markdown
# Task 243 completion record

{{TASK_243_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 243 | `FS` | The owner approved a separate superseding-decision task and its exact scope. |

### Objective

Create or update only the focused advisory-fallback issue authorized by the owner. Do not commence implementation. If no fallback was approved, record `SKIPPED — EXISTING MERGE PATH` and do not create an issue.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the Task-local owner decision, PS #149, its current PR, the advisory evidence, and all repository dependencies.
2. If the owner approved the existing PS #149 merge path and did not approve a fallback, record `SKIPPED — EXISTING MERGE PATH` and stop.
3. If a fallback was approved, search for the separately approved focused issue. Update it when it already represents the exact scope; otherwise create it.
4. Record the canonical approval, owner, start date, expiry, exact fallback scope, affected paths, replacement contract, validator/case requirements, compensating controls, validation, rollback, and real native dependencies.
5. Read the issue and dependencies back. Do not create a branch, edit bytes, update a PR, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

## Task 245 — commence an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 244 completion record

~~~markdown
# Task 244 completion record

{{TASK_244_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 244 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement an approved advisory fallback. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 244 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Re-query the approved fallback issue, exact owner authorization, native dependencies, both current main refs, and active implementation PR inventory.
3. Require the approved start date to have arrived, every prerequisite to be complete, and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, approval, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and validation plan. Do not edit or update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 246 — implement an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 245 completion record

~~~markdown
# Task 245 completion record

{{TASK_245_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 245 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement an approved advisory fallback without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 245 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Verify that no feature implementation issue or PR is active. If the PS #149 PR is still active, preserve its exact head and tree. Close it only when the Task 243 approval expressly authorizes closure. Otherwise, do not start fallback implementation.
3. Prepare the complete fallback from the approved scope.
4. Include the current audit, every accepted finding, each reason, and each compensating control.
5. Include the canonical approval and expiry, exact replacement contract bytes, validator and case changes, and every dependent version, digest, and name pin.
6. Include validation and rollback instructions.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 247 — create or update the candidate PR for an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 247 PR URL | `{{TASK_247_PR_URL}}` | Produced by this task; replace {{TASK_247_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 246 completion record

~~~markdown
# Task 246 completion record

{{TASK_246_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 246 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement an approved advisory fallback; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 246 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Create one focused fallback PR under the separately approved task. Run initial local validation and required checks. Record its head SHA and tree.
3. Prove on the candidate that the validator does not enter `advisory-expired`. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 248 — run the Anthropic Claude Code review loop on the fallback PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.** Skip this task when Task 247 is skipped.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 247 PR URL | `{{TASK_247_PR_URL}}` | Produced by Task 247; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 247 completion record

~~~markdown
# Task 247 completion record

{{TASK_247_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 247 | `FS` | The approved fallback candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the fallback PR. Do not merge. If bytes change, rerun Task 247 validation and expiry proof.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 249 — run the independent final quality check on the fallback PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session. Skip this task when Task 247 is skipped.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 247 PR URL | `{{TASK_247_PR_URL}}` | Produced by Task 247; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 248 completion record

~~~markdown
# Task 248 completion record

{{TASK_248_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 248 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require exact approval/scope, advisory-expiry, contract, validator, case, pin, rollback, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 248 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 248.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 248.

## Task 250 — merge the approved fallback PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 247 PR URL | `{{TASK_247_PR_URL}}` | Produced by Task 247; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 249 completion record

~~~markdown
# Task 249 completion record

{{TASK_249_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 249 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If the approved fallback path was not selected and the predecessor review/quality tasks were skipped, record `SKIPPED — EXISTING MERGE PATH` and stop.
2. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
4. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
5. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 251 — publish the landed handoff for the approved fallback PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 247 PR URL | `{{TASK_247_PR_URL}}` | Produced by Task 247; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 250 completion record

~~~markdown
# Task 250 completion record

{{TASK_250_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 250 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If the predecessor recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
4. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 252 — check whether the PS #149 candidate requires a post-fallback refresh

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 241 completion record

~~~markdown
# Task 241 completion record

{{TASK_241_COMPLETION_RECORD}}
~~~

#### Task 243 completion record

~~~markdown
# Task 243 completion record

{{TASK_243_COMPLETION_RECORD}}
~~~

#### Task 251 completion record

~~~markdown
# Task 251 completion record

{{TASK_251_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 241 | `FS` | The PS #149 candidate passed review and the independent quality check. |
| Task 243 | `FS` | The owner approved the current merge path or supplied a later applicable approval. |
| Task 251 | `FS` when applicable | The approved fallback is landed. |

### Objective

Decide whether the reviewed PS #149 candidate remains merge-ready after the approved fallback path. Do not edit, update a PR, request review, approve, merge, or publish a handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. If Task 251 changed an input or overlapping path, rebase or update the PS #149 candidate. Return to Task 240 for the Claude Code review loop and Task 241 for a new independent quality check. Obtain a new Task 243 approval when the prior approval does not cover the resulting bytes.

### Procedure

1. Re-query the PS #149 candidate, the fallback branch result, all dependencies, decisions, settings, approvals, reviews, checks, and current `main`.
2. Compare the reviewed candidate head/tree and every input or overlapping path with the state after the fallback branch.
3. If no relevant byte, input, setting, approval, or evidence changed, output `MERGE_READY` for the exact current PS #149 head/tree.
4. If refresh is required, output `REFRESH_REQUIRED` with the exact scope. Repeat Tasks 238 and 265 as new update instances, then repeat Tasks 240 and 267. Repeat Task 243 when the prior approval does not cover the resulting bytes or settings. Then repeat this task.
5. Do not perform any refresh action in this task.

### Candidate-refresh gate

Apply this gate only if Procedure step 2 changes the PS #149 PR:

1. Complete the affected issue scope, local validation, PR-body correction, and initial required checks on the new candidate.
2. Record the new base, head SHA and tree, affected paths, issue links, validation, runtime identities, and changed decision or rollback evidence.
3. Stop the merge action. Return to Task 240 for a new Claude Code review loop and Task 241 for a new independent final quality check.
4. Require a new Task 243 approval when the current approval does not cover the new bytes or settings. Do not resume this task until all three records apply to the same current head and tree.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

### Complete when

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

## Task 253 — merge the PS #149 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 252 completion record

~~~markdown
# Task 252 completion record

{{TASK_252_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 252 | `FS` | Require `MERGE_READY` for the exact current PS #149 head/tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Revalidate the approved candidate head, tree, checks, dependencies, decisions, settings, and approval.
2. Stop if any item still differs from the approved evidence.
3. Apply the task-local merge gate below.
4. Merge one PS #149 PR and close the issue.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 254 — publish the landed handoff for the PS #149 P3 implementation

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 239 PR URL | `{{TASK_239_PR_URL}}` | Produced by Task 239; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 253 completion record

~~~markdown
# Task 253 completion record

{{TASK_253_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 253 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved task-specific procedure

1. Post the permanent P3 handoff. Include source and landed identities, paths and blobs, common mechanism choice, package and tool identities, validation, runtime evidence, decision records, review-loop result, final quality-check result, intentional differences, and rollback instructions.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

### Complete when

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

## Task 255 — update Terraform #24 from the landed PS P3 handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 225 completion record

~~~markdown
# Task 225 completion record

{{TASK_225_COMPLETION_RECORD}}
~~~

#### Task 254 completion record

~~~markdown
# Task 254 completion record

{{TASK_254_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 254 | `FS` | Use the final landed PS #149 commit and handoff. |
| Task 225 | `FS` | Terraform #23, the native predecessor of #24, is closed. |

### Objective

Complete only this leaf action: update Terraform #24 from the landed PS P3 handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Issue update:**

Replace all placeholders with verified identities and update Terraform #24:

> PSStyleGuide dependency and npm governance landed through `<PS_P3_PR_URL>` at `<PS_P3_LANDED_COMMIT>`, tree `<PS_P3_LANDED_TREE>`. Use permanent handoff `<PS_P3_HANDOFF_URL>` as the implementation source.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read every relevant PS source blob from that commit. Do not independently select a package-manager policy, audit parser, process runner, exception schema, Husky installer, workflow topology, or evidence format. Adapt repository identity, issue URLs, package findings, generated filenames, check names, and Terraform-specific dependency graph.
>
> Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, and failure truth. Refresh the reciprocal matrix at start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

### Complete when

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

## Task 256 — commence Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 255 completion record

~~~markdown
# Task 255 completion record

{{TASK_255_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 255 | `FS` | The issue update is complete and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #24. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Explicitly commence only Terraform #24 from the landed PS P3 source.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 257 — implement Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 256 completion record

~~~markdown
# Task 256 completion record

{{TASK_256_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 256 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #24 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the capability; do not independently redesign the named contracts.
2. Preserve all common controls from Task 255 and record every repository-specific adaptation.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 258 — create or update the candidate PR for Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 258 PR URL | `{{TASK_258_PR_URL}}` | Produced by this task; replace {{TASK_258_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 257 completion record

~~~markdown
# Task 257 completion record

{{TASK_257_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 257 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #24; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation.
2. Create or update one focused Terraform #24 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

### Complete when

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

## Task 259 — run the Anthropic Claude Code review loop on the Terraform #24 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 258 PR URL | `{{TASK_258_PR_URL}}` | Produced by Task 258; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 258 completion record

~~~markdown
# Task 258 completion record

{{TASK_258_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 258 | `FS` | The Terraform #24 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 258 validation and refresh the reciprocal matrix.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 260 — run the independent final quality check on the Terraform #24 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 258 PR URL | `{{TASK_258_PR_URL}}` | Produced by Task 258; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 259 completion record

~~~markdown
# Task 259 completion record

{{TASK_259_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 259 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #24 package, audit, exception, Husky, workflow, issue, comment, deferral, PR-body, validation, checks, and mergeability coverage. Return to Task 259 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 259.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 259.

## Task 261 — merge the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 258 PR URL | `{{TASK_258_PR_URL}}` | Produced by Task 258; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 260 completion record

~~~markdown
# Task 260 completion record

{{TASK_260_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 260 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 262 — publish the landed handoff for the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 258 PR URL | `{{TASK_258_PR_URL}}` | Produced by Task 258; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 261 completion record

~~~markdown
# Task 261 completion record

{{TASK_261_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 261 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

### Complete when

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

## Task 263 — reverse-compare T3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 262 completion record

~~~markdown
# Task 262 completion record

{{TASK_262_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 262 | `FS` | Use only the landed PS #149 and Terraform #24 commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 263 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 263 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 264 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 265 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 267 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 263 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 266 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 263 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 263 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 264 — create or update the focused repair issue for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 264 issue URL | `{{TASK_264_ISSUE_URL}}` | Produced by this task; replace {{TASK_264_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 263 completion record

~~~markdown
# Task 263 completion record

{{TASK_263_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 263 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 263's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 265 — commence the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 264 issue URL | `{{TASK_264_ISSUE_URL}}` | Produced by Task 264; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 264 completion record

~~~markdown
# Task 264 completion record

{{TASK_264_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 264 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 264 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 266 — implement the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 264 issue URL | `{{TASK_264_ISSUE_URL}}` | Produced by Task 264; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 265 completion record

~~~markdown
# Task 265 completion record

{{TASK_265_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 265 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 265 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 267 — create or update the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 264 issue URL | `{{TASK_264_ISSUE_URL}}` | Produced by Task 264; resolve globally before execution |
| Task 267 PR URL | `{{TASK_267_PR_URL}}` | Produced by this task; replace {{TASK_267_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 266 completion record

~~~markdown
# Task 266 completion record

{{TASK_266_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 266 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 266 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 268 — run the Anthropic Claude Code review loop on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 267 PR URL | `{{TASK_267_PR_URL}}` | Produced by Task 267; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 267 completion record

~~~markdown
# Task 267 completion record

{{TASK_267_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 267 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 267 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 267's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 268 for the new head before Task 269. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 269 — run the independent final quality check on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 267 PR URL | `{{TASK_267_PR_URL}}` | Produced by Task 267; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 268 completion record

~~~markdown
# Task 268 completion record

{{TASK_268_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 268 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 268 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 267's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 268 for the new head, and then repeat Task 269 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 270 — merge the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 267 PR URL | `{{TASK_267_PR_URL}}` | Produced by Task 267; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 269 completion record

~~~markdown
# Task 269 completion record

{{TASK_269_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 269 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 269 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 271 — publish the landed repair handoff for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 267 PR URL | `{{TASK_267_PR_URL}}` | Produced by Task 267; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 270 completion record

~~~markdown
# Task 270 completion record

{{TASK_270_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 270 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 270 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 272 — perform the final Terraform T3 recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 271 completion record

~~~markdown
# Task 271 completion record

{{TASK_271_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 271 | `FS` | Use the final landed PS state from Task 271. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 272 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 272 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 273 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 275 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 282 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 282 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 273 — create or update the focused repair issue for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 273 issue URL | `{{TASK_273_ISSUE_URL}}` | Produced by this task; replace {{TASK_273_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 272 completion record

~~~markdown
# Task 272 completion record

{{TASK_272_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 272 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 272's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 274 — commence the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 273 issue URL | `{{TASK_273_ISSUE_URL}}` | Produced by Task 273; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 273 completion record

~~~markdown
# Task 273 completion record

{{TASK_273_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 273 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 273 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 275 — implement the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 273 issue URL | `{{TASK_273_ISSUE_URL}}` | Produced by Task 273; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 274 completion record

~~~markdown
# Task 274 completion record

{{TASK_274_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 274 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 274 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 276 — create or update the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 273 issue URL | `{{TASK_273_ISSUE_URL}}` | Produced by Task 273; resolve globally before execution |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by this task; replace {{TASK_276_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 275 completion record

~~~markdown
# Task 275 completion record

{{TASK_275_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 275 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 275 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 277 — run the Anthropic Claude Code review loop on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 276 completion record

~~~markdown
# Task 276 completion record

{{TASK_276_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 276 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 276 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 276's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 277 for the new head before Task 278. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 278 — run the independent final quality check on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 277 completion record

~~~markdown
# Task 277 completion record

{{TASK_277_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 277 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 277 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 276's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 277 for the new head, and then repeat Task 278 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 279 — merge the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 278 completion record

~~~markdown
# Task 278 completion record

{{TASK_278_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 278 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 278 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 280 — publish the landed repair handoff for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 276 PR URL | `{{TASK_276_PR_URL}}` | Produced by Task 276; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 279 completion record

~~~markdown
# Task 279 completion record

{{TASK_279_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 279 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 279 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 281 — recheck the reciprocal result for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 272 completion record

~~~markdown
# Task 272 completion record

{{TASK_272_COMPLETION_RECORD}}
~~~

#### Task 280 completion record

~~~markdown
# Task 280 completion record

{{TASK_280_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 272 | `FS` | Use the original pinned comparison and branch result. |
| Task 280 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 266–280 as a new numbered-range instance. After its new handoff, repeat Task 281.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 282 — publish the fixed-point closure for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 281 completion record

~~~markdown
# Task 281 completion record

{{TASK_281_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 281 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 281 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 283 — commence PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 282 completion record

~~~markdown
# Task 282 completion record

{{TASK_282_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 282 | `FS` | PS #149 and its full reciprocal cycle are closed. |

### Objective

Explicitly commence only the implementation governed by implement PS #151. Do not edit implementation bytes in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #151.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 284 — implement PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 283 completion record

~~~markdown
# Task 283 completion record

{{TASK_283_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 283 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #151 without creating or updating a pull request.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query PS #151 and all landed policy and P3 contracts.
2. Implement the issue's Node permission-model confinement and duplicate-key preflight controls without silently attributing them to PS #162.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 285 — create or update the candidate PR for PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 285 PR URL | `{{TASK_285_PR_URL}}` | Produced by this task; replace {{TASK_285_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 284 completion record

~~~markdown
# Task 284 completion record

{{TASK_284_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 284 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #151; do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Create or update one focused PS #151 PR. Run initial local validation and required checks. Record its head SHA and tree, affected controls, files and blobs, runtimes, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

### Complete when

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

## Task 286 — run the Anthropic Claude Code review loop on the PS #151 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 285 PR URL | `{{TASK_285_PR_URL}}` | Produced by Task 285; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 285 completion record

~~~markdown
# Task 285 completion record

{{TASK_285_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 285 | `FS` | The PS #151 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Anthropic Claude Code review loop on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local Anthropic Claude Code review-loop prompt below against the PS #151 PR. Do not merge. If bytes change, rerun Task 285 validation.

### Anthropic Claude Code review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a dedicated Anthropic Claude Code session:

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 287 — run the independent final quality check on the PS #151 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 285 PR URL | `{{TASK_285_PR_URL}}` | Produced by Task 285; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 286 completion record

~~~markdown
# Task 286 completion record

{{TASK_286_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 286 | `FS` | The Claude Code review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #151 Node permission-model, duplicate-key preflight, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 286 after any head change.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Claude Code review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 286.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 286.

## Task 288 — merge the PS #151 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 285 PR URL | `{{TASK_285_PR_URL}}` | Produced by Task 285; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 287 completion record

~~~markdown
# Task 287 completion record

{{TASK_287_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 287 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every authorization gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 289 — publish the landed handoff for the PS #151 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 285 PR URL | `{{TASK_285_PR_URL}}` | Produced by Task 285; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 288 completion record

~~~markdown
# Task 288 completion record

{{TASK_288_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 288 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

### Complete when

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

## Task 290 — compare PS #151 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 289 completion record

~~~markdown
# Task 289 completion record

{{TASK_289_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 289 | `FS` | Use the final landed PS #151 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 290 | Compare the landed Node permission-model and duplicate-key controls with the Terraform validator and workflow topology. |
| Task 291 | If an applicable control is absent or weaker, create one focused Terraform issue from the landed PS commit and blobs, then run the four task-local conditional repair PR lifecycle actions. |
| Task 290 | If Terraform is equivalent, post a pinned no-change record. |
| Task 290 | If a control is structurally inapplicable, post a pinned non-applicability record. |
| Task 290 | Do not create a placeholder issue only for symmetry. |
| Task 290 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 290 | Map files by role, not only by filename. |
| Task 290 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 293 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 291 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 292 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 294 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 290 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 291 — create or update the focused repair issue for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 290 completion record

~~~markdown
# Task 290 completion record

{{TASK_290_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 290 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 290's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 292 — commence the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 291 completion record

~~~markdown
# Task 291 completion record

{{TASK_291_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 291 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 291 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 293 — implement the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 292 completion record

~~~markdown
# Task 292 completion record

{{TASK_292_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 292 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 292 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 294 — create or update the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 294 PR URL | `{{TASK_294_PR_URL}}` | Produced by this task; replace {{TASK_294_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 293 completion record

~~~markdown
# Task 293 completion record

{{TASK_293_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 293 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 293 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 295 — run the Anthropic Claude Code review loop on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 294 PR URL | `{{TASK_294_PR_URL}}` | Produced by Task 294; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 294 completion record

~~~markdown
# Task 294 completion record

{{TASK_294_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 294 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 294 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 294's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 295 for the new head before Task 296. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 296 — run the independent final quality check on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 294 PR URL | `{{TASK_294_PR_URL}}` | Produced by Task 294; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 295 completion record

~~~markdown
# Task 295 completion record

{{TASK_295_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 295 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 295 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 294's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 295 for the new head, and then repeat Task 296 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 297 — merge the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 294 PR URL | `{{TASK_294_PR_URL}}` | Produced by Task 294; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 296 completion record

~~~markdown
# Task 296 completion record

{{TASK_296_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 296 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 296 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 298 — publish the landed repair handoff for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 294 PR URL | `{{TASK_294_PR_URL}}` | Produced by Task 294; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 297 completion record

~~~markdown
# Task 297 completion record

{{TASK_297_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 297 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 297 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 299 — reverse-compare the PS #151 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 298 completion record

~~~markdown
# Task 298 completion record

{{TASK_298_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 298 | `FS` | Use the final Task 298 Terraform result. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 299 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 299 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 300 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 301 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 303 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 308 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 302 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 309 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 309 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 300 — create or update the focused repair issue for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 299 completion record

~~~markdown
# Task 299 completion record

{{TASK_299_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 299 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 299's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 301 — commence the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 300 completion record

~~~markdown
# Task 300 completion record

{{TASK_300_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 300 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 300 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 302 — implement the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 301 completion record

~~~markdown
# Task 301 completion record

{{TASK_301_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 301 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 301 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 303 — create or update the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 303 PR URL | `{{TASK_303_PR_URL}}` | Produced by this task; replace {{TASK_303_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 302 completion record

~~~markdown
# Task 302 completion record

{{TASK_302_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 302 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 302 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 304 — run the Anthropic Claude Code review loop on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 303 PR URL | `{{TASK_303_PR_URL}}` | Produced by Task 303; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 303 completion record

~~~markdown
# Task 303 completion record

{{TASK_303_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 303 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 303 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 303's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 304 for the new head before Task 305. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 305 — run the independent final quality check on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 303 PR URL | `{{TASK_303_PR_URL}}` | Produced by Task 303; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 304 completion record

~~~markdown
# Task 304 completion record

{{TASK_304_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 304 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 304 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 303's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 304 for the new head, and then repeat Task 305 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 306 — merge the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 303 PR URL | `{{TASK_303_PR_URL}}` | Produced by Task 303; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 305 completion record

~~~markdown
# Task 305 completion record

{{TASK_305_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 305 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 305 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 307 — publish the landed repair handoff for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 303 PR URL | `{{TASK_303_PR_URL}}` | Produced by Task 303; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 306 completion record

~~~markdown
# Task 306 completion record

{{TASK_306_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 306 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 306 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 308 — recheck the reciprocal result for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 299 completion record

~~~markdown
# Task 299 completion record

{{TASK_299_COMPLETION_RECORD}}
~~~

#### Task 307 completion record

~~~markdown
# Task 307 completion record

{{TASK_307_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 299 | `FS` | Use the original pinned comparison and branch result. |
| Task 307 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 293–307 as a new numbered-range instance. After its new handoff, repeat Task 308.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 309 — publish the fixed-point closure for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 308 completion record

~~~markdown
# Task 308 completion record

{{TASK_308_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 308 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 308 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 310 — check the independent residual triggers

> **Execution class: Coding agent executable.** This is a read-only trigger check and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | — | No technical predecessor. Run here to avoid interrupting the known capability cycles. Re-run when a listed input changes. |

Keep PS #155 and #156 open and independent. Do not add either issue as a blocker for PS #159, #152, #147, Terraform #21, or Terraform #22 without a new material trigger.

### Objective

Complete only this leaf action: check the independent residual triggers. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Trigger check:**

1. Re-query the current issues, repositories, supported hosts, native helpers, candidate files, and external runners.
2. Revisit PS #155 only if the candidate validator permits a competing writer, Windows PowerShell 5.1 support ends and a portable API becomes available, or a later task adopts a suitable native helper.
3. Revisit PS #156 only if the candidate files change or a suitable external runner becomes available.
4. Classify each trigger as `present` or `absent` and link its evidence.
5. If all triggers are absent, post a dated no-trigger record and stop.
6. If a trigger is present, record the affected issue and exact material evidence. Do not start implementation in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 321 must evaluate.

### Complete when

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 321 must evaluate.

## Task 311 — assess one triggered residual cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 309 completion record

~~~markdown
# Task 309 completion record

{{TASK_309_COMPLETION_RECORD}}
~~~

#### Task 310 completion record

~~~markdown
# Task 310 completion record

{{TASK_310_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 310 | `FS` | A material trigger is documented. |
| Task 309 | `FS` | The preceding known fixed-point cycle is closed. |
| Prior completed instance of Tasks 304–321 | `FS` | Close one residual cycle before starting another. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 311 | Validate the trigger and complete the required finding-specific decision process. |
| Task 312 | Create one focused issue with only its real dependencies. Issue creation does not start implementation. |
| Task 313 | Verify the implementation slot is free. Explicitly start only that residual issue. |
| Task 315 | Prepare one focused PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 311 | Record the complete landed identity, validation, review, and rollback evidence. |
| Task 311 | Do not change repository settings without exact administrator authorization. |
| Task 320 | If the other repository has the same role, compare the landed result there. |
| Task 314 | Create a reciprocal issue before implementation only when a common difference exists. |
| Task 313 | Start the reciprocal issue only after issue setup and a free implementation slot. Run the four task-local conditional repair PR lifecycle actions, then run the reverse comparison. |
| Task 321 | Close the cycle only when each common behavior is `same` or a proved intentional difference with equal security and failure strength. |
| Task 311 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 311 | Map files by role, not only by filename. |
| Task 311 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 314 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 312 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 313 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 315 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 320 | Compare the landed destination result back against the source repository. |
| Task 312 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 315 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 314 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 321 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 321 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 312 — create or update the focused repair issue for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 312 issue URL | `{{TASK_312_ISSUE_URL}}` | Produced by this task; replace {{TASK_312_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 311 completion record

~~~markdown
# Task 311 completion record

{{TASK_311_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 311 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 311's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 313 — commence the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 312 issue URL | `{{TASK_312_ISSUE_URL}}` | Produced by Task 312; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 312 completion record

~~~markdown
# Task 312 completion record

{{TASK_312_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 312 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 312 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 314 — implement the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 312 issue URL | `{{TASK_312_ISSUE_URL}}` | Produced by Task 312; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 313 completion record

~~~markdown
# Task 313 completion record

{{TASK_313_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 313 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 313 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 315 — create or update the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 312 issue URL | `{{TASK_312_ISSUE_URL}}` | Produced by Task 312; resolve globally before execution |
| Task 315 PR URL | `{{TASK_315_PR_URL}}` | Produced by this task; replace {{TASK_315_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 314 completion record

~~~markdown
# Task 314 completion record

{{TASK_314_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 314 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 314 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 316 — run the Anthropic Claude Code review loop on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 315 PR URL | `{{TASK_315_PR_URL}}` | Produced by Task 315; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 315 completion record

~~~markdown
# Task 315 completion record

{{TASK_315_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 315 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 315 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 315's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 316 for the new head before Task 317. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 317 — run the independent final quality check on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 315 PR URL | `{{TASK_315_PR_URL}}` | Produced by Task 315; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 316 completion record

~~~markdown
# Task 316 completion record

{{TASK_316_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 316 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 316 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 315's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 316 for the new head, and then repeat Task 317 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 318 — merge the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 315 PR URL | `{{TASK_315_PR_URL}}` | Produced by Task 315; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 317 completion record

~~~markdown
# Task 317 completion record

{{TASK_317_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 317 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 317 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 319 — publish the landed repair handoff for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_311_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 311; resolve globally before execution |
| Task 315 PR URL | `{{TASK_315_PR_URL}}` | Produced by Task 315; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 318 completion record

~~~markdown
# Task 318 completion record

{{TASK_318_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 318 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 318 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 320 — recheck the reciprocal result for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 311 completion record

~~~markdown
# Task 311 completion record

{{TASK_311_COMPLETION_RECORD}}
~~~

#### Task 319 completion record

~~~markdown
# Task 319 completion record

{{TASK_319_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 311 | `FS` | Use the original pinned comparison and branch result. |
| Task 319 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 305–319 as a new numbered-range instance. After its new handoff, repeat Task 320.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 321 — publish the fixed-point closure for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 320 completion record

~~~markdown
# Task 320 completion record

{{TASK_320_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 320 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 320 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 322 — assess one future PS-first paired capability cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 309 completion record

~~~markdown
# Task 309 completion record

{{TASK_309_COMPLETION_RECORD}}
~~~

#### Task 321 completion record

~~~markdown
# Task 321 completion record

{{TASK_321_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 309 | `FS` | All currently known capability cycles are closed. |
| Every applicable Task 321 instance | `FS` | Triggered residual work is closed. |
| Prior completed instance of Tasks 315–332 | `FS` | The prior future cycle has a permanent fixed-point record. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 323 | Create or update the PS issue. Classify it as `common capability`, `repository-specific capability`, or `administrative only`. Issue creation does not start implementation. |
| Task 322 | Record landed inputs and blobs, intended paths, real native dependencies, expected differences, and any mapped Terraform issue. |
| Task 324 | Verify the implementation slot is free. Explicitly start only the PS implementation. |
| Task 326 | Prepare one PS PR and run the four task-local conditional repair PR lifecycle actions. Post a permanent handoff with exact landed identities and matrix rows. |
| Task 323 | Create or update the Terraform issue only from that landed PS handoff. Do not replace placeholders before the PS PR lands, and never use a branch, reviewed head, test-merge ref, or anticipated squash SHA as the landed commit. |
| Task 324 | After issue setup and a free implementation slot, explicitly start only the Terraform adaptation. Do not create a shared runtime dependency. |
| Task 322 | Run the four task-local conditional repair PR lifecycle actions for the Terraform PR. |
| Task 323 | Run the reverse PS comparison. Create a focused PS sync-back issue before any correction and start it only after issue setup and a free implementation slot. |
| Task 322 | If PS changes, compare Terraform again. Run the four task-local conditional repair PR lifecycle actions for any required Terraform repair. |
| Task 324 | Apply the row-direction stop rule. Repeat until the cycle reaches a fixed point. Do not start the next PS capability before the permanent closure record exists. |
| Task 322 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 322 | Map files by role, not only by filename. |
| Task 322 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 325 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 323 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 324 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 326 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 331 | Compare the landed destination result back against the source repository. |
| Task 323 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 326 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 325 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 332 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 332 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Anthropic Claude Code review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 323 — create or update the focused repair issue for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 323 issue URL | `{{TASK_323_ISSUE_URL}}` | Produced by this task; replace {{TASK_323_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 322 completion record

~~~markdown
# Task 322 completion record

{{TASK_322_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 322 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Read Task 322's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 324 — commence the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 323 issue URL | `{{TASK_323_ISSUE_URL}}` | Produced by Task 323; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 323 completion record

~~~markdown
# Task 323 completion record

{{TASK_323_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 323 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 323 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 325 — implement the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 323 issue URL | `{{TASK_323_ISSUE_URL}}` | Produced by Task 323; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 324 completion record

~~~markdown
# Task 324 completion record

{{TASK_324_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 324 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-specific scope and immutable inputs

### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Anthropic Claude Code review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 324 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 326 — create or update the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 323 issue URL | `{{TASK_323_ISSUE_URL}}` | Produced by Task 323; resolve globally before execution |
| Task 326 PR URL | `{{TASK_326_PR_URL}}` | Produced by this task; replace {{TASK_326_PR_URL}} globally when the exact URL is known |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 325 completion record

~~~markdown
# Task 325 completion record

{{TASK_325_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 325 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 325 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Claude Code review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 327 — run the Anthropic Claude Code review loop on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Anthropic Claude Code.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 326 PR URL | `{{TASK_326_PR_URL}}` | Produced by Task 326; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 326 completion record

~~~markdown
# Task 326 completion record

{{TASK_326_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 326 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Anthropic Claude Code review loop. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 326 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 326's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a dedicated Anthropic Claude Code session.
4. If reviewable repository bytes change, repeat Task 327 for the new head before Task 328. Do not merge.

### Anthropic Claude Code review-loop prompt

~~~text
Use Anthropic Claude Code to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Read the repository-local root CLAUDE.md before you act. Follow its current "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. Do not use a summary of CLAUDE.md as a substitute for reading it.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
3. Before each round, run the review-readiness gate. Confirm every fix commit is reachable from the PR head; update the PR body to the exact current head/tree, versions, identities, commands, and results; then read the body and head back through the API and compare them with the committed files. Do not request review while the body or identity evidence is stale.
4. Record baselines for each bot across review submissions, inline review comments, and PR-level comments. Request Copilot with `request_copilot_review` (or the repository-equivalent structured action). Request Codex every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a result for the round only when it is newer than the applicable baseline and is explicitly anchored to the recorded PR head SHA. A stale result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine CLAUDE.md comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as CLAUDE.md requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
10. Re-request both reviewers after every head change. A stale review on an earlier head never counts as clean.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as CLAUDE.md permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under CLAUDE.md; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 328 — run the independent final quality check on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 326 PR URL | `{{TASK_326_PR_URL}}` | Produced by Task 326; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 327 completion record

~~~markdown
# Task 327 completion record

{{TASK_327_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 327 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 327 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 326's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Claude session.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 327 for the new head, and then repeat Task 328 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Read the repository-local root CLAUDE.md before you act. Apply its current code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Claude Code review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under CLAUDE.md. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required CLAUDE.md steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full CLAUDE.md decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
11. Cross-check the PR title and description against the complete final diff, commits, issue scope, tests, validation results, security effects, generated artifacts, breaking changes, intentional differences, deferred-issue links, and rollback information. Update the PR description directly when tooling and authority permit. Otherwise, produce a redline-style replacement that shows exact deletions and additions for the operator. Re-run this quality check after a body correction.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Anthropic Claude Code review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 329 — merge the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 326 PR URL | `{{TASK_326_PR_URL}}` | Produced by Task 326; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 328 completion record

~~~markdown
# Task 328 completion record

{{TASK_328_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 328 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Claude Code terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, a new comment or review arrived, a check is incomplete or failed, the PR is not mergeable, an approval is missing, or a dependency or authorization condition is unmet.
4. Use the approved merge method. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 328 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and authorized.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 330 — publish the landed repair handoff for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_322_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 322; resolve globally before execution |
| Task 326 PR URL | `{{TASK_326_PR_URL}}` | Produced by Task 326; resolve globally before execution |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 329 completion record

~~~markdown
# Task 329 completion record

{{TASK_329_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 329 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. If Task 329 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 331 — recheck the reciprocal result for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 322 completion record

~~~markdown
# Task 322 completion record

{{TASK_322_COMPLETION_RECORD}}
~~~

#### Task 330 completion record

~~~markdown
# Task 330 completion record

{{TASK_330_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 322 | `FS` | Use the original pinned comparison and branch result. |
| Task 330 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Anthropic Claude Code review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 316–330 as a new numbered-range instance. After its new handoff, repeat Task 331.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 332 — publish the fixed-point closure for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 331 completion record

~~~markdown
# Task 331 completion record

{{TASK_331_COMPLETION_RECORD}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 331 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

1. Re-query the final identities and the Task 331 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Run or verify every validation and evidence action named in this task against the recorded current identities. Record exact commands, runtime identities, native exit codes, URLs, SHA values, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without starting a successor action if a predecessor result is missing, an identity changed unexpectedly, required authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the requested state transition would weaken security, failure truth, review coverage, or the reciprocal fixed-point contract. State the exact blocker and the next required decision.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 333 — verify final completion

> **Execution class: Coding agent executable.** This is a read-only final audit and evidence-publication task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Completion and authorization records are intentionally outside the table. Replace each token inside its Markdown fence globally with the exact record before execution.

#### Task 310 completion record

~~~markdown
# Task 310 completion record

{{TASK_310_COMPLETION_RECORD}}
~~~

#### Task 321 completion record

~~~markdown
# Task 321 completion record

{{TASK_321_COMPLETION_RECORD}}
~~~

#### Task 332 completion record

~~~markdown
# Task 332 completion record

{{TASK_332_COMPLETION_RECORD}}
~~~

#### Completed history and Tasks 1–309 aggregate completion records

~~~markdown
# Completed history and Tasks 1–309 aggregate completion records

{{COMPLETED_HISTORY_AND_TASKS_001_TO_309_COMPLETION_RECORDS}}
~~~

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed history and Tasks 1–309 | `FS` | Every known task and reciprocal cycle is complete. |
| Every applicable Task 321 and Task 332 instance | `FS` | All triggered residual and in-scope future cycles are complete. |
| Task 310 | `FS` | The latest residual-trigger record is current. |

### Objective

Complete only this leaf action: verify final completion. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

1. Re-query GitHub and fetch both current `origin/main` refs before any issue write, branch rewrite, merge, or settings change in this task.
2. Open or identify one focused issue before each implementation. Issue creation does not start implementation.
3. Read every cross-repository input only from a recorded landed commit and Git blob. Do not copy from a moving branch.
4. Verify that the implementation slot is free before any feature branch, implementation edit, or implementation PR starts.
5. Keep candidate preparation, the Anthropic Claude Code review loop, the independent final quality check, and merge as separate actions. Perform only the lifecycle action or conditional sequence assigned to this task. Do not merge a PR directly from an implementation or comparison action.
6. Do not change repository settings unless the applicable administrator issue authorizes the exact request.
7. Replace every placeholder before posting text. Do not fabricate a SHA, URL, PR number, tree, blob, or evidence identity.
8. Use native dependencies only for real completion prerequisites. A tracker can list children without a false blocking edge.
9. Validate each finding before editing. List exhaustive options, create a finding-specific weighted rubric, score the options in a table, and select the best option.
10. State each selected action in ASD-STE100-compliant language. Keep decision analysis in `TEMP-*` files, not in permanent planning or implementation documents.
11. Record each applicable issue and PR URL, base, reviewed head and tree, merge method, landed commit and tree, affected path, Git blob, validation command, runtime identity, and review outcome.

### Procedure

**Completion audit:**

Verify all of the following:

- Every implementation had a focused issue record and a separate explicit commencement record. No two feature implementation issues or PRs were active at the same time.
- Every cross-repository input was pinned to a landed commit and blob. No work copied from a moving branch or introduced a shared runtime dependency, reusable cross-repository workflow, submodule, package, or third source of truth.
- Every PR completed a discrete Anthropic Claude Code review loop and then a discrete independent fresh-agent quality check against the same final head SHA and tree before merge. Every permanent cycle record contains both gate results and both repositories' issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, validation, runtime identities, and review outcomes.
- Every merged PR passed the whole-PR comment and deferral audit. No unfinished reviewer finding or illegitimate deferral remains. Every legitimate deferral has a cited, self-contained GitHub issue with correct native dependencies.
- Every merged PR title and body accurately describe its final landed diff, issue scope, validation, security effects, intentional differences, deferred-issue links, and rollback information.
- Every required reciprocal matrix contains each applicable row exactly once. Every common row is `same`. Every intentional difference names both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition. No blocker remains.
- Every posted issue body and comment contains verified identities. No placeholder or fabricated SHA, URL, PR number, tree, or blob remains.
- Every native dependency represents a real prerequisite. No tracker falsely blocks implementation, and no setting changed without exact administrator authorization.
- Initial cycles 1 through 5 closed in order with their Terraform comparisons, required repairs, reverse PS comparisons, and permanent fixed-point records.
- PS #159 and its four children are closed with a permanent landed handoff that names both repositories' final commits and blobs.
- PS #163 closed without duplicating volatile protocol text.
- Terraform #31 contains all five initial cycle records and closed without an aggregate foundation implementation PR.
- PR #164 was reviewed and landed through PS #160, and its reciprocal cycle reached a fixed point.
- The generators share the canonical tracked-present/tracked-absent publication and structured-result behavior.
- Both generators use fixed root/destination authority, complete BOM-less UTF-8/LF payloads, same-directory durable candidate verification, one `File.Replace` or non-overwriting `File.Move` publication call, truthful `ReplacementStateUncertain` reporting, no post-publication rollback claim, and reusable raw NUL-delimited Git validation with exact native-status handling.
- Repository-code jobs in both repositories contain no actions and have no repository-token scopes.
- Both repositories use separate action-only publication jobs, verified official Node distributions in action-free Node jobs, strict offline policy parsing and fixtures, machine-readable authority for roles, inputs, defaults, supply identities and case allocation, and the same script-version grammar and PowerShell authoring rules.
- Both repositories have the same common workflow-policy behavior and case coverage, except for recorded repository-specific literals. No workflow writer existed before P1B/T1B introduced and proved it.
- PS #158 is complete, and both repositories have a read-only, reproducible supply-freeze method.
- Terraform #21 closed from landed PS PR #153 after the reverse PS comparison and all sync-back work.
- The global sweep inventoried all common roles, resolved every untracked blocker through focused cycles, and retained final two-repository evidence.
- PS #147 and #152 are complete, Terraform #22 consumed the landed P1B handoff, and the P1B/T1B cycle has no blocker.
- PS #148 and Terraform #23 each have a landed result and the required cross-repository applicability record.
- PS #149 landed after the deadline-path decision. Terraform #24 consumed its handoff after #23. The P3/T3 cycle reached a fixed point.
- PS #151 has an applicable Terraform follow-up or a pinned structural non-applicability record.
- The advisory decision did not expire without completed PS #149 or a separately approved, fully pinned superseding decision.
- PS #155 and #156 remained independent unless a documented trigger created a focused blocker.
- Every later common capability used a landed PS handoff, a Terraform adaptation, a reverse PS comparison, and fixed-point closure before the next PS implementation.
- No shared cross-repository runtime dependency exists.

### Validation and evidence

Run or verify every validation and evidence action named in this task against current recorded identities. Record exact commands, runtime identities, native exit codes, URLs, SHAs, checks, and results. Re-query affected GitHub state after any authorized write.

### Stop and escalation conditions

Stop without performing a successor action if a predecessor result is missing, an identity changed unexpectedly, authority is absent, validation or a required check fails, a placeholder cannot be resolved from its named source, or the state transition would weaken security, failure truth, review coverage, or fixed-point integrity. Record the exact blocker and next required decision.

### Exact output

Post one permanent final audit that identifies the final PS and Terraform commits and trees, links every fixed-point record, lists all validation and runtime evidence, records every intentional difference and non-applicability result, and states that no blocker remains.

### Complete when

Post one permanent final audit that identifies the final PS and Terraform commits and trees, links every fixed-point record, lists all validation and runtime evidence, records every intentional difference and non-applicability result, and states that no blocker remains.

## References

- [PS issue #145](https://github.com/franklesniak/PSStyleGuide/issues/145) and [PS PR #150](https://github.com/franklesniak/PSStyleGuide/pull/150)
- [PS issue #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153)
- PS issues [#147](https://github.com/franklesniak/PSStyleGuide/issues/147), [#148](https://github.com/franklesniak/PSStyleGuide/issues/148), [#149](https://github.com/franklesniak/PSStyleGuide/issues/149), [#151](https://github.com/franklesniak/PSStyleGuide/issues/151), [#152](https://github.com/franklesniak/PSStyleGuide/issues/152), [#155](https://github.com/franklesniak/PSStyleGuide/issues/155), [#156](https://github.com/franklesniak/PSStyleGuide/issues/156), and [#158](https://github.com/franklesniak/PSStyleGuide/issues/158)
- [PS foundation umbrella #159](https://github.com/franklesniak/PSStyleGuide/issues/159), child issues [#160](https://github.com/franklesniak/PSStyleGuide/issues/160), [#161](https://github.com/franklesniak/PSStyleGuide/issues/161), and [#162](https://github.com/franklesniak/PSStyleGuide/issues/162), and [merged PR #164](https://github.com/franklesniak/PSStyleGuide/pull/164)
- [PS issue #166](https://github.com/franklesniak/PSStyleGuide/issues/166) and [merged PR #165](https://github.com/franklesniak/PSStyleGuide/pull/165)
- [PS Claude-command issue #163](https://github.com/franklesniak/PSStyleGuide/issues/163)
- [PS #161 draft PR #167](https://github.com/franklesniak/PSStyleGuide/pull/167), [generator-check failure](https://github.com/franklesniak/PSStyleGuide/actions/runs/31856644635/job/94942553633), and [policy-check failure](https://github.com/franklesniak/PSStyleGuide/actions/runs/31856644627/job/94942553647)
- [Terraform issue #20](https://github.com/franklesniak/TerraformStyleGuide/issues/20), [PR #26](https://github.com/franklesniak/TerraformStyleGuide/pull/26), [PR #27](https://github.com/franklesniak/TerraformStyleGuide/pull/27), and [PR #30](https://github.com/franklesniak/TerraformStyleGuide/pull/30)
- Terraform issues [#21](https://github.com/franklesniak/TerraformStyleGuide/issues/21), [#22](https://github.com/franklesniak/TerraformStyleGuide/issues/22), [#23](https://github.com/franklesniak/TerraformStyleGuide/issues/23), [#24](https://github.com/franklesniak/TerraformStyleGuide/issues/24), and [#31](https://github.com/franklesniak/TerraformStyleGuide/issues/31)
- [Terraform issue #32](https://github.com/franklesniak/TerraformStyleGuide/issues/32) and [merged PR #33](https://github.com/franklesniak/TerraformStyleGuide/pull/33)
- [GitHub secure-use guidance for Actions](https://docs.github.com/en/actions/reference/security/secure-use)
- [GitHub Actions runner authentication design](https://github.com/actions/runner/blob/main/docs/design/auth.md)
- [GitHub Actions `GITHUB_TOKEN`](https://docs.github.com/en/actions/concepts/security/github_token)
- [GitHub workflow permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions)
- [Node.js permission model](https://nodejs.org/api/permissions.html)
- [.NET `File.Replace`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace)
- [.NET `File.Move`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.move)
- [Git pathname format and `-z`](https://git-scm.com/docs/git-status#_pathname_format_notes_and_z)
- [GitHub issue dependencies](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-issue-dependencies)
