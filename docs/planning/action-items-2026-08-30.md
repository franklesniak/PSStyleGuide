<!-- markdownlint-disable MD013 -->
<!-- markdownlint-configure-file { "MD024": { "siblings_only": true } } -->

# Chronological PSStyleGuide-first cross-repository convergence plan

## Purpose

Correct the metadata-policy and review-loop defects exposed by merged PSStyleGuide PR #174 before publishing its landed handoff. First remove only clause-proved PSStyleGuide paperwork and replace complete-history metadata transition enforcement with one published-baseline-to-final-state evaluation on PSStyleGuide `main`. Then correct the permanent review-loop architecture on remote planning branch `planning-CRT-PR-852`. Keep the two PRs on their proper bases and run them serially.

After both corrective lifecycles land, publish the corrected PS handoff and prioritize byte convergence across research-misc, PSStyleGuide, and TerraformStyleGuide for the exact 28 paths changed by PR #174. Byte identity at the same path is the default. A difference is legitimate only when verbatim adoption fails because of a recorded incompatible repository fact and the smallest equally safe substitution wins a finding-specific evaluation. Then resume the remaining PSStyleGuide-first task slate.

This plan contains numbered atomic leaf tasks. Every task has one primary action or lifecycle gate and one objectively testable result. Tasks share the compact execution policy in `docs/planning/coding-agent-loop.md`. A task carries only its variables, dependencies, objective, task-specific controls, procedure, validation delta, output, and completion condition. Shared controls do not need to be copied into every task.

## Execution classes

- `Coding agent executable`: A coding agent can complete the task with the stated tools and authority. A task-local human approval remains a required input when stated.
- `Human execution required`: A repository owner or administrator must make or approve the decision. A coding agent can prepare evidence but cannot supply approval.
- `Not executable; tracking/control only`: The item records a control state and cannot itself authorize implementation. No current task uses this class.

## Shared compact execution policy

Use one active-task state, one final validation result, and targeted remote readback. Do not create result catalogs, prompt-change reconciliation records, routing-activation records, per-command approval records, or duplicate evidence for facts that Git, GitHub, CI, or review state already prove.

Classify each task at the highest applicable risk tier:

| Tier | Work | Minimum control |
| --- | --- | --- |
| R0 | Read-only inspection, planning, and local analysis | Targeted reads and a truthful result |
| R1 | Reversible routine work, append-only commits, non-force topic pushes, issue or PR updates, comments, and review requests | Relevant validation, exact precondition, native result, and targeted readback |
| R2 | Trust roots, workflows, security policy, required checks, default-branch non-force bootstrap updates, and sensitive convergence | Full applicable validation, exact identities, clean state, required independent review, and targeted readback |
| R3 | Merge, force, deletion, settings, credentials, permissions, protections, and gate changes | Standing plan authority for an on-plan merge; separate explicit authority for other R3 actions; all R2 controls, current green gates, required review, and final readiness check |

The instruction to execute this plan authorizes in-scope R0 and R1 work. It also authorizes an R2 action when the current task expressly requires that action and all R2 controls pass. It is standing authority for an on-plan merge. A merge is on-plan only when the current task expressly names it; the repository, PR, target branch, head commit, tree, and scope match the task; required review and checks pass for the same immutable head; no material change or unresolved feedback remains; the PR is mergeable; and the repository-permitted merge method does not bypass a control. Do not request separate operator approval for an on-plan merge.

Request an operator decision when a merge is off-plan, work expands scope, or the task assigns a material decision to a human. Never infer permission for a force push, deletion, settings change, credential or permission change, protection change, administrator override, or gate bypass. These exceptional R3 actions require separate explicit authority even when a task names them.

Notify the operator as soon as a future exceptional action and its readiness conditions are known. The notification is not a stop condition. While authority is pending, continue every safe in-scope action that does not cross that boundary, including implementation, local validation, non-force topic publication, PR correction, CI diagnosis, review requests, finding repair, and final readiness work. Enter `waiting_human` only when the next concrete action requires the operator and no independent safe in-scope work remains. If the exceptional action is itself necessary to clear one final gate, complete every independent gate, identify the exact residual gate and cause, and do not weaken or bypass it.

Use `pending`, `active`, `validating`, `ready`, `waiting_external`, `waiting_human`, `complete`, and `blocked`. A failed validation or new finding returns the task to `active`. Use `waiting_human` only when the next concrete action needs a real human decision or exceptional authority and no independent safe in-scope work remains. Use `blocked` only when the same real blocker persists and no safe work remains.

Before a remote write, check only the affected repository, target, operation, local identity, clean state, expected preimage, force mode, and required gates. Execute once, validate the native response, and read back the affected object. Paginate only a connection whose completeness is required for the decision.

Run cheap affected checks first and the full applicable gate set once on final unchanged bytes. Reuse a passing result while its inputs remain unchanged. Failed, skipped, neutral, canceled, missing, pending, stale, timed-out, expired, and ambiguous results are not success. Do not merge with red CI or unresolved required review. An anticipated approval boundary does not defer CI or review that can run safely before that boundary; request authority early, continue toward a clean reviewed head, and stop only at the exact action that needs it.

For a non-mechanical finding, validate it, list reasonable options, create a finding-specific weighted rubric, score the options in a table, state the selected option in ASD-STE100 language, and then implement it. Do not use churn or implementation effort to outweigh correctness or usability.

Use the predecessor results named in `Task variables` and `Dependencies`. Keep only immutable values that a later task still needs in the closed `predecessor_outputs` map in compact state. Key each value by its producing task and exact output name, record its last consumer, and prune it after that consumer completes. During ingestion, require each producer to be complete and each final consumer to remain incomplete. Reject a non-finite JSON number or a number whose absolute value exceeds 9007199254740991; encode a larger exact integer as a string. When `current_task.review` exists, require `current_task.head` to equal `current_task.review.reviewInput.head`. Do not paste predecessor completion records into the plan or require a local result when the referenced Git or GitHub object is available. Task-specific controls override this shared policy only where they are stricter and materially relevant to the task.

The compact policy controls orchestration mechanics. A task-specific objective, scope rule, security condition, external GitHub object, validation requirement, and `Complete when` predicate remains binding. A legacy instruction to create a separate result, manifest, record, tracker, evidence catalog, prompt hash, routing proof, or per-command record is not binding. Store required result fields in the task's one final result. An old result name such as `TERMINALLY_CLEAN` names that final result; it does not require another file or catalog. Keep separate durable evidence only when an R2 or R3 control, an external API object, or the task's product output expressly requires it.

For PR work, generate and read back an accurate reviewer-facing body before review. Keep live task and review state out of that body. Request one required review set for the final reviewed input. Generate a Copilot REST request from the typed reviewer specification with exact login `copilot-pull-request-reviewer[bot]`; reject the display name `Copilot`. Normalize every API collection through the tested helper; an empty collection is not a match. Persist the unique request-event, review-run, submitted-review, and conversation-comment baselines with the in-flight attempt. A code, reviewed-diff, or material scope, behavior, or risk change invalidates that review. A verified factual correction in compact state or a comment-only publication does not. Serialize review pairs across different inputs. If authenticated reviewed-input drift makes an unrequested old-input channel impossible, record a typed `SUPERSEDED` disposition only after every recorded request for that input is terminal. Validate the disposition against request existence, an incomplete channel set, one matching head, a known successor head, and a time that is not earlier than any described request. If that key becomes current again, retain the disposition and resume its original incomplete pair without a duplicate request. Require submitted-review commits to match the reviewed head. Require every metric reason record to contain only a nonempty reason string and Boolean material value. Attribute a headless Codex conversation result through its authenticated author, request time, baseline exclusion, reviewed-input key, and predecessor-pair order. After a confirmed remote write, use targeted readback; do not repeat the write because local result recording failed. If an accepted Copilot request has no matching readback, record `RECONCILING` and continue other safe work. After at least 120 seconds, require complete negative readback from request events, requested reviewers, submitted reviews, and Copilot review runs before `NO_EFFECT`. Permit one retry for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`.

For each confirmed terminal request, persist one closed `terminalResultRef` that identifies the result kind, immutable result, and observed time while the complete result stays in its separate channel result collection. During compact-state ingestion, cross-validate the reference against the correct channel, actor, request baseline, request time, reviewed head when available, safe headless-comment evidence, and the next different-input request boundary. Reject a missing, duplicate, stale, baseline, wrong-actor, wrong-head, wrong-channel, or wrong-time reference. Do not add this reference to an unconfirmed or nonterminal request.

Keep every frozen reviewed head in `reviewerRequestsPerHead`, including a head that received zero requests. During ingestion, require the current head and every request head to occur in that map, and require each count to equal the persisted request history. Pass that map into request-decision calls that evaluate retained supersessions. Revalidate its current head, request heads, exact counts, and key syntax there before using only those validated keys, the current head, and request heads as known successor identities for retained supersessions.

The machine-readable review-input and mutation contract is `docs/planning/review-loop-policy.json`. Its deterministic implementation and scenarios are `docs/planning/review-loop-policy.mjs` and `docs/planning/review-loop-policy.test.mjs`. These files validate decisions; they do not perform GitHub writes. Task-local PR publication, review, and quality prompts remain complete without requiring an executor to read the shared files.

Use exactly these semantic mutation classes:

- `CODE_OR_DIFF`: The commit, tree, code, or reviewed diff changed. Require one new review pair.
- `MATERIAL_SCOPE_BEHAVIOR_RISK`: The same head now represents a materially different scope, behavior, or risk. Record the reason and require one new review pair.
- `NON_MATERIAL_FACT`: A factual identity or bounded evidence field changed without changing reviewed meaning. Do not request review again.
- `RESULT_OR_STATE`: Task state, polling state, reviewer IDs, results, quality results, metrics, or audit records changed. Keep the change outside reviewer input and do not request review again.
- `COMMENT_ONLY`: A separate comment or landed handoff changed. Do not request review again.

The reviewed input is the final head, tree, complete diff identity, and frozen reviewer-facing scope, behavior, and risk semantics. Raw PR-body byte inequality is not a review invalidation rule. A public mutation becomes confirmed only after a successful native response and matching authenticated readback. After confirmation, a local serialization failure can change only compact state; it cannot authorize a retry. A typed `NO_EFFECT` review-request disposition is not confirmation or ambiguity: it requires the full bounded negative proof above and permits only the one recorded retry.

## Execution-order map

| Task range | Ordered result |
| --- | --- |
| 1–9 | Prove clause provenance; implement, review, merge, and hand off the PS metadata-policy correction on `main`. |
| 10–18 | Inventory and correct the review-loop architecture; review, merge, and hand off the change on `planning-CRT-PR-852`. |
| 19–40 | Publish the corrected PS PR #174 handoff, converge the exact 28-path catalog through Terraform and selected-target reciprocal repairs, publish the three-repository fixed point, and close umbrella #172. |
| 41–68 | Validate, implement, review, merge, compare, and reciprocally close PS #168. |
| 69–97 | Perform the landed cycle-2 reverse comparison; close PS #169, #170, and #171 through separate PR lifecycles and comparisons; publish the cycle-2 fixed point. |
| 98–125 | Implement, review, merge, and reciprocally close PS #162; post the initial three-cycle handoff. |
| 126–154 | Update, implement, review, merge, and reciprocally close PS #158; close PS #159. |
| 155–182 | Implement, review, merge, and reciprocally close PS #163; close Terraform #31. |
| 183–209 | Implement, review, merge, and reciprocally close Terraform #21. |
| 210–222 | Inventory and close all untracked cross-repository blockers. |
| 223–260 | Prepare, review, decide, prove, land, and reciprocally close P1B/T1B. |
| 261–287 | Implement, review, merge, and close the PS #148 applicability cycle. |
| 288–305 | Implement, review, merge, and close Terraform #23 and its PS applicability result. |
| 306–323 | Prepare and review PS #149, make the fixed-date decision, process any approved fallback, and land P3. |
| 324–351 | Implement, review, merge, and reciprocally close Terraform #24. |
| 352–378 | Implement, review, merge, and close the PS #151 applicability cycle. |
| 379–401 | Check residual triggers, close triggered work, and run later paired cycles when applicable. |
| 402–402 | Publish the final completion audit. |

Task 311's historical date is not an authorization expiry or a reason to stop. If the original date was missed, perform the truthful late evaluation with current evidence. The advisory expiration is `2026-10-29T23:59:59Z`. Expiration is never success. Every declared executable edge is `FS` or a task-local conditional repeat. The read-only PS #152 evidence task has an `FS` edge from Task 225 because Task 226 requires Task 225's result.

## Dependency relationship reference

| Code | Relationship | Meaning |
| --- | --- | --- |
| `FS` | Finish-to-start | The predecessor must finish before the successor starts. |
| `FF` | Finish-to-finish | The successor cannot finish before the predecessor finishes. |
| `SS` | Start-to-start | The successor cannot start before the predecessor starts. |
| `SF` | Start-to-finish | The predecessor cannot finish before the successor starts. |

The graph contains no legitimate `SF` relationship. Former finish-to-finish tracker conditions remain separate closure leaves.

## Verified current state

This state was re-queried at `2026-08-30T18:23:01.3795819+00:00` through authenticated structured GitHub REST and GraphQL reads, terminal pagination, exact Git-object reads, and the failed-job log. Every state-changing task must re-query its task-local identities before it acts.

### Completed source Tasks 1–8 and merged PR #174

- Source Tasks 1–8 are completed history and are not executable items in this plan. They created umbrella [#172](https://github.com/franklesniak/PSStyleGuide/issues/172), created and commenced focused issue [#173](https://github.com/franklesniak/PSStyleGuide/issues/173), produced candidate head `eaf81552ba69c2f610b4963a55320114a306c7a6` and tree `9e7319498954119af7c6a51b4d276ac421544b70`, created and reviewed [PR #174](https://github.com/franklesniak/PSStyleGuide/pull/174), completed the independent quality gate, and merged it.
- PR #174 is merged. Its base is `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`; reviewed head is `eaf81552ba69c2f610b4963a55320114a306c7a6`; merge commit is `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; head and merge tree are `9e7319498954119af7c6a51b4d276ac421544b70`; merge parents are `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b` and `eaf81552ba69c2f610b4963a55320114a306c7a6`.
- Complete pagination returned 49 commits, 28 files, 203 review submissions, 252 inline comments, 54 conversation comments, 361 timeline items, 56 body edits, and 125 resolved plus zero unresolved review threads. The PR body is 50,528 UTF-8 bytes with SHA-256 `4e1e55ffb4dca8f1c9955ac36fc00ec25e541d45ee8dfa9492ca72908cf7122d`. Its only closing issue is #173.
- The final PR head has nine completed successful check runs. The merge commit has four successful checks and one failed `Validate agent instructions` check. Preserve both facts; do not rewrite five successes or treat the failure as an accepted success.
- Issue #173 is closed as completed. Its commencement record is comment `5372266971`. Umbrella #172 remains open and has #173 as its one current sub-issue. No native dependency edge was added. PS and Terraform currently have zero open PRs.

### Failed landed validation and corrective boundary

- [Run 33309460684](https://github.com/franklesniak/PSStyleGuide/actions/runs/33309460684) is an immutable failed `push` run on merge commit `34fb7eb492c46cdf9949a52c357a5dd04f873dab`. [Job 99251756566](https://github.com/franklesniak/PSStyleGuide/actions/runs/33309460684/job/99251756566) failed `Validate instruction capacity and capabilities` with native exit 1.
- The failure named five final documents: `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md`, `.github/workflows/scripts-README.md`, `docs/ISSUE_EVALUATION_PROMPT.md`, `docs/decisions/0001-accept-in-repository-trust-root.md`, and `docs/decisions/0002-accept-unverifiable-baseline-provenance.md`. Do not edit them only to change metadata. Their final document states are valid under the selected published-baseline model.
- Do not rerun the old workflow and expect another result. Its event and commit inputs are immutable. Validate the corrected implementation on a new candidate and landed commit.

### Current policy and planning identities

- PSStyleGuide `main` is `34fb7eb492c46cdf9949a52c357a5dd04f873dab`, tree `9e7319498954119af7c6a51b4d276ac421544b70`. Its `.github/instructions/docs.instructions.md` is blob `0e087aba4d4a1721df461f17d0c694ec7ed607c1`, 44,830 bytes, SHA-256 `49598d84aca3d6dbf350e6937a5dfc84c92bb7b4a4f033c29e95bd5bba635b28`.
- copilot-repo-template `main` is `ec067b893bb8ac6f7bf2491f0cf8b38ce66fe5b6`, tree `83cfad3830c781affbb9eb65887030eda923dab9`. Its `.github/instructions/docs.instructions.md` is blob `f6db0497d2e46b6f39d52185579c7aaa046fb48b`, 64,188 bytes, SHA-256 `4a099d33d4548db75b08d7dac40b3032481193033c1cf6bf5a4e3c108d22bb80`.
- Remote branch `planning-CRT-PR-852` is `4f0896728dc0275b6c4fa19f7442335ae2e26bf4`. The five inspected `docs/planning/**` controller and plan files are absent from PS `main`; their planning-branch blobs are: `coding-agent-loop.md` `f3feb8348fa7f25ee1860237c10de40ac42420fb`, `coding-agent-loop-without-model-routing.md` `c05fff4728800c52567586fb29e85dae805dca69`, `coding-agent-loop-performance-plan-2026-08-25.md` `22861d9b6e4279a67e8e15db0cea022b7e2861fc`, `prompt-action-items-update.md` `233aabf1c5da79cf3736feab6844901f39e419c0`, and this source plan `64a5ded019eb64f1757c2700d38ad2df531fca42`.
- The present working checkout is on `planning-CRT-PR-852` and contains unrelated modified and untracked planning evidence. Preserve it. The future corrective implementations use isolated worktrees on their respective bases.

### Three-repository PR #174 convergence baseline

- The closed priority catalog is the exact 28 paths changed by PR #174. At planning time, research-misc uses PR #78 merge `51dbeb560dd518b904cf1b571dc7d01fd8045749`, tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`; PS uses PR #174 merge `34fb7eb492c46cdf9949a52c357a5dd04f873dab`, tree `9e7319498954119af7c6a51b4d276ac421544b70`; Terraform `main` is `fe28313e118497e2567bf306b74ec596dd10eb64`, tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- Output Task 9's final metadata-policy landing supersedes the PS planning baseline for every changed catalog cell. Output Task 19 must recompute the full catalog before publication.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

### Current dependency slate

- PS #159 is open with #160 and #161 closed and #162 and #158 open. Native edges remain #160 → #161 → #162. PS #158 remains nonblocking.
- PS #147 remains blocked by open administrator issue #152 and closed #146, then blocks #148; the chain continues #148 → #149 → #151. PS #163, #155, #156, #168–#172, and #175 remain open without native blockers.
- PS #168–#171 have no comments, closing PRs, parent, sub-issues, or dependency edges. Their source-plan sequencing remains plan-level order, not a fabricated native dependency.
- Terraform #31 remains open with #32 and #34 closed. Terraform #21 remains open with closed blockers #20 and #28; the chain continues #21 → #22 → #23 → #24 → #25. Terraform `main` remains `fe28313e118497e2567bf306b74ec596dd10eb64`.
- copilot-repo-template issues #858–#863 remain open with zero blocking or blocked-by edges. They are separate follow-ups and do not authorize modification of copilot-repo-template in Tasks 1–9.
- Research-misc PR #78 remains merged. Parent PR #21 remains open and draft at head `51dbeb560dd518b904cf1b571dc7d01fd8045749` on `agent/bash-pid1-runtime-contract`. It is not authority to mutate unrelated PID 1 application work.

### Preserved completed foundation history

- PS #161 remains closed through merged PR #167 at merge `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`; its permanent `READY_FOR_TERRAFORM_COMPARISON` handoff remains issue comment `5334317038`.
- Terraform #34 remains closed through merged PR #35 at merge `fe28313e118497e2567bf306b74ec596dd10eb64`; its permanent `READY_FOR_PS_COMPARISON` handoff remains issue comment `5336853959`.
- These immutable histories remain downstream inputs. Current branch tips are mutable and must be re-queried by each state-changing task.

## Immutable current foundation inputs

| Role | Identity |
| --- | --- |
| Completed PR #174 reviewed head/tree | `eaf81552ba69c2f610b4963a55320114a306c7a6` / `9e7319498954119af7c6a51b4d276ac421544b70` |
| Completed PR #174 merge/tree | `34fb7eb492c46cdf9949a52c357a5dd04f873dab` / `9e7319498954119af7c6a51b4d276ac421544b70` |
| Current PS policy document | commit `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; blob `0e087aba4d4a1721df461f17d0c694ec7ed607c1` |
| Current template policy document | commit `ec067b893bb8ac6f7bf2491f0cf8b38ce66fe5b6`; blob `f6db0497d2e46b6f39d52185579c7aaa046fb48b` |
| Failed landed validation | run `33309460684`; job `99251756566`; commit `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; conclusion `failure` |
| Planning correction base | remote `planning-CRT-PR-852` at `4f0896728dc0275b6c4fa19f7442335ae2e26bf4` |
| Research governance source | PR #78 merge `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16` |
| Terraform planning baseline | `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151` |
| Corrected PS landing | `{{PS_METADATA_POLICY_LANDED_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_LANDED_TREE_SHA}}`; produced by Task 8 and published by Task 9 |
| Corrected planning landing | `{{REVIEW_LOOP_LANDED_COMMIT_SHA}}` / `{{REVIEW_LOOP_LANDED_TREE_SHA}}`; produced by Task 17 and published by Task 18 |

## Chronological task slate

## Task 1 — create or update the PS metadata-policy corrective issue

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Corrective issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Search all open and closed issues and PRs; reuse only an exact match, otherwise create one |
| PS main/document | `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; `0e087aba4d4a1721df461f17d0c694ec7ed607c1` | Verified planning snapshot; re-query before mutation |
| Template main/document | `ec067b893bb8ac6f7bf2491f0cf8b38ce66fe5b6`; `f6db0497d2e46b6f39d52185579c7aaa046fb48b` | Verified planning snapshot; re-query before mutation |
| Incident | PR #174; run `33309460684`; job `99251756566` | Immutable evidence |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed source Tasks 1–8 | `FS` | PR #174 is merged, issue #173 is closed, umbrella #172 remains open, and the landed failure is readable. |
| Current PS and template policy inputs | `FS` | Both current main commits, trees, policy blobs, byte counts, and hashes are readable. |

### Objective

Create or update one focused PS issue that authorizes only the metadata-policy and validator correction on PS `main`. Do not commence implementation, create a branch or PR, modify repository bytes, request review, merge, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. The issue must reproduce the hard preservation boundary: do not remove or weaken any rule derived from copilot-repo-template `.github/instructions/docs.instructions.md`; uncertain provenance is retained.
2. Include the exact current PS/template main and policy-document identities, PR #174 and failed-run identities, all five policy simplifications, validator scope, 12 required regression scenarios, validation/reporting requirements, non-goals, branch separation, and one-pair-per-final-head PR protocol.
3. State that copilot-repo-template is read-only in this task. State that the five documents named by the failed landed job must not be edited merely to change metadata.
4. Search for an existing exact corrective issue before creating one. Relate it to umbrella #172 only if the native relationship accurately represents tracking; never create a false blocked-by edge for chronological order.

### Procedure

1. Re-query PS and template `main`, both policy documents, PR #174, run 33309460684/job 99251756566, issues #172, #173, #175, all open PRs, and relationship surfaces.
2. Search all PS issues and PRs for an exact existing corrective owner. Build a requirement matrix against the supplied work item.
3. Create one focused issue if no exact owner exists; otherwise update only stale or missing scope. Keep issue creation distinct from commencement.
4. Add an accurate tracking relationship to #172 only when supported. Read back the issue body, edits, relationships, state, actor, timestamp, byte count, and SHA-256.
5. Confirm no affirmative commencement marker, linked implementation branch, closing PR, repository/ref mutation, or settings change exists. Return the issue identity and stop.

### Validation and evidence

Require complete pagination, exact PS/template identities, exact incident identity, full requirement-matrix coverage, no unresolved placeholder in the posted body, accurate optional relationship, one read-back body identity, and proof that implementation did not commence.

### Stop and escalation conditions

In addition to the common stops, stop if provenance scope is weakened, template modification is implied, the failed run is described as mutable, a false native dependency would be needed, or another open issue already owns the exact correction.

### Exact output

One authenticated `PS_METADATA_POLICY_ISSUE_READY` record contains `{{PS_METADATA_POLICY_ISSUE_URL}}`, IDs, state, relationships, body bytes/SHA-256, exact immutable inputs, create-or-update disposition, and proof that implementation did not commence.

### Complete when

One self-contained focused issue owns the complete metadata-policy correction, exact readback proves its state, and no implementation or repository mutation occurred.

## Task 2 — commence the PS metadata-policy correction

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Task 1 |
| Target base | `{{PS_METADATA_POLICY_BASE_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_BASE_TREE_SHA}}` | Re-query PS `main` immediately before commencement |
| Intended branch | `{{PS_METADATA_POLICY_BRANCH}}` | Choose a unique focused branch name; prove local and remote absence before recording it |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 1 | `FS` | The focused issue is open, self-contained, accurately related, and contains no commencement marker. |
| Free implementation slot | `FS` | No other focused implementation issue or PR owns the one-at-a-time slot. |

### Objective

Record one exact implementation commencement on the focused issue. Do not create or move a branch, modify bytes, commit, push, create a PR, request review, merge, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Revalidate every issue requirement and immutable incident identity. Commencement does not authorize copilot-repo-template modification or planning-branch changes.
2. Record the then-current PS `main` commit/tree as the only permitted base and a unique intended branch that does not yet exist.
3. The commencement comment must name the exact authorized path families, the five forbidden metadata-only document edits, the serial slot owner, and the next task. It must not claim implementation or validation success.

### Procedure

1. Re-query Task 1's issue and complete relationships, current PS `main`, open issues/PRs, branches, worktrees, and the implementation slot.
2. Prove the intended branch is absent locally and remotely and no competing affirmative commencement owns the slot.
3. Post one `IMPLEMENTATION COMMENCED` comment with exact base commit/tree, intended branch, authorized scope, exclusions, actor, UTC time, and Task 3 as the next action.
4. Read back the comment and timeline. Confirm no ref, worktree, file, PR, review, merge, closure, or setting changed. Stop.

### Validation and evidence

Require one exact commencement, a free serial slot, exact issue/base/branch identities, an absent intended branch, full readback, and zero repository or ref mutation.

### Stop and escalation conditions

Stop if the issue changed materially, PS `main` cannot be pinned, the branch already exists, the slot is occupied, authorization is ambiguous, or the comment would overstate implementation.

### Exact output

One authenticated `PS_METADATA_POLICY_IMPLEMENTATION_COMMENCED` record contains the issue/comment URL and IDs, exact base commit/tree, intended branch, authorized scope, slot evidence, and proof of zero repository/ref mutation.

### Complete when

The focused issue owns one exact serial commencement record and no implementation action occurred.

## Task 3 — establish clause-level metadata-policy provenance

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Task 2 |
| PS base | `{{PS_METADATA_POLICY_BASE_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_BASE_TREE_SHA}}` | Task 2 |
| Template snapshot | `{{TEMPLATE_MAIN_COMMIT_SHA}}` / `{{TEMPLATE_DOCS_BLOB_SHA}}` | Re-query current template `main` at task start |
| Temporary provenance record | `{{PS_METADATA_PROVENANCE_PATH}}` | Create under a `TEMP-*` path outside permanent governance scope |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 2 | `FS` | The focused issue owns the free slot and pins the PS base and intended branch. |

### Objective

Produce the concise clause-level provenance proof required before any policy edit. Do not edit permanent repository files, create or move a branch, commit, push, create a PR, request review, merge, or close an issue.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Compare the PS and template documents at clause level, not section level. Read the exact blobs pinned at task start.
2. Classify each relevant clause exactly once as `TEMPLATE_EXACT`, `TEMPLATE_ADAPTED`, `PS_REQUIRED_REPOSITORY_ADAPTATION`, `PS_ONLY_SUBSTANTIVE`, `PS_ONLY_PAPERWORK`, or `UNCERTAIN_RETAIN`.
3. A proposed deletion is permitted only when the record proves `PS_ONLY_PAPERWORK`. Different wording is not proof. When a section mixes template-derived and PS-only content, isolate the smallest PS-only clause.
4. Preserve current template rules for Tier 1 fields and placement, published baseline and finalization-point synchronization, ADR Context/Decision/Consequences/Alternatives/Date semantics, and unresolved normative placeholder prohibition.
5. The record is temporary evidence, not a new governance document. Record exact source commit/tree/blob/bytes/SHA-256 and stable clause anchors.

### Procedure

1. Re-query both repositories and issue state. Create an isolated read-only worktree or object view at the PS base; read the template document from the exact current template commit/blob.
2. Extract relevant clauses with stable section/line/text hashes. Compare exact text, adapted wording, semantic requirements, repository-specific facts, examples, and older-template provenance.
3. Classify every clause affected by Tier 1 exception, metadata synchronization, URL/validator coupling, ADR status, and placeholder inspection. Record retained uncertainty explicitly.
4. Build a deletion proof that maps each planned deletion to exactly one `PS_ONLY_PAPERWORK` clause and each retained template-derived semantic to its PS destination.
5. Validate record schema, full clause coverage, unique classifications, source identities, and absence of permanent-file changes. Return the record identity and stop.

### Validation and evidence

Require current PS/template identities, one classification per relevant clause, deletion proof only for `PS_ONLY_PAPERWORK`, explicit `UNCERTAIN_RETAIN` handling, retained-template matrix, record bytes/SHA-256, and zero permanent-file/ref/public mutation.

### Stop and escalation conditions

Stop if either document changes, a clause has mixed or uncertain provenance that cannot be isolated, a deletion lacks proof, template semantics conflict, or analysis would require changing copilot-repo-template.

### Exact output

One `PS_METADATA_CLAUSE_PROVENANCE_READY` record contains the temporary path, bytes/SHA-256, exact source identities, clause counts by class, every deletion mapping, every uncertainty, and proof that no permanent bytes changed.

### Complete when

Every relevant clause has one supported class, every proposed deletion is proved `PS_ONLY_PAPERWORK`, all uncertain clauses are retained, and no permanent file changed.

## Task 4 — implement the PS metadata-policy and validator correction

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Task 3 |
| Base commit/tree | `{{PS_METADATA_POLICY_BASE_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_BASE_TREE_SHA}}` | Task 2; revalidate before branch creation |
| Working branch | `{{PS_METADATA_POLICY_BRANCH}}` | Task 2; create non-forcing from the exact base only in this task |
| Provenance record | `{{PS_METADATA_PROVENANCE_PATH}}`; `{{PS_METADATA_PROVENANCE_SHA256}}` | Task 3 |
| Candidate commit/tree | `{{PS_METADATA_POLICY_CANDIDATE_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_CANDIDATE_TREE_SHA}}` | Produce after implementation and validation |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 2 | `FS` | The issue owns the slot and exact base/branch intent. |
| Task 3 | `FS` | Every proposed documentation deletion is clause-proved `PS_ONLY_PAPERWORK`; uncertain clauses are retained. |

### Objective

Create one isolated, commit-ready PS candidate that removes only proved paperwork and changes metadata validation from complete-history traversal to one published-baseline-to-final-state evaluation. Do not push, create or update a PR, request review, merge, close an issue, publish a handoff, or modify copilot-repo-template.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Keep `.github/copilot-instructions.md` as the Tier 1 visible-header exception. Remove only the documentation requirement to duplicate that exception as `RequiresMetadata = $false`; an internal path mapping can remain.
2. Replace per-visible-parent, intermediate feature-commit, same-day per-commit, squash-history, and full authenticated-range metadata enforcement with the template's published-baseline and finalization-point model. For PRs compare the base branch's published document state with the final PR head. For pushes compare the before and after published states. Do not walk internal topic transitions.
3. Keep PS repository URL and placeholder safety. Remove only the sentence that makes documentation own `Test-AgentInstructions.ps1` architecture, exact-input-revision claims, and manually synchronized validator ownership.
4. Keep all template-derived ADR requirements, including Context, Decision, Consequences, Alternatives Considered, and Date. Define one decision lifecycle status representation with reconciled values and prove an ADR does not need separate metadata and narrative status values.
5. Keep the semantic ban on unresolved normative `TBD`, `TODO`, `FIXME`, and equivalents. Remove the PS-only manual inspection mandate. Do not claim a `check-prohibited-placeholders` hook exists in PS; the verified PS repository does not contain that hook.
6. Inspect `.github/workflows/Test-AgentInstructions.ps1`, `.github/workflows/Test-AgentInstructionParserManifest.mjs`, `.github/workflows/agent-instructions.yml`, all permanent callers, parser manifests, fixtures, and tests. Remove transition marker/range machinery only when provenance and consumer analysis prove it is PS-only and unused after the new design.
7. Preserve required metadata fields and placement, final Last Updated/Version consistency, future-date rejection, parser safety, repository containment, trusted Git-object/workflow-revision controls, protected-input parsing, event/ref guards, branch guards, and failure truth.
8. Do not edit `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md`, `.github/workflows/scripts-README.md`, `docs/ISSUE_EVALUATION_PROMPT.md`, `docs/decisions/0001-accept-in-repository-trust-root.md`, or `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` merely to change metadata.
9. Required deterministic cases include: a multi-commit branch whose final metadata satisfies the published-baseline rule passes even when an intermediate commit violates the old policy; a genuinely invalid final state fails; an ADR needs one lifecycle status; and every retained template-derived documentation requirement still passes.

### Procedure

1. Validate Tasks 2 and 3, re-query the issue and refs, create an isolated worktree at the exact base, and create the intended branch without force. Re-read all applicable repository instructions.
2. Build a permanent-surface and consumer map before deletion. Record every range parameter, marker, workflow input, function, fixture, and test that exists only for complete-history transition enforcement.
3. Edit only the smallest clause-proved policy deltas and the permanent validator/workflow/test surfaces required by the final-state design. Preserve unrelated PR #174 behavior and every protected control.
4. Add or update focused fixtures for the Tier 1 exception, published-baseline finalization, one ADR lifecycle status, placeholder semantics, multi-commit-final-pass, invalid-final-fail, and retained-template requirements.
5. Run focused policy/parser/metadata tests first. Run the complete agent-instruction self-test, parser-manifest suite, workflow-policy suite, PowerShell analysis, Markdown lint, affected package scripts, `git diff --check`, and two clean full pre-commit passes after focused checks pass.
6. Record PowerShell, Node, npm, Python, pre-commit, and Markdown tool versions. Preserve every native exit and bounded failure output.
7. Audit the final diff against the provenance record. Prove every deleted clause is `PS_ONLY_PAPERWORK`, no template-derived semantic was weakened, the five valid documents are unchanged, and no unrelated path changed.
8. Commit the exact candidate locally. Read back commit/tree/parents, changed paths, blobs, modes, bytes, SHA-256 values, validation identities, clean worktree, and zero remote/public mutation. Stop.

### Validation and evidence

Require the full provenance proof; focused and broad test commands, versions, runtimes, and native exits; all four final-state regression cases; complete agent-instruction and parser suites; workflow-policy, Markdown, PSScriptAnalyzer, `git diff --check`, and two clean pre-commit passes; exact unchanged proof for the five valid documents; path-scoped diff; clean worktree; and no push or PR.

### Stop and escalation conditions

Stop if a deletion is not `PS_ONLY_PAPERWORK`, provenance is uncertain, a template-derived rule would weaken, a security/parser/ref guard regresses, the new final-state model accepts invalid final metadata, any unrelated test fails, a five-document byte changes only for metadata, or the base/slot drifts.

### Exact output

One `PS_METADATA_POLICY_CANDIDATE_READY` record contains the local commit/tree/parent, complete changed-path and blob inventory, clause-deletion proof, retained-rule matrix, removed transition consumers, focused/full validation results, unchanged-five-document proof, and zero remote/public mutation.

### Complete when

One clean local candidate implements the complete selected policy without removing template-derived semantics, all affected validation passes, the five valid documents are unchanged, and no PR or remote mutation occurred.

## Task 5 — create or update the PS metadata-policy corrective PR and freeze its reviewer-facing body

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Task 4 |
| Candidate commit/tree | `{{PS_METADATA_POLICY_CANDIDATE_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_CANDIDATE_TREE_SHA}}` | Task 4 |
| Target base | `main` at `{{PS_METADATA_POLICY_PR_BASE_SHA}}` | Re-query immediately before push and PR mutation; must preserve candidate ancestry |
| Candidate PR | `{{PS_METADATA_POLICY_PR_URL}}` | Reuse an exact existing PR or create one after a non-force push |
| Frozen body identity | `{{PS_METADATA_POLICY_PR_BODY_BYTES}}` / `{{PS_METADATA_POLICY_PR_BODY_SHA256}}` | Generate, validate, publish, and read back in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 4 | `FS` | The local candidate is clean, complete, provenance-validated, and not pushed. |

### Objective

Push the exact candidate without force, create or update one accurate PR to PS `main`, semantically validate and freeze the reviewer-facing body, and keep mutable orchestration evidence separate. Do not request review, run the review loop, merge, close an issue, or publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Fetch current `main` and the intended remote branch. Stop on unexpected divergence. Verify candidate ancestry, outgoing commits, changed paths, tree, worktree cleanliness, and branch protection before an explicit non-force push.
2. Generate the body from typed or deterministically recomputed data. Verify every commit, tree, blob, byte count, hash, check result, provenance row, retained/removed requirement, failed-run statement, scope, risk, relationship, and rollback statement.
3. Validate Markdown transport with literal backticks, Unicode, and control-character rejection. Do not use fragile expandable PowerShell strings or exact-line replacement when a typed serializer is practical.
4. The body must not contain live task state, polling state, reviewer IDs, review results, or terminal results. Define a separate compact state and the optional append-only terminal comment location.
5. Freeze the body only after semantic validation and authenticated readback. Record its bytes/SHA-256 and body-edit count. A later semantic materiality classifier, not raw byte inequality, controls review disposition.

### Procedure

1. Validate Task 4. Re-query issue/umbrella state, current `main`, open PRs, branches, permissions, checks, and the slot.
2. Fetch without force. Inspect every outgoing commit and path. Push the exact candidate using an explicit non-force refspec and reconcile the remote head/tree.
3. Search for an exact PR. Create or update only that PR with `main` as base and the focused branch as head. Preserve closing semantics only for the focused issue; umbrella #172 is non-closing.
4. Build, validate, and publish the complete body. Run body fixtures for backticks, Unicode, prohibited control characters, empty/singleton/multiple API collections, and exact identity recomputation.
5. Read back PR base/head/tree, body, edit history, relationships, draft state, merge state, checks, byte count, and hash. Freeze the body and update compact state.
6. Confirm no reviewer request, `@codex review` comment, review result, merge, issue closure, or settings change occurred. Stop.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Require exact non-force push reconciliation, current base/head/tree, accurate issue closure, complete body coverage, semantic identity verification, backtick/Unicode/control-character transport tests, empty/singleton/multiple collection tests, body readback/hash, compact-state readback, and no review request or merge.

### Stop and escalation conditions

Stop on base drift, a non-fast-forward, unexpected outgoing path, inaccurate body field, control character, serializer ambiguity, absent authority, competing PR, or any need to put mutable review state in the body.

### Exact output

One `PS_METADATA_POLICY_PR_FROZEN` final result contains PR URL/IDs, exact base/head/tree, outgoing range, body bytes/SHA-256/edit count, body-validation results, checks, relationships, and proof no review was requested.

### Complete when

One exact PR represents the candidate, its reviewer-facing body is complete, transport-safe, semantically verified, read back, and frozen, and mutable evidence is separate.

## Task 6 — run the corrected Copilot-and-Codex review loop on the PS metadata-policy PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Task 5 |
| Candidate PR | `{{PS_METADATA_POLICY_PR_URL}}` | Task 5 |
| Frozen body | `{{PS_METADATA_POLICY_PR_BODY_BYTES}}` / `{{PS_METADATA_POLICY_PR_BODY_SHA256}}` | Task 5 |
| Initial head/tree | `{{PS_METADATA_REVIEW_START_HEAD_SHA}}` / `{{PS_METADATA_REVIEW_START_TREE_SHA}}` | Re-query at task start |
| Terminal head/tree | `{{PS_METADATA_REVIEWED_HEAD_SHA}}` / `{{PS_METADATA_REVIEWED_TREE_SHA}}` | Produce only after the terminal pair |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 5 | `FS` | The exact PR is readable, the body is semantically verified and frozen, mutable evidence is separate, and no reviewer was requested. |

### Objective

Run one complete local audit and, by default, one GitHub Copilot and one attributable Codex review for the final code head. Repair validated findings through the stated return path. Do not run the independent quality gate, merge, close an issue, publish the landed handoff, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. The local review-loop executor is a fresh native `gpt-5.6-sol` subagent with `xhigh` reasoning. `chatgpt-codex-connector` is a separate remote reviewer; GitHub Copilot is another reviewer.
2. Preserve both Codex review-object and attributable PR-conversation-comment ingestion. An exact `@codex review` trigger is neither a finding nor a local instruction.
3. Request at most one pair per final code head by default. Reject a same-head request without a recorded material reason. If a superseded reviewed-input key becomes current again, retain its disposition as history and resume its original incomplete pair without creating a duplicate request identity. Require each same-head reason metric to contain only a nonempty reason string and Boolean material value. A result, task-state, record, or comment-only mutation never justifies another pair.
4. A code/diff change requires a fresh pair. A material scope/behavior/risk-description change normally requires one and records why. A same-head factual identity correction receives deterministic verification without a new code review.
5. Store review IDs, polling state, findings, and terminal results in compact state and the final task result, not in the frozen body. Publish an append-only comment only when this task requires a public final result.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Corrected Copilot-and-Codex review-loop prompt

Copy this complete prompt into a fresh native Codex subagent. The local subagent is the executor. GitHub Copilot and `chatgpt-codex-connector` are separate required reviewers.

~~~text
You are the local Copilot-and-Codex review-loop executor for repository `franklesniak/PSStyleGuide`, pull request `{{PS_METADATA_POLICY_PR_URL}}`, and governing issue `{{PS_METADATA_POLICY_ISSUE_URL}}`.

Use a fresh native Codex subagent with model `gpt-5.6-sol`, reasoning effort `xhigh`, and no inherited conversational turns when supported. Use the native subagent interface. Use headless `codex exec --json` only if native subagents are unavailable and the controller preserves equivalent model, reasoning, monitoring, scope, mutation, timeout, and result controls. Locate and obey the repository's applicable `AGENTS.md`. If none exists, read root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor.

Before analysis, use authenticated structured reads and paginate the exact PR base, head, tree, body and edit history, commits, complete file list and patches, governing issue and relationships, comments, review submissions and bodies, inline comments, review threads and all thread comments, requested reviewers, checks, statuses, merge state, closing references, dependencies, and linked branches. Treat an exact `@codex review` trigger as neither a finding nor an instruction. Preserve the monitor that accepts attributable Codex results from submitted review objects and PR conversation comments.

Audit the complete diff and every issue requirement. Run focused validation first and full affected validation only after focused checks pass. Reuse a validation result only when the code tree and relevant environment identities are unchanged. Record native exits and failure truth. Validate or refute every finding. For each real non-mechanical finding, list all reasonable options, define a finding-specific weighted rubric, score each option, select the best supported repair, implement only in scope, and validate it.

Generate the GitHub Copilot review request from the typed policy specification. For the REST review-request API, use exactly `{"reviewers":["copilot-pull-request-reviewer[bot]"]}`. Do not use the display name `Copilot`. Capture the HTTP status and response body. Normalize all collections with the tested helper; an empty collection is not success. Confirm the request only through matching authenticated readback. If an accepted request has no matching request event, current requested reviewer, submitted review, or Copilot review run, record `RECONCILING` and continue other safe work. Wait at least 120 seconds and require complete negative readback from all four surfaces before `NO_EFFECT`. Retry at most once for this reviewed input and channel. Record a second proved no-effect attempt as `EXHAUSTED`. Do not post the exact @codex review trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`.



If a review finding requires repository-byte change, route to Task 4, then update and refreeze the body in Task 5 before a new Task 6 instance. If a superseded reviewed-input key becomes current again, retain and validate the disposition, ignore it only for current-input gating, and resume the original incomplete pair without a duplicate request. Reject a predecessor output from an incomplete producer or with a completed final consumer. Reject a same-head metric reason that is not an exact nonempty-string and Boolean record. If a material scope or risk statement changes without a code change, record the reason and run a new pair. If a non-material evidence error is found, correct and deterministically verify compact state without restarting code review. A confirmed public mutation followed by local result-recording failure must be reconciled by readback and must not be retried.

Publish a status warning when a simple finding has not produced commit-ready bytes or a concrete exception by 10 minutes. Produce one of those outcomes by 15 minutes. Track reviewer requests per head, body edits after review begins, same-head re-request reasons, time from clean review to recognition, and time from first clean pair to merge. Do not rerun reviewers as a substitute for deterministic validation.

Return `TERMINALLY_CLEAN` only when the local audit, one GitHub Copilot review, and one attributable Codex review are clean on the same final code head and reviewed input; all findings and threads are terminal; checks and validations are truthful; the frozen body is accurate; mutable results are separate; and no merge occurred. Otherwise return `REVIEW_BLOCKED` with the exact defect, materiality class, and route through Task 4 or Task 5, followed by new Task 6 and Task 7 instances only when the classifier requires them. Do not merge, enable auto-merge, close an issue, or publish the landed handoff.
~~~

### Procedure

1. Copy the complete task-local prompt into one fresh native Codex subagent and monitor it through a terminal result. Use the documented fallback only if native subagents are unavailable.
2. Independently verify complete pagination, issue requirements, provenance proof, current head/tree, frozen body semantics, decisions, repairs, pushes, checks, comments, reviews, and threads returned by the subagent.
3. Record reviewer requests per head, body edits after review begins, same-head request reasons, clean-review recognition latency, and finding-to-ready latency.
4. Reject stale-head evidence, an unvisited page, a trigger treated as a finding, an unresolved synthetic key, an invalid final metadata state, or a template-derived weakening.
5. Keep the terminal audit in the final task result. Publish and read back an external comment only when the task requires it. Stop before quality checking.

### Validation and evidence

Require fresh native-subagent identity and requested model/reasoning, exact current head/tree, frozen semantic input, full provenance and issue coverage, focused and full validation, one clean Copilot review, one clean attributable Codex result, zero unresolved findings/threads/deferrals, truthful checks, materiality records for any mutation, and no merge.

### Stop and escalation conditions

Stop on stale head, body semantic drift, incomplete monitor surfaces, an unproved same-head request, unresolved finding, invalid final metadata, provenance regression, failed check, material mutation without review, or absent review capability.

### Exact output

One authenticated `PS_METADATA_POLICY_TERMINALLY_CLEAN` record contains final head/tree, frozen body identity, local subagent identity, one Copilot result, one attributable Codex result and trigger, complete inventories, decisions, validation, mutation classes, request metrics, and proof no merge occurred.

### Complete when

The local audit and one clean reviewer pair are terminal on the same final code head and frozen semantic input; every finding is closed; no same-head request lacks a material reason; and the PR remains unmerged.

## Task 7 — run the independent final quality check on the PS metadata-policy PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Candidate PR | `{{PS_METADATA_POLICY_PR_URL}}` | Task 6 |
| Required head/tree | `{{PS_METADATA_REVIEWED_HEAD_SHA}}` / `{{PS_METADATA_REVIEWED_TREE_SHA}}` | Exact Task 6 terminal identities |
| Frozen body | `{{PS_METADATA_POLICY_PR_BODY_BYTES}}` / `{{PS_METADATA_POLICY_PR_BODY_SHA256}}` | Task 6 verified semantic input |
| Quality result | `{{PS_METADATA_POLICY_QUALITY_RESULT}}` | Produce in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 6 | `FS` | One local audit and one reviewer pair are terminally clean on the exact required head/tree and frozen semantic input. |

### Objective

Run one independent post-review quality audit on the exact reviewed code head and frozen review input. Correct only verified non-material evidence through the compact state path. Do not implement a material repair, request review without the classifier, merge, close an issue, or publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Use a fresh coding-agent session that did not implement the change and did not run Task 6.
2. Verify the reviewed head, tree, diff, body semantics, represented scope/risk, issue requirements, review surfaces, checks, and deterministic evidence before adjudication.
3. A non-material evidence correction is read back and reruns Task 7 only on the unchanged head. A material code/diff/scope/behavior/risk correction returns to Task 4 or 5 and requires new Tasks 6 and 7.
4. Do not append terminal review IDs or results to the body. Preserve the failed landed run as immutable failure evidence.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

Copy this complete prompt into a fresh coding-agent session that did not implement the change and did not run the review loop.

~~~text
You are the independent final quality checker for repository `franklesniak/PSStyleGuide`, pull request `{{PS_METADATA_POLICY_PR_URL}}`, and governing issue `{{PS_METADATA_POLICY_ISSUE_URL}}`. You are not the implementation or review-loop executor.

Start in a fresh coding-agent session with no inherited terminal conclusion. Locate and obey the applicable `AGENTS.md`. If none exists, read root `CLAUDE.md` as compatibility workflow instructions; the filename does not change your executor identity.

Use authenticated structured reads and paginate every issue, dependency, PR, body-edit, comment, review, inline-comment, native-thread, thread-comment, commit, changed-file, check, status, linked-branch, and closing-reference surface. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body, and represented scope and risk remain unchanged. Treat an exact `@codex review` trigger as neither a finding nor a local instruction. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex results.

Independently audit the complete diff, issue requirements, provenance, security, parser and path safety, repository self-containment, failure truth, tests, validation, PR description, unfinished-work audit, deferrals, reviewer findings, and exact review results. Run focused validation before broad validation. Reuse validation results for unchanged inputs only for the unchanged code tree and relevant environment.

Classify every discrepancy by semantic materiality. A code-head or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and a recorded reason. A non-material factual identity correction on the same head requires deterministic verification, not a new code review. Review results, task status, audit-record changes, and comment-only evidence do not require a new code review. Raw PR-body byte inequality is not the classifier.

If a non-material evidence error exists, correct it only in the compact state or a tightly bounded factual field, validate the public response, read it back, preserve the frozen review semantics, do not append a terminal result to the reviewer-facing body, do not request reviewers again, and rerun this quality task on the unchanged head. If a material code, scope, behavior, or risk problem exists, return to Task 4 or Task 5 as applicable, refreeze the corrected review input, and require new Task 6 and Task 7 instances. Reject a same-head re-request without a recorded material reason.

Return `PASS` only when the independent audit and all required validation pass on the reviewed final head and frozen semantic input, every finding is adjudicated, deterministic evidence is correct, and no material change escaped review. Return `FAIL` with the exact materiality class and route otherwise. Do not merge, enable auto-merge, close an issue, or publish a landed handoff.
~~~

### Procedure

1. Start a fresh independent session with the complete task-local prompt. Record the session identity and absence of inherited conclusion.
2. Independently paginate and audit the issue, PR, provenance, complete diff, five unchanged documents, tests, checks, reviews, comments, threads, frozen body, and exact head/tree.
3. Run focused validation, then the full affected suites. Record versions, commands, runtimes, native exits, and any reused result with its unchanged inputs in the final task result.
4. Classify any discrepancy. Correct only non-material compact state with validated response and readback; rerun this task without new review. Route material defects to Tasks 4/5 and require Tasks 6/7 anew.
5. Return the exact quality result and stop before merge.

### Validation and evidence

Require fresh-session identity, exact Task 6 head/tree, unchanged frozen semantics, complete independent pagination and provenance audit, focused/full test results, five-document unchanged proof, correct materiality disposition, zero unresolved work, and no unauthorized mutation or merge.

### Stop and escalation conditions

Stop and return the exact route if the head/diff changes, scope or risk changes materially, evidence cannot be deterministically corrected, a template-derived rule weakens, the five valid documents changed only for metadata, validation fails, or a same-head reviewer request is proposed without material reason.

### Exact output

One `PS_METADATA_POLICY_FINAL_QUALITY_PASS` record contains the independent session identity, exact head/tree/body semantics, provenance and requirements matrix, commands/runtimes/results, mutation classifications, any non-material readback, and proof no merge occurred.

### Complete when

A fresh independent session returns `PASS` for the exact reviewed head and frozen semantic input, all deterministic evidence is correct, and every material change path has received the required review.

## Task 8 — merge the PS metadata-policy corrective PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Candidate PR | `{{PS_METADATA_POLICY_PR_URL}}` | Tasks 6 and 7 |
| Gated head/tree | `{{PS_METADATA_REVIEWED_HEAD_SHA}}` / `{{PS_METADATA_REVIEWED_TREE_SHA}}` | Must equal Tasks 6 and 7 |
| Landed commit/tree | `{{PS_METADATA_POLICY_LANDED_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_LANDED_TREE_SHA}}` | Resolve after merge |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 6 | `FS` | One reviewer pair is terminally clean on the gated head/tree and frozen semantic input. |
| Task 7 | `FS` | Independent quality passes on the same gated head/tree and input. |

### Objective

Merge only the exact head/tree that passed Tasks 6 and 7 while the merge remains on-plan. Do not repair, update the body, request review, publish the handoff, perform cross-repository comparison, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Re-query the complete PR and issue state. Require current head/tree and frozen semantic input to equal both gates.
2. Classify every post-gate mutation. Comment-only results and compact-state updates do not invalidate review. Any code/diff or material scope/behavior/risk change blocks merge and returns through Tasks 4–7.
3. Require all applicable checks terminal and truthful, zero unresolved threads/findings, accurate closing reference, mergeability, allowed merge method, and no competing slot owner.
4. Treat this task and the instruction to execute the plan as standing merge authority only while every on-plan condition in the shared policy remains true. Do not request or record separate per-merge approval.

### Procedure

1. Validate both results and confirm that the merge remains on-plan. Re-query PR base/head/tree/body semantics, issues, dependencies, comments, reviews, threads, checks, statuses, merge state, and current `main`.
2. Prove no material change occurred after either gate and no same-head request was needed for record-only publication.
3. Use one permitted non-bypassing merge method. Do not enable auto-merge unless this task expressly requires it.
4. Read back PR merge state, merge commit/tree/parents, `main`, issue state/reason, umbrella state, checks, and changed-path blobs.
5. Confirm the old run remains failed and no five-document metadata-only edit was introduced. Release the slot and stop before handoff.

### Validation and evidence

Require exact gate equality, an on-plan merge classification, truthful terminal checks, zero unresolved work, semantic mutation audit, allowed merge method, landed object readback, focused issue terminal state, unchanged failure truth, and no successor publication.

### Stop and escalation conditions

Stop if the merge is off-plan, head/tree/body semantics drift, a material mutation occurred, a required check is not success, mergeability is false/unknown, an issue relationship is wrong, or the merge method would bypass protection. Do not stop only to obtain separate operator approval.

### Exact output

One authenticated `PS_METADATA_POLICY_MERGED` record contains the on-plan classification, PR/method/time/actor, gated head/tree/body, landed commit/tree/parents, final issue/main/check state, mutation audit, and failed-run preservation.

### Complete when

The exact reviewed and independently quality-checked head lands on PS `main` under standing plan authority, readback is complete, the focused issue reaches its correct terminal state, and no handoff or comparison occurred.

## Task 9 — publish the PS metadata-policy landed handoff

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{PS_METADATA_POLICY_ISSUE_URL}}` | Task 8 |
| Landed commit/tree | `{{PS_METADATA_POLICY_LANDED_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_LANDED_TREE_SHA}}` | Task 8 |
| Provenance record | `{{PS_METADATA_PROVENANCE_PATH}}` / `{{PS_METADATA_PROVENANCE_SHA256}}` | Task 3, revalidated by Tasks 4 and 7 |
| Handoff comment | `{{PS_METADATA_POLICY_HANDOFF_COMMENT_URL}}` | Create and read back in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 3 | `FS` | The exact provenance proof remains valid for the landed bytes. |
| Task 8 | `FS` | The gated metadata-policy PR is merged and its landed state is exact. |

### Objective

Publish one permanent, read-back landed handoff for the metadata-policy correction. Do not modify repository bytes or PR body, rerun reviewers, start the planning-branch implementation, perform the cross-repository comparison, close umbrella #172, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Publish the task-required landed handoff as an append-only issue comment, never by modifying the reviewer-facing PR body.
2. Include changed files, every removed `PS_ONLY_PAPERWORK` clause, every retained template-derived requirement, all uncertainty retained, old-to-new validator architecture, deleted transition consumers, regression results, commands/runtimes/native exits, checks/reviews, landed objects, and remaining risks.
3. Preserve run 33309460684/job 99251756566 as an immutable failure and state that it was not rerun to seek a different result.
4. Prove the five documents named by the failed run were not edited merely for metadata. Identify the exact next action as Task 10; do not start it.

### Procedure

1. Validate Task 8 and re-query landed `main`, PR, issue, umbrella, reviews, comments, threads, checks, statuses, and exact changed-path blobs.
2. Reconcile the landed diff with the Task 3 provenance record and Task 4 validation. Recompute body-independent evidence and confirm the frozen PR body was not modified after review for results.
3. Prepare one handoff. Validate Markdown transport, control-character absence, exact identities, and failure truth.
4. Post the handoff to the focused issue and, only when the umbrella tracking model requires it, an exact cross-reference to #172. Validate the response, write the local result, and read back the public comment.
5. Confirm no reviewer request, PR-body edit, branch/ref mutation, new implementation commencement, comparison, closure, or setting change. Stop.

### Validation and evidence

Require exact landed objects, complete lifecycle pagination, final provenance proof, removed/retained/uncertain clause matrix, focused/full validation and runtime/native exits, five-document unchanged proof, immutable failed-run statement, exact comment bytes/hash/readback, and no same-head re-review or successor action.

### Stop and escalation conditions

Stop if landed bytes differ from the gated candidate, provenance no longer covers a deletion, a template-derived semantic weakened, failure truth is misstated, handoff transport is corrupt, public mutation outcome is ambiguous, or a body edit/reviewer retry would be used for the result.

### Exact output

One authenticated `PS_METADATA_POLICY_LANDED_HANDOFF` record contains `{{PS_METADATA_POLICY_HANDOFF_COMMENT_URL}}`, landed commit/tree/blobs, provenance proof, retained and removed requirements, validation/review/check results, failed-run preservation, risks, and exact next action Task 10.

### Complete when

A permanent, exact, read-back handoff proves the complete landed correction and no planning-branch implementation, reviewer retry, PR-body result edit, or cross-repository comparison occurred.

## Task 10 — create or update the planning-branch review-loop corrective issue

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Corrective issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Search all open/closed work items; reuse only an exact match, otherwise create one |
| Planning base | remote `planning-CRT-PR-852` at `{{REVIEW_LOOP_ISSUE_BASE_SHA}}` / `{{REVIEW_LOOP_ISSUE_BASE_TREE_SHA}}` | Re-query immediately before mutation; planning snapshot was `4f0896728dc0275b6c4fa19f7442335ae2e26bf4` |
| Metadata handoff | `{{PS_METADATA_POLICY_HANDOFF_COMMENT_URL}}` | Task 9 |
| Incident | PR #174 and its complete review/body-edit history | Immutable root-cause input |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 9 | `FS` | The metadata-policy correction is landed, documented, and its serial slot is released. |
| Planning branch inventory | `FS` | The remote base exists and the controlling files are absent from `main`. |

### Objective

Create or update one focused issue for the permanent review-loop architecture correction on `planning-CRT-PR-852`. Do not commence implementation, edit planning files, create a branch/PR, request review, merge, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. The issue must require a complete inventory of every permanent instruction, controller, script, and task template that can request or repeat review. Temporary RCA artifacts are evidence, not the only search scope or permanent production inputs.
2. Include body freeze, compact state, one pair per final code head, semantic mutation classes, post-review ordering, preserved dual-channel Codex monitor, publication idempotency, all required collection/transport/serialization tests, 10/15-minute controls, and five requested metrics.
3. Include all nine review-loop regression scenarios from the supplied work item. Metadata regression scenarios 10–12 are satisfied and linked through Task 9; do not duplicate the metadata implementation on the planning branch.
4. State that the PR must target `planning-CRT-PR-852`, not `main`, unless a new live inventory proves the controlling files moved. Do not copy planning-only files into `main`.

### Procedure

1. Re-query Task 9, planning branch ref/tree, current `main`, open work items/PRs, PR #174, planning-file blobs, and the serial slot.
2. Search all PS work items for an exact existing review-loop corrective owner. Build a full issue-body requirement matrix.
3. Create one issue if none exists; otherwise update only stale or missing scope. Link PR #174 and Task 9 evidence by exact references. Add no false blocked-by edge.
4. Read back body, edits, relationships, state, actor, timestamp, bytes, and SHA-256. Confirm no commencement marker, branch link, closing PR, ref/file mutation, or review request. Stop.

### Validation and evidence

Require exact planning-base and incident identities, complete requirement matrix, all nine scenarios, correct base boundary, accurate relationship state, exact body readback/hash, and no implementation commencement.

### Stop and escalation conditions

Stop if controlling files are no longer on the named base, an exact issue already owns the work, the issue would mix metadata-main implementation, a relationship would be false, or scope omits a permanent request surface.

### Exact output

One authenticated `REVIEW_LOOP_CORRECTIVE_ISSUE_READY` record contains issue URL/IDs, planning base commit/tree, body bytes/hash, relationships, complete architecture/scenario scope, and proof no implementation commenced.

### Complete when

One self-contained issue owns the complete review-loop correction on the correct planning base and no implementation or ref mutation occurred.

## Task 11 — commence the planning-branch review-loop correction

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Task 10 |
| Target base | `{{REVIEW_LOOP_BASE_COMMIT_SHA}}` / `{{REVIEW_LOOP_BASE_TREE_SHA}}` | Re-query remote `planning-CRT-PR-852` immediately before commencement |
| Intended branch | `{{REVIEW_LOOP_CORRECTIVE_BRANCH}}` | Choose a unique focused branch; prove absence |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 10 | `FS` | The issue is self-contained, open, and has no commencement marker. |
| Free implementation slot | `FS` | Task 9 released the metadata slot and no other implementation owns it. |

### Objective

Record one exact review-loop implementation commencement on the focused issue. Do not create or move a branch, edit files, commit, push, create a PR, request review, merge, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Pin the current remote `planning-CRT-PR-852` commit/tree. Do not use dirty bytes from the present checkout as the base.
2. Name the complete permanent-surface inventory as the first implementation input and state that no production edit can occur before Task 12 completes.
3. Reserve exactly one serial slot and name Task 12 as the next action. The commencement record is not implementation or protected-file authorization beyond the issue's exact scope.

### Procedure

1. Re-query Task 10's issue, planning and main refs, branches, worktrees, open PRs/issues, and the slot.
2. Prove the intended branch is absent locally/remotely and the planning base is reachable and unambiguous.
3. Post one commencement comment with exact base commit/tree, intended branch, permanent-surface scope, exclusions, slot owner, actor/time, and Task 12 as next action.
4. Read back the comment/timeline and prove no ref, file, PR, review, merge, closure, or settings mutation. Stop.

### Validation and evidence

Require one exact commencement, free slot, pinned planning base, absent intended branch, full comment/timeline readback, and zero repository/ref mutation.

### Stop and escalation conditions

Stop if the planning ref drifts unexpectedly, intended branch exists, the current checkout would need to be copied, the slot is occupied, or the comment would overstate implementation.

### Exact output

One authenticated `REVIEW_LOOP_IMPLEMENTATION_COMMENCED` record contains issue/comment identities, exact base commit/tree, intended branch, authorized scope, slot evidence, and zero-mutation proof.

### Complete when

The focused issue owns one exact serial commencement and no implementation action occurred.

## Task 12 — inventory every permanent review-loop mutation and request surface

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Task 11 |
| Planning base | `{{REVIEW_LOOP_BASE_COMMIT_SHA}}` / `{{REVIEW_LOOP_BASE_TREE_SHA}}` | Task 11 |
| Temporary inventory | `{{REVIEW_LOOP_SURFACE_INVENTORY_PATH}}` | Create under `TEMP-*` outside permanent source |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 11 | `FS` | The issue owns the slot and pins the planning base and intended branch. |

### Objective

Produce a complete machine-readable inventory of permanent surfaces that construct, mutate, review, monitor, retry, or publish PR state. Do not edit permanent files, create/move a branch, commit, push, create a PR, request review, or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Search the exact planning tree for `@codex review`, Copilot review requests, PR body construction or mutation, body hashes/edit histories, reviewer IDs/results, polling state, results, retry decisions, mutation serializers, readbacks, same-head conditions, materiality, and review/quality prompts.
2. Cover at least `docs/planning/coding-agent-loop.md`, `docs/planning/coding-agent-loop-without-model-routing.md`, `docs/planning/coding-agent-loop-performance-plan-2026-08-25.md`, `docs/planning/prompt-action-items-update.md`, action-plan generators/validators when tracked, and every permanent task template or script discovered. Do not assume the list is complete until the search proves it.
3. Distinguish tracked permanent source, generated permanent artifact, compact state, and historical temporary result. Never promote historical results to production source.
4. For each surface, record producer/consumer, mutation target, review invalidation key, retry/idempotency behavior, collection cardinality assumptions, transport mechanism, dual-channel monitor role, test coverage, and required disposition.

### Procedure

1. Re-query the planning base and issue. Inspect the exact Git tree and tracked files from an isolated read-only checkout.
2. Run bounded repository-wide text and structural searches for every named review request, body mutation, result, retry, monitor, controller, task-template, and serializer concept.
3. Trace producers to consumers and public mutations. Identify every path capable of a same-head request or of modifying reviewer input after review begins.
4. Verify that submitted review objects and attributable Codex conversation comments are both represented; mark any proposed removal as forbidden.
5. Write the temporary inventory, validate schema and full search coverage, record bytes/SHA-256 and search commands/exits, prove no permanent/ref/public mutation, and stop.

### Validation and evidence

Require exact base identity, repository-wide tracked search coverage, one inventory row per producer/consumer/mutation surface, explicit same-head and serializer analysis, preserved dual-channel paths, schema validation, inventory hash, and zero permanent/ref/public mutation.

### Stop and escalation conditions

Stop if the base changes, a permanent surface cannot be read, search scope is truncated, a producer/consumer chain is unresolved, temporary evidence is mistaken for production, or full inventory would require unauthorized external mutation.

### Exact output

One `REVIEW_LOOP_SURFACE_INVENTORY_READY` record contains the temporary path/hash, exact base/tree, search commands/exits, complete surface rows, current defect paths, preserved monitor paths, and zero-mutation proof.

### Complete when

Every permanent review-request and reviewer-input mutation surface has one traced disposition and no permanent file changed.

## Task 13 — implement the corrected review-loop architecture on the planning branch

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Task 12 |
| Base commit/tree | `{{REVIEW_LOOP_BASE_COMMIT_SHA}}` / `{{REVIEW_LOOP_BASE_TREE_SHA}}` | Task 11; revalidate before branch creation |
| Working branch | `{{REVIEW_LOOP_CORRECTIVE_BRANCH}}` | Task 11; create from the exact planning base only now |
| Surface inventory | `{{REVIEW_LOOP_SURFACE_INVENTORY_PATH}}` / `{{REVIEW_LOOP_SURFACE_INVENTORY_SHA256}}` | Task 12 |
| Candidate commit/tree | `{{REVIEW_LOOP_CANDIDATE_COMMIT_SHA}}` / `{{REVIEW_LOOP_CANDIDATE_TREE_SHA}}` | Produce after validation |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 11 | `FS` | The issue owns the slot and exact planning base/branch intent. |
| Task 12 | `FS` | Every permanent request, mutation, monitor, serializer, and template surface has a traced disposition. |

### Objective

Create one isolated, commit-ready planning-branch candidate that prevents record/body bookkeeping from invalidating clean same-head reviews while preserving material-change review safety and dual-channel monitoring. Do not push, create/update a PR, request review, merge, close an issue, or copy planning-only files into `main`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Generate and semantically verify the reviewer-facing body before external review. Freeze it; exclude task state, review IDs/results, polling state, and terminal results.
2. Store mutable orchestration evidence separately. A review result must never modify an artifact whose modification invalidates that review.
3. Default to one Codex and one Copilot review per final code head. Require a new pair for code/diff change and normally for material scope/behavior/risk-description change. Do not rerequest for factual identity correction, result, task state, audit record, or comment-only publication. Reject an unjustified same-head request.
4. Implement explicit semantic mutation classes. Do not use raw PR-body byte inequality as the decision.
5. Make post-review quality verify unchanged reviewed head/input, adjudicate findings, and confirm deterministic evidence. Non-material correction does not restart code review; material code/scope/risk returns to implementation.
6. Retain both submitted-review and attributable PR-conversation-comment ingestion for Codex.
7. Replace fragile exact-line and expandable-string body construction where practical. Normalize empty, singleton, and multiple API collections through the tested helper. Preserve Markdown backticks, Unicode, and control-character rejection.
8. Generate exact typed reviewer requests, including the documented Copilot REST login. Treat a successful public API response and authenticated readback as the idempotency boundary. A later local serialization failure cannot repeat the public mutation. Add bounded no-effect reconciliation and a one-retry maximum without weakening confirmed-request suppression.
9. Publish a 10-minute warning for a simple finding and produce commit-ready bytes or a concrete exception by 15 minutes. Track reviewer requests/head, body edits after review, same-head reasons, clean-review recognition time, and clean-pair-to-merge time.
10. Required deterministic scenarios: clean pair plus compact-state update creates no request; non-material same-head correction creates no request; H1→H2 requires a pair; authenticated H1→H2 drift can supersede an impossible missing old-head channel without synthetic evidence only after every recorded old-input request is terminal; a live requested channel remains pending across same-head drift; a superseded input can become current again and resume its original incomplete pair; malformed, complete-pair, head-mismatched, pre-request, or requestless supersessions fail during ingestion; a terminal unconfirmed Copilot request requires a typed repository-authorized non-functional disposition; `current_task.head` must match the review-input head; non-finite or out-of-portable-range JSON numbers fail before parsing can change them; material same-head scope/risk change records reason and requires review; malformed same-head metric reasons fail during construction; unjustified same-head request is rejected; submitted-review and headless conversation-comment Codex results are attributed safely; the Copilot REST request uses only `copilot-pull-request-reviewer[bot]`; empty/singleton/multiple reviewer collections work and an empty response is not success; conversation baselines use unique node identities and duplicate JSON members fail ingestion; PowerShell-significant Markdown backticks and Unicode transport safely; confirmed public mutation plus result-recording failure reconciles without retry; an accepted no-effect request stays `RECONCILING` for at least 120 seconds, then permits one retry and exhausts after a second proved no-effect attempt; contradictory persisted mutation states fail schema validation; predecessor outputs survive restart until their final consumer and are then pruned; and semantic ingestion rejects future producers and expired retained outputs.

### Procedure

1. Validate Tasks 11 and 12. Create an isolated worktree and focused branch from the exact remote planning base. Read applicable repository instructions.
2. Convert the inventory into a closed implementation matrix. Edit every permanent controlling surface that would otherwise retain the defect; do not edit historical temporary results as production code.
3. Add typed review-input and mutation-class schemas, deterministic body verification/freeze, compact state, same-head rejection with material-reason field, dual-channel monitor reconciliation, publication response/readback state, and the task-required performance measurements.
4. Add focused tests for all required deterministic scenarios and for control-character rejection, Markdown transport, collection cardinality, ambiguous/successful mutation reconciliation, and retry suppression.
5. Update every permanent review-loop and quality task template in scope. Keep complete copy-paste prompts; do not replace them with a shared operative reference.
6. Run focused classifier, body serializer, monitor, collection, and publication tests first. Then run all affected controller/manager suites, PowerShell parsing/PSScriptAnalyzer where applicable, Markdown lint, plan/task-template validators, `git diff --check`, and two clean pre-commit passes.
7. Audit the final diff against the inventory. Prove every defect surface is corrected, dual-channel behavior remains, no temporary result became source, no unrelated planning work was overwritten, and no `main`-only path was created.
8. Commit locally. Read back commit/tree/parent, paths/blobs/modes/hashes, tests/runtimes/native exits, clean worktree, and zero remote/public mutation. Stop.

### Validation and evidence

Require all deterministic scenarios; focused then full controller/manager/template suites; collection/backtick/Unicode/control-character/serializer/readback/retry tests; preserved dual-channel monitor proof; 10/15-minute and metric fields; Markdown, PowerShell, `git diff --check`, and two pre-commit passes; exact inventory closure; clean local commit; and no push/PR.

### Stop and escalation conditions

Stop if a permanent surface remains unclassified, same-head review can repeat without material reason, record mutation invalidates review, raw body-byte inequality controls disposition, dual-channel ingestion regresses, a successful mutation can be retried after local failure, tests fail, or unrelated planning bytes would be overwritten.

### Exact output

One `REVIEW_LOOP_CANDIDATE_READY` record contains local commit/tree/parent, changed-surface matrix, all deterministic scenario results, complete validation/runtimes/exits, metric/performance schema, dual-channel proof, inventory closure, and zero remote/public mutation.

### Complete when

One clean planning candidate corrects every inventoried permanent defect surface, all required scenarios and full affected validation pass, and no PR or remote mutation occurred.

## Task 14 — create or update the review-loop corrective PR and freeze its reviewer-facing body

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Task 13 |
| Candidate commit/tree | `{{REVIEW_LOOP_CANDIDATE_COMMIT_SHA}}` / `{{REVIEW_LOOP_CANDIDATE_TREE_SHA}}` | Task 13 |
| Target base | `planning-CRT-PR-852` at `{{REVIEW_LOOP_PR_BASE_SHA}}` | Re-query immediately before push and PR mutation |
| Candidate PR | `{{REVIEW_LOOP_CORRECTIVE_PR_URL}}` | Reuse an exact existing PR or create one after non-force push |
| Frozen body | `{{REVIEW_LOOP_PR_BODY_BYTES}}` / `{{REVIEW_LOOP_PR_BODY_SHA256}}` | Produce and read back in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 13 | `FS` | The local planning candidate is complete, clean, fully validated, and not pushed. |

### Objective

Push the exact candidate without force, create or update one PR targeting `planning-CRT-PR-852`, semantically validate and freeze its reviewer-facing body, and keep mutable orchestration evidence separate. Do not request review, merge, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Prove the current target is the remote planning branch and the candidate contains no unrelated or `main`-copied bytes. Stop on base drift that invalidates ancestry or inventory.
2. Generate the body from typed data and verify every commit, tree, blob, byte/hash, scenario, check, changed surface, scope/risk, incident relationship, and rollback statement.
3. Validate empty/singleton/multiple collection transport, PowerShell-significant backticks, Unicode, and control-character rejection using the candidate implementation itself where possible.
4. Exclude live task state, polling, review IDs/results, and terminal results. Freeze the semantic body only after authenticated readback and update compact state.

### Procedure

1. Validate Task 13 and re-query issue, planning base/ref, branches, open PRs, permissions, checks, and slot.
2. Fetch without force, verify full outgoing range and path closure, push the exact commit with an explicit non-force refspec, and reconcile remote head/tree.
3. Create or update one PR with base `planning-CRT-PR-852`. Do not target `main` and do not close umbrella #172.
4. Generate, validate, publish, and read back the complete reviewer body. Run the transport, collection, and control-character fixtures.
5. Record exact PR/head/base/tree/body/edit/relationship/check identities in the final task result. Confirm no reviewer request, trigger comment, review result, merge, issue closure, or setting change. Stop.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Require non-force push reconciliation, exact planning base/head/tree, path closure, accurate PR relationships, semantic body verification, all transport/cardinality fixtures, frozen body hash, compact-state readback, and no review request or merge.

### Stop and escalation conditions

Stop on base/ancestry drift, unexpected path, wrong PR base, inaccurate body, control character, serializer ambiguity, competing PR, or any need to put mutable evidence in the body.

### Exact output

One `REVIEW_LOOP_PR_FROZEN` final result contains PR URL/IDs, exact base/head/tree/outgoing range, body bytes/hash/edit count, semantic/transport results, checks, and proof no review was requested.

### Complete when

One exact planning-base PR represents the candidate and has a complete, transport-safe, read-back, frozen reviewer body with compact state.

## Task 15 — run the corrected Copilot-and-Codex review loop on the review-loop PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Task 14 |
| Candidate PR | `{{REVIEW_LOOP_CORRECTIVE_PR_URL}}` | Task 14 |
| Frozen body | `{{REVIEW_LOOP_PR_BODY_BYTES}}` / `{{REVIEW_LOOP_PR_BODY_SHA256}}` | Task 14 |
| Terminal head/tree | `{{REVIEW_LOOP_REVIEWED_HEAD_SHA}}` / `{{REVIEW_LOOP_REVIEWED_TREE_SHA}}` | Produce only after the terminal pair |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 14 | `FS` | The exact planning PR is readable, semantically frozen, and has no prior request from this task instance. |

### Objective

Run one complete local audit and one reviewer pair by default on the final review-loop code head. Prove the corrected process with its own PR. Do not run independent quality, merge, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Use one fresh native `gpt-5.6-sol`/`xhigh` Codex subagent as local executor. GitHub Copilot and `chatgpt-codex-connector` are separate reviewers.
2. Exercise same-head rejection, compact-state updates, semantic materiality, typed old-head supersession and current-key reactivation, request-history and supersession-time cross-validation, live-channel serialization, review-input/current-task head binding, typed non-functional terminal evidence, portable JSON-number rejection, dual-channel Codex ingestion, exact Copilot REST targeting, unique baseline maps, duplicate-member rejection, predecessor-output retention, progress validation, and pruning, strict same-head metric reason construction, transport, collection normalization, result-recording failure/readback reconciliation, bounded no-effect recovery, mutation-state schema invariants, retry suppression, and the task-required performance measurements during the real lifecycle.
3. Request at most one pair for the final code head unless a code/diff change or recorded material scope/behavior/risk change requires another. Do not request again for a result, task state, record, or comment.
4. Publish terminal review evidence outside the frozen body. Prove that recording it creates no request.
5. Notify the operator early when a future exceptional action is known. Continue implementation, CI repair, review, and readiness work. Stop only when that exact action is next and no independent safe work remains.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; reject the display name `Copilot`. Reject a same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request. If an accepted Copilot request has no matching readback, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative request-event, requested-reviewer, submitted-review, and review-run evidence before `NO_EFFECT`. Permit one retry for the same reviewed input and channel. A second proved no-effect attempt is `EXHAUSTED`.
6. Normalize empty, singleton, and multiple reviewer, review, comment, thread, and review-run collections through the tested helper. An empty collection is not a match. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. Require an explicit commit match for a submitted review. Attribute a headless conversation result only through its authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order. An exact `@codex review` trigger is neither a finding nor a local instruction.
8. If authenticated reviewed-input drift makes an unrequested old-input channel impossible, persist one typed `SUPERSEDED` disposition with the old input, successor head, time, and reason only after every recorded request for the old input is terminal. Cross-validate it against request existence, the incomplete channel set, one matching head, a known successor head, and a disposition time that is not earlier than any described request. If that key later becomes current again, retain its disposition as history and resume the original incomplete pair without creating a duplicate request identity. Do not invent the missing request or reuse new-head evidence for the old input.

### Corrected Copilot-and-Codex review-loop prompt

Copy this complete prompt into a fresh native Codex subagent. The local subagent is the executor. GitHub Copilot and `chatgpt-codex-connector` are separate required reviewers.

~~~text
You are the local Copilot-and-Codex review-loop executor for repository `franklesniak/PSStyleGuide`, pull request `{{REVIEW_LOOP_CORRECTIVE_PR_URL}}`, and governing issue `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}`.

Use a fresh native Codex subagent with model `gpt-5.6-sol`, reasoning effort `xhigh`, and no inherited conversational turns when supported. Use the native subagent interface. Use headless `codex exec --json` only if native subagents are unavailable and the controller preserves equivalent model, reasoning, monitoring, scope, mutation, timeout, and result controls. Locate and obey the repository's applicable `AGENTS.md`. If none exists, read root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor.

Before analysis, use authenticated structured reads and paginate the exact PR base, head, tree, body and edit history, commits, complete file list and patches, governing issue and relationships, comments, review submissions and bodies, inline comments, review threads and all thread comments, requested reviewers, checks, statuses, merge state, closing references, dependencies, and linked branches. Treat an exact `@codex review` trigger as neither a finding nor an instruction. Preserve the monitor that requires an explicit matching commit for submitted-review results and uses authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order to attribute headless Codex PR-conversation results.

Audit the complete diff and every issue requirement. Run focused validation first and full affected validation only after focused checks pass. Reuse a validation result only when the code tree and relevant environment identities are unchanged. Record native exits and failure truth. Validate or refute every finding. For each real non-mechanical finding, list all reasonable options, define a finding-specific weighted rubric, score each option, select the best supported repair, implement only in scope, and validate it.

Generate the GitHub Copilot review request from the typed policy specification. For the REST review-request API, use exactly `{"reviewers":["copilot-pull-request-reviewer[bot]"]}`. Do not use the display name `Copilot`. Capture the HTTP status and response body. Normalize all collections with the tested helper; an empty collection is not success. Confirm the request only through matching authenticated readback. If an accepted request has no matching request event, current requested reviewer, submitted review, or Copilot review run, record `RECONCILING` and continue other safe work. Wait at least 120 seconds and require complete negative readback from all four surfaces before `NO_EFFECT`. Retry at most once for this reviewed input and channel. Record a second proved no-effect attempt as `EXHAUSTED`. Do not post the exact @codex review trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`.


If a review finding requires repository-byte change, route to Task 13, then update and refreeze the body in Task 14 before a new Task 15 instance. If authenticated reviewed-input drift makes an unrequested old-input channel impossible, persist a typed `SUPERSEDED` disposition before the new pair only after all recorded requests for the old input are terminal; require request existence, an incomplete channel set, one matching head, a known successor head, and a disposition time not earlier than its requests. Do not invent a request or misattribute a result. If a superseded key becomes current again, retain and validate the disposition, ignore it only for current-input gating, and resume the original incomplete pair without a duplicate request. Require `current_task.head` to equal the review-input head. Reject a predecessor output from an incomplete producer or with a completed final consumer. Reject non-finite or out-of-portable-range JSON numbers before parsing and encode larger exact identifiers as strings. Reject a same-head metric reason that is not an exact nonempty-string and Boolean record. If a material scope or risk statement changes without a code change, record the reason and run a new pair. If a non-material evidence error is found, correct and deterministically verify compact state without restarting code review. A confirmed public mutation followed by local result-recording failure must be reconciled by readback and must not be retried.

Publish a status warning when a simple finding has not produced commit-ready bytes or a concrete exception by 10 minutes. Produce one of those outcomes by 15 minutes. Track reviewer requests per head, body edits after review begins, same-head re-request reasons, time from clean review to recognition, and time from first clean pair to merge. Do not rerun reviewers as a substitute for deterministic validation.

Return `TERMINALLY_CLEAN` only when the local audit, one GitHub Copilot review, and one attributable Codex review are clean on the same final code head and reviewed input; all findings and threads are terminal; checks and validations are truthful; the frozen body is accurate; mutable results are separate; and no merge occurred. Otherwise return `REVIEW_BLOCKED` with the exact defect, materiality class, and route through Task 13 or Task 14, followed by new Task 15 and Task 16 instances only when the classifier requires them. Do not merge, enable auto-merge, close an issue, or publish the landed handoff.
~~~

### Procedure

1. Copy the complete prompt into one fresh native subagent and monitor it to terminal state.
2. Independently verify the entire surface inventory, complete diff, all deterministic scenarios, validations, current head/tree, frozen input, reviews, comments, threads, checks, materiality decisions, and metrics.
3. Attempt the deterministic same-head suppression fixtures and confirm no public reviewer request is repeated.
4. Read back any separate terminal result and prove it did not modify reviewer input or trigger another pair.
5. Return the exact terminal review record and stop before quality checking.

### Validation and evidence

Require fresh local executor identity/model/reasoning, complete pagination, inventory closure, all deterministic scenarios, frozen input, one clean Copilot and one clean attributable Codex result for the final reviewed input, same-head suppression, dual-channel ingestion, truthful checks, metrics, and no merge.

### Stop and escalation conditions

Stop on unjustified same-head request, record/body coupling, monitor regression, serializer retry risk, failed scenario/check, stale head, material change without review, or unavailable required reviewer.

### Exact output

One authenticated `REVIEW_LOOP_TERMINALLY_CLEAN` final result contains final head/tree/body, local and reviewer results, trigger, complete inventories, materiality and retry evidence, nine scenarios, task-required measurements, and proof no merge occurred.

### Complete when

The local audit and one clean reviewer pair are terminal on the same final code head and frozen semantics, recording results caused no rerequest, and the PR remains unmerged.

## Task 16 — run the independent final quality check on the review-loop PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Candidate PR | `{{REVIEW_LOOP_CORRECTIVE_PR_URL}}` | Task 15 |
| Required head/tree | `{{REVIEW_LOOP_REVIEWED_HEAD_SHA}}` / `{{REVIEW_LOOP_REVIEWED_TREE_SHA}}` | Task 15 |
| Frozen body | `{{REVIEW_LOOP_PR_BODY_BYTES}}` / `{{REVIEW_LOOP_PR_BODY_SHA256}}` | Task 15 verified input |
| Quality result | `{{REVIEW_LOOP_QUALITY_RESULT}}` | Produce in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 15 | `FS` | The local audit and one reviewer pair are terminally clean on the exact required head/tree and frozen semantics. |

### Objective

Run one fresh independent post-review audit of the corrected architecture and its real PR lifecycle. Correct only non-material compact state without code rereview. Do not implement a material repair, merge, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Verify unchanged reviewed head/diff/body semantics, inventory closure, all deterministic scenarios, dual-channel monitor, publication idempotency, and performance controls.
2. Independently prove that an append-only result or local serialization failure cannot request reviewers again on the same head.
3. Non-material evidence correction reruns Task 16 only. A material code/diff/scope/behavior/risk correction returns through Tasks 13/14 and requires new Tasks 15/16.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

Copy this complete prompt into a fresh coding-agent session that did not implement the change and did not run the review loop.

~~~text
You are the independent final quality checker for repository `franklesniak/PSStyleGuide`, pull request `{{REVIEW_LOOP_CORRECTIVE_PR_URL}}`, and governing issue `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}`. You are not the implementation or review-loop executor.

Start in a fresh coding-agent session with no inherited terminal conclusion. Locate and obey the applicable `AGENTS.md`. If none exists, read root `CLAUDE.md` as compatibility workflow instructions; the filename does not change your executor identity.

Use authenticated structured reads and paginate every issue, dependency, PR, body-edit, comment, review, inline-comment, native-thread, thread-comment, commit, changed-file, check, status, linked-branch, and closing-reference surface. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body, and represented scope and risk remain unchanged. Treat an exact `@codex review` trigger as neither a finding nor a local instruction. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex results.

Independently audit the complete diff, issue requirements, provenance, security, parser and path safety, repository self-containment, failure truth, tests, validation, PR description, unfinished-work audit, deferrals, reviewer findings, and exact review results. Run focused validation before broad validation. Reuse validation results for unchanged inputs only for the unchanged code tree and relevant environment.

Classify every discrepancy by semantic materiality. A code-head or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and a recorded reason. A non-material factual identity correction on the same head requires deterministic verification, not a new code review. Review results, task status, audit-record changes, and comment-only evidence do not require a new code review. Raw PR-body byte inequality is not the classifier.

If a non-material evidence error exists, correct it only in the compact state or a tightly bounded factual field, validate the public response, read it back, preserve the frozen review semantics, do not append a terminal result to the reviewer-facing body, do not request reviewers again, and rerun this quality task on the unchanged head. If a material code, scope, behavior, or risk problem exists, return to Task 13 or Task 14 as applicable, refreeze the corrected review input, and require new Task 15 and Task 16 instances. Reject a same-head re-request without a recorded material reason.

Return `PASS` only when the independent audit and all required validation pass on the reviewed final head and frozen semantic input, every finding is adjudicated, deterministic evidence is correct, and no material change escaped review. Return `FAIL` with the exact materiality class and route otherwise. Do not merge, enable auto-merge, close an issue, or publish a landed handoff.
~~~

### Procedure

1. Start a fresh independent session with the complete prompt and record its identity.
2. Paginate and audit the issue, PR, inventory, diff, permanent surfaces, reviews/comments/threads, checks, frozen body, and exact head/tree.
3. Run focused classifier/serializer/monitor/publication tests, then all full affected controller, manager, template, Markdown, PowerShell, and pre-commit validation.
4. Reconcile metrics and actual Task 15 behavior with the required one-pair policy. Classify any discrepancy and route it correctly.
5. Return the exact quality result and stop before merge.

### Validation and evidence

Require fresh-session identity, exact Task 15 head/tree/frozen semantics, complete independent pagination, all deterministic scenarios and full suites, dual-channel proof, idempotency/retry proof, performance metrics, correct materiality disposition, zero unresolved work, and no merge.

### Stop and escalation conditions

Stop on head/diff drift, material semantic change, unjustified reviewer request, monitor regression, retry risk, missing metrics, failed validation, or non-material evidence that cannot be safely reconciled.

### Exact output

One `REVIEW_LOOP_FINAL_QUALITY_PASS` record contains independent identity, exact head/tree/body, inventory and scenario matrices, commands/runtimes/exits, actual request/body-edit metrics, materiality disposition, and proof no merge occurred.

### Complete when

A fresh independent session returns `PASS` for the exact reviewed head and frozen semantics, all deterministic evidence is correct, and the corrected architecture is proved by tests and the real PR lifecycle.

## Task 17 — merge the review-loop corrective PR into the planning branch

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Candidate PR | `{{REVIEW_LOOP_CORRECTIVE_PR_URL}}` | Tasks 15 and 16 |
| Gated head/tree | `{{REVIEW_LOOP_REVIEWED_HEAD_SHA}}` / `{{REVIEW_LOOP_REVIEWED_TREE_SHA}}` | Must equal Tasks 15 and 16 |
| Landed commit/tree | `{{REVIEW_LOOP_LANDED_COMMIT_SHA}}` / `{{REVIEW_LOOP_LANDED_TREE_SHA}}` | Resolve after merge into `planning-CRT-PR-852` |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 15 | `FS` | One reviewer pair is terminally clean on the gated head/tree and frozen input. |
| Task 16 | `FS` | Independent quality passes on the same gated head/tree/input. |

### Objective

Merge only the exact gated head into `planning-CRT-PR-852` while the merge remains on-plan. Do not merge to `main`, repair, update reviewer input, request review for results, publish handoff, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Require exact equality with both gates and verify every post-gate mutation semantically. Separate comment/record results do not invalidate review; code/diff or material scope/risk change does.
2. Require the PR base to be `planning-CRT-PR-852`, all applicable checks truthful, no unresolved findings, one-pair policy satisfied, and a permitted merge method.
3. Treat this task and the instruction to execute the plan as standing merge authority only while every on-plan condition in the shared policy remains true. Do not request or record separate per-merge approval. Do not copy the resulting planning files into `main`.

### Procedure

1. Validate both results and confirm that the merge remains on-plan. Re-query complete PR/issue/review/check/relationship state and current planning target.
2. Prove unchanged gated head/tree/frozen semantics and no unreviewed material mutation.
3. Merge once using a permitted non-bypassing method into `planning-CRT-PR-852`.
4. Read back merge commit/tree/parents, target ref, PR and issue states, checks, paths/blobs, request/body-edit metrics, and slot release.
5. Confirm `main` did not receive planning-only files. Stop before handoff.

### Validation and evidence

Require an on-plan merge classification, exact gate equality, correct target base, semantic mutation audit, truthful checks, zero unresolved work, one-pair metrics, allowed merge, landed readback, issue terminal state, and no `main` planning-file change.

### Stop and escalation conditions

Stop if the merge is off-plan, target is not the planning branch, head/tree/body semantics drift, a material mutation lacks review, checks fail, merge would bypass protection, or `main` would be modified. Do not stop only to obtain separate operator approval.

### Exact output

One authenticated `REVIEW_LOOP_MERGED` record contains the on-plan classification, PR/method/time/actor, gated and landed identities, target ref, final issue/check state, mutation/request metrics, and proof no planning-only file landed on `main`.

### Complete when

The exact reviewed and quality-checked head lands on `planning-CRT-PR-852` under standing plan authority, readback is complete, and no handoff or `main` mutation occurred.

## Task 18 — publish the review-loop landed handoff

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Focused issue | `{{REVIEW_LOOP_CORRECTIVE_ISSUE_URL}}` | Task 17 |
| Landed commit/tree | `{{REVIEW_LOOP_LANDED_COMMIT_SHA}}` / `{{REVIEW_LOOP_LANDED_TREE_SHA}}` | Task 17 |
| Surface inventory | `{{REVIEW_LOOP_SURFACE_INVENTORY_PATH}}` / `{{REVIEW_LOOP_SURFACE_INVENTORY_SHA256}}` | Task 12, validated through Task 16 |
| Handoff comment | `{{REVIEW_LOOP_HANDOFF_COMMENT_URL}}` | Create and read back in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 12 | `FS` | The final implementation closes every inventoried permanent surface. |
| Task 17 | `FS` | The gated PR is merged into the planning branch and exact landed objects are readable. |

### Objective

Publish one permanent, read-back handoff for the corrected review-loop architecture. Do not modify the PR body, request review, edit repository bytes, start Task 19 comparison work, close umbrella #172, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Publish the task-required terminal handoff as an append-only issue comment, never in the reviewer-facing body.
2. Include changed permanent surfaces, inventory closure, nine regression scenarios, full validation, dual-channel monitor proof, serializer/readback/retry proof, performance controls, reviewer requests/head, body edits after review, same-head reasons, clean-review recognition latency, clean-pair-to-merge time, landed objects, and residual risks.
3. Prove the handoff publication itself did not request another review. Identify Task 19 as next action and do not start it.

### Procedure

1. Validate Task 17 and re-query landed planning ref, PR, issue, reviews, comments, threads, checks, metrics, and exact changed blobs.
2. Reconcile landed bytes with the Task 12 inventory and Task 13 scenario/validation records. Confirm frozen body semantics and one-pair policy remained intact through merge.
3. Prepare and transport-validate one handoff. Post it to the focused issue, validate the response, update compact state, and read back the public comment.
4. Verify no PR-body edit, reviewer request, ref/file mutation, new implementation commencement, Task 19 action, umbrella closure, or setting change. Stop.

### Validation and evidence

Require exact landed objects and full lifecycle pagination, inventory closure, nine scenarios/full tests, monitor/idempotency/performance proofs, all required metrics, exact handoff body/hash/readback, and no same-head rereview or successor action.

### Stop and escalation conditions

Stop if landed bytes differ, an inventory row remains open, a scenario/check fails, metrics are missing, the handoff would alter reviewer input, public mutation is ambiguous, or publication causes a reviewer request.

### Exact output

One authenticated `REVIEW_LOOP_LANDED_HANDOFF` record contains comment URL/IDs, landed planning identities, changed surfaces, inventory/scenario/validation evidence, review/body/request metrics, residual risks, and exact next action Task 19.

### Complete when

A permanent, exact, read-back handoff proves the corrected architecture and its own one-pair lifecycle, and no reviewer retry, body result edit, or Task 19 action occurred.

## Task 19 — publish the corrected landed PS PR #174 handoff

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Resolve each variable from its named live read or predecessor before use. Do not replace a value that does not yet exist.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Umbrella issue | `https://github.com/franklesniak/PSStyleGuide/issues/172` | Completed source Task 1; verify current state |
| Focused port issue | `https://github.com/franklesniak/PSStyleGuide/issues/173` | Completed source Tasks 2–8; verify closed state |
| Merged port PR | `https://github.com/franklesniak/PSStyleGuide/pull/174` | Completed source Tasks 5–8 |
| Metadata-policy handoff | `{{PS_METADATA_POLICY_HANDOFF_COMMENT_URL}}` | Task 9 |
| Review-loop handoff | `{{REVIEW_LOOP_HANDOFF_COMMENT_URL}}` | Task 18 |
| Corrected PS commit/tree | `{{PS_METADATA_POLICY_LANDED_COMMIT_SHA}}` / `{{PS_METADATA_POLICY_LANDED_TREE_SHA}}` | Task 9; re-query current PS `main` and explain any later authorized movement |
| Corrected port handoff | `{{PS_PR174_CORRECTED_HANDOFF_COMMENT_URL}}` | Create and read back in this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Completed source Task 8 | `FS` | PR #174 is merged, issue #173 is closed, and exact merge/failure evidence is readable. |
| Task 9 | `FS` | The metadata-policy correction is landed on PS `main` with full provenance and validation handoff. |
| Task 18 | `FS` | The review-loop correction is landed on `planning-CRT-PR-852` with full architecture handoff. |

### Objective

Publish the permanent corrected PS landed handoff that supersedes the incomplete pre-correction handoff boundary and pins the exact input for the 28-path three-repository comparison. Do not implement Terraform work, create/update a Terraform issue, request review, change repository bytes, close umbrella #172, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific controls

1. Reconcile completed PR #174, its reviews and checks, the immutable landed failure, the metadata-policy correction, and the planning-branch review-loop correction. Do not rewrite any historical result.
2. Recompute every one of the exact 28 PR #174 catalog paths at the final corrected PS commit. Record mode, blob, bytes, SHA-256, attributes, line endings, and whether Task 9 changed the PR #174 merge baseline.
3. The closed priority catalog is:

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

1. Publish the final PS state, not the obsolete pre-port baseline. The Task 9 metadata-policy landing supersedes PR #174 cells it changed. The Task 18 planning landing is process evidence and does not copy planning-only files to `main`.
2. Record complete review, quality, checks, validation, issue/PR relationships, provenance, deferred work, accepted residuals, rollback, and unfinished-work audit. Keep copilot-repo-template #858–#863 and PS #175 as separate follow-ups unless live evidence proves a real dependency.
3. Publish results outside reviewer-facing PR bodies. A handoff comment does not justify same-head rereview.

### Procedure

1. Validate the completed source Task 8 record and Tasks 9 and 18. Re-query PR #174, issues #172/#173/#175, current PS `main`, planning branch, all reviews/comments/threads/checks/statuses, and open PR/slot state.
2. Recompute the complete 28-path corrected PS catalog from exact landed Git objects. Reconcile each cell with PR #174, Task 9, review, quality, and landed evidence.
3. Audit all three lifecycles for unfinished work, follow-up promises, accepted residuals, deferrals, body edits, same-head reviewer requests, and failure truth. Require every legitimate external follow-up to be self-contained and accurately related.
4. Prepare one handoff with source research merge, PR #174 base/head/merge, corrected PS landing, planning correction landing, 28-path PS objects, validations/runtimes/exits, reviews, checks, provenance, failures, exceptions, risks, rollback, and exact next action Task 20.
5. Transport-validate and post the handoff to #173 or #172 according to the current tracking model, with an exact cross-reference on the other surface when required. Validate response and read back every comment identity/body/hash.
6. Confirm no reviewer request, PR-body edit, Terraform issue/branch/PR, implementation commencement, comparison result, umbrella closure, or setting mutation. Stop.

### Validation and evidence

Require immutable completed and corrective inputs, complete pagination, exact current refs, all 28 PS cells once, raw-object identities, review/quality/check truth, failed-run preservation, provenance and deferral audits, exact handoff transport/readback/hash, and no Terraform or repository mutation.

### Stop and escalation conditions

Stop if a corrective handoff is incomplete, current PS movement lacks authority/evidence, any catalog cell is missing, PR #174 history is rewritten, failure truth is weakened, a follow-up relationship is false, or publication outcome is ambiguous.

### Exact output

One authenticated `PS_PR174_CORRECTED_LANDED_HANDOFF` record contains `{{PS_PR174_CORRECTED_HANDOFF_COMMENT_URL}}`, all public result identities, exact completed/corrective landings, the complete 28-path PS catalog, reviews/checks/validation/provenance/failure evidence, and proof Task 20 did not start.

### Complete when

A permanent, exact, read-back corrected PS handoff pins every required input for Task 20 and no Terraform, comparison, closure, reviewer, PR-body, or repository mutation occurred.

## Task 20 — compare the research source with landed PS and authorize the Terraform disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Source repository/commit | `franklesniak/research-misc` / `51dbeb560dd518b904cf1b571dc7d01fd8045749` | Immutable initial source |
| Landed PS commit/tree | `{{PS_PR78_LANDED_COMMIT_SHA}}` / `{{PS_PR78_LANDED_TREE_SHA}}` | Task 19 handoff |
| Current Terraform baseline | `{{TF_PR78_COMPARISON_BASE_COMMIT_SHA}}` / `{{TF_PR78_COMPARISON_BASE_TREE_SHA}}` | Re-query `franklesniak/TerraformStyleGuide` `main` at task start |
| Comparison result | `{{TF_PR78_DISPOSITION}}` | Exact `TERRAFORM_PORT_REQUIRED` or `TERRAFORM_ALREADY_CONFORMANT` |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 19 | `FS` | The landed PS handoff is complete, immutable, and read back. |

### Objective

Perform a read-only research-to-PS comparison, include current Terraform applicability, and publish the exact Terraform disposition. Do not create or update the Terraform issue, commence implementation, edit repository bytes, create a PR, merge, close the umbrella, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Research-to-PS comparison boundary

Compare the immutable research merge with the landed PS commit. Produce the strongest reviewed common input for Terraform. Do not require Terraform to copy a PS-specific exception. Do not accept a PS difference only because it is already merged; it remains a blocker if the required proof is incomplete.

### Procedure

1. Validate Task 19 and pin the immutable research merge, landed PS commit, current Terraform `main`, and every catalog blob. Re-query all relevant issue/PR state and the free slot.
2. For all 28 paths, compare research and PS raw bytes, Git blobs, file modes, attributes, runtime roles, validation coverage, and behavior. Re-run every exception pressure test and mark unsupported exceptions `BLOCKER`.
3. Inspect current Terraform for the same 28 paths and role-equivalent behavior. Do not infer applicability from file presence or repository name.
4. Produce a closed matrix with one row per path and columns for research, PS, current Terraform, common bytes, proved exceptions, required target behavior, required validation, and blocker state.
5. Emit `TERRAFORM_ALREADY_CONFORMANT` only when current Terraform already satisfies the complete three-repository contract and has equivalent reviewed evidence. Otherwise emit `TERRAFORM_PORT_REQUIRED` with the exact immutable source/PS blobs and smallest target requirements.
6. Publish the pinned comparison to the umbrella and PS focused issue. Read it back and verify its body identity.
7. Return the comparison record and stop before Task 21.

### Validation and evidence

Require exact immutable inputs, all 28 rows exactly once, raw-byte and behavior comparisons, pressure-tested exceptions, current Terraform identity, no unexplained absence, one exact disposition, read-back comparison evidence, a free serial slot, and no Terraform or repository mutation.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One authenticated `TF_PR78_DISPOSITION` record states `TERRAFORM_PORT_REQUIRED` or `TERRAFORM_ALREADY_CONFORMANT`, contains the complete pinned matrix and target requirements, and proves that Terraform implementation did not start.

### Complete when

The complete research/PS/Terraform applicability matrix is public and exact, one technically supported Terraform disposition is read back, and no implementation or issue mutation occurred.

## Task 21 — create or update the focused Terraform issue for the PR #78 port

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Disposition | `{{TF_PR78_DISPOSITION}}` | Task 20 exact result |
| Umbrella issue | `{{PR78_UMBRELLA_ISSUE_URL}}` | completed source Task 1 record |
| Focused Terraform issue | `{{TF_PR78_PORT_ISSUE_URL}}` | On `TERRAFORM_PORT_REQUIRED`, search and reuse an exact issue or create one; in the conformant branch this is inapplicable |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 20 | `FS` | The pinned comparison emits one exact Terraform disposition and implementation has not started. |

### Objective

On `TERRAFORM_PORT_REQUIRED`, create or update one focused Terraform implementation issue. On `TERRAFORM_ALREADY_CONFORMANT`, publish only `SKIPPED — TERRAFORM_ALREADY_CONFORMANT`. Do not commence, edit repository bytes, create a branch/PR, request review, merge, close the umbrella, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Focused Terraform issue boundary

The focused issue authorizes only the Terraform changes required by the Task 20 matrix. It must not import PS-only exceptions, pre-implement Terraform issues #21 through #25 or tracker #31 work, or create a shared dependency. Cross-repository tracker links are references, not native parent or dependency edges.

### Procedure

1. Validate Task 20 and re-query the comparison evidence, current Terraform `main`, issues, PRs, dependencies, linked branches, and implementation slot.
2. If the disposition is `TERRAFORM_ALREADY_CONFORMANT`, verify that it remains current, record the exact skip with current commit/tree, and stop.
3. Search for an exact focused Terraform issue. Do not duplicate or broaden issues #21 through #25, #31, #32, or #34.
4. Build an issue-body matrix for every Task 20 target row, immutable research and PS input, target path/role, byte-identity default, exception test, validation, review gates, handoff, three-way recheck, exclusion, and terminal state.
5. Create or update only that issue. Link the PS umbrella and Task 20 evidence by exact cross-reference; do not invent a cross-repository native parent or dependency.
6. Read back all issue state and prove no commencement marker, branch, commit, or closing PR exists.
7. Return the issue or skip result and stop.

### Validation and evidence

Require the current disposition, complete GitHub pagination, exact body coverage/readback on the repair branch, correct cross-reference without false native relationship, no already-knowable placeholder, no implementation marker, unchanged refs/bytes, and an exact conformant skip when applicable.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_PORT_ISSUE_READY` record contains the focused issue identity/body/relationships and proof of no commencement, or one `SKIPPED — TERRAFORM_ALREADY_CONFORMANT` record contains the exact current conformant identity.

### Complete when

The required focused Terraform issue is self-contained and ready for separate commencement, or the verified conformant branch has an exact skip; no implementation or PR state changed.

## Task 22 — commence the focused Terraform PR #78 port

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Focused issue | `{{TF_PR78_PORT_ISSUE_URL}}` | Task 21 repair-required result; inapplicable on the skip branch |
| Intended branch | `{{TF_PR78_PORT_BRANCH}}` | Select but do not create after verifying current `main` and the free slot |
| Commencement comment | `{{TF_PR78_COMMENCEMENT_COMMENT_URL}}` | Created/read back on the repair-required branch |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 21 | `FS` | A focused issue is ready, or the exact conformant skip is current. |

### Objective

On the repair-required branch, post the exact Terraform commencement state only. On the conformant branch, propagate the exact skip. Do not edit bytes, create/move a branch, commit, push, create/update a PR, review, merge, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Focused Terraform issue boundary

The focused issue authorizes only the Terraform changes required by the Task 20 matrix. It must not import PS-only exceptions, pre-implement Terraform issues #21 through #25 or tracker #31 work, or create a shared dependency. Cross-repository tracker links are references, not native parent or dependency edges.

### Procedure

1. Validate Task 21. If it is the conformant skip, re-query the unchanged disposition, emit `SKIPPED — TERRAFORM_ALREADY_CONFORMANT`, and stop.
2. Re-query the focused issue, umbrella, Terraform `main`, all implementation issues/PRs, refs/worktrees, and the one-at-a-time slot.
3. Verify immutable research and PS inputs and select an unused branch without creating it.
4. Post one `IMPLEMENTATION COMMENCED` comment with actor/time, target baseline, intended branch, source/PS commits, exact target matrix, umbrella, and predecessor result.
5. Read back the comment/timeline and prove that no implementation or PR mutation occurred.
6. Return the exact commencement result and stop.

### Validation and evidence

Require an exact conditional branch, free slot, current issue/body and source objects, one read-back commencement comment on the repair branch, unchanged refs/bytes/PRs, or an exact current skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_IMPLEMENTATION_COMMENCED` record contains the issue/comment/baseline/branch and proof of no byte change, or one exact propagated conformant skip.

### Complete when

The focused Terraform issue owns the slot through one exact commencement record, or the conformant branch is skipped; no repository byte, ref, or PR changed.

## Task 23 — implement the focused Terraform PR #78 port candidate

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Focused issue | `{{TF_PR78_PORT_ISSUE_URL}}` | Task 22 repair-required record |
| Working branch | `{{TF_PR78_PORT_BRANCH}}` | Task 22 intent; create only on the repair-required branch |
| Candidate commit/tree | `{{TF_PR78_CANDIDATE_COMMIT_SHA}}` / `{{TF_PR78_CANDIDATE_TREE_SHA}}` | Created locally after full validation |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 22 | `FS` | Terraform implementation is commenced with a free slot, or the conformant skip is exact. |

### Objective

On the repair-required branch, create one locally committed Terraform candidate for the complete Task 20 matrix. On the conformant branch, propagate the skip. Do not push, create/update a PR, request review, merge, publish a handoff, close an issue, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Terraform candidate controls

- Use the Task 20 research/PS matrix as the immutable target authority. Do not copy from moving branches.
- Preserve Terraform-specific workflows, examples, provider guidance, package behavior, and planned issues #21 through #25.
- Treat privileged workflow, instruction, parser, hook, attribute, runtime, package, and lock changes as one security contract.
- A PS exception is not automatically applicable. Re-run the exception pressure test against Terraform.

### Procedure

1. Validate Task 22. On the conformant branch, re-query the result, emit the exact skip, and stop.
2. Fetch/pin current Terraform `main`, immutable research and landed PS commits/blobs, focused issue, and catalog. Create the recorded branch from the verified baseline with a clean worktree.
3. Test verbatim common-byte adoption for every target row. Re-run a finding-specific option/rubric decision for each real Terraform incompatibility and select the smallest divergence.
4. Implement the complete applicable governance, parser, workflow, hooks, runtime, package/lock, metadata, and mutation-test contract without pre-implementing unrelated Terraform work.
5. Build all 28 Terraform rows with research, PS, baseline, and candidate blobs/SHA-256 values, status, negative test, and exception dossier.
6. Run the full Terraform validation: agent-instruction/mutation contract, parser-manifest tests, workflow/YAML checks, Markdown lint, hooks/pre-commit, package/lock validation, trust-root/security tests, existing Terraform validations, and `git diff --check`.
7. Audit scope, secrets, modes, attributes, line endings, placeholders, unfinished work, and complete outgoing range. Commit locally and record exact commit/tree. Do not push.
8. Return the candidate or skip result and stop.

### Validation and evidence

Require all applicable 28 rows, exact immutable inputs/candidate outputs, proved minimal exceptions, full native validation success, no unrelated/secret change, clean committed worktree, unchanged remote/PR state, or an exact conformant skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_CANDIDATE_READY` record contains branch/base/candidate commit/tree, complete path/blob/SHA-256 and exception matrices, decisions, commands/runtimes/results, and proof of no push, or one exact skip.

### Complete when

One locally committed Terraform candidate satisfies the complete target contract with all validation passing, or the conformant branch is skipped; no remote or PR state changed.

## Task 24 — create or update the Terraform candidate PR for the PR #78 port

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Focused issue | `{{TF_PR78_PORT_ISSUE_URL}}` | Task 23 candidate branch |
| Candidate head/tree | `{{TF_PR78_CANDIDATE_COMMIT_SHA}}` / `{{TF_PR78_CANDIDATE_TREE_SHA}}` | Task 23 |
| Candidate PR | `{{TF_PR78_PORT_PR_URL}}` | Reuse/create one exact PR to `main`; inapplicable on the skip branch |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 23 | `FS` | The local Terraform candidate is complete and clean, or the conformant skip is exact. |

### Objective

On the repair-required branch, non-force push the exact candidate and create/update one accurate Terraform PR. On the conformant branch, propagate the skip. Do not request Codex review, quality-check, merge, close an issue, publish a handoff, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Terraform candidate controls

- Use the Task 20 research/PS matrix as the immutable target authority. Do not copy from moving branches.
- Preserve Terraform-specific workflows, examples, provider guidance, package behavior, and planned issues #21 through #25.
- Treat privileged workflow, instruction, parser, hook, attribute, runtime, package, and lock changes as one security contract.
- A PS exception is not automatically applicable. Re-run the exception pressure test against Terraform.

### Procedure

1. Validate Task 23. On the skip branch, re-query and emit the exact skip.
2. Re-query Terraform `main`, remote refs/PRs, issue/umbrella links, rules, and current checks.
3. Verify the complete outgoing range, ancestry, tree, allowed paths, clean worktree, and secrets; push with an explicit non-force update and read back exact head/tree.
4. Reuse one exact PR or create it to `main`. Make title/body exact for the issue, umbrella, source and PS handoff, target matrix, exceptions, security/failure behavior, validation, checks, upstream follow-ups, and rollback. Close only the focused issue on merge.
5. Run/await initial checks truthfully. Do not post `@codex review`.
6. Paginate/read back all PR and issue state. Return the PR or skip result.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Require exact remote head/tree, one accurate PR, correct issue/umbrella references, complete diff, truthful checks, no stale placeholder, no review/merge/closure, or an exact conformant skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_PR_READY` record contains the PR identity/base/head/tree/body/commits/files/checks and issue relationship, or one exact skip; no review or merge occurred.

### Complete when

One accurate Terraform candidate PR exposes the exact gated candidate, or the conformant branch is skipped; no review, quality, merge, handoff, or closure action occurred.

## Task 25 — run the Copilot-and-Codex review loop on the Terraform PR #78 port PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Candidate PR/issue | `{{TF_PR78_PORT_PR_URL}}` / `{{TF_PR78_PORT_ISSUE_URL}}` | Task 24 repair-required record |
| Terminal head/tree | `{{TF_PR78_REVIEWED_HEAD_SHA}}` / `{{TF_PR78_REVIEWED_TREE_SHA}}` | Produced only by the terminal loop |
| Conditional state | `{{TF_PR78_DISPOSITION}}` | Propagate the exact conformant skip when applicable |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 24 | `FS` | The exact Terraform PR is ready without review, or the conformant skip is exact. |

### Objective

On the repair-required branch, run a complete local Codex audit and the GitHub Copilot and remote Codex code reviews. Repeat all three review actions on every changed head. On the conformant branch, propagate the skip. Do not quality-check, merge, close, hand off, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Terraform candidate controls

- Use the Task 20 research/PS matrix as the immutable target authority. Do not copy from moving branches.
- Preserve Terraform-specific workflows, examples, provider guidance, package behavior, and planned issues #21 through #25.
- Treat privileged workflow, instruction, parser, hook, attribute, runtime, package, and lock changes as one security contract.
- A PS exception is not automatically applicable. Re-run the exception pressure test against Terraform.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Copy this complete prompt into a fresh native Codex subagent. The local subagent is the executor. GitHub Copilot and `chatgpt-codex-connector` are separate, required code reviewers.

~~~text
You are the local Copilot-and-Codex review-loop executor for repository `franklesniak/TerraformStyleGuide`, pull request `{{TF_PR78_PORT_PR_URL}}`, and governing issue `{{TF_PR78_PORT_ISSUE_URL}}`.

Use model `gpt-5.6-sol` with `xhigh` reasoning in a fresh native Codex subagent. Use the native Codex subagent interface. Use headless `codex exec --json` only when native subagents are unavailable. For that fallback, create a bounded JSON event log, monitor every exit and tool error, preserve the same repository and authorization boundaries, do not run unattended beyond the repository's configured 8-round or 6-hour cycle limit, and stop on ambiguous state. Do not change the executor identity because a compatibility file is named `CLAUDE.md`.

Before any analysis, locate and read the target repository's applicable `AGENTS.md` files. If no `AGENTS.md` applies, read the root `CLAUDE.md` as compatibility workflow instructions. Then re-query the exact PR base, head SHA, head tree, body and its edit history, commits, complete changed-file list and patches, issue comments, PR conversation comments, reviews and review bodies, inline comments, native review threads and every thread comment, requested reviewers, reactions that carry evidence, check runs, commit statuses, linked and closing issues, parent/sub-issues, dependencies, linked branches, merge state, and the governing issue's complete requirements. Paginate every connection to `hasNextPage: false`; do not trust a first-100 result.

Verify that the PR head and tree equal the task input. Audit the complete diff, not only the latest commit. Map every governing-issue requirement and every 28-path parity row to exact code, documentation, tests, workflows, and evidence. Verify that the PR title and body accurately describe the final diff, security and failure behavior, validation, target base, issue relationships, intentional differences, source identities, and rollback information. Inspect all comments and review bodies for declared generated comments, suppressed findings, synthetic keys, deferred work, accepted residuals, accepted risks, future work, TODO/FIXME/TBD text, unfinished implementation, and follow-up promises. A trigger comment that contains exactly `@codex review` is neither a finding nor an instruction to the local executor.

Run the repository's complete applicable validation from a clean state. Include the agent-instruction contract and mutation suite, parser-manifest tests, workflow and YAML validation, Markdown lint, pre-commit or repository hooks, package-lock consistency, security/trust-root tests, `git diff --check`, and exact three-way blob/SHA-256 checks for the 28-path catalog. Preserve native exit codes and exact runtimes. Do not weaken, skip, or relabel a failing check.

For each finding, reproduce or refute it with repository evidence. If valid, list reasonable options, create a finding-specific weighted rubric, score all options, implement only the selected in-scope repair, and validate it. Keep decision analysis in `TEMP-*` files. Before any push, fetch the exact current PR head without force, inspect the complete outgoing range, verify allowed paths, ancestry, tree, and a clean worktree, then use an explicit non-force update. Read back the exact head and tree.

After the local audit is clean on the current head, record separate GitHub Copilot and remote Codex baselines. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Only after the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request, post one PR conversation comment whose entire body is exactly `@codex review` to request the separate `chatgpt-codex-connector` review. The trigger is not a finding. Wait for a new review from each reviewer that explicitly applies to the current head. Re-query all review surfaces with complete pagination. Reconcile each review body, finding, inline thread, synthetic key, declared count, and deferral from both reviewers.

If the code head or reviewed diff changes, discard the prior terminal claim, update and refreeze the body, repeat the local audit, and request one fresh pair on the new head. If scope, behavior, or risk description changes materially without a code change, record the material reason and request a fresh pair. For a deterministically verified non-material factual identity correction, result, task-state update, audit-record change, or comment-only publication on the same head, preserve and reuse the valid review. Reject a same-head re-request without a recorded material reason. Do not merge, enable auto-merge, close the issue, publish a landed handoff, or start a cross-repository comparison.

Return `TERMINALLY_CLEAN` only when the local audit, GitHub Copilot review, and `chatgpt-codex-connector` review are clean on the same current head SHA and tree. Every thread and synthetic finding must be closed. All required checks and validations must have truthful terminal results. The issue and PR descriptions must be accurate. No unfinished work or illegitimate deferral can remain. The exact review results must be public and readable. Otherwise, return `REVIEW_BLOCKED` with the exact blocker and the required return path: Task 23 for bytes or Task 24 for PR-only metadata, followed by a new Task 25 instance.
~~~

### Procedure

1. If Task 24 is the conformant skip, re-query and emit the exact skip without invoking a reviewer.
2. Otherwise copy the full task-local prompt into a fresh native Codex subagent and monitor it to a terminal result.
3. Independently verify every returned page, finding, decision, push, review, check, validation, exception, and exact final head/tree.
4. Require a new loop after a code-head/reviewed-diff change or recorded material scope/behavior/risk change. Deterministically verify non-material same-head evidence corrections without a new code review.
5. Return the terminal review or skip result and stop before quality checking.

### Validation and evidence

Require correct conditional handling. On repair, require a native subagent or controlled fallback, the required model and effort, applicable instructions, complete pagination, a clean local audit, clean GitHub Copilot and remote Codex reviews on one head/tree, complete issue/parity/deferral/check validation, and no merge. On skip, require exact current evidence.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_TERMINALLY_CLEAN` record contains the final head/tree, the local audit evidence, the GitHub Copilot review ID and commit, and the remote Codex review result, or one exact conformant skip. No quality or merge action occurred.

### Complete when

The local audit, GitHub Copilot review, and remote Codex review are terminally clean on the same Terraform head/tree, or the conformant branch is skipped. No unresolved work or successor action remains in this leaf.

## Task 26 — run the independent final quality check on the Terraform PR #78 port PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Candidate PR | `{{TF_PR78_PORT_PR_URL}}` | Task 25 repair-required result |
| Required head/tree | `{{TF_PR78_REVIEWED_HEAD_SHA}}` / `{{TF_PR78_REVIEWED_TREE_SHA}}` | Exact Task 25 terminal identities |
| Conditional state | `{{TF_PR78_DISPOSITION}}` | Propagate exact conformant skip when applicable |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 25 | `FS` | The review loop is terminally clean on one head/tree, or the conformant skip is exact. |

### Objective

On the repair-required branch, run a fresh independent quality audit on the exact reviewed head/tree. On the conformant branch, propagate the skip. Do not mutate, merge, close, hand off, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Terraform candidate controls

- Use the Task 20 research/PS matrix as the immutable target authority. Do not copy from moving branches.
- Preserve Terraform-specific workflows, examples, provider guidance, package behavior, and planned issues #21 through #25.
- Treat privileged workflow, instruction, parser, hook, attribute, runtime, package, and lock changes as one security contract.
- A PS exception is not automatically applicable. Re-run the exception pressure test against Terraform.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

Copy this complete prompt into a fresh coding-agent session that did not implement the change and did not run the Copilot-and-Codex review loop.

~~~text
You are the independent final quality checker for repository `franklesniak/TerraformStyleGuide`, pull request `{{TF_PR78_PORT_PR_URL}}`, and governing issue `{{TF_PR78_PORT_ISSUE_URL}}`. You are not the implementation or review-loop executor.

Start in a fresh coding-agent session with no inherited terminal conclusion. Locate and read the target repository's applicable `AGENTS.md`. If no `AGENTS.md` applies, read the root `CLAUDE.md` as compatibility workflow instructions; the compatibility filename does not change your executor identity.

Use authenticated structured reads and paginate the complete issue, dependency, PR, edit, comment, review, inline-comment, native-thread, thread-comment, commit, changed-file, check, status, linked-issue, and closing-reference state to completion. Re-resolve the current base, head SHA, and head tree. Require them to match the exact `TERMINALLY_CLEAN` review result. Treat `@codex review` trigger comments as neither findings nor local instructions.

Independently audit the entire diff and all governing-issue requirements. Verify the accurate PR title/body, exact target base, security and failure behavior, repository self-containment, no credential exposure, complete validation, no unfinished or hidden work, no unresolved review or synthetic finding, no illegitimate deferral, and truthful treatment of failed or accepted-residual checks. Recompute the complete 28-path three-repository matrix from immutable commits and raw blobs. Require byte identity by default and require the complete pressure-test evidence for every exception.

Run the full applicable repository validation from a clean state. Include the complete agent-instruction and mutation suite, manifest validator, workflow/YAML validation, Markdown lint, repository hooks or pre-commit, package and lock consistency, trust-root/security tests, `git diff --check`, and raw-byte Git-blob/SHA-256 comparisons. Record commands, runtimes, native exit codes, and exact outputs. Do not repair, push, comment, change PR metadata, merge, enable auto-merge, close an issue, or publish a handoff in this task.

Return `PASS` only when all evidence applies to the exact same final head and tree as the review result. If any repository byte, head, issue requirement, PR description, evidence-bearing metadata, review state, or required check changes, return `FAIL — EVIDENCE_INVALIDATED`; require the operator to use Task 23 or Task 24 as applicable, then new Tasks 25 and 16 instances, rerun the full local audit and Copilot-and-Codex review loop, and then run a new independent quality check. If a defect exists without a byte change, return `FAIL` with exact evidence and the same return path. Do not merge.
~~~

### Procedure

1. Propagate an exact current skip without starting a session when Task 25 skipped.
2. Otherwise start a fresh independent coding-agent session with the full task-local prompt.
3. Independently verify its complete reads, validations, parity matrix, and exact reviewed head/tree.
4. Any byte/head/evidence change returns to implementation or PR update, then new review and quality instances.
5. Return the PASS or skip result and stop.

### Validation and evidence

Require correct conditional handling; on repair, fresh-session identity, complete independent audit, exact review-head/tree equality, all validations/checks/requirements/parity evidence, zero unresolved work, and no mutation/merge; on skip, exact current evidence.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_FINAL_QUALITY_PASS` record contains the independent exact-head evidence, or one exact conformant skip.

### Complete when

A fresh session returns PASS on the exact reviewed Terraform head/tree, or the conformant branch is skipped; the PR remains unchanged and unmerged.

## Task 27 — merge the reviewed Terraform PR #78 port PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed on the repair-required branch |
| Candidate PR | `{{TF_PR78_PORT_PR_URL}}` | Tasks 25 and 16 |
| Gated head/tree | `{{TF_PR78_REVIEWED_HEAD_SHA}}` / `{{TF_PR78_REVIEWED_TREE_SHA}}` | Must equal both gate results |
| Landed commit/tree | `{{TF_PR78_LANDED_COMMIT_SHA}}` / `{{TF_PR78_LANDED_TREE_SHA}}` | Resolve after merge; on skip, use the Task 20 conformant commit/tree |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 25 | `FS` | Review is terminally clean or skipped as already conformant. |
| Task 26 | `FS` | Independent quality passes on the same head/tree or propagates that skip. |

### Objective

On the repair-required branch, merge only the exact Terraform head/tree that passed both gates. On the conformant branch, record the exact no-merge skip. Do not hand off, compare, close the umbrella, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Terraform candidate controls

- Use the Task 20 research/PS matrix as the immutable target authority. Do not copy from moving branches.
- Preserve Terraform-specific workflows, examples, provider guidance, package behavior, and planned issues #21 through #25.
- Treat privileged workflow, instruction, parser, hook, attribute, runtime, package, and lock changes as one security contract.
- A PS exception is not automatically applicable. Re-run the exception pressure test against Terraform.

### Procedure

1. If both predecessors skipped, re-query the exact conformant commit/tree, emit `SKIPPED — TERRAFORM_ALREADY_CONFORMANT`, and stop.
2. Otherwise paginate/re-query the PR and issue; require current head/tree equality with both gates, accurate metadata, all required checks, zero new invalidating state, and mergeability.
3. Any change returns to implementation/PR update and new review/quality instances.
4. Use one allowed non-bypassing merge method. Read back merge commit/tree/parents, `main`, focused-issue terminal state, umbrella state, checks, and all 28 landed blobs.
5. Confirm only the focused issue closed and stop.

### Review-evidence materiality gate

### Validation and evidence

Require correct conditional branch, same-head/tree gates and current reviews/checks on repair, exact merge/readback and focused-issue closure, open umbrella, complete landed blobs, or an exact no-merge conformant skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `TF_PR78_MERGED` record contains the complete merge and landed state, or one `SKIPPED — TERRAFORM_ALREADY_CONFORMANT` record contains the exact current conformant commit/tree.

### Complete when

The exact gated Terraform head/tree is merged and read back, or no merge is required because the verified conformant commit/tree remains current; no successor occurred.

## Task 28 — publish the landed Terraform PR #78 port handoff

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed |
| Umbrella issue | `{{PR78_UMBRELLA_ISSUE_URL}}` | completed source Task 1 |
| Focused issue | `{{TF_PR78_PORT_ISSUE_URL}}` | Task 27 repair branch; inapplicable on conformant skip |
| Landed/current commit/tree | `{{TF_PR78_LANDED_COMMIT_SHA}}` / `{{TF_PR78_LANDED_TREE_SHA}}` | Task 27 exact result |
| Handoff comment | `{{TF_PR78_HANDOFF_COMMENT_URL}}` | Post/read back on repair branch; exact no-change result on skip branch |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 27 | `FS` | Terraform is landed through both gates or proved already conformant. |

### Objective

Publish the permanent Terraform landed or no-change handoff. Do not edit repository bytes, change relationships, start a repair, merge, close the umbrella, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Terraform candidate controls

- Use the Task 20 research/PS matrix as the immutable target authority. Do not copy from moving branches.
- Preserve Terraform-specific workflows, examples, provider guidance, package behavior, and planned issues #21 through #25.
- Treat privileged workflow, instruction, parser, hook, attribute, runtime, package, and lock changes as one security contract.
- A PS exception is not automatically applicable. Re-run the exception pressure test against Terraform.

### Procedure

1. Validate Task 27 and pin the exact landed or conformant commit/tree. Re-query complete lifecycle evidence.
2. Recompute all 28 path blobs/SHA-256 values and reconcile them with research, PS, Task 20, and all Terraform gate records.
3. On the repair branch, audit all PR comments/reviews/deferrals and prepare the complete landed handoff. On the conformant branch, prepare an equally pinned no-change handoff that proves why no issue/PR existed.
4. Publish the result to the focused issue when it exists and to the umbrella. Include source/PS inputs, target base, reviewed/landed identities, paths, exceptions, validation, checks, reviewers, residuals/deferrals, rollback, and Task 29 as the sole next action.
5. Read back exact comment identities/hashes and return the handoff.

### Validation and evidence

Require exact immutable target objects, all 28 identities, complete repair lifecycle evidence or complete no-change proof, truthful checks, complete deferral audit, accurate upstream boundary, exact readback, and no implementation/closure mutation.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One authenticated `TF_PR78_LANDED_HANDOFF` record contains the complete landed or conformant no-change evidence and every posted comment identity.

### Complete when

A permanent exact Terraform handoff proves the final target result and authorizes only Task 29; no repair or closure action occurred.

## Task 29 — perform the first three-repository PR #78 fixed-point comparison

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Research commit/tree | `51dbeb560dd518b904cf1b571dc7d01fd8045749` / `ec9c46e3770ab79169d5520f2f743cb1d1853c16` | Immutable source unless a later reviewed repair lands |
| Landed PS commit/tree | `{{PS_PR78_LANDED_COMMIT_SHA}}` / `{{PS_PR78_LANDED_TREE_SHA}}` | Task 19 |
| Landed/current Terraform commit/tree | `{{TF_PR78_LANDED_COMMIT_SHA}}` / `{{TF_PR78_LANDED_TREE_SHA}}` | Task 28 |
| Comparison disposition | `{{PR78_THREE_WAY_DISPOSITION}}` | Exact `FIXED_POINT_READY` or `REPAIR_REQUIRED` |
| Selected target | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` | Resolve only for `REPAIR_REQUIRED`; exactly one of the three repositories |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 19 | `FS` | The permanent landed PS handoff is exact. |
| Task 28 | `FS` | The permanent landed or no-change Terraform handoff is exact. |

### Objective

Perform and publish the first complete three-repository comparison. Select one repair target when required. Do not create/update an issue, commence, edit bytes, create a PR, merge, publish a repair handoff, close the fixed point, close the umbrella, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Fixed-point comparison rule

Use the exact current landed commits from all three repositories. `FIXED_POINT_READY` requires every catalog cell to be byte-identical or to have the complete pressure-tested repository-specific exception dossier. If one or more blockers remain, select exactly one target repository for the next serial repair. Record other blockers but do not authorize parallel work.

### Procedure

1. Validate both handoffs. Re-query and pin the current research branch commit, PS `main`, Terraform `main`, parent PR #21, every related issue/PR, and all 84 path/repository cells.
2. Compare raw blobs, SHA-256 values, modes, attributes, operative behavior, tests, runtime selectors, trust roots, metadata-transition rules, and review evidence for each catalog path.
3. Re-run every exception negative test and alternatives analysis. Mark a stale, broad, convenient, absent-only, or security-weaker exception `BLOCKER`.
4. For any reviewer-created common improvement, determine which reviewed bytes are technically strongest. Do not prefer the oldest source automatically.
5. If no blocker exists, emit `FIXED_POINT_READY`. Otherwise list all blockers and use a finding-specific rubric to select exactly one target repository, base ref, exact path set, source blobs, required validation, and terminal state. For research-misc, the base is `agent/bash-pid1-runtime-contract`; do not write directly into parent PR #21.
6. Publish the full matrix/disposition to the umbrella and related focused issues. Read back exact bodies/hashes.
7. Return the comparison result and stop.

### Validation and evidence

Require current exact commits/trees, all 84 cells and 13 path rows exactly once, raw-byte and behavior evidence, all exception dossiers, strongest-common-byte decisions, complete lifecycle/review evidence, one exact disposition, no more than one selected target, public readback, and no implementation mutation.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One authenticated `PR78_THREE_WAY_COMPARISON` record states `FIXED_POINT_READY` or `REPAIR_REQUIRED`; on repair it contains exactly one selected repository/base/path set and complete immutable repair inputs.

### Complete when

The first complete three-repository matrix and one exact disposition are public and read back; no repair, fixed-point publication, or umbrella closure occurred.

## Task 30 — create or update one selected-target PR #78 fixed-point repair issue

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Latest comparison | `{{LATEST_PR78_THREE_WAY_COMPARISON_RECORD}}` | Task 29 for the first instance; latest completed Task 38 record for a repeat |
| Disposition | `{{PR78_THREE_WAY_DISPOSITION}}` | Exact `FIXED_POINT_READY` or `REPAIR_REQUIRED` |
| Selected repository/base | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` / `{{PR78_SELECTED_REPAIR_BASE_REF}}` | Resolve only on `REPAIR_REQUIRED` from the latest comparison |
| Focused repair issue | `{{PR78_SELECTED_REPAIR_ISSUE_URL}}` | Search/reuse/create only on `REPAIR_REQUIRED` |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | `1` for the first repair; increment by one for each repeated Tasks 30–38 instance |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 or the latest completed repair-loop comparison | `FS` | Use the newest exact disposition. No prior repair issue or PR remains active. |

### Objective

On `REPAIR_REQUIRED`, create or update one focused issue in exactly one selected repository. On `FIXED_POINT_READY`, record `SKIPPED — FIXED_POINT_READY`. Do not commence, edit bytes, create a branch/PR, review, merge, close the fixed point/umbrella, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected-target issue rule

Use the latest `REPAIR_REQUIRED` comparison: Task 29 for the first instance or the latest completed Task 38 instance for a repeat. Create one focused issue in exactly one selected target. In PS, a native sub-issue link to the umbrella is permitted as tracking. In Terraform or research-misc, use an exact cross-reference; do not fabricate a cross-repository parent or dependency. For research-misc, target the stacked base `agent/bash-pid1-runtime-contract` and keep PID 1 application work out of scope.

### Procedure

1. Re-query and validate the latest comparison, all three refs, every prior loop issue/PR/handoff, and the cycle slot. Reject a stale or non-latest input.
2. If `FIXED_POINT_READY`, revalidate the terminal matrix, emit the exact skip, and stop.
3. On `REPAIR_REQUIRED`, verify exactly one selected repository/base/path set. Search that repository for an exact focused issue and ensure no duplicate or broader issue owns the same blockers.
4. Build the issue body from the exact selected blocker rows, canonical source blobs, target baseline, exception decisions, validation, review/quality/merge/handoff/recheck lifecycle, exclusions, and terminal result.
5. Create/update only that issue. Add the permitted tracker relationship or cross-reference. Do not add a native dependency only for chronology.
6. Read back the issue, relationships, body identity, and absence of commencement/branch/PR state. Return the issue or skip result.

### Validation and evidence

Require the latest comparison, exact conditional branch, one selected target on repair, complete issue-body/readback/relationship evidence, no duplicate or false dependency, no commencement/repository mutation, or an exact current fixed-point skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_ISSUE_READY` record contains selected repository/base/issue/body/relationships/loop instance and proof of no commencement, or one exact `SKIPPED — FIXED_POINT_READY` record.

### Complete when

One self-contained selected-target issue is ready and no implementation began, or the current fixed point produces an exact skip.

## Task 31 — commence one selected-target PR #78 fixed-point repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository/base | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` / `{{PR78_SELECTED_REPAIR_BASE_REF}}` | Task 30 repair branch |
| Focused issue | `{{PR78_SELECTED_REPAIR_ISSUE_URL}}` | Task 30 |
| Intended branch | `{{PR78_SELECTED_REPAIR_BRANCH}}` | Select but do not create on repair branch |
| Commencement comment | `{{PR78_SELECTED_REPAIR_COMMENCEMENT_COMMENT_URL}}` | Post/read back on repair branch |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Task 30 |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 30 in the same loop instance | `FS` | The selected issue is ready, or the exact fixed-point skip is current. |

### Objective

On the repair branch, post only the selected-target commencement record. On the fixed-point branch, propagate the exact skip. Do not edit bytes, create/move a branch, commit, push, create/update a PR, review, merge, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected-target issue rule

Use the latest `REPAIR_REQUIRED` comparison: Task 29 for the first instance or the latest completed Task 38 instance for a repeat. Create one focused issue in exactly one selected target. In PS, a native sub-issue link to the umbrella is permitted as tracking. In Terraform or research-misc, use an exact cross-reference; do not fabricate a cross-repository parent or dependency. For research-misc, target the stacked base `agent/bash-pid1-runtime-contract` and keep PID 1 application work out of scope.

### Procedure

1. Validate Task 30 and re-query the selected issue/repository/base, all three refs, prior repair state, worktrees, open PRs, and the cycle slot.
2. If Task 30 skipped, revalidate and emit the same exact skip.
3. Verify source blobs and select an unused focused branch. For research-misc, base it on the exact current `agent/bash-pid1-runtime-contract` and do not create or change the branch in this task.
4. Post one `IMPLEMENTATION COMMENCED` record with loop instance, actor/time, selected blockers, base/branch, immutable inputs, and umbrella. Read it back.
5. Prove no byte/ref/PR mutation and return the commencement or skip result.

### Validation and evidence

Require correct conditional handling, free cycle slot, exact issue/base/source objects, one read-back commencement on repair, unchanged repository/PR state, or an exact skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_COMMENCED` record contains selected issue/comment/base/branch/loop instance and no byte change, or one exact fixed-point skip.

### Complete when

One selected repair owns the slot through an exact commencement record, or the fixed-point branch is skipped; no implementation or PR change occurred.

## Task 32 — implement one selected-target PR #78 fixed-point repair candidate

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository/base | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` / `{{PR78_SELECTED_REPAIR_BASE_REF}}` | Task 31 |
| Focused issue/branch | `{{PR78_SELECTED_REPAIR_ISSUE_URL}}` / `{{PR78_SELECTED_REPAIR_BRANCH}}` | Task 31 |
| Selected path set | `{{PR78_SELECTED_REPAIR_PATH_SET}}` | Latest comparison; exact blocker paths only |
| Candidate commit/tree | `{{PR78_SELECTED_REPAIR_CANDIDATE_COMMIT_SHA}}` / `{{PR78_SELECTED_REPAIR_CANDIDATE_TREE_SHA}}` | Create locally on repair branch |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Task 31 |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 31 in the same loop instance | `FS` | One selected repair owns the slot, or the fixed-point skip is exact. |

### Objective

On the repair branch, create one locally committed candidate that addresses only the selected blockers. On the fixed-point branch, propagate the skip. Do not push, create/update a PR, review, merge, hand off, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected repair implementation rule

Change only the exact blocker path set selected by the latest comparison. Use the strongest reviewed common bytes and preserve proved target exceptions. A research-misc repair must be isolated from PID 1 application behavior and must prepare a stacked candidate for the verified governance base. A PS or Terraform repair must preserve unrelated planned work.

### Procedure

1. Validate Task 31. Propagate an exact current skip when applicable.
2. Pin the selected base and all three current landed/source commits and blobs. Create the recorded branch from the exact base with a clean worktree.
3. Reproduce each selected blocker. Enumerate alignment options and use a finding-specific rubric. Implement the smallest change that restores common bytes or a fully proved target exception.
4. Do not change an unselected path unless validation proves an inseparable dependency; if so, stop for a new comparison decision instead of expanding silently.
5. Run the selected repository's complete applicable validation, the agent-instruction and mutation contract, parser/workflow/YAML/Markdown/hook/package/lock/trust-root tests, three-way blob checks, and `git diff --check`.
6. Audit the diff/outgoing range, commit locally, record exact commit/tree/path blobs/SHA-256/decisions, and keep the remote unchanged.
7. Return the candidate or skip result.

### Validation and evidence

Require exact selected blocker reproduction and closure, no unselected or unrelated change, full repository and three-way validation, proved exceptions, clean committed worktree, unchanged remote/PR state, or an exact skip.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_CANDIDATE_READY` record contains selected repository/base/issue/loop/candidate/path matrix/decisions/validation and no push, or one exact skip.

### Complete when

One local selected-target candidate closes only the authorized blockers with full validation, or the fixed-point branch is skipped; no remote or PR state changed.

## Task 33 — create or update one selected-target PR #78 fixed-point repair PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository/base | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` / `{{PR78_SELECTED_REPAIR_BASE_REF}}` | Task 32 |
| Focused issue | `{{PR78_SELECTED_REPAIR_ISSUE_URL}}` | Task 32 |
| Candidate head/tree | `{{PR78_SELECTED_REPAIR_CANDIDATE_COMMIT_SHA}}` / `{{PR78_SELECTED_REPAIR_CANDIDATE_TREE_SHA}}` | Task 32 |
| Repair PR | `{{PR78_SELECTED_REPAIR_PR_URL}}` | Reuse/create one exact PR to the selected base |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Task 32 |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 32 in the same loop instance | `FS` | The local selected candidate is complete, or the skip is exact. |

### Objective

On the repair branch, non-force push and expose exactly one accurate selected-target PR. On the fixed-point branch, propagate the skip. Do not request review, quality-check, merge, hand off, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected repair implementation rule

Change only the exact blocker path set selected by the latest comparison. Use the strongest reviewed common bytes and preserve proved target exceptions. A research-misc repair must be isolated from PID 1 application behavior and must prepare a stacked candidate for the verified governance base. A PS or Terraform repair must preserve unrelated planned work.

### Procedure

1. Validate Task 32 and conditional state. Re-query target base, refs/PRs, issue, umbrella, permissions, and rules.
2. On repair, verify complete outgoing range, ancestry, tree, allowed path set, clean worktree, and secrets; push non-forcing and read back exact head/tree.
3. Reuse/create one PR to the exact selected base. For research-misc, target `agent/bash-pid1-runtime-contract` and do not modify parent PR #21 directly.
4. Make the PR title/body exact for issue/umbrella/loop, selected blockers, all three source identities, path/blob matrix, decisions, security/failure behavior, validation/checks, deferrals, and rollback. Close only the focused repair issue on merge.
5. Run/await initial checks truthfully. Do not post `@codex review`. Paginate/read back complete state.
6. Return the PR or skip result.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Require correct conditional handling; on repair, exact non-force head/tree, one accurate PR to exact base, complete authorized diff, issue links, truthful checks, no review/merge; on skip, exact current evidence.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_PR_READY` record contains target/base/PR/head/tree/body/files/checks/issue/loop, or one exact skip.

### Complete when

One exact selected-target PR exposes the authorized candidate, or the fixed-point branch is skipped; no review or successor action occurred.

## Task 34 — run the Copilot-and-Codex review loop on one selected-target PR #78 fixed-point repair PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` | Task 33 repair branch |
| Repair PR/issue | `{{PR78_SELECTED_REPAIR_PR_URL}}` / `{{PR78_SELECTED_REPAIR_ISSUE_URL}}` | Task 33 |
| Terminal head/tree | `{{PR78_SELECTED_REPAIR_REVIEWED_HEAD_SHA}}` / `{{PR78_SELECTED_REPAIR_REVIEWED_TREE_SHA}}` | Terminal review-loop output |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Task 33 |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 33 in the same loop instance | `FS` | The exact repair PR is ready without review, or the skip is exact. |

### Objective

On the repair branch, run a complete local Codex audit and the GitHub Copilot and remote Codex code reviews. Repeat all three review actions after every evidence change. On the fixed-point branch, propagate the skip. Do not quality-check, merge, hand off, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected repair implementation rule

Change only the exact blocker path set selected by the latest comparison. Use the strongest reviewed common bytes and preserve proved target exceptions. A research-misc repair must be isolated from PID 1 application behavior and must prepare a stacked candidate for the verified governance base. A PS or Terraform repair must preserve unrelated planned work.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Copy this complete prompt into a fresh native Codex subagent. The local subagent is the executor. GitHub Copilot and `chatgpt-codex-connector` are separate, required code reviewers.

~~~text
You are the local Copilot-and-Codex review-loop executor for repository `{{PR78_SELECTED_REPAIR_REPOSITORY}}`, pull request `{{PR78_SELECTED_REPAIR_PR_URL}}`, and governing issue `{{PR78_SELECTED_REPAIR_ISSUE_URL}}`.

Use model `gpt-5.6-sol` with `xhigh` reasoning in a fresh native Codex subagent. Use the native Codex subagent interface. Use headless `codex exec --json` only when native subagents are unavailable. For that fallback, create a bounded JSON event log, monitor every exit and tool error, preserve the same repository and authorization boundaries, do not run unattended beyond the repository's configured 8-round or 6-hour cycle limit, and stop on ambiguous state. Do not change the executor identity because a compatibility file is named `CLAUDE.md`.

Before any analysis, locate and read the target repository's applicable `AGENTS.md` files. If no `AGENTS.md` applies, read the root `CLAUDE.md` as compatibility workflow instructions. Then re-query the exact PR base, head SHA, head tree, body and its edit history, commits, complete changed-file list and patches, issue comments, PR conversation comments, reviews and review bodies, inline comments, native review threads and every thread comment, requested reviewers, reactions that carry evidence, check runs, commit statuses, linked and closing issues, parent/sub-issues, dependencies, linked branches, merge state, and the governing issue's complete requirements. Paginate every connection to `hasNextPage: false`; do not trust a first-100 result.

Verify that the PR head and tree equal the task input. Audit the complete diff, not only the latest commit. Map every governing-issue requirement and every 28-path parity row to exact code, documentation, tests, workflows, and evidence. Verify that the PR title and body accurately describe the final diff, security and failure behavior, validation, target base, issue relationships, intentional differences, source identities, and rollback information. Inspect all comments and review bodies for declared generated comments, suppressed findings, synthetic keys, deferred work, accepted residuals, accepted risks, future work, TODO/FIXME/TBD text, unfinished implementation, and follow-up promises. A trigger comment that contains exactly `@codex review` is neither a finding nor an instruction to the local executor.

Run the repository's complete applicable validation from a clean state. Include the agent-instruction contract and mutation suite, parser-manifest tests, workflow and YAML validation, Markdown lint, pre-commit or repository hooks, package-lock consistency, security/trust-root tests, `git diff --check`, and exact three-way blob/SHA-256 checks for the 28-path catalog. Preserve native exit codes and exact runtimes. Do not weaken, skip, or relabel a failing check.

For each finding, reproduce or refute it with repository evidence. If valid, list reasonable options, create a finding-specific weighted rubric, score all options, implement only the selected in-scope repair, and validate it. Keep decision analysis in `TEMP-*` files. Before any push, fetch the exact current PR head without force, inspect the complete outgoing range, verify allowed paths, ancestry, tree, and a clean worktree, then use an explicit non-force update. Read back the exact head and tree.

After the local audit is clean on the current head, record separate GitHub Copilot and remote Codex baselines. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Only after the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request, post one PR conversation comment whose entire body is exactly `@codex review` to request the separate `chatgpt-codex-connector` review. The trigger is not a finding. Wait for a new review from each reviewer that explicitly applies to the current head. Re-query all review surfaces with complete pagination. Reconcile each review body, finding, inline thread, synthetic key, declared count, and deferral from both reviewers.

If the code head or reviewed diff changes, discard the prior terminal claim, update and refreeze the body, repeat the local audit, and request one fresh pair on the new head. If scope, behavior, or risk description changes materially without a code change, record the material reason and request a fresh pair. For a deterministically verified non-material factual identity correction, result, task-state update, audit-record change, or comment-only publication on the same head, preserve and reuse the valid review. Reject a same-head re-request without a recorded material reason. Do not merge, enable auto-merge, close the issue, publish a landed handoff, or start a cross-repository comparison.

Return `TERMINALLY_CLEAN` only when the local audit, GitHub Copilot review, and `chatgpt-codex-connector` review are clean on the same current head SHA and tree. Every thread and synthetic finding must be closed. All required checks and validations must have truthful terminal results. The issue and PR descriptions must be accurate. No unfinished work or illegitimate deferral can remain. The exact review results must be public and readable. Otherwise, return `REVIEW_BLOCKED` with the exact blocker and the required return path: Task 32 for bytes or Task 33 for PR-only metadata, followed by a new Task 34 instance in the same loop.
~~~

### Procedure

1. If Task 33 skipped, re-query and emit the same exact skip without invoking reviewers.
2. Otherwise copy the full prompt into a fresh native Codex subagent and monitor to terminal state.
3. Independently verify complete pagination, decisions, repairs, pushes, validations, checks, parity/deferral audits, remote review, and exact final head/tree.
4. Require a new loop after a code-head/reviewed-diff change or recorded material scope/behavior/risk change. Deterministically verify non-material same-head evidence corrections without a new code review.
5. Return the terminal review or skip result and stop.

### Validation and evidence

Require correct conditional handling. On repair, require the required model and effort, a native executor or controlled fallback, applicable instructions, complete pagination, a clean local audit, clean GitHub Copilot and remote Codex reviews on one head/tree, all blocker/issue/parity/check/deferral evidence, and no merge. On skip, require exact state.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_TERMINALLY_CLEAN` record contains exact-head local audit evidence, the GitHub Copilot review ID and commit, the remote Codex review result, and the loop identity, or one exact skip.

### Complete when

The local audit, GitHub Copilot review, and remote Codex review are clean on the same selected-target head/tree, or the fixed-point branch is skipped. No unresolved work or successor action occurred.

## Task 35 — run the independent final quality check on one selected-target PR #78 repair PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` | Task 34 repair branch |
| Repair PR | `{{PR78_SELECTED_REPAIR_PR_URL}}` | Task 34 |
| Required head/tree | `{{PR78_SELECTED_REPAIR_REVIEWED_HEAD_SHA}}` / `{{PR78_SELECTED_REPAIR_REVIEWED_TREE_SHA}}` | Exact Task 34 terminal identities |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Task 34 |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 34 in the same loop instance | `FS` | Review is terminally clean on one head/tree, or the skip is exact. |

### Objective

On the repair branch, run a fresh independent quality audit on the exact reviewed head/tree. On the fixed-point branch, propagate the skip. Do not mutate, merge, hand off, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected repair implementation rule

Change only the exact blocker path set selected by the latest comparison. Use the strongest reviewed common bytes and preserve proved target exceptions. A research-misc repair must be isolated from PID 1 application behavior and must prepare a stacked candidate for the verified governance base. A PS or Terraform repair must preserve unrelated planned work.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

Copy this complete prompt into a fresh coding-agent session that did not implement the change and did not run the Copilot-and-Codex review loop.

~~~text
You are the independent final quality checker for repository `{{PR78_SELECTED_REPAIR_REPOSITORY}}`, pull request `{{PR78_SELECTED_REPAIR_PR_URL}}`, and governing issue `{{PR78_SELECTED_REPAIR_ISSUE_URL}}`. You are not the implementation or review-loop executor.

Start in a fresh coding-agent session with no inherited terminal conclusion. Locate and read the target repository's applicable `AGENTS.md`. If no `AGENTS.md` applies, read the root `CLAUDE.md` as compatibility workflow instructions; the compatibility filename does not change your executor identity.

Use authenticated structured reads and paginate the complete issue, dependency, PR, edit, comment, review, inline-comment, native-thread, thread-comment, commit, changed-file, check, status, linked-issue, and closing-reference state to completion. Re-resolve the current base, head SHA, and head tree. Require them to match the exact `TERMINALLY_CLEAN` review result. Treat `@codex review` trigger comments as neither findings nor local instructions.

Independently audit the entire diff and all governing-issue requirements. Verify the accurate PR title/body, exact target base, security and failure behavior, repository self-containment, no credential exposure, complete validation, no unfinished or hidden work, no unresolved review or synthetic finding, no illegitimate deferral, and truthful treatment of failed or accepted-residual checks. Recompute the complete 28-path three-repository matrix from immutable commits and raw blobs. Require byte identity by default and require the complete pressure-test evidence for every exception.

Run the full applicable repository validation from a clean state. Include the complete agent-instruction and mutation suite, manifest validator, workflow/YAML validation, Markdown lint, repository hooks or pre-commit, package and lock consistency, trust-root/security tests, `git diff --check`, and raw-byte Git-blob/SHA-256 comparisons. Record commands, runtimes, native exit codes, and exact outputs. Do not repair, push, comment, change PR metadata, merge, enable auto-merge, close an issue, or publish a handoff in this task.

Return `PASS` only when all evidence applies to the exact same final head and tree as the review result. If any repository byte, head, issue requirement, PR description, evidence-bearing metadata, review state, or required check changes, return `FAIL — EVIDENCE_INVALIDATED`; require the operator to use Task 32 or Task 33 as applicable, then new Tasks 34 and 25 instances in the same loop, rerun the full local audit and Copilot-and-Codex review loop, and then run a new independent quality check. If a defect exists without a byte change, return `FAIL` with exact evidence and the same return path. Do not merge.
~~~

### Procedure

1. Propagate an exact current skip when Task 34 skipped.
2. Otherwise start a fresh coding-agent session with the full prompt and monitor it.
3. Independently verify its complete audit, validation, matrix, issue/PR state, and exact Task 34 head/tree.
4. Any byte/head/evidence change returns to Task 32 or 23 and then new Tasks 34 and 25 instances.
5. Return the PASS or skip result.

### Validation and evidence

Require correct conditional handling; on repair, fresh session, complete independent pagination/diff/issue/parity/validation/check audit, exact reviewed head/tree, zero unresolved work, and no mutation/merge; on skip, exact state.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_FINAL_QUALITY_PASS` record contains the independent exact-head evidence and loop identity, or one exact skip.

### Complete when

A fresh session returns PASS on the exact reviewed selected-target head/tree, or the fixed-point branch is skipped; the PR remains unchanged and unmerged.

## Task 36 — merge one reviewed selected-target PR #78 fixed-point repair PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository/base | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` / `{{PR78_SELECTED_REPAIR_BASE_REF}}` | Tasks 34 and 25 |
| Repair PR | `{{PR78_SELECTED_REPAIR_PR_URL}}` | Tasks 34 and 25 |
| Gated head/tree | `{{PR78_SELECTED_REPAIR_REVIEWED_HEAD_SHA}}` / `{{PR78_SELECTED_REPAIR_REVIEWED_TREE_SHA}}` | Must equal both gates |
| Landed commit/tree | `{{PR78_SELECTED_REPAIR_LANDED_COMMIT_SHA}}` / `{{PR78_SELECTED_REPAIR_LANDED_TREE_SHA}}` | Resolve after merge |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Same loop instance |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 34 in the same loop instance | `FS` | Review is terminally clean or skipped. |
| Task 35 in the same loop instance | `FS` | Independent quality passes on the same head/tree or skips. |

### Objective

On the repair branch, merge only the exact selected-target head/tree that passed both gates. On the fixed-point branch, record the no-merge skip. Do not hand off, recompare, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected repair implementation rule

Change only the exact blocker path set selected by the latest comparison. Use the strongest reviewed common bytes and preserve proved target exceptions. A research-misc repair must be isolated from PID 1 application behavior and must prepare a stacked candidate for the verified governance base. A PS or Terraform repair must preserve unrelated planned work.

### Procedure

1. If both predecessors skipped, re-query the fixed-point state, emit the exact no-merge skip, and stop.
2. Otherwise paginate/re-query PR/issue/base and require current head/tree equality with both gates, accurate metadata, terminal checks, resolved threads, and no invalidating state.
3. Any change returns to Task 32 or 23 and new Tasks 34/25.
4. Merge once with an allowed non-bypassing method. Read back merge commit/tree/parents, selected base, focused-issue state, umbrella state, checks, and exact catalog blobs.
5. Confirm only the focused repair issue closed and return the merge result.

### Review-evidence materiality gate

### Validation and evidence

Require correct conditional branch; on repair, exact same-head gates, current checks/reviews, allowed merge, landed/base/issue/blob readback and open umbrella; on skip, exact no-merge state.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_MERGED` record contains selected repository/base/PR/gated and landed identities/checks/issue/loop, or one exact no-merge skip.

### Complete when

The exact gated selected-target head/tree is merged and read back, or the fixed-point branch is skipped; no handoff or comparison occurred.

## Task 37 — publish one landed selected-target PR #78 repair handoff

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Selected repository | `{{PR78_SELECTED_REPAIR_REPOSITORY}}` | Task 36 repair branch |
| Focused issue | `{{PR78_SELECTED_REPAIR_ISSUE_URL}}` | Task 36 |
| Landed commit/tree | `{{PR78_SELECTED_REPAIR_LANDED_COMMIT_SHA}}` / `{{PR78_SELECTED_REPAIR_LANDED_TREE_SHA}}` | Task 36 |
| Handoff comment | `{{PR78_SELECTED_REPAIR_HANDOFF_COMMENT_URL}}` | Post/read back on repair branch |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Task 36 |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 36 in the same loop instance | `FS` | The gated repair is landed, or the fixed-point skip is exact. |

### Objective

On the repair branch, publish one permanent landed repair handoff. On the fixed-point branch, propagate the skip. Do not edit bytes, start another repair, recompare, close, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Selected repair implementation rule

Change only the exact blocker path set selected by the latest comparison. Use the strongest reviewed common bytes and preserve proved target exceptions. A research-misc repair must be isolated from PID 1 application behavior and must prepare a stacked candidate for the verified governance base. A PS or Terraform repair must preserve unrelated planned work.

### Procedure

1. Validate Task 36 and conditional state. Pin the landed object and re-query complete lifecycle evidence.
2. On repair, recompute affected and complete catalog blobs/SHA-256 values; audit all reviews/checks/comments/deferrals and prepare a complete landed handoff with all three repository inputs.
3. Post to the focused issue and umbrella; include loop, issue/PR/base, reviewed/landed identities, paths, decisions, validation/checks/reviews, residuals/deferrals, rollback, and Task 38 as the sole next action.
4. Read back exact bodies/hashes. On skip, emit the exact skip without posting a repair handoff.
5. Return the handoff or skip result.

### Validation and evidence

Require correct conditional handling; on repair, immutable landed objects, complete gated lifecycle and catalog evidence, truthful checks/deferrals, exact comment readback and no repository mutation; on skip, exact state.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One `PR78_SELECTED_REPAIR_LANDED_HANDOFF` record contains complete landed and posted evidence, or one exact fixed-point skip.

### Complete when

A permanent selected-target handoff exists and is read back, or the fixed-point branch is skipped; no recomparison or closure occurred.

## Task 38 — recompare the three repositories and repeat the PR #78 repair loop when required

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Initial comparison | `{{TASK_029_COMPLETION_RECORD}}` | Task 29 |
| Latest repair handoff or skip | `{{TASK_037_COMPLETION_RECORD}}` | Same loop instance |
| Current research/PS/Terraform commits and trees | `{{PR78_CURRENT_THREE_REPOSITORY_IDENTITIES}}` | Re-query all three at task start |
| Disposition | `{{PR78_LATEST_FIXED_POINT_DISPOSITION}}` | Exact `FIXED_POINT_READY` or `REPAIR_REQUIRED` |
| Loop instance | `{{PR78_REPAIR_LOOP_INSTANCE}}` | Current instance; increment before a repeat |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 29 | `FS` | The initial full comparison exists. |
| Task 37 in the same loop instance | `FS` | The selected repair handoff or exact fixed-point skip is complete. |

### Objective

Recompute and publish the complete three-repository matrix. Emit `FIXED_POINT_READY` or one selected-target `REPAIR_REQUIRED`. Repeat Tasks 30–38 only after this leaf finishes and only as a new serial instance. Do not publish final closure, close the umbrella, start Task 41, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Recursive fixed-point rule

Always re-query and recompare all three current landed refs, even when Tasks 30–37 skipped. If the result is `REPAIR_REQUIRED`, repeat Tasks 30–38 as a new serial instance with the latest comparison record and an incremented loop number. Do not start the next source-plan task or publish closure while any blocker remains.

### Procedure

1. Validate Task 29 and the latest Task 37 instance. Re-query/pin the current research governance base, PS `main`, Terraform `main`, parent PR #21, every loop issue/PR/handoff, and all 84 cells.
2. Recompute raw blobs/SHA-256 values, modes, attributes, behavior, validation, review evidence, and exception dossiers for all 28 paths.
3. Confirm that each landed repair changed only selected blockers and did not create reverse drift. Re-run every exception pressure test.
4. Emit `FIXED_POINT_READY` only with zero blockers. Otherwise list every blocker and select exactly one next repository/base/path set through a finding-specific rubric.
5. Publish/read back the latest matrix and disposition on the umbrella and applicable issues.
6. If `REPAIR_REQUIRED`, stop this instance, increment the loop number, and repeat Tasks 30–38 with this exact record. Keep one active repair. If `FIXED_POINT_READY`, authorize Task 39 only.

### Validation and evidence

Require current exact refs, all 84 cells and 28 rows once, complete byte/behavior/review/exception evidence, no reverse drift, one disposition, at most one selected target, exact public readback, correct repeat boundary, and no closure or next-slate action.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One authenticated `PR78_LATEST_THREE_WAY_COMPARISON` record states `FIXED_POINT_READY` or `REPAIR_REQUIRED`; the latter contains one next target and exact repeat inputs, while the former contains zero blockers.

### Complete when

The latest complete matrix is public and exact; either zero blockers authorize Task 39 or one selected target authorizes a new Tasks 30–38 instance; no parallel repair or closure occurred.

## Task 39 — publish the PR #78 three-repository fixed-point closure

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Umbrella issue | `{{PR78_UMBRELLA_ISSUE_URL}}` | completed source Task 1 |
| Latest comparison | `{{PR78_LATEST_THREE_WAY_COMPARISON_RECORD}}` | Latest Task 38 instance; must state `FIXED_POINT_READY` |
| Final research/PS/Terraform commits and trees | `{{PR78_FINAL_THREE_REPOSITORY_IDENTITIES}}` | Latest Task 38 exact inputs |
| Closure comments | `{{PR78_FIXED_POINT_COMMENT_URLS}}` | Post/read back by this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Latest Task 38 instance | `FS` | The complete current matrix states `FIXED_POINT_READY` with zero blockers. |

### Objective

Publish the permanent PR #78 fixed-point evidence on the umbrella and relevant work items. Do not edit repository bytes, change issue relationships, merge, close the umbrella or another issue, start Task 41, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Fixed-point publication rule

The closure record is evidence publication only. It must preserve all legitimate repository-specific exceptions and must not claim literal equality for an exception. Use one canonical body and verify byte-identical comment bodies wherever GitHub permits the same record to be posted.

### Procedure

1. Validate the latest Task 38 and re-query all three refs, every issue/PR/review/check/handoff, upstream issues #858–#863, parent PR #21, and all final catalog blobs.
2. Prove zero blockers and that each exception dossier has exact blobs, negative test, alternatives, smallest divergence, equal/stronger security and failure behavior, owner, and review/expiry trigger.
3. Audit every lifecycle for separate issue setup, commencement, implementation, PR, review, independent quality, merge, handoff, comparisons, reruns after changed bytes, and one-at-a-time execution.
4. Prepare one canonical fixed-point record with PR #78 source review truth, final commits/trees, all 84 cells and SHA-256 values, exception dossiers, validation/runtimes/checks/review results, deferred-work and upstream-follow-up disposition, repair-loop history, and proof that no blocker remains.
5. Post/read back the record on the umbrella, merged PR #78, initial focused issues, and repair issues where applicable. Compare exact body bytes/hashes and explain any GitHub-added transport difference without changing content.
6. Return the closure result and stop before closing the umbrella.

### Validation and evidence

Require current final refs, zero blockers, complete catalog/exception/lifecycle/review/check/deferral/upstream evidence, exact canonical-body readbacks and hashes, no unresolved placeholder, no issue closure, and no repository/settings mutation.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One authenticated `PR78_FIXED_POINT_PUBLISHED` record contains every final identity, matrix row, exception, lifecycle result, canonical comment URL/body hash, and proof that the umbrella remains open.

### Complete when

Permanent public evidence proves the reviewed three-repository fixed point with zero blockers and exact legitimate exceptions, every posted body is read back, and the umbrella remains open.

## Task 40 — close the PR #78 convergence umbrella

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each variable with the exact value from its stated predecessor or live read before using it. A conditional variable is resolved only on the branch that uses it; a skip record must name the skipped variable and its inapplicability.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Umbrella issue | `{{PR78_UMBRELLA_ISSUE_URL}}` | Task 39 |
| Fixed-point record | `{{PR78_FIXED_POINT_COMMENT_URLS}}` | Task 39 exact readbacks |
| Final three-repository identities | `{{PR78_FINAL_THREE_REPOSITORY_IDENTITIES}}` | Task 39 |
| Closure result | `{{PR78_UMBRELLA_CLOSURE_RECORD}}` | Produced/read back by this task |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 39 | `FS` | The permanent fixed-point record is public, exact, and proves zero blockers while the umbrella remains open. |

### Objective

Close only the PR #78 convergence umbrella after final readback. Do not edit repository bytes, close upstream issues #858–#863, change another issue or relationship, create/update/merge a PR, start Task 41, or change settings.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific source and parity contract

#### Immutable initial source result

- Source repository: `franklesniak/research-misc`.
- Verified branch: `agent/bash-pid1-runtime-contract`. The unprefixed `bash-pid1-runtime-contract` name returned 404 and must not be used.
- Source PR: `https://github.com/franklesniak/research-misc/pull/78`; merged at `2026-08-21T12:17:29Z`.
- Reviewed head: `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Immutable merge commit: `51dbeb560dd518b904cf1b571dc7d01fd8045749`; tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`.
- Completed PR #174 baseline: `34fb7eb492c46cdf9949a52c357a5dd04f873dab`; tree `9e7319498954119af7c6a51b4d276ac421544b70`. Task 19 provides the later corrected PS landing and supersedes any changed cell.
- Initial Terraform baseline: `fe28313e118497e2567bf306b74ec596dd10eb64`; tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`.
- PR inventory at the planning read: 87 commits, 13 files, 221 reviews, 226 inline comments, 155 conversation comments, 411 timeline items, 67 resolved and zero unresolved review threads, and 46 PR-body edits. Complete pagination must be repeated before reliance.
- Exact reviewed-head checks: 12 successes and one known Copilot setup failure. Preserve that failure truth. Copilot review `4990544692` generated no new comments, and remote Codex comment `5366181295` found no major issue on the same head.

#### Closed 28-path PR #174 convergence catalog

This catalog is closed to the exact 28 paths changed by merged PSStyleGuide PR #174. The planning cells below are immutable baselines. Task 19's corrected PS landing supersedes any PS cell it changed. Every state-changing or comparison task must re-read all three exact commits and raw blobs before reliance.

| Path | research-misc PR #78 merge | PS PR #174 merge baseline | Terraform `main` baseline |
| --- | --- | --- | --- |
| `.codex/config.toml` | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | `89eb76dba2ac64bf811cf4272b033a30de241d80` / 205 bytes | Absent |
| `.github/instructions/docs.instructions.md` | `96c50a1f4213d90646553c8daf4bf7d93fa27cbe` / 54529 bytes | `0e087aba4d4a1721df461f17d0c694ec7ed607c1` / 44830 bytes | Absent |
| `.github/instructions/yaml.instructions.md` | `3f013b485e333fcfe8480c926361a642f5ebc8a4` / 54492 bytes | `edef19560d3403985d1417af4772d74bbf9eacf4` / 54645 bytes | Absent |
| `.github/workflows/agent-instructions.yml` | `d4f380f61ce00092a416dac4ed3eaf6d11cbb7bc` / 5742 bytes | `c51f75b55f403994af7123a2a9a5f53bbb506d65` / 11087 bytes | Absent |
| `.github/workflows/copilot-setup-steps.yml` | `bd4f100bf2f713fa86f72c489e1fff5cde8e1ead` / 7711 bytes | `0600bee7b8adfc1b827d9a5ba52f6586321ac8ea` / 3435 bytes | Absent |
| `.github/workflows/devcontainer-ci.yml` | `5599eb27bd42af85bed376e82a60bff4524dcc70` / 30461 bytes | `dde42437f9f5d579abee2e944321488a2ba4f793` / 3863 bytes | Absent |
| `.github/workflows/lint-nested-markdown.js` | Absent | `b20dc7f318cf3e14e743eb4bb0bf193be7ad1f62` / 13668 bytes | `c258fab1df61c810915b38aeb4ff4046507b23dd` / 9391 bytes |
| `.github/workflows/lint-staged-markdown.mjs` | Absent | `56c5caffa2adbbc5cac095dc4c172e8391b3569c` / 3448 bytes | Absent |
| `.github/workflows/MARKDOWN-LINTING-IMPLEMENTATION.md` | Absent | `bed8c585a495c3cab2875cac036a5717acd82bb6` / 8689 bytes | `62ee8c2b007a2d71d1557adcbd78fc3aac8ea487` / 8186 bytes |
| `.github/workflows/markdownlint.yml` | Absent | `5b92641c09b4dd77ad1ba213b873d06bc5f07ad3` / 8494 bytes | `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0` / 35733 bytes |
| `.github/workflows/package.json` | Absent | `84bd0421fcf17984220a408e42c5e0523ef65be6` / 1021 bytes | `2b88a0ac85d3a8b7286040e6b1f6c4ddb4d3bce1` / 1068 bytes |
| `.github/workflows/scripts-README.md` | Absent | `0350d4979fd5ba109ba4dbb910e1593debd0138d` / 3038 bytes | `3df4e960aa264efb5b31714af170323352fb1d0c` / 2493 bytes |
| `.github/workflows/Test-AgentInstructionParserManifest.mjs` | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | `0716a34b6b7f9cf22bcf60d242582174c17b85b3` / 38982 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.ps1` | `a9e23ed70deda2a9de12fc8962282ad167d4bde9` / 306638 bytes | `ae348330da2a304cbd3fc00488308725d9f0519e` / 563607 bytes | Absent |
| `.github/workflows/Test-AgentInstructions.SelfTest.ps1` | Absent | `ccccb64cefbcf62c6ca8eb2a601d3fd8bc463da4` / 9828 bytes | Absent |
| `.github/workflows/Validate-WorkflowPolicy.mjs` | Absent | `30151f0e79ebb4d979032d31993283461176346f` / 38901 bytes | `1a8eed12ebe81ac1727eb0bade15ef2aa76548c5` / 362706 bytes |
| `.github/workflows/workflow-policy-cases.json` | Absent | `d5e6ea8835e459c559af6ba3c018bfea1719aa7c` / 13777 bytes | Absent |
| `.github/workflows/workflow-policy-contract.json` | Absent | `eb15cac87e0f69510a55aeff9661f3fd5087fb96` / 21888 bytes | Absent |
| `.gitignore` | `e42d3bbc695f8bb78fb6a49a5672f973803719a3` / 194 bytes | `7429fc67b0c38c8ad6b57211c88797b5e8f13140` / 250 bytes | `4d3494d0e1f5a68a7ff61997526269483f1e3ab1` / 171 bytes |
| `.pre-commit-config.yaml` | `6a138c59a0ce62ad9679c815e024c9d6e9902ccf` / 7318 bytes | `f0ee1964dca454cb6ce7a03bce798a053bd89e28` / 3034 bytes | Absent |
| `AGENTS.md` | `63a09836e309e3e0e9dc1e6abff7807b5c3e77b1` / 32537 bytes | `bc95547ce9681b04f66df0e59a9abc56d7d262cb` / 29557 bytes | Absent |
| `CLAUDE.md` | `c5e8dd2eb9622d2b95edc80876a23997060b285d` / 82250 bytes | `93251edf95df2218a3b31730015fdf55ca8a0e20` / 79276 bytes | `c4100bfa36526643d04f6a5571d1ea2dd807b2d3` / 16652 bytes |
| `docs/decisions/0001-accept-in-repository-trust-root.md` | Absent | `0386f596033b6871f01bb4c7c8f471b1087476bd` / 12177 bytes | Absent |
| `docs/decisions/0002-accept-unverifiable-baseline-provenance.md` | Absent | `63c4682c034e78d408c0f862bed29a5d6a17b267` / 6534 bytes | Absent |
| `docs/ISSUE_EVALUATION_PROMPT.md` | Absent | `6835e35d318e3f8af8f48ba0d7d773f10475918b` / 2214 bytes | `34770d1a044785a8d544567b5ece736d5b7cebfe` / 1807 bytes |
| `package-lock.json` | `e9efa0381e686a265c41eb36662671791da9e837` / 48914 bytes | `6c860228c010e7179d249138e00d2a3b257a063a` / 68486 bytes | Absent |
| `package.json` | `062a0fc09368701f07a9db3d8b09877866463644` / 723 bytes | `b844833a75abfc4189dfe9626bdbc4c64db46f71` / 1164 bytes | Absent |
| `STYLE_GUIDE_RATIONALE.md` | Absent | `70fbd66c7d000939d7d84e0767bdf25320ce714e` / 134847 bytes | `bdb1fd9f6343a12ed4711b67ed169094c275c6e6` / 72233 bytes |

Git blob equality across repositories is byte equality. Also compute SHA-256 over raw bytes. Absence is a difference that requires proof; it is not automatic non-applicability. The desired terminal state is byte identity at the same path. Permit only the smallest repository-specific substitution after verbatim adoption fails because of a recorded incompatible repository fact and a finding-specific weighted evaluation selects the difference.

#### Three-repository parity rule

1. Evaluate every catalog path in research-misc, PSStyleGuide, and TerraformStyleGuide. Do not omit a path because it is absent in a target.
2. The default result is byte identity at the same path. Classify each path/repository cell exactly once as `BYTE_IDENTICAL`, `PROVED_REPOSITORY_SPECIFIC_DIFFERENCE`, or `BLOCKER`.
3. Treat absence as a difference that requires proof. Repository name, current absence, convenience, review churn, or different history is not sufficient by itself.
4. Before accepting a difference, test verbatim adoption, list all reasonable alignment options, and prove the incompatible repository fact. Select the smallest divergence that preserves equal or stronger security, failure truth, validation, and review behavior.
5. Record both exact blobs and SHA-256 values, the negative test, the selected difference, its owner, and its review or expiry trigger. A broad whole-file fork is invalid when a smaller substitution or common design is feasible.
6. Preserve each repository as self-contained. Do not add a shared package, shared runtime workflow, submodule, cross-repository download, or third source of truth.
7. If review changes a common byte, treat the change as a new three-way comparison input. Do not overwrite a reviewed improvement with the original source only to force equality.
8. Upstream copilot-repo-template issues #858 through #863 remain separate open follow-ups with no native dependency on this cycle. Do not implement or close them in this work unless a new, explicit authority changes scope.

#### Fixed-point publication rule

The closure record is evidence publication only. It must preserve all legitimate repository-specific exceptions and must not claim literal equality for an exception. Use one canonical body and verify byte-identical comment bodies wherever GitHub permits the same record to be posted.

### Procedure

1. Validate Task 39 and re-query the umbrella, all focused issues and PRs, repair loops, final comments, all three refs, parent PR #21, and upstream issues #858–#863.
2. Require every focused implementation or repair issue to have the correct terminal state, every PR to be merged or explicitly absent on a conformant skip, every handoff and comparison readable, and the final matrix to remain current with zero blockers.
3. Confirm that upstream #858–#863 remain separate work and are not falsely closed, blocked, or claimed complete by this cycle.
4. Close the umbrella with reason `COMPLETED`. Do not close any other item.
5. Read back state, state reason, closing actor/time, timeline, fixed-point comment, final relationships, and absence of a closing PR that misrepresents tracker closure.
6. Return the exact umbrella closure record and stop. Output Task 41 is a separate leaf.

### Validation and evidence

Require current fixed-point evidence, zero blockers, all focused work terminal, correct skips, open upstream follow-ups, one umbrella-only close action/readback, completed reason, exact final identities, and no repository/PR/settings or other issue mutation.

### Stop and escalation conditions

Stop without starting a successor if an input object is unavailable, a ref or body changed unexpectedly, complete pagination is impossible, an applicable placeholder cannot be resolved, the one-at-a-time slot is occupied, the requested action is outside the plan's standing authority, a native dependency is false, a proposed exception is not proved, a review or check is stale, validation fails, an unresolved finding or illegitimate deferral remains, or the requested state would weaken security, failure truth, review coverage, repository self-containment, or fixed-point integrity. Record the exact blocker and the next required decision.

### Exact output

One authenticated `PR78_UMBRELLA_CLOSED` record contains the umbrella URL/IDs/state reason/actor/time, Task 39 fixed-point identities, final relationships, all three commits/trees, and proof that no other item or repository state changed.

### Complete when

The umbrella alone is closed as completed after exact fixed-point readback, every focused item is terminal, upstream follow-ups remain separate, and Task 41 has not started.

## Task 41 — validate and update PS #168 for PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 1,852 UTF-8 bytes; SHA-256 `998fe1c993a6e2fefd933b9a3f6612b193383b8acf704ef0a9d487589597d710` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 40 | `FS` | The PR #78 three-repository fixed point is public and its umbrella is closed; use its final commits and blobs as current governance inputs. |
| Completed PS #161 and Terraform #34 handoffs | `FS` | PR #167 and PR #35 are merged; no implementation issue or PR currently owns the one-at-a-time slot. |

### Objective

Validate the existing issue as the complete, self-contained authority for PR-body identity synchronization. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_168_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_168_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 42 — commence PS PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 41 | `FS` | PS #168 is open, self-contained, current, and ready; no implementation has commenced and the one-at-a-time slot is free. |

### Objective

Explicitly commence only the implementation governed by implement PS PR-body identity synchronization. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 43 — implement PS PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 42 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS PR-body identity synchronization without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 44 — create or update the candidate PR for PS PR-body identity synchronization

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 44 PR URL | `{{TASK_044_PR_URL}}` | Produced by this task; replace {{TASK_044_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 43 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS PR-body identity synchronization; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

- Preserve issue #168's exact durability objective: derive every PR-body identity from the checked-out head, compute the canonical policy-contract digest with `validatorIdentity` excluded by the validator's exact algorithm, and emit one deterministic Markdown block for the current head and tree.
- Detect a stale or partial identity block before automated review. Update all coupled generator, path-verifier, build-step, contract, and validator facts as one unit, or fail without changing the PR body.
- Authenticate every PR-body update through an exact readback. Never store credentials, API responses, or unrelated PR text in tracked artifacts.
- Do not change the generator algorithm or exact-path algorithm delivered by PS PR #167. Use a finding-specific decision to select the smallest repository-owned command, validation entry point, fixtures, and documentation paths.
- Require deterministic output, stale-block negative controls, partial-block negative controls, hostile or malformed value controls, exact head/tree binding, a no-change control, repository Markdown validation, and successful current-head CI.
- After the PS handoff, compare applicability in Terraform. Preserve byte-identical common CI behavior except proved repository-specific names and path roles.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PS #168 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #168 candidate PR exists with complete initial validation and an accurate PR body.

## Task 45 — run the Copilot-and-Codex review loop on the PS #168 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 44 PR URL | `{{TASK_044_PR_URL}}` | Produced by Task 44; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 44 | `FS` | The PS #168 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #168 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #168 PR. Do not merge. If the loop changes bytes, rerun the Task 44 validation and continue on the new head.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 46 — run the independent final quality check on the PS #168 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 44 PR URL | `{{TASK_044_PR_URL}}` | Produced by Task 44; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 45 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #168 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #168 PR. Require full issue-contract, workflow-topology, policy-case, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 45 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 45.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 45.

## Task 47 — merge the PS #168 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 44 PR URL | `{{TASK_044_PR_URL}}` | Produced by Task 44; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 46 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 48 — publish the landed handoff for the PS #168 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #168 URL | `https://github.com/franklesniak/PSStyleGuide/issues/168` | Verified existing GitHub issue |
| Task 44 PR URL | `{{TASK_044_PR_URL}}` | Produced by Task 44; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 47 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #168 is closed and the final landed policy contract is ready for Task 57.

### Complete when

PS #168 is closed and the final landed policy contract is ready for Task 57.

## Task 49 — compare the PR-body identity synchronization cycle in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 48 | `FS` | Use the final landed PS #168 commit and policy contract. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 49 | Pin all Task 48 workflow, policy, contract, case, and validation blobs. |
| Task 49 | Complete the full foundation catalog and workflow-topology table in Terraform. |
| Task 49 | Apply all blockers from Task 48's required topology and policy. |
| Task 53 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 49 | Update PS #168, PS #159, and Terraform #31 with full identities and validation. |
| Task 49 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 49 | Map files by role, not only by filename. |
| Task 49 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 52 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 50 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 51 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 53 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 49 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 50 — create or update the focused repair issue for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 50 issue URL | `{{TASK_050_ISSUE_URL}}` | Produced by this task; replace {{TASK_050_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 49 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 49's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 51 — commence the focused repair for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 50 issue URL | `{{TASK_050_ISSUE_URL}}` | Produced by Task 50; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 50 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 50 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 52 — implement the focused repair for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 50 issue URL | `{{TASK_050_ISSUE_URL}}` | Produced by Task 50; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 51 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 51 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 53 — create or update the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 50 issue URL | `{{TASK_050_ISSUE_URL}}` | Produced by Task 50; resolve globally before execution |
| Task 53 PR URL | `{{TASK_053_PR_URL}}` | Produced by this task; replace {{TASK_053_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 52 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 52 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 54 — run the Copilot-and-Codex review loop on the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 53 PR URL | `{{TASK_053_PR_URL}}` | Produced by Task 53; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 53 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 53 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 53's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 54 for the new head before Task 55. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 55 — run the independent final quality check on the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 53 PR URL | `{{TASK_053_PR_URL}}` | Produced by Task 53; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 54 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 54 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 53's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 54 for the new head, and then repeat Task 55 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 56 — merge the focused repair PR for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 53 PR URL | `{{TASK_053_PR_URL}}` | Produced by Task 53; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 55 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 55 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 57 — publish the landed repair handoff for the PR-body identity synchronization cycle in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 53 PR URL | `{{TASK_053_PR_URL}}` | Produced by Task 53; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 56 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 56 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 58 — reverse-compare the PR-body identity synchronization cycle in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 57 | `FS` | Use only the final landed commits from Tasks 48 and 17. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 58 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 58 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 59 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 60 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 62 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 67 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 61 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 68 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 68 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 59 — create or update the focused repair issue for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 59 issue URL | `{{TASK_059_ISSUE_URL}}` | Produced by this task; replace {{TASK_059_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 58 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 58's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 60 — commence the focused repair for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 59 issue URL | `{{TASK_059_ISSUE_URL}}` | Produced by Task 59; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 59 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 59 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 61 — implement the focused repair for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 59 issue URL | `{{TASK_059_ISSUE_URL}}` | Produced by Task 59; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 60 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 60 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 62 — create or update the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 59 issue URL | `{{TASK_059_ISSUE_URL}}` | Produced by Task 59; resolve globally before execution |
| Task 62 PR URL | `{{TASK_062_PR_URL}}` | Produced by this task; replace {{TASK_062_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 61 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 61 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 63 — run the Copilot-and-Codex review loop on the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 62 PR URL | `{{TASK_062_PR_URL}}` | Produced by Task 62; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 62 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 62 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 62's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 63 for the new head before Task 64. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 64 — run the independent final quality check on the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 62 PR URL | `{{TASK_062_PR_URL}}` | Produced by Task 62; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 63 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 63 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 62's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 63 for the new head, and then repeat Task 64 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 65 — merge the focused repair PR for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 62 PR URL | `{{TASK_062_PR_URL}}` | Produced by Task 62; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 64 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 64 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 66 — publish the landed repair handoff for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 62 PR URL | `{{TASK_062_PR_URL}}` | Produced by Task 62; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 65 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 65 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 67 — recheck the reciprocal result for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 58 | `FS` | Use the original pinned comparison and branch result. |
| Task 66 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 52–66 as a new numbered-range instance. After its new handoff, repeat Task 67.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 68 — publish the fixed-point closure for the PR-body identity synchronization cycle in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 67 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 67 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 69 — reverse-compare cycle 2 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 68 | `FS` | The PS #168 reciprocal cycle is closed, and its final identities are recorded separately. |
| Completed PS #161 landed handoff | `FS` | Use only landed commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and comment `5334317038` for the PS cycle-2 input. |
| Completed Terraform #34 landed handoff | `FS` | Use only landed commit `fe28313e118497e2567bf306b74ec596dd10eb64`, tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, and comment `5336853959` for the Terraform cycle-2 input. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 69 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 69 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 70 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 71 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 73 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 96 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 72 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 97 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 97 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Current blocker reconciliation

Current authenticated evidence identifies three distinct PS blockers tracked by #169, #170, and #171. A `REPAIR_REQUIRED` result must name all three, preserve their separate scopes, and order #169 first. Emit `NO_REPAIR` only if fresh landed-byte evidence proves every requirement in all three issues already satisfied and the issues have a defensible terminal disposition. If the branch differs from the current evidence, stop after publishing the comparison and run a new ordering decision before Task 70.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 70 — validate and update PS #169 for BSD identity dispatch and candidate-publication binding

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/169` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 6,843 UTF-8 bytes; SHA-256 `f10c8d7e4dc6b0a7938f8e4f17bf193c7deada650fcf8d2824323375f1e903f6` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 69 | `FS` | Use its current pinned comparison and exact target-issue disposition. |

### Objective

Validate the existing issue as the complete, self-contained authority for BSD identity dispatch and candidate-publication binding. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 69 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_169_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_169_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 71 — commence the PS #169 generator identity repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #169 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/169}` | Verified existing PS #169 issue; re-query before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 70 | `FS` | A focused issue exists, and the current issue-ready branch is active. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 69 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. Require Task 70 to return the issue-ready record. Stop if it did not.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

## Task 72 — implement the PS #169 generator identity repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #169 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/169}` | Verified existing PS #169 issue; re-query before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 71 | `FS` | The focused issue is commenced, and the current issue-ready branch is active. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 69 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. If Task 71 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

## Task 73 — create or update the PS #169 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #169 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/169}` | Verified existing PS #169 issue; re-query before execution |
| Task 73 PR URL | `{{TASK_073_PR_URL}}` | Produced by this task; replace {{TASK_073_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 72 | `FS` | Use the committed repair candidate, and the current issue-ready branch is active. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Issue-specific scope

- Keep the Windows handle identity implementation. Select GNU `stat -Lc` only on Linux; select BSD `stat -f` on macOS and FreeBSD; reject an unknown non-Windows platform. Preserve exact link-count, device, inode, and hard-link refusal.
- Keep both post-publication final identity reads and every byte and serialization check. Require the stable final identity to equal the captured candidate identity. Report a post-publication mismatch as `ReplacementStateUncertain`; do not claim rollback.
- Limit the focused repair to `.github/workflows/Generate-StyleGuideArtifacts.ps1`, `.github/workflows/Validate-WorkflowPolicy.mjs`, and `.github/workflows/build.yml`. Do not change guides, generated artifacts, package files, lockfiles, action pins or inputs, permissions, triggers, job topology, artifact roles, credential roles, or settings.
- Pin and mutation-test platform dispatch and final-to-candidate equality. Run both PowerShell parsers, PSScriptAnalyzer with zero Warning/Error findings, GNU/BSD/unknown-platform controls, actual `File.Replace` and `File.Move` identity controls, the same-byte different-identity failure, policy preflight and catalog, and two generator passes on Windows PowerShell 5.1, Windows PowerShell 7, and native ext4 PowerShell 7.
- Start only from Terraform PR #35's landed commit, tree, blobs, review result, and the Task 69 ordered comparison. Never copy from its deleted or moving feature branch.

### Procedure

1. If Task 72 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

## Task 74 — run the Copilot-and-Codex review loop on the PS #169 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 73 PR URL | `{{TASK_073_PR_URL}}` | Produced by Task 73; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 73 | `FS` | The focused repair PR is review-ready, and the current issue-ready branch is active. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 73 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 73's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 74 for the new head before Task 75. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

## Task 75 — run the independent final quality check on the PS #169 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 73 PR URL | `{{TASK_073_PR_URL}}` | Produced by Task 73; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 74 | `FS` | A terminal-clean record applies to the current repair head/tree, and the current issue-ready branch is active. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 74 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 73's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 74 for the new head, and then repeat Task 75 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

## Task 76 — merge the PS #169 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 73 PR URL | `{{TASK_073_PR_URL}}` | Produced by Task 73; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 75 | `FS` | Both review gates apply to the same current head/tree, and the current issue-ready branch is active. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 75 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

## Task 77 — publish the landed handoff for PS #169

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 73 PR URL | `{{TASK_073_PR_URL}}` | Produced by Task 73; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 76 | `FS` | The repair PR is merged with an actual landed identity, and the current issue-ready branch is active. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 76 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

## Task 78 — compare landed PS #169 with Terraform and authorize PS #170

> **Execution class: Coding agent executable.** This is a read-only comparison plus evidence-publication leaf.

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally from its named predecessor before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repositories | `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by this task |
| Completed PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/169` | Require closed-by-merged-PR evidence |
| Next PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/170` | Verified open issue; this task does not edit it |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 69 | `FS` | Its pinned Terraform PR #35 and PS comparison evidence remains current for the property sequence. |
| Task 77 | `FS` | PS #169 is closed by a reviewed merged PR and has an authenticated landed handoff. |

### Objective

Compare the landed PS #169 result with the immutable Terraform PR #35 implementation and current canonical refs. Prove that the completed issue introduced no reciprocal regression. Publish the pinned comparison and authorize PS #170 only when it is the exact next remaining PS blocker. Do not update or commence #170, edit repository bytes, create a PR, merge, or change settings.

### Execution controls

1. Fetch and pin both current `origin/main` refs, the completed PS PR's reviewed and landed identities, Terraform PR #35 merge `fe28313e118497e2567bf306b74ec596dd10eb64` and tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, and every compared Git blob.
2. Read source bytes only from recorded landed commits. Do not copy or compare from a moving feature branch.
3. Compare the complete common foundation catalog and the property scope below. Classify each applicable row exactly once as `same`, `intentional difference`, or `blocker`.
4. Prove every intentional difference with both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition.
5. Re-query issue #170, its body edits, comments, dependencies, cross-references, linked branches, closing references, and implementation markers with complete pagination.
6. The only permitted public write is the exact comparison evidence comment on the applicable existing trackers or issues. Do not edit an issue body or relationship in this task.
7. Stop if a new Terraform repair is required, #170 is not the next remaining blocker, a row changes direction twice, or any landed identity is unavailable. Use a new decision before changing the planned order.

### Property scope

Compare GNU/BSD/unknown-platform dispatch, link-count/device/inode semantics, hard-link refusal, final identity stability, final-to-candidate equality, post-publication uncertainty truth, policy mutations, runtime matrices, and every coupled version or identity. Require #169 exact scope to be closed before #170 starts.

### Procedure

1. Validate both predecessor records and re-query all current identities and review/check evidence.
2. Build the complete role-to-path and property matrix from the immutable landed commits. Include repository-specific substitutions explicitly.
3. Verify that every #169 requirement is present in the landed PS bytes and that the corresponding Terraform behavior is equal or a proved intentional difference.
4. Verify that the only planned remaining blocker is the exact #170 scope. Do not treat an issue cross-reference as implementation or completion evidence.
5. Publish one authenticated comparison record that states `REPAIR_REQUIRED` with target PS #170, exact pinned inputs, affected roles and paths, required validation, and expected terminal state.
6. Read back the comment, verify its exact body identity, and stop without starting Task 79.

### Validation and evidence

Require complete GitHub pagination, exact commit/tree/blob identities, each applicable catalog row exactly once, no unexplained difference, successful landed checks, closed #169 evidence, open and uncommenced #170 evidence, and an authenticated comparison-comment readback.

### Stop and escalation conditions

Stop if the comparison can emit `FIXED_POINT_READY`, targets Terraform, identifies a blocker outside #170, finds #169 incomplete, or finds changed identities or unfinished review. Record the exact result and run a new ordering decision; do not force the expected branch.

### Exact output

One pinned comparison record emits `REPAIR_REQUIRED` for PS #170 and proves that PS #169 is landed, reviewed, reciprocally safe, and no longer a blocker.

### Complete when

Authenticated readback proves the comparison record is complete and exact, #169 is reciprocally closed, and PS #170 is the only authorized next issue; no implementation has commenced.

## Task 79 — validate and update PS #170 for culture-invariant rationale anchors

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/170` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 4,333 UTF-8 bytes; SHA-256 `59771ad2e7d87d19f4b9a3bf8e8343a7e78d6ef5b51ac9ca00254e85d99dd547` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 78 | `FS` | Use its current pinned comparison and exact target-issue disposition. |

### Objective

Validate the existing issue as the complete, self-contained authority for culture-invariant rationale anchors. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 78 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_170_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_170_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 80 — commence the PS #170 culture-invariant anchor repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #170 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/170}` | Verified existing PS #170 issue; re-query before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 79 | `FS` | A focused issue exists, and the current issue-ready branch is active. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 78 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. Require Task 79 to return the issue-ready record. Stop if it did not.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

## Task 81 — implement the PS #170 culture-invariant anchor repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #170 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/170}` | Verified existing PS #170 issue; re-query before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 80 | `FS` | The focused issue is commenced, and the current issue-ready branch is active. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 78 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. If Task 80 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

## Task 82 — create or update the PS #170 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #170 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/170}` | Verified existing PS #170 issue; re-query before execution |
| Task 82 PR URL | `{{TASK_082_PR_URL}}` | Produced by this task; replace {{TASK_082_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 81 | `FS` | Use the committed repair candidate, and the current issue-ready branch is active. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Issue-specific scope

- Replace both affected rationale-anchor conversions with `.ToLowerInvariant()` or an equivalently narrow invariant-culture operation. Do not change process-wide culture, ASCII filtering, or dash normalization without a separately measured defect and decision.
- Bind both conversions in the current PS machine-readable policy authority and case catalog, or prove an equally strong existing authority. Add independently reachable mutations that restore culture-sensitive casing at each conversion and fail their dedicated invariant-casing rule before a digest backstop.
- Use PS baseline commit `af1ea77bb7c422d2776bb6e26ba376a5010f6f6b`, tree `215fc660c63e6c59f38f865faa3a441452c36605`, and generator blob `e19080ef9108fcdec66b634037b891a94352723d` only to reproduce the original defect; re-read the canonical baseline produced by Task 78 before editing.
- Run both PowerShell parsers, PSScriptAnalyzer, extracted `en-US` and `tr-TR` `Identity`/`identity` controls for both helpers, policy preflight and every catalog case exactly once, the two-run three-host generator matrix, artifact byte equality and idempotence, generated-drift checks, exact scope, current-head CI, review, and landed handoff.
- Keep this issue separate from #169 and link the landed Terraform repair in the final handoff.

### Procedure

1. If Task 81 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

## Task 83 — run the Copilot-and-Codex review loop on the PS #170 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 82 PR URL | `{{TASK_082_PR_URL}}` | Produced by Task 82; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 82 | `FS` | The focused repair PR is review-ready, and the current issue-ready branch is active. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 82 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 82's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 83 for the new head before Task 84. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

## Task 84 — run the independent final quality check on the PS #170 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 82 PR URL | `{{TASK_082_PR_URL}}` | Produced by Task 82; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 83 | `FS` | A terminal-clean record applies to the current repair head/tree, and the current issue-ready branch is active. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 83 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 82's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 83 for the new head, and then repeat Task 84 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

## Task 85 — merge the PS #170 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 82 PR URL | `{{TASK_082_PR_URL}}` | Produced by Task 82; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 84 | `FS` | Both review gates apply to the same current head/tree, and the current issue-ready branch is active. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 84 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

## Task 86 — publish the landed handoff for PS #170

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 82 PR URL | `{{TASK_082_PR_URL}}` | Produced by Task 82; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 85 | `FS` | The repair PR is merged with an actual landed identity, and the current issue-ready branch is active. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 85 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

## Task 87 — compare landed PS #170 with Terraform and authorize PS #171

> **Execution class: Coding agent executable.** This is a read-only comparison plus evidence-publication leaf.

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally from its named predecessor before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repositories | `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by this task |
| Completed PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/170` | Require closed-by-merged-PR evidence |
| Next PS issue | `https://github.com/franklesniak/PSStyleGuide/issues/171` | Verified open issue; this task does not edit it |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 78 | `FS` | Its pinned Terraform PR #35 and PS comparison evidence remains current for the property sequence. |
| Task 86 | `FS` | PS #170 is closed by a reviewed merged PR and has an authenticated landed handoff. |

### Objective

Compare the landed PS #170 result with the immutable Terraform PR #35 implementation and current canonical refs. Prove that the completed issue introduced no reciprocal regression. Publish the pinned comparison and authorize PS #171 only when it is the exact next remaining PS blocker. Do not update or commence #171, edit repository bytes, create a PR, merge, or change settings.

### Execution controls

1. Fetch and pin both current `origin/main` refs, the completed PS PR's reviewed and landed identities, Terraform PR #35 merge `fe28313e118497e2567bf306b74ec596dd10eb64` and tree `cc20f596fda26ec7374adc5fe235ea02c80ee151`, and every compared Git blob.
2. Read source bytes only from recorded landed commits. Do not copy or compare from a moving feature branch.
3. Compare the complete common foundation catalog and the property scope below. Classify each applicable row exactly once as `same`, `intentional difference`, or `blocker`.
4. Prove every intentional difference with both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition.
5. Re-query issue #171, its body edits, comments, dependencies, cross-references, linked branches, closing references, and implementation markers with complete pagination.
6. The only permitted public write is the exact comparison evidence comment on the applicable existing trackers or issues. Do not edit an issue body or relationship in this task.
7. Stop if a new Terraform repair is required, #171 is not the next remaining blocker, a row changes direction twice, or any landed identity is unavailable. Use a new decision before changing the planned order.

### Property scope

Compare both invariant anchor conversions, ASCII filtering and dash normalization, policy authority, independent mutation fixtures, culture-control outputs, generator versions and identities, runtime matrices, generated-byte stability, and every intentional repository-specific difference. Require #170 exact scope to be closed before #171 starts.

### Procedure

1. Validate both predecessor records and re-query all current identities and review/check evidence.
2. Build the complete role-to-path and property matrix from the immutable landed commits. Include repository-specific substitutions explicitly.
3. Verify that every #170 requirement is present in the landed PS bytes and that the corresponding Terraform behavior is equal or a proved intentional difference.
4. Verify that the only planned remaining blocker is the exact #171 scope. Do not treat an issue cross-reference as implementation or completion evidence.
5. Publish one authenticated comparison record that states `REPAIR_REQUIRED` with target PS #171, exact pinned inputs, affected roles and paths, required validation, and expected terminal state.
6. Read back the comment, verify its exact body identity, and stop without starting Task 88.

### Validation and evidence

Require complete GitHub pagination, exact commit/tree/blob identities, each applicable catalog row exactly once, no unexplained difference, successful landed checks, closed #170 evidence, open and uncommenced #171 evidence, and an authenticated comparison-comment readback.

### Stop and escalation conditions

Stop if the comparison can emit `FIXED_POINT_READY`, targets Terraform, identifies a blocker outside #171, finds #170 incomplete, or finds changed identities or unfinished review. Record the exact result and run a new ordering decision; do not force the expected branch.

### Exact output

One pinned comparison record emits `REPAIR_REQUIRED` for PS #171 and proves that PS #170 is landed, reviewed, reciprocally safe, and no longer a blocker.

### Complete when

Authenticated readback proves the comparison record is complete and exact, #170 is reciprocally closed, and PS #171 is the only authorized next issue; no implementation has commenced.

## Task 88 — validate and update PS #171 for bounded field-specific generator diagnostics

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Replace each unresolved token globally with the exact value from its named source before execution.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by this task |
| Issue URL | `https://github.com/franklesniak/PSStyleGuide/issues/171` | Verified existing open issue; re-query before execution |
| Planning-baseline body | 5,171 UTF-8 bytes; SHA-256 `6a2e10e574348a283b60334b4b95ec14d03af71d937799e862460aabdfa9c9c1` | Authenticated read at the verified-state timestamp; current GitHub state controls |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 87 | `FS` | Use its current pinned comparison and exact target-issue disposition. |

### Objective

Validate the existing issue as the complete, self-contained authority for bounded field-specific generator diagnostics. Update only missing or stale issue metadata that current repository and GitHub evidence proves necessary. Do not commence implementation, edit a repository byte, create a branch or PR, post an implementation comment, merge, close the issue, or change a repository setting.

### Execution controls

1. Use authenticated structured GitHub reads and paginate the issue body edits, comments, timeline, parent, sub-issues, blocked-by, blocking, tracked issues, linked branches, and closing references.
2. Re-query both repository `main` refs, all open implementation issues and PRs, and the one-at-a-time implementation slot.
3. Preserve the existing issue body unless a specific statement is false, incomplete, or no longer self-contained. Make one exact issue-body update only when evidence requires it.
4. Add a native parent or dependency only when it is a real completion prerequisite. A cross-reference, tracker membership, shared file, or desired order is not a blocking edge.
5. Keep issue update and implementation commencement separate. This task must not create a feature branch, edit tracked bytes, push, open a PR, or post `IMPLEMENTATION COMMENCED`.
6. Apply the issue-specific scope below. Do not merge this issue with another issue's implementation authority.
7. Read back every authorized change and compare its exact body, relationships, state, and timestamps. Preserve failure truth and stop after this task.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 87 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. Capture the complete current issue and repository baseline.
2. Build a requirement-to-body matrix for every scope, exclusion, validation, evidence, relationship, terminal, and reopen condition above.
3. If the body is already complete and current, record `ISSUE_READY_UNCHANGED`. If it is not, validate the defect, use a finding-specific decision process, update only the issue body or real relationship, read it back, and record `ISSUE_READY_UPDATED`.
4. Confirm that no implementation commenced and that the implementation slot remains free.
5. Return the issue URL, node ID, state, body bytes and SHA-256, relationship inventory, cross-references, exact mutation or no-change result, and PS_171_ISSUE_READY. Stop before the commencement task.

### Validation and evidence

Require complete pagination with no unvisited page, an exact issue-body readback, no unresolved placeholder, correct native relationships, no closing PR, no implementation marker, and unchanged repository bytes and refs.

### Stop and escalation conditions

Stop if the issue is closed or superseded without proved terminal evidence, another implementation owns the slot, a requested relationship is not a real prerequisite, the current body conflicts with repository evidence, or an update would broaden the issue beyond its proved scope. Record the exact conflict and do not commence work.

### Exact output

One authenticated `PS_171_ISSUE_READY` record states `ISSUE_READY_UNCHANGED` or `ISSUE_READY_UPDATED` and contains the exact issue identity, body identity, relationship state, scope matrix, and proof that implementation did not commence.

### Complete when

The issue is open, self-contained, current, and ready for its separate commencement leaf; exact readback proves its body and real relationships; no implementation or repository mutation occurred.

## Task 89 — commence the PS #171 bounded diagnostic repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #171 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/171}` | Verified existing PS #171 issue; re-query before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 88 | `FS` | A focused issue exists, and the current issue-ready branch is active. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 87 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. Require Task 88 to return the issue-ready record. Stop if it did not.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, for the current issue-ready branch.

## Task 90 — implement the PS #171 bounded diagnostic repair

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #171 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/171}` | Verified existing PS #171 issue; re-query before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 89 | `FS` | The focused issue is commenced, and the current issue-ready branch is active. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 87 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. If Task 89 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, for the current issue-ready branch.

## Task 91 — create or update the PS #171 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS #171 issue URL | `{https://github.com/franklesniak/PSStyleGuide/issues/171}` | Verified existing PS #171 issue; re-query before execution |
| Task 91 PR URL | `{{TASK_091_PR_URL}}` | Produced by this task; replace {{TASK_091_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 90 | `FS` | Use the committed repair candidate, and the current issue-ready branch is active. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Issue-specific scope

- Evaluate result checks in fixed order: `NativeExit`, `Schema`, `GeneratorVersion`, `Overall`, `Phase`, `Category`, `NativeOutcome`, and `ResultExitCode`, when those fields remain in the landed schema. Accumulate every failure; do not stop after native exit or the first JSON field.
- Render null as `<null>`. Sanitize each actual value to printable ASCII, keep at most 64 sanitized characters, and add `...` only after truncation. Keep native process status separate from the generator result exit code.
- Preserve successful-result behavior, ordered artifact validation, and generated bytes. Bind the names and order, null marker, sanitization, bound, truncation marker, native/result distinction, accumulator, and deciding guard in the current PS policy authority with isolated negative cases and valid/hostile controls.
- Coordinate with open PS #162. Do not weaken its no-publication boundary. If actual values cannot safely appear in the current repository-code job, implement and document a stronger compatible channel that preserves named distinctions and fixed ordering without exposing checkout-controlled content.
- Re-read the canonical baseline produced by Task 87 and current #162 body before design. Run both parsers, PSScriptAnalyzer, policy preflight and all cases once, hostile and valid controls, the complete two-run three-host generator matrix, generated-drift and exact-scope checks, current-head CI, review, and landed handoff.

### Procedure

1. If Task 90 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, for the current issue-ready branch.

## Task 92 — run the Copilot-and-Codex review loop on the PS #171 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 91 PR URL | `{{TASK_091_PR_URL}}` | Produced by Task 91; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 91 | `FS` | The focused repair PR is review-ready, and the current issue-ready branch is active. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 91 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 91's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 92 for the new head before Task 93. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, for the current issue-ready branch.

## Task 93 — run the independent final quality check on the PS #171 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 91 PR URL | `{{TASK_091_PR_URL}}` | Produced by Task 91; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 92 | `FS` | A terminal-clean record applies to the current repair head/tree, and the current issue-ready branch is active. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 92 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 91's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 92 for the new head, and then repeat Task 93 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, for the current issue-ready branch.

## Task 94 — merge the PS #171 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 91 PR URL | `{{TASK_091_PR_URL}}` | Produced by Task 91; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 93 | `FS` | Both review gates apply to the same current head/tree, and the current issue-ready branch is active. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 93 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, for the current issue-ready branch.

## Task 95 — publish the landed handoff for PS #171

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 91 PR URL | `{{TASK_091_PR_URL}}` | Produced by Task 91; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 94 | `FS` | The repair PR is merged with an actual landed identity, and the current issue-ready branch is active. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 94 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, for the current issue-ready branch.

## Task 96 — perform the final reciprocal recheck for PS #169, #170, and #171

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 69 | `FS` | Use the original pinned cycle-2 comparison and immutable PS #161 and Terraform #34 inputs. |
| Task 78 | `FS` | PS #169 has its landed handoff and reciprocal comparison. |
| Task 87 | `FS` | PS #170 has its landed handoff and reciprocal comparison. |
| Task 95 | `FS` | PS #171 is closed by its reviewed merged PR and has an authenticated landed handoff. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat the applicable issue-through-handoff leaf range—Tasks 70–77 for #169, Tasks 79–86 for #170, or Tasks 88–95 for #171—as a new numbered-range instance; if Terraform is the target, create the equivalent eight discrete Terraform leaves before this recheck. After its new handoff, repeat Task 96.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 97 — publish the fixed-point closure for PS #169, #170, and #171

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 96 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Required issue closure evidence

- PS #169, #170, and #171 are each closed by their own reviewed and quality-checked merged PR.
- Each PR has an authenticated landed handoff and a comparison tied to its exact landed commit and tree.
- The final comparison includes every requirement from all three issue bodies, every current intentional difference, and every effect of PS #162's future no-publication boundary.
- A tracker record must not close a still-open implementation issue or convert a cross-reference into a false dependency.

### Procedure

1. Re-query the final identities and the Task 96 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 98 — commence PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 97 | `FS` | Cycle 2 has a permanent fixed-point record. |

### Objective

Explicitly commence only the implementation governed by implement PS workflow isolation and policy convergence. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final PS commit recorded by Task 97 is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 with Task 97's exact final PS commit before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 97 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from Task 97's final PS handoff before editing. Do not use the older PS #161 landing as the mutable editing baseline.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 99 — implement PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 98 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS workflow isolation and policy convergence without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final PS commit recorded by Task 97 is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 with Task 97's exact final PS commit before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 97 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from Task 97's final PS handoff before editing. Do not use the older PS #161 landing as the mutable editing baseline.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Work from the exact commenced baseline in an isolated branch or worktree.
2. Implement the complete task-local contract and preserve every stated scope boundary.
3. Run all task-specific local validation, commit the candidate, and record the commit, tree, paths, blobs, runtimes, exit codes, and results. Do not push or update a PR.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 100 — create or update the candidate PR for PS workflow isolation and policy convergence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 100 PR URL | `{{TASK_100_PR_URL}}` | Produced by this task; replace {{TASK_100_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 99 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS workflow isolation and policy convergence; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable inputs

- Pre-PR-#164 PS build: `208a8b492c1f32e02c0731449db95890b256fc51`.
- PS Markdown workflow: `4afa4f3aba03ffe3796ca72223cb0ebe26c8f34a`.
- Pre-PR-#164 PS policy validator: `00263451aa8287a36ad0694143d96741c4cdd1f9`.
- Pre-PR-#164 PS policy contract: `36374dbf88cb280126f780052e5d860ec96af9a7`.
- PS policy cases: `252423934501a1a5ffa7a9d33ea0f001b4068d11`.
- Terraform build: `f68dfe2da4842d2086b079e09328e3298856123e`.
- Terraform Markdown workflow: `6a64b2a3b5b737bb7f69dc971b1b21b298013ab0`.
- Terraform policy validator: `fb17e06f2f456a0d7df510b0bb34c52d88b7893b`.
- The final PS commit recorded by Task 97 is the editing baseline. Replace `<PS_GENERATOR_LANDED_COMMIT>` in PS #162 with Task 97's exact final PS commit before implementation. Re-read all overlapping paths from that commit.

### Required topology and policy

The landed #161 baseline already contains the v2 generator and exact-path verifier, the thin v2 `build.yml` caller, and their closed contract/validator identity integration. Treat those bytes as immutable starting state. Do not roll them back, relabel them as v1, or count that minimal integration as #162 implementation. This task owns only the broader #162 workflow topology, action-free repository-code boundary, Node acquisition, policy-case expansion, and isolation work stated below.

1. Repository-code jobs must declare `permissions: {}` and contain no `uses:` key.
2. Acquire the exact triggering commit anonymously through a fixed absolute Git executable. Fail if the public anonymous model is unavailable.
3. Prove a credential-free remote and configuration state before repository code runs.
4. In Node jobs, download the exact official Node distribution from a reviewed URL, verify its reviewed digest before extraction, and assert exact Node/npm identities.
5. Separate policy validation from Markdown lint and generator verification from artifact publication. Use different jobs and runners.
6. A publication job must run no repository code and use only reviewed, full-SHA actions with a closed input/default contract.
7. Do not publish repository-code job diagnostics. If bounded diagnostics remain necessary, use only commit-derived or independently produced evidence through a separate safe design.
8. Preserve unfiltered pull-request and push coverage. Keep every job read-only. P1B owns the first writer.
9. Retain `workflow-policy-contract.json` as the machine-readable authority and `workflow-policy-cases.json` as the external case catalog.
10. Port every applicable Terraform PR #26/#30 invariant and negative fixture: job isolation, action-role multiset, immutable acquisition, runner communication, credential absence, fixed tools, control-surface integrity, worktree containment, Node supply verification, strict YAML/JSON shapes, native statuses, terminal graph behavior, and publish-job restrictions.
11. Run policy parsing and all fixtures strictly offline. Do not reduce Terraform coverage to preserve PS's smaller case count. Do not copy Terraform repository literals or historical decision text.
12. Keep PS #151 separate unless that issue is expressly amended and all of its acceptance evidence is satisfied.

### Comparison, validation, and handoff

1. Complete the full foundation catalog against the final Task 97 state.
2. Add a topology table that lists every workflow, job, step, permission, condition, action, authored input, reviewed default, credential, repository-code execution point, produced byte set, consumed byte set, and side effect.
3. Treat an action or repository-token scope in a repository-code job, an unverified Node distribution, an unexplained missing Terraform negative case, or a weaker failure postcondition as a blocker.
4. Run all offline fixtures, both Markdown surfaces, generator drift, exact-path checks, clean installs, policy preflight and full validation, action/default provenance checks, and pull-request/push graph tests.
5. Record the final case count and prove that every case runs exactly once. Require all initial checks to pass at the candidate head.
6. Create or update one focused PS #162 PR. Record all source and candidate blobs, contract and catalog versions/digests, Node/npm/archive identities, action pins and manifest defaults, final topology, case allocation, runtime results, candidate head/tree, and intentional differences. Stop before merge.

- Verified PR #164 starting-state build blob: `284b08257a48aacc0fe7e474b315f9eb482e5c62`.
- Verified PR #164 starting-state policy-validator blob: `24e47314a62bb31cc6332cba6e56116efc7602bd`.
- Verified PR #164 starting-state policy-contract blob: `e1b991d3ba5233750cf91d422f1fa844bb7004f0`.
- Re-read every overlapping path from Task 97's final PS handoff before editing. Do not use the older PS #161 landing as the mutable editing baseline.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query the governing issue, remote branch, base branch, active implementation slot, and required checks.
2. Push the committed candidate safely. Create or update only one focused PR that links the issue and accurately describes the complete diff, evidence, security effects, intentional differences, rollback, and deferred-work disposition.
3. Read the PR back, record its base, head/tree, commits, paths and blobs, body edit state, and issue links. Require all initial checks to pass. Stop before review or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #162 candidate PR exists with complete initial validation and an accurate PR body.

## Task 101 — run the Copilot-and-Codex review loop on the PS #162 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 100 PR URL | `{{TASK_100_PR_URL}}` | Produced by Task 100; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 100 | `FS` | The PS #162 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #162 PR. Do not merge. If the loop changes bytes, rerun the Task 100 validation and continue on the new head.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 102 — run the independent final quality check on the PS #162 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 100 PR URL | `{{TASK_100_PR_URL}}` | Produced by Task 100; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 101 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #162 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below against the PS #162 PR. Require full issue-contract, workflow-topology, policy-case, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 101 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 101.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 101.

## Task 103 — merge the PS #162 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 100 PR URL | `{{TASK_100_PR_URL}}` | Produced by Task 100; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 102 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 104 — publish the landed handoff for the PS #162 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #162 URL | `https://github.com/franklesniak/PSStyleGuide/issues/162` | Verified existing GitHub issue |
| Task 100 PR URL | `{{TASK_100_PR_URL}}` | Produced by Task 100; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 103 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #162 is closed and the final landed policy contract is ready for Task 113.

### Complete when

PS #162 is closed and the final landed policy contract is ready for Task 113.

## Task 105 — compare cycle 3 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 104 | `FS` | Use the final landed PS #162 commit and policy contract. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 105 | Pin all Task 104 workflow, policy, contract, case, and validation blobs. |
| Task 105 | Complete the full foundation catalog and workflow-topology table in Terraform. |
| Task 105 | Apply all blockers from Task 104's required topology and policy. |
| Task 109 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 105 | Update PS #162, PS #159, and Terraform #31 with full identities and validation. |
| Task 105 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 105 | Map files by role, not only by filename. |
| Task 105 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 108 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 106 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 107 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 109 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 105 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 106 — create or update the focused repair issue for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 issue URL | `{{TASK_106_ISSUE_URL}}` | Produced by this task; replace {{TASK_106_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 105 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 105's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 107 — commence the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 issue URL | `{{TASK_106_ISSUE_URL}}` | Produced by Task 106; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 106 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 106 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 108 — implement the focused repair for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 issue URL | `{{TASK_106_ISSUE_URL}}` | Produced by Task 106; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 107 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 107 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 109 — create or update the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 106 issue URL | `{{TASK_106_ISSUE_URL}}` | Produced by Task 106; resolve globally before execution |
| Task 109 PR URL | `{{TASK_109_PR_URL}}` | Produced by this task; replace {{TASK_109_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 108 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 108 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 110 — run the Copilot-and-Codex review loop on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 109 PR URL | `{{TASK_109_PR_URL}}` | Produced by Task 109; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 109 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 109 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 109's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 110 for the new head before Task 111. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 111 — run the independent final quality check on the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 109 PR URL | `{{TASK_109_PR_URL}}` | Produced by Task 109; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 110 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 110 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 109's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 110 for the new head, and then repeat Task 111 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 112 — merge the focused repair PR for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 109 PR URL | `{{TASK_109_PR_URL}}` | Produced by Task 109; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 111 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 111 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 113 — publish the landed repair handoff for cycle 3 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 109 PR URL | `{{TASK_109_PR_URL}}` | Produced by Task 109; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 112 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 112 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 114 — reverse-compare cycle 3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 113 | `FS` | Use only the final landed commits from Tasks 104 and 73. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 114 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 114 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 115 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 116 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 118 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 123 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 117 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 124 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 124 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 115 — create or update the focused repair issue for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 115 issue URL | `{{TASK_115_ISSUE_URL}}` | Produced by this task; replace {{TASK_115_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 114 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 114's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 116 — commence the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 115 issue URL | `{{TASK_115_ISSUE_URL}}` | Produced by Task 115; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 115 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 115 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 117 — implement the focused repair for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 115 issue URL | `{{TASK_115_ISSUE_URL}}` | Produced by Task 115; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 116 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 116 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 118 — create or update the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 115 issue URL | `{{TASK_115_ISSUE_URL}}` | Produced by Task 115; resolve globally before execution |
| Task 118 PR URL | `{{TASK_118_PR_URL}}` | Produced by this task; replace {{TASK_118_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 117 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 117 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 119 — run the Copilot-and-Codex review loop on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 118 PR URL | `{{TASK_118_PR_URL}}` | Produced by Task 118; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 118 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 118 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 118's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 119 for the new head before Task 120. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 120 — run the independent final quality check on the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 118 PR URL | `{{TASK_118_PR_URL}}` | Produced by Task 118; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 119 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 119 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 118's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 119 for the new head, and then repeat Task 120 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 121 — merge the focused repair PR for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 118 PR URL | `{{TASK_118_PR_URL}}` | Produced by Task 118; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 120 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 120 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 122 — publish the landed repair handoff for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 118 PR URL | `{{TASK_118_PR_URL}}` | Produced by Task 118; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 121 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 121 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 123 — recheck the reciprocal result for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 114 | `FS` | Use the original pinned comparison and branch result. |
| Task 122 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 108–122 as a new numbered-range instance. After its new handoff, repeat Task 123.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 124 — publish the fixed-point closure for cycle 3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 123 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 123 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 125 — post the initial three-cycle handoff

> **Execution class: Coding agent executable.** This is an evidence-publication task, not an implementation task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 124 | `FS` | Cycles 1 through 3 have permanent fixed-point records. |

### Objective

Complete only this leaf action: post the initial three-cycle handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Post one interim handoff to PS #159 and Terraform #31. List cycles 1 through 3, every PS and Terraform issue and PR, bases, reviewed heads/trees, merge methods, landed commits/trees, source and destination blobs, matrices, validation, review outcomes, intentional differences, and fixed-point result. State that PS #158 remains nonblocking and has no native blocking edge to PS #147 or #152. Verify every identifier before posting. Do not close either tracker.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

### Complete when

Both trackers contain a permanent, internally consistent handoff for cycles 1 through 3.

## Task 126 — update PS #158 with the landed workflow-policy starting point

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 125 | `FS` | The final cycle 3 policy contract and interim handoff exist. |

### Objective

Update only the existing PS #158 issue record with the final landed workflow-policy starting point. Do not commence implementation.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query PS #158, its parent #159, the landed workflow-policy PR and commit, and both repository main refs.
2. Replace `<PS_WORKFLOW_POLICY_PR_URL>` and `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` in the task-local text below with the exact predecessor outputs.
3. Post the text as a planning comment without replacing the issue body. Read the comment back and verify both identities and the nonblocking scope.
4. Do not create a feature branch, edit implementation files, open a PR, or occupy the implementation slot.

### Exact issue update

Before implementation, replace both placeholders and post this planning comment to PS #158 without replacing its body:

> This issue is a child of [PS issue #159](https://github.com/franklesniak/PSStyleGuide/issues/159) and starts after `<PS_WORKFLOW_POLICY_PR_URL>` lands. Re-read the final landed `workflow-policy-contract.json` from `<PS_WORKFLOW_POLICY_LANDED_COMMIT>` before adapting the recorder. Preserve this issue's existing nonblocking scope and immutable Terraform PR #27 source pins.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #158 contains the verified starting-point comment, and implementation has not started.

### Complete when

PS #158 contains the verified starting-point comment, and implementation has not started.

## Task 127 — commence the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 126 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement the PS read-only supply-freeze recorder. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 128 — implement the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 127 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS read-only supply-freeze recorder without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-read the final Task 124 policy contract and the immutable Terraform PR #27 inputs.
2. Preserve the issue's existing nonblocking scope.
3. Keep the recorder read-only and capture its output outside the repository.
4. Prove that it does not mutate a manifest, lockfile, installed tree, contract, generated output, staged set, or worktree.
5. Complete a documented reduced reciprocal matrix for the narrow recorder surface. Include every applicable foundation row and prove why omitted rows are inapplicable.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 129 — create or update the candidate PR for the PS read-only supply-freeze recorder

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 129 PR URL | `{{TASK_129_PR_URL}}` | Produced by this task; replace {{TASK_129_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 128 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS read-only supply-freeze recorder; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #158 PR. Run initial validation and required checks.
2. Record the candidate head/tree, all source and candidate blobs, tool/runtime identities, exact output location, mutation-absence evidence, validation, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #158 candidate PR exists with complete initial validation and an accurate PR body.

## Task 130 — run the Copilot-and-Codex review loop on the PS #158 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 129 PR URL | `{{TASK_129_PR_URL}}` | Produced by Task 129; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 129 | `FS` | The PS #158 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #158 PR. Do not merge. If the loop changes bytes, rerun the Task 129 validation.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 131 — run the independent final quality check on the PS #158 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 129 PR URL | `{{TASK_129_PR_URL}}` | Produced by Task 129; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 130 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #158 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #158 issue coverage, immutable-source verification, read-only and mutation-absence proof, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 130 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 130.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 130.

## Task 132 — merge the PS #158 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 129 PR URL | `{{TASK_129_PR_URL}}` | Produced by Task 129; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 131 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 133 — publish the landed handoff for the PS #158 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #158 URL | `https://github.com/franklesniak/PSStyleGuide/issues/158` | Verified existing GitHub issue |
| Task 129 PR URL | `{{TASK_129_PR_URL}}` | Produced by Task 129; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 132 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #158 is closed and its permanent handoff is ready for Terraform.

### Complete when

PS #158 is closed and its permanent handoff is ready for Terraform.

## Task 134 — compare cycle 4 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 133 | `FS` | Use the final landed PS #158 recorder and handoff. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 134 | Pin the PS source and landed commits and blobs and the immutable Terraform PR #27 inputs. |
| Task 134 | Compare the read-only recorder roles with a documented reduced matrix. |
| Task 137 | Prove that both implementations preserve repository state and capture output outside the repository. |
| Task 138 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 134 | Update PS #158, PS #159, and Terraform #31 with all identities, validation, and differences. |
| Task 134 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 134 | Map files by role, not only by filename. |
| Task 134 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 137 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 135 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 136 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 138 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 134 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 135 — create or update the focused repair issue for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 issue URL | `{{TASK_135_ISSUE_URL}}` | Produced by this task; replace {{TASK_135_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 134 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 134's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 136 — commence the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 issue URL | `{{TASK_135_ISSUE_URL}}` | Produced by Task 135; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 135 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 135 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 137 — implement the focused repair for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 issue URL | `{{TASK_135_ISSUE_URL}}` | Produced by Task 135; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 136 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 136 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 138 — create or update the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 135 issue URL | `{{TASK_135_ISSUE_URL}}` | Produced by Task 135; resolve globally before execution |
| Task 138 PR URL | `{{TASK_138_PR_URL}}` | Produced by this task; replace {{TASK_138_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 137 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 137 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 139 — run the Copilot-and-Codex review loop on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 138 PR URL | `{{TASK_138_PR_URL}}` | Produced by Task 138; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 138 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 138 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 138's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 139 for the new head before Task 140. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 140 — run the independent final quality check on the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 138 PR URL | `{{TASK_138_PR_URL}}` | Produced by Task 138; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 139 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 139 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 138's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 139 for the new head, and then repeat Task 140 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 141 — merge the focused repair PR for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 138 PR URL | `{{TASK_138_PR_URL}}` | Produced by Task 138; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 140 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 140 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 142 — publish the landed repair handoff for cycle 4 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 138 PR URL | `{{TASK_138_PR_URL}}` | Produced by Task 138; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 141 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 141 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 143 — reverse-compare cycle 4 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 142 | `FS` | Use only the final landed commits from Tasks 133 and 102. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 143 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 143 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 144 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 145 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 147 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 152 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 146 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 153 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 153 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 144 — create or update the focused repair issue for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 144 issue URL | `{{TASK_144_ISSUE_URL}}` | Produced by this task; replace {{TASK_144_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 143 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 143's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 145 — commence the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 144 issue URL | `{{TASK_144_ISSUE_URL}}` | Produced by Task 144; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 144 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 144 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 146 — implement the focused repair for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 144 issue URL | `{{TASK_144_ISSUE_URL}}` | Produced by Task 144; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 145 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 145 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 147 — create or update the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 144 issue URL | `{{TASK_144_ISSUE_URL}}` | Produced by Task 144; resolve globally before execution |
| Task 147 PR URL | `{{TASK_147_PR_URL}}` | Produced by this task; replace {{TASK_147_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 146 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 146 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 148 — run the Copilot-and-Codex review loop on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 147 PR URL | `{{TASK_147_PR_URL}}` | Produced by Task 147; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 147 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 147 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 147's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 148 for the new head before Task 149. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 149 — run the independent final quality check on the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 147 PR URL | `{{TASK_147_PR_URL}}` | Produced by Task 147; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 148 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 148 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 147's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 148 for the new head, and then repeat Task 149 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 150 — merge the focused repair PR for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 147 PR URL | `{{TASK_147_PR_URL}}` | Produced by Task 147; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 149 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 149 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 151 — publish the landed repair handoff for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 147 PR URL | `{{TASK_147_PR_URL}}` | Produced by Task 147; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 150 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 150 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 152 — recheck the reciprocal result for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 143 | `FS` | Use the original pinned comparison and branch result. |
| Task 151 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 137–151 as a new numbered-range instance. After its new handoff, repeat Task 152.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 153 — publish the fixed-point closure for cycle 4 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 152 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 152 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 154 — close the PS foundation umbrella

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #159 URL | `https://github.com/franklesniak/PSStyleGuide/issues/159` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 153 | `FS` | PS #160, #161, #162, and #158 and all four reciprocal cycles are complete. |

### Objective

Complete only this leaf action: close the PS foundation umbrella. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query PS #159 and its four native children.
2. Verify that #160, #161, #162, and #158 are closed and that every cycle has a permanent fixed-point record.
3. Post the foundation handoff. Include every issue and PR URL, base, reviewed head/tree, merge method, landed commit/tree, affected path and blob, complete or reduced matrix, validation command and result, runtime identity, review outcome, intentional difference, and reciprocal closure result.
4. Verify that Terraform #31 links the same four cycle records.
5. Close PS #159. Do not create or merge an aggregate foundation PR.

### Validation and evidence

### Stop and escalation conditions

### Exact output

PS #159 is closed with complete, matching evidence in both repositories.

### Complete when

PS #159 is closed with complete, matching evidence in both repositories.

## Task 155 — commence the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 154 | `FS` | The foundation umbrella is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement the PS Claude review-loop command. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Procedure

1. Re-query the governing issue, every native dependency, both current main refs, and the active implementation-PR inventory.
2. Verify the one-at-a-time implementation slot is free and all immutable inputs are reachable.
3. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and predecessor handoff. Do not edit or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 156 — implement the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 155 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement the PS Claude review-loop command without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Reconfirm the existing owner authorization because the new file is an agent entry point.
2. Add a thin, repository-local command wrapper.
3. Accept a pull-request URL. Require the URL when the caller does not supply it.
4. Delegate the authoritative process to the repository-local root `CLAUDE.md`.
5. Treat Codex and Copilot as co-equal reviewers, as `CLAUDE.md` requires.
6. Do not duplicate a step count, gate count, round limit, or other volatile protocol detail.
7. Do not fetch instructions from a moving external branch or add a shared runtime dependency.
8. Record a line-by-line comparison with the pinned Terraform input. Explain each intentional difference.
9. Complete a documented reduced reciprocal matrix. Run Markdown lint and all applicable local agent-instruction checks.
10. Keep the issue and PR separate from foundation, supply-freeze, and infrastructure-conformance work.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 157 — create or update the candidate PR for the PS Claude review-loop command

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 157 PR URL | `{{TASK_157_PR_URL}}` | Produced by this task; replace {{TASK_157_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 156 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement the PS Claude review-loop command; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable design input

Read TerraformStyleGuide `.claude/commands/review-loop.md` from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`, blob `7b0e41361a8ab7259245ad5f0d86d9300008347d`. Do not copy it unchanged: it says `all six steps`, but PSStyleGuide's landed `CLAUDE.md` defines nine review-comment handling steps.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Create or update one focused PS #163 PR. Record the candidate head/tree, pinned input, line comparison, validation, and initial checks. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

### Complete when

The PS #163 candidate PR exists with complete initial validation and an accurate PR body.

## Task 158 — run the Copilot-and-Codex review loop on the PS #163 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 157 PR URL | `{{TASK_157_PR_URL}}` | Produced by Task 157; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 157 | `FS` | The PS #163 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #163 PR. Do not merge. If the loop changes bytes, rerun the Task 157 validation.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 159 — run the independent final quality check on the PS #163 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 157 PR URL | `{{TASK_157_PR_URL}}` | Produced by Task 157; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 158 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #163 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require PS #163 issue coverage, protected-entry-point authorization, pinned-input comparison, absence of volatile protocol duplication, comments, deferrals, PR-body accuracy, validation, and mergeability. Return to Task 158 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 158.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 158.

## Task 160 — merge the PS #163 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 157 PR URL | `{{TASK_157_PR_URL}}` | Produced by Task 157; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 159 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 161 — publish the landed handoff for the PS #163 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #163 URL | `https://github.com/franklesniak/PSStyleGuide/issues/163` | Verified existing GitHub issue |
| Task 157 PR URL | `{{TASK_157_PR_URL}}` | Produced by Task 157; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 160 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

### Complete when

PS #163 is closed and its permanent handoff is ready for Terraform comparison.

## Task 162 — compare cycle 5 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 161 | `FS` | Use the final landed PS #163 command. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 162 | Pin the PS command and authoritative `CLAUDE.md` blobs and the immutable Terraform command blob. |
| Task 162 | Compare both thin wrappers line by line with a documented reduced matrix. |
| Task 162 | Preserve each repository's authoritative local instruction file and repository identity. |
| Task 166 | Post a pinned no-change record or prepare one focused Terraform repair and run the four task-local conditional repair PR lifecycle actions. |
| Task 162 | Update PS #163 and Terraform #31 with all identities, validation, and intentional differences. |
| Task 162 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 162 | Map files by role, not only by filename. |
| Task 162 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 165 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 163 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 164 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 166 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 162 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 163 — create or update the focused repair issue for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 issue URL | `{{TASK_163_ISSUE_URL}}` | Produced by this task; replace {{TASK_163_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 162 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 162's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 164 — commence the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 issue URL | `{{TASK_163_ISSUE_URL}}` | Produced by Task 163; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 163 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 163 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 165 — implement the focused repair for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 issue URL | `{{TASK_163_ISSUE_URL}}` | Produced by Task 163; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 164 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 164 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 166 — create or update the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 163 issue URL | `{{TASK_163_ISSUE_URL}}` | Produced by Task 163; resolve globally before execution |
| Task 166 PR URL | `{{TASK_166_PR_URL}}` | Produced by this task; replace {{TASK_166_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 165 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 165 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 167 — run the Copilot-and-Codex review loop on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 166 PR URL | `{{TASK_166_PR_URL}}` | Produced by Task 166; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 166 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 166 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 166's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 167 for the new head before Task 168. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 168 — run the independent final quality check on the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 166 PR URL | `{{TASK_166_PR_URL}}` | Produced by Task 166; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 167 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 167 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 166's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 167 for the new head, and then repeat Task 168 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 169 — merge the focused repair PR for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 166 PR URL | `{{TASK_166_PR_URL}}` | Produced by Task 166; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 168 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 168 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 170 — publish the landed repair handoff for cycle 5 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 166 PR URL | `{{TASK_166_PR_URL}}` | Produced by Task 166; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 169 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 169 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 171 — reverse-compare cycle 5 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 170 | `FS` | Use only the final landed commits from Tasks 161 and 130. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 171 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 171 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 172 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 173 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 175 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 180 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 174 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 181 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 181 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 172 — create or update the focused repair issue for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by this task; replace {{TASK_172_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 171 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 171's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 173 — commence the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by Task 172; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 172 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 172 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 174 — implement the focused repair for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by Task 172; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 173 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 173 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 175 — create or update the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 172 issue URL | `{{TASK_172_ISSUE_URL}}` | Produced by Task 172; resolve globally before execution |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by this task; replace {{TASK_175_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 174 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 174 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 176 — run the Copilot-and-Codex review loop on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 175 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 175 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 175's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 176 for the new head before Task 177. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 177 — run the independent final quality check on the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 176 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 176 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 175's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 176 for the new head, and then repeat Task 177 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 178 — merge the focused repair PR for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 177 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 177 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 179 — publish the landed repair handoff for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 175 PR URL | `{{TASK_175_PR_URL}}` | Produced by Task 175; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 178 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 178 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 180 — recheck the reciprocal result for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 171 | `FS` | Use the original pinned comparison and branch result. |
| Task 179 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 165–179 as a new numbered-range instance. After its new handoff, repeat Task 180.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 181 — publish the fixed-point closure for cycle 5 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 180 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 180 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 182 — close the Terraform initial-convergence tracker

> **Execution class: Coding agent executable.** This is an issue-evidence and closure task, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #31 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/31` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 181 | `FS` | All five initial reciprocal cycles have permanent closure records. |

### Objective

Complete only this leaf action: close the Terraform initial-convergence tracker. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Tracker contract

Terraform #31 retains evidence; it does not authorize an aggregate implementation PR. Its five ordered records are:

1. PS infrastructure conformance from Terraform PR #30.
2. PS generator and path convergence from Terraform PRs #26 and #30.
3. PS workflow and policy convergence from Terraform PRs #26 and #30.
4. PS #158 supply-freeze adaptation from Terraform PR #27.
5. PS Claude-command parity from commit `fbdecbae787055a2117d4ada83ae294a7decfe62`.

### Procedure

1. Re-query Terraform #31.
2. Verify that each cycle record contains the PS issue and landed PR; source and destination commits and blobs; a complete foundation matrix or justified reduced matrix; every focused Terraform sync issue and PR; the reverse PS comparison; validation and intentional differences; and the permanent fixed-point result.
3. Reconcile the tracker against PS #159 and PS #163. Correct a record only with verified evidence.
4. Close Terraform #31. Do not create or merge an aggregate foundation implementation PR.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

### Complete when

Terraform #31 is closed and its five records match the permanent records in PSStyleGuide.

## Task 183 — commence Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 182 | `FS` | The five initial cycles are closed and the final Terraform baseline is known. |

Terraform #21 exists, but its creation did not start implementation. Do not start Terraform #22 or #24 in this task.

### Objective

Explicitly commence only the implementation governed by implement Terraform #21 from the landed PS P1A source. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 182 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Verify the five Task 182 closure records and the free implementation slot.
2. Explicitly commence only Terraform #21.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 184 — implement Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 183 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #21 from the landed PS P1A source without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 182 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Re-query Terraform #21. Record its four source blobs, four destination paths, source and destination identities, and expected repository-specific differences.
2. Classify the capability as common.
3. Adapt the landed PS implementation. Do not redesign it independently or introduce a shared dependency.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 185 — create or update the candidate PR for Terraform #21 from the landed PS P1A source

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by this task; replace {{TASK_185_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 184 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #21 from the landed PS P1A source; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Immutable source and scope

- PS source issue and PR: [PS #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153).
- PS landed commit: `2d56357d9f52c76734027174bf62278e6f3d4cd6`.
- PS landed tree: `46f5f4e8627eb341ddc4ef0c8d52483dc4006b50`.
- Terraform #21 must retain its four pinned PS source blobs and exactly four affected paths.
- Preserve the 115-row PS allocation and 141-row Terraform allocation.
- Do not copy `CLAUDE.md`.
- Rebase only Terraform destination assumptions on the final Task 182 Terraform baseline. Do not change the immutable PS source pin.

### Procedure

1. Create or update one focused Terraform #21 PR. Run initial local validation and required checks.
2. Record its base, candidate head/tree, affected paths and destination blobs, validation, runtime identities, and issue links. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

### Complete when

The Terraform #21 candidate PR exists with complete initial validation and an accurate PR body. Keep #21 open.

## Task 186 — run the Copilot-and-Codex review loop on the Terraform #21 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 185 | `FS` | The Terraform #21 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below in TerraformStyleGuide against the Terraform #21 PR. Apply that repository's current root `AGENTS.md` instructions, or read its root `CLAUDE.md` as compatibility workflow instructions when `AGENTS.md` is absent. Do not merge. If the loop changes bytes, rerun the Task 185 validation.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 187 — run the independent final quality check on the Terraform #21 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 186 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #21 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require all Terraform #21 requirements, four-path scope, 115/141-row allocation, comments, deferrals, PR-body accuracy, validation, checks, and mergeability. Return to Task 186 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 186.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 186.

## Task 188 — merge the Terraform #21 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 187 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 189 — publish the landed handoff for the Terraform #21 implementation

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 185 PR URL | `{{TASK_185_PR_URL}}` | Produced by Task 185; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 188 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

### Complete when

The Terraform #21 implementation is landed and its permanent implementation handoff contains every required identity and evidence item.

## Task 190 — reverse-compare Terraform #21 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 189 | `FS` | Use the landed Terraform #21 result and immutable PS P1A commit. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 190 | Replace every placeholder in the reverse-comparison record with a verified identity. |
| Task 190 | Read PS only from `2d56357d9f52c76734027174bf62278e6f3d4cd6` and Terraform only from the Task 189 landed commit. Record every compared blob and map files by role. |
| Task 190 | Complete every applicable foundation row and the exact four-file mapping. Preserve the stated 115-row PS and 141-row Terraform allocations. |
| Task 190 | If no common difference remains, make no PS change and post a pinned reverse-comparison record. |
| Task 191 | If Terraform added or corrected common behavior, create or update one focused PS sync-back issue. Pin both landed commits and blobs. |
| Task 192 | Start the sync-back only after issue setup and a free implementation slot. Prepare one focused PS PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 190 | If PS changes, compare its new landed commit in Terraform. For a required Terraform repair, run the same four task-local conditional repair PR lifecycle actions. |
| Task 190 | Stop for a new decision process if a row changes direction twice. |
| Task 190 | Post the current reciprocal evidence on Terraform #21 and the PS P1A handoff. Do not close Terraform #21 until Task 209 confirms the final Terraform state. |
| Task 190 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 190 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 191 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 192 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 194 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 190 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 193 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 190 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 190 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 191 — create or update the focused repair issue for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 190 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 190's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 192 — commence the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 191 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 191 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 193 — implement the focused repair for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 192 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 192 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 194 — create or update the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by this task; replace {{TASK_194_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 193 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 193 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 195 — run the Copilot-and-Codex review loop on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 194 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 194 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 194's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 195 for the new head before Task 196. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 196 — run the independent final quality check on the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 195 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 195 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 194's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 195 for the new head, and then repeat Task 196 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 197 — merge the focused repair PR for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 196 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 196 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 198 — publish the landed repair handoff for Terraform #21 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #21 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/21` | Verified existing GitHub issue |
| Task 194 PR URL | `{{TASK_194_PR_URL}}` | Produced by Task 194; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 197 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 197 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 199 — perform the final Terraform recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 198 | `FS` | Use the final landed PS state from Task 198. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 199 | Compare the final Task 198 PS landed commit against Terraform. |
| Task 200 | If a common difference remains, create a focused Terraform issue before implementation. Use the task-local execution controls, conditional repair PR lifecycle, and final reciprocal-recheck controls below until the row is `same` or a proved intentional difference. |
| Task 199 | Verify that every common row is `same`, every intentional difference has complete evidence, and no row is a blocker. |
| Task 209 | Post one permanent closure record on Terraform #21 and the PS P1A handoff. Include both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, affected paths, source and destination blobs, final matrix digest, validation, runtime identities, review outcomes, and explicit non-applicability results. |
| Task 199 | Close Terraform #21. |
| Task 199 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 199 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 200 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 202 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 209 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 209 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 200 — create or update the focused repair issue for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 200 issue URL | `{{TASK_200_ISSUE_URL}}` | Produced by this task; replace {{TASK_200_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 199 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 199's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 201 — commence the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 200 issue URL | `{{TASK_200_ISSUE_URL}}` | Produced by Task 200; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 200 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 200 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 202 — implement the focused repair for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 200 issue URL | `{{TASK_200_ISSUE_URL}}` | Produced by Task 200; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 201 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 201 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 203 — create or update the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 200 issue URL | `{{TASK_200_ISSUE_URL}}` | Produced by Task 200; resolve globally before execution |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by this task; replace {{TASK_203_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 202 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 202 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 204 — run the Copilot-and-Codex review loop on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 203 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 203 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 203's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 204 for the new head before Task 205. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 205 — run the independent final quality check on the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 204 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 204 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 203's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 204 for the new head, and then repeat Task 205 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 206 — merge the focused repair PR for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 205 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 205 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 207 — publish the landed repair handoff for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 203 PR URL | `{{TASK_203_PR_URL}}` | Produced by Task 203; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 206 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 206 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 208 — recheck the reciprocal result for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 199 | `FS` | Use the original pinned comparison and branch result. |
| Task 207 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 193–207 as a new numbered-range instance. After its new handoff, repeat Task 208.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 209 — publish the fixed-point closure for the final Terraform recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 208 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 208 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 210 — inventory all untracked cross-repository differences

> **Execution class: Coding agent executable.** This is read-only analysis and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 209 | `FS` | The P1A/Terraform #21 reciprocal cycle is closed. |

### Objective

Complete only this leaf action: inventory all untracked cross-repository differences. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch both current `main` commits and trees in clean checkouts. Pin them as the sweep baselines.
2. Inventory every path that controls generation, validation, workflow policy, Actions permissions and pins, Node/npm acquisition, Markdown lint, Git/path checks, supply-freeze evidence, Dependabot, agent review commands, issue handoffs, and contributor instructions.
3. Map files by role. Do not infer role equality or difference from filenames alone.
4. For every common role, compare bytes and semantics, success and negative cases, failure classification, cleanup, credentials, permissions, native statuses, platform coverage, evidence retention, documentation, and validation commands.
5. Run the full foundation catalog for foundation roles. Use a documented reduced matrix only for a narrow non-foundation role.
6. Classify each row as `same`, `intentional difference`, `tracked work`, or `untracked blocker`.
7. Accept an intentional difference only under the task-local intentional-difference evidence requirements below. Link the owning issue for every `tracked work` row.
8. Treat a missing, duplicate, unknown, renamed, empty, or unexplained row as an `untracked blocker`.
9. For each untracked blocker, validate the finding and complete the required finding-specific decision process before proposing a repair.
10. Publish a read-only inventory that identifies the behind repository, affected role and blobs, evidence, owner/disposition, and proposed focused issue for each blocker. Do not edit implementation files in this task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

### Complete when

Every inventoried role has one supported classification and owner. The report contains both baseline identities, file/blob maps, matrices and digests, evidence, and the exact ordered list of untracked blockers.

## Task 211 — assess one untracked-blocker reciprocal cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 210 | `FS` | The complete read-only inventory and ordered blocker list exist. |
| Prior completed instance of Tasks 204–221 | `FS` | Close the prior repair cycle before the next instance starts. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 211 | Select the next untracked blocker in the published order. |
| Task 212 | Create or update one focused issue in the behind repository. Pin the other repository's landed commit and source blobs. |
| Task 213 | Issue creation does not start implementation. Start only after issue setup and a free implementation slot. |
| Task 211 | Keep both repositories self-contained. Do not combine unrelated fixes or change settings without exact authorization. |
| Task 215 | Prepare one focused repair PR. Run the candidate, Copilot-and-Codex review-loop, independent final quality-check, and merge actions as discrete lifecycle steps. |
| Task 220 | Compare the landed repair in the other repository. Post a pinned no-change record or run the same four discrete steps for one focused reciprocal repair. |
| Task 211 | Run the reverse comparison. Stop for a new decision process if a row changes direction twice. |
| Task 221 | Close the cycle only when every common row is `same`, all intentional differences have complete evidence, and no blocker remains. |
| Task 221 | Post the permanent closure record with both repositories' final issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, matrix digest, validation, runtimes, review outcomes, and non-applicability results. |
| Task 211 | Repeat Task 221 only if another ordered blocker remains. |
| Task 211 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 211 | Map files by role, not only by filename. |
| Task 211 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 214 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 212 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 213 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 215 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 220 | Compare the landed destination result back against the source repository. |
| Task 212 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 215 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 214 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 221 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 221 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 212 — create or update the focused repair issue for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 212 issue URL | `{{TASK_212_ISSUE_URL}}` | Produced by this task; replace {{TASK_212_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 211 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 211's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 213 — commence the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 212 issue URL | `{{TASK_212_ISSUE_URL}}` | Produced by Task 212; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 212 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 212 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 214 — implement the focused repair for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 212 issue URL | `{{TASK_212_ISSUE_URL}}` | Produced by Task 212; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 213 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 213 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 215 — create or update the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 212 issue URL | `{{TASK_212_ISSUE_URL}}` | Produced by Task 212; resolve globally before execution |
| Task 215 PR URL | `{{TASK_215_PR_URL}}` | Produced by this task; replace {{TASK_215_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 214 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 214 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 216 — run the Copilot-and-Codex review loop on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 215 PR URL | `{{TASK_215_PR_URL}}` | Produced by Task 215; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 215 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 215 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 215's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 216 for the new head before Task 217. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 217 — run the independent final quality check on the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 215 PR URL | `{{TASK_215_PR_URL}}` | Produced by Task 215; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 216 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 216 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 215's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 216 for the new head, and then repeat Task 217 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 218 — merge the focused repair PR for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 215 PR URL | `{{TASK_215_PR_URL}}` | Produced by Task 215; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 217 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 217 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 219 — publish the landed repair handoff for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_211_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 211; resolve globally before execution |
| Task 215 PR URL | `{{TASK_215_PR_URL}}` | Produced by Task 215; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 218 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 218 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 220 — recheck the reciprocal result for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 211 | `FS` | Use the original pinned comparison and branch result. |
| Task 219 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 205–219 as a new numbered-range instance. After its new handoff, repeat Task 220.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 221 — publish the fixed-point closure for one untracked-blocker reciprocal cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 220 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 220 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 222 — finalize the global consistency sweep

> **Execution class: Coding agent executable.** This is a verification and evidence-publication task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 210 | `FS` | The inventory exists. |
| Every applicable Task 221 instance | `FS` | All untracked blocker cycles are closed. |

### Objective

Complete only this leaf action: finalize the global consistency sweep. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch both final `main` commits and trees.
2. Re-run the complete inventory, file-role map, foundation catalog, and every required reduced matrix.
3. Verify that every role has an owner and disposition, each `tracked work` row links its live planned issue, and no `untracked blocker` remains.
4. Post the final sweep record. Include final commits/trees, file/blob map, matrices and digests, validation, runtimes, intentional differences, explicit non-applicability results, and zero untracked blockers.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Validation and evidence

### Stop and escalation conditions

### Exact output

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

### Complete when

The permanent global-sweep record proves zero untracked blockers and identifies every remaining planned work item.

## Task 223 — commence the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 222 | `FS` | The global sweep proves zero untracked blockers. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P1B writer candidate and decision evidence. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Verify that the implementation slot is free. Explicitly start only #147.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 224 — prepare the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 223 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P1B writer candidate and decision evidence without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-read the final PS foundation handoff and every landed contract.
2. Reconcile each PS #147 assumption with those contracts.
3. Implement the writer and terminal check `Build Style Guide Artifacts / approve_candidate`.
4. Prepare an amendment or replacement for `docs/decisions/0001-accept-in-repository-trust-root.md`.
5. Record workflow identity, permissions, required-check name, unique evidence ref, source pins, and rollback inputs.
6. Prepare the exact temporary and persistent settings requests and proof plan for PS #152.
7. Do not give the writer effective `contents: write` access before Task 229 records human approval.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 225 — create or update the candidate PR for the PS P1B writer candidate and decision evidence

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 225 PR URL | `{{TASK_225_PR_URL}}` | Produced by this task; replace {{TASK_225_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 224 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P1B writer candidate and decision evidence; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Create or update one PS #147 PR. Reach one locally validated candidate head. Record its exact head and tree.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

### Complete when

One locally validated P1B candidate and the complete decision/settings evidence package are ready. Keep the PR unmerged.

## Task 226 — prepare read-only PS #152 settings evidence

> **Execution class: Coding agent executable.** This is read-only preparation and starts after Task 225 completes.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 225 | `FS` | Begin after the candidate PR is complete and its exact identity is available in the Task 225 completion record. |

### Objective

Complete only this leaf action: prepare read-only PS #152 settings evidence. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query PS #152 and current rules, classic branch protection, `main`, application identity, and writer permissions.
2. Preserve the Terraform PR #29 bypass-eligibility finding and the limited 2026-08-01 approval scope.
3. Map every requested temporary and persistent setting to the Task 225 candidate.
4. Prepare the non-mutating bypass-eligibility check, field-equivalent temporary-rule request, effective-bypass and rejection drills, evidence-retention plan, cleanup proof, exact-restoration proof, persistent-ruleset request, and rollback plan.
5. Identify every decision that needs owner or administrator approval. Do not infer approval from prior evidence.
6. Do not change a setting, create a rule, or give the writer effective write access.

### Validation and evidence

### Stop and escalation conditions

### Exact output

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

### Complete when

PS #152 contains a complete, current, read-only evidence package that an owner can approve or reject without guessing.

## Task 227 — run the Copilot-and-Codex review loop on the PS #147 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |
| Task 225 PR URL | `{{TASK_225_PR_URL}}` | Produced by Task 225; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 225 | `FS` | The PS #147 candidate PR exists and initial checks pass. |
| Task 226 | `FS` | The read-only PS #152 evidence package is complete. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #147 PR. Do not merge or change repository settings. If the loop changes bytes or the writer design, rerun the applicable Task 225 validation and refresh Task 226 settings evidence before the next round.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree, and the PS #152 evidence still maps to that exact candidate.

## Task 228 — run the independent final quality check on the PS #147 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |
| Task 225 PR URL | `{{TASK_225_PR_URL}}` | Produced by Task 225; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 227 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #147 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require full PS #147 issue coverage; writer, evidence, lease/refspec, credential, terminal-result, rollback, comment, deferral, PR-body, validation, and mergeability checks; and consistency with the PS #152 settings request. Return to Task 227 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 227, and the settings evidence still applies.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 227, and the settings evidence still applies.

## Task 229 — approve or reject the P1B authority decisions

> **Execution class: Human execution required.** The repository owner or administrator must decide each item.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 226 | `FS` | Review the exact settings requests and proof plan. |
| Task 228 | `FS` | Review the exact candidate bytes and independent quality-check result. |

### Objective

Complete only this leaf action: approve or reject the P1B authority decisions. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

**Human actions:**

1. Review and approve or reject the proposed trust-root decision.
2. Review and approve or reject each temporary and persistent PS #152 settings request.
3. If integration ID `15368` is unavailable, select and approve another writer design.
4. Do not approve a substitute rule without also approving the writer design.
5. If the design permits human bypass, approve or reject that bypass in a separate decision.
6. Record every approval or rejection, condition, owner, canonical location, and expiry.
7. Do not reuse an approval for different bytes or settings.

### Validation and evidence

### Stop and escalation conditions

### Exact output

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 225 or Task 226 before work continues. After a candidate-byte change, rerun Tasks 227 and 185.

### Complete when

The canonical records contain an explicit disposition for every required decision. A rejection sends the affected design back to Task 225 or Task 226 before work continues. After a candidate-byte change, rerun Tasks 227 and 185.

## Task 230 — prove and activate the approved PS #152 settings

> **Execution class: Coding agent executable.** Recorded human approval is a required input. Do not merge PS #147 in this task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| PS issue #152 URL | `https://github.com/franklesniak/PSStyleGuide/issues/152` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 229 | `FS` | Every applicable approval exists and applies to the current candidate and exact settings. |

### Objective

Complete only this leaf action: prove and activate the approved PS #152 settings. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Verify all approvals. Stop if one is absent, expired, conditional on unmet work, or for different bytes or settings.
2. Revalidate the reviewed PS #147 head.
3. Start the bounded temporary proof under PS #152.
4. Export current rules, classic protection, `main`, application identity, and writer permissions.
5. Perform the non-mutating bypass-eligibility check.
6. If integration ID `15368` is unavailable, stop and return to Task 229.
7. Create the approved temporary field-equivalent rule.
8. Run the effective-bypass and rejection drills. Retain the required evidence.
9. Delete the temporary rule and evidence ref. Prove exact restoration.
10. Revalidate the reviewed PS #147 head.
11. As the final settings action, create the approved persistent ruleset immediately before Task 232.
12. Retain the create response, ruleset ID, normalized JSON, digest, effective rules, exact required check and integration, `main` before and after the setting change, and rollback proof.
13. Close PS #152 only after the persistent rule is active and evidence is complete.
14. Do not copy Terraform ruleset IDs, digests, effective-rule JSON, or audit records.

### Validation and evidence

### Stop and escalation conditions

### Exact output

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

### Complete when

PS #152 is closed with a proved active persistent rule, complete retained evidence, exact restoration proof for temporary state, and rollback proof.

## Task 231 — merge PS P1B

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 225 PR URL | `{{TASK_225_PR_URL}}` | Produced by Task 225; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 230 | `FS` | PS #152 is closed and the approved persistent rule is active. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved task-specific procedure

1. Revalidate the reviewed PS #147 head, checks, workflow identity, required check, active ruleset, application identity, and approvals.
2. Stop if any byte, setting, identity, or approval differs from the proved state.
3. Apply the task-local merge gate below. Require the Task 227 terminal-clean record and Task 228 independent `PASS` record to match the current head and tree.
4. Merge the reviewed P1B PR and close PS #147.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 232 — publish the landed handoff for PS P1B

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #147 URL | `https://github.com/franklesniak/PSStyleGuide/issues/147` | Verified existing GitHub issue |
| Task 225 PR URL | `{{TASK_225_PR_URL}}` | Produced by Task 225; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 231 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved task-specific procedure

1. Post the permanent P1B handoff for Terraform #22. Include all source and landed identities, paths and blobs, permission and credential boundaries, candidate consumption, ancestry/lease/refspec and native-status behavior, failure and cleanup postconditions, ruleset evidence, rollback proof, validation, review-loop result, final quality-check result, and review outcome.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

### Complete when

PS #147 is closed by the proved merge and the permanent landed handoff is sufficient to implement Terraform #22 without redesign.

## Task 233 — update Terraform #22 from the landed PS P1B handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 232 | `FS` | Use the landed PS P1B commit and permanent handoff. |
| Task 209 | `FS` | Terraform #21 is closed. |

### Objective

Complete only this leaf action: update Terraform #22 from the landed PS P1B handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

**Issue update:**

Replace every placeholder with a verified identity and add or amend a `Starting point — reuse landed PSStyleGuide P1B` section in Terraform #22:

> PSStyleGuide P1B landed through `<PS_P1B_PR_URL>` at `<PS_P1B_LANDED_COMMIT>`, tree `<PS_P1B_LANDED_TREE>`. Use that landed implementation and permanent handoff `<PS_P1B_HANDOFF_URL>` as the T1B implementation source. Do not rebuild the writer, evidence protocol, lease/refspec checks, credential boundary, terminal result, or rollback design.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Record every PS source blob and every adapted Terraform destination blob. Change repository names, generated filenames, schema/type prefixes, check names, ruleset names, and local evidence identifiers. Preserve common permissions, job isolation, credential materialization, candidate consumption, ancestry/lease/refspec checks, native statuses, failure postconditions, cleanup, and audit truth.
>
> Use Terraform's own administrator issue and live settings evidence. Never copy PS ruleset IDs, request digests, application/rule responses, `main` values, or audit records. Refresh the reciprocal matrix at implementation start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

### Complete when

Terraform #22 contains verified P1B source pins, scope, expected differences, settings boundary, and explicit start conditions.

## Task 234 — commence Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 233 | `FS` | The issue record is complete, Terraform #21 is closed, and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #22. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Explicitly commence only Terraform #22 from the landed PS P1B commit and final Terraform baseline.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 235 — implement Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 234 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #22 without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the writer and evidence protocol. Preserve the common controls named in Task 233.
2. Use only Terraform's authorized settings and live evidence.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 236 — create or update the candidate PR for Terraform #22

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 236 PR URL | `{{TASK_236_PR_URL}}` | Produced by this task; replace {{TASK_236_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 235 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #22; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation. Treat unexplained weaker behavior as a blocker.
2. Create or update one focused Terraform #22 PR. Run initial validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

### Complete when

The Terraform #22 candidate PR exists with complete source, destination, validation, settings, and audit evidence.

## Task 237 — run the Copilot-and-Codex review loop on the Terraform #22 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 236 PR URL | `{{TASK_236_PR_URL}}` | Produced by Task 236; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 236 | `FS` | The Terraform #22 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below in TerraformStyleGuide. Do not merge or change settings without exact authorization. If bytes change, rerun Task 236 validation and refresh the reciprocal matrix.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 238 — run the independent final quality check on the Terraform #22 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 236 PR URL | `{{TASK_236_PR_URL}}` | Produced by Task 236; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 237 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #22 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #22 issue, writer, settings, permissions, credentials, evidence, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 237 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 237.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 237.

## Task 239 — merge the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 236 PR URL | `{{TASK_236_PR_URL}}` | Produced by Task 236; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 238 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 240 — publish the landed handoff for the Terraform #22 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #22 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/22` | Verified existing GitHub issue |
| Task 236 PR URL | `{{TASK_236_PR_URL}}` | Produced by Task 236; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 239 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

### Complete when

Terraform #22 is closed by the landed PR and its complete handoff is ready for reverse comparison.

## Task 241 — reverse-compare T1B in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 240 | `FS` | Use only the landed P1B and T1B commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 241 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 241 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 242 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 243 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 245 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 241 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 244 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 241 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 241 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 242 — create or update the focused repair issue for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 242 issue URL | `{{TASK_242_ISSUE_URL}}` | Produced by this task; replace {{TASK_242_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 241 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 241's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 243 — commence the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 242 issue URL | `{{TASK_242_ISSUE_URL}}` | Produced by Task 242; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 242 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 242 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 244 — implement the focused repair for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 242 issue URL | `{{TASK_242_ISSUE_URL}}` | Produced by Task 242; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 243 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 243 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 245 — create or update the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 242 issue URL | `{{TASK_242_ISSUE_URL}}` | Produced by Task 242; resolve globally before execution |
| Task 245 PR URL | `{{TASK_245_PR_URL}}` | Produced by this task; replace {{TASK_245_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 244 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 244 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 246 — run the Copilot-and-Codex review loop on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 245 PR URL | `{{TASK_245_PR_URL}}` | Produced by Task 245; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 245 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 245 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 245's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 246 for the new head before Task 247. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 247 — run the independent final quality check on the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 245 PR URL | `{{TASK_245_PR_URL}}` | Produced by Task 245; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 246 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 246 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 245's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 246 for the new head, and then repeat Task 247 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 248 — merge the focused repair PR for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 245 PR URL | `{{TASK_245_PR_URL}}` | Produced by Task 245; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 247 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 247 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 249 — publish the landed repair handoff for T1B in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 245 PR URL | `{{TASK_245_PR_URL}}` | Produced by Task 245; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 248 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 248 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 250 — perform the final Terraform T1B recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 249 | `FS` | Use the final landed PS state from Task 249. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 250 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 250 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 251 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 253 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 260 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 260 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 251 — create or update the focused repair issue for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 251 issue URL | `{{TASK_251_ISSUE_URL}}` | Produced by this task; replace {{TASK_251_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 250 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 250's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 252 — commence the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 251 issue URL | `{{TASK_251_ISSUE_URL}}` | Produced by Task 251; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 251 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 251 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 253 — implement the focused repair for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 251 issue URL | `{{TASK_251_ISSUE_URL}}` | Produced by Task 251; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 252 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 252 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 254 — create or update the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 251 issue URL | `{{TASK_251_ISSUE_URL}}` | Produced by Task 251; resolve globally before execution |
| Task 254 PR URL | `{{TASK_254_PR_URL}}` | Produced by this task; replace {{TASK_254_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 253 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 253 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 255 — run the Copilot-and-Codex review loop on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 254 PR URL | `{{TASK_254_PR_URL}}` | Produced by Task 254; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 254 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 254 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 254's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 255 for the new head before Task 256. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 256 — run the independent final quality check on the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 254 PR URL | `{{TASK_254_PR_URL}}` | Produced by Task 254; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 255 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 255 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 254's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 255 for the new head, and then repeat Task 256 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 257 — merge the focused repair PR for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 254 PR URL | `{{TASK_254_PR_URL}}` | Produced by Task 254; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 256 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 256 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 258 — publish the landed repair handoff for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 254 PR URL | `{{TASK_254_PR_URL}}` | Produced by Task 254; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 257 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 257 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 259 — recheck the reciprocal result for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 250 | `FS` | Use the original pinned comparison and branch result. |
| Task 258 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 244–258 as a new numbered-range instance. After its new handoff, repeat Task 259.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 260 — publish the fixed-point closure for the final Terraform T1B recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 259 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 259 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 261 — commence PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 260 | `FS` | The P1B/T1B cycle is closed and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement PS #148. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Explicitly commence only PS #148.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 262 — implement PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 261 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #148 without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query PS #148 and its native dependency. Classify the capability and pin all landed inputs and affected paths.
2. Implement the issue contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 263 — create or update the candidate PR for PS #148

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 263 PR URL | `{{TASK_263_PR_URL}}` | Produced by this task; replace {{TASK_263_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 262 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #148; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Create or update one focused PS #148 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

### Complete when

The PS #148 candidate PR exists with all required identities, initial validation, and issue-contract evidence.

## Task 264 — run the Copilot-and-Codex review loop on the PS #148 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 263 PR URL | `{{TASK_263_PR_URL}}` | Produced by Task 263; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 263 | `FS` | The PS #148 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #148 PR. Do not merge. If bytes change, rerun Task 263 validation.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 265 — run the independent final quality check on the PS #148 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 263 PR URL | `{{TASK_263_PR_URL}}` | Produced by Task 263; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 264 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #148 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #148 issue-contract, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 264 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 264.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 264.

## Task 266 — merge the PS #148 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 263 PR URL | `{{TASK_263_PR_URL}}` | Produced by Task 263; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 265 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 267 — publish the landed handoff for the PS #148 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 263 PR URL | `{{TASK_263_PR_URL}}` | Produced by Task 263; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 266 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

### Complete when

PS #148 is closed by the landed PR and its handoff is ready for Terraform comparison.

## Task 268 — compare PS #148 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 267 | `FS` | Use the final landed PS #148 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 268 | Compare the affected PS #148 role with Terraform. |
| Task 269 | If common behavior is absent or weaker, create one focused Terraform issue from the landed PS handoff, then run the four task-local conditional repair PR lifecycle actions. |
| Task 268 | If Terraform is already equivalent, post a pinned no-change record. |
| Task 268 | If the blank-line example is PowerShell-specific, post a pinned non-applicability record. |
| Task 268 | Do not create an issue only for symmetry. |
| Task 268 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 268 | Map files by role, not only by filename. |
| Task 268 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 271 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 269 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 270 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 272 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 268 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 269 — create or update the focused repair issue for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 268 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 268's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 270 — commence the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 269 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 269 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 271 — implement the focused repair for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 270 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 270 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 272 — create or update the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 272 PR URL | `{{TASK_272_PR_URL}}` | Produced by this task; replace {{TASK_272_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 271 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 271 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 273 — run the Copilot-and-Codex review loop on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 272 PR URL | `{{TASK_272_PR_URL}}` | Produced by Task 272; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 272 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 272 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 272's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 273 for the new head before Task 274. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 274 — run the independent final quality check on the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 272 PR URL | `{{TASK_272_PR_URL}}` | Produced by Task 272; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 273 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 273 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 272's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 273 for the new head, and then repeat Task 274 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 275 — merge the focused repair PR for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 272 PR URL | `{{TASK_272_PR_URL}}` | Produced by Task 272; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 274 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 274 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 276 — publish the landed repair handoff for PS #148 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 272 PR URL | `{{TASK_272_PR_URL}}` | Produced by Task 272; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 275 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 275 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 277 — reverse-compare the PS #148 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 276 | `FS` | Use the final landed or no-change result from Task 276. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 277 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 277 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 278 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 279 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 281 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 286 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 280 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 287 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 287 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 278 — create or update the focused repair issue for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 277 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 277's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 279 — commence the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 278 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 278 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 280 — implement the focused repair for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 279 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 279 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 281 — create or update the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 281 PR URL | `{{TASK_281_PR_URL}}` | Produced by this task; replace {{TASK_281_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 280 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 280 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 282 — run the Copilot-and-Codex review loop on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 281 PR URL | `{{TASK_281_PR_URL}}` | Produced by Task 281; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 281 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 281 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 281's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 282 for the new head before Task 283. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 283 — run the independent final quality check on the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 281 PR URL | `{{TASK_281_PR_URL}}` | Produced by Task 281; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 282 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 282 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 281's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 282 for the new head, and then repeat Task 283 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 284 — merge the focused repair PR for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 281 PR URL | `{{TASK_281_PR_URL}}` | Produced by Task 281; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 283 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 283 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 285 — publish the landed repair handoff for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |
| Task 281 PR URL | `{{TASK_281_PR_URL}}` | Produced by Task 281; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 284 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 284 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 286 — recheck the reciprocal result for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 277 | `FS` | Use the original pinned comparison and branch result. |
| Task 285 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 271–285 as a new numbered-range instance. After its new handoff, repeat Task 286.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 287 — publish the fixed-point closure for the PS #148 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #148 URL | `https://github.com/franklesniak/PSStyleGuide/issues/148` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 286 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 286 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 288 — commence Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 287 | `FS` | The PS #148 cycle is closed and the implementation slot is free. |
| Task 240 | `FS` | Terraform #22, the native predecessor of #23, is closed. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #23. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Explicitly commence only Terraform #23.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 289 — implement Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 288 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #23 without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query Terraform #23 and verify its native prerequisites.
2. Classify it as Terraform-specific unless comparison proves a common PS role.
3. Implement its state-version recovery contract.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 290 — create or update the candidate PR for Terraform #23

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 290 PR URL | `{{TASK_290_PR_URL}}` | Produced by this task; replace {{TASK_290_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 289 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #23; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Create or update one focused Terraform #23 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

### Complete when

The Terraform #23 candidate PR exists with complete initial identity, validation, failure, recovery, and evidence records.

## Task 291 — run the Copilot-and-Codex review loop on the Terraform #23 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 290 PR URL | `{{TASK_290_PR_URL}}` | Produced by Task 290; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 290 | `FS` | The Terraform #23 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 290 validation.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 292 — run the independent final quality check on the Terraform #23 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 290 PR URL | `{{TASK_290_PR_URL}}` | Produced by Task 290; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 291 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #23 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #23 state-version recovery, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 291 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 291.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 291.

## Task 293 — merge the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 290 PR URL | `{{TASK_290_PR_URL}}` | Produced by Task 290; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 292 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 294 — publish the landed handoff for the Terraform #23 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 290 PR URL | `{{TASK_290_PR_URL}}` | Produced by Task 290; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 293 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

### Complete when

Terraform #23 is closed by the landed PR and its handoff is ready for PS applicability review.

## Task 295 — evaluate Terraform #23 applicability in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 294 | `FS` | Use the landed Terraform #23 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 295 | Search PS for a role affected by Terraform #23's state-version recovery behavior. |
| Task 296 | If PS has a common role and is absent or weaker, create one focused PS issue, then use the task-local execution controls, conditional repair PR lifecycle, and reciprocal fixed-point controls below. |
| Task 295 | If PS is already equivalent, post a pinned no-change record. |
| Task 295 | If PS has no corresponding role, post a pinned non-applicability record. |
| Task 295 | Do not invent a PS issue only for symmetry. |
| Task 295 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 295 | Map files by role, not only by filename. |
| Task 295 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 298 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 296 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 297 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 299 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 304 | Compare the landed destination result back against the source repository. |
| Task 296 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 299 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 298 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 305 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 305 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 296 — create or update the focused repair issue for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 295 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 295's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 297 — commence the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 296 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 296 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 298 — implement the focused repair for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 297 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 297 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 299 — create or update the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 299 PR URL | `{{TASK_299_PR_URL}}` | Produced by this task; replace {{TASK_299_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 298 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 298 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 300 — run the Copilot-and-Codex review loop on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 299 PR URL | `{{TASK_299_PR_URL}}` | Produced by Task 299; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 299 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 299 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 299's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 300 for the new head before Task 301. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 301 — run the independent final quality check on the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 299 PR URL | `{{TASK_299_PR_URL}}` | Produced by Task 299; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 300 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 300 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 299's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 300 for the new head, and then repeat Task 301 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 302 — merge the focused repair PR for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 299 PR URL | `{{TASK_299_PR_URL}}` | Produced by Task 299; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 301 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 301 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 303 — publish the landed repair handoff for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |
| Task 299 PR URL | `{{TASK_299_PR_URL}}` | Produced by Task 299; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 302 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 302 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 304 — recheck the reciprocal result for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 295 | `FS` | Use the original pinned comparison and branch result. |
| Task 303 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 289–303 as a new numbered-range instance. After its new handoff, repeat Task 304.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 305 — publish the fixed-point closure for Terraform #23 applicability in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #23 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/23` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 304 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 304 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 306 — commence the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 305 | `FS` | Terraform #23 and its PS applicability cycle are complete. |
| Task 232 | `FS` | The P1B PS handoff exists. |
| Task 267 | `FS` | The P2 PS handoff exists. |
| Completed PS #145 and #146 | `FS` | The P1 and P1A handoffs exist. |

### Objective

Explicitly commence only the implementation governed by prepare the PS P3 candidate for merge. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #149.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 307 — prepare the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 306 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate prepare the PS P3 candidate for merge without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Verify the P1, P1A, P1B, and P2 landed handoffs and all native issue dependencies.
2. Re-query PS #149, Terraform #24, and current dependency, package-manager, audit, process-runner, exception, Husky, workflow, and evidence contracts.
3. Pin every source commit and blob. Record intended paths and expected repository-specific differences.
4. Implement the complete PS #149 contract. Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, failure truth, and evidence retention.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 308 — create or update the candidate PR for the PS P3 candidate for merge

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by this task; replace {{TASK_308_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 307 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for prepare the PS P3 candidate for merge; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Scope

PS #149 owns the PS Husky preparation mechanism and dependency/update-governance convergence. Compare Terraform #24's current contract and Terraform's direct `prepare` command. Select one common mechanism unless a proved repository-specific need requires a difference.

### Procedure

1. Create or update one PS #149 PR. Reach one locally validated candidate head. Record its base, head/tree, checks, paths and blobs, validation, runtime identities, decision-record changes, and rollback inputs.
2. Stop before review-loop execution or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #149 has one locally validated candidate PR and a verified evidence package.

### Complete when

PS #149 has one locally validated candidate PR and a verified evidence package.

## Task 309 — run the Copilot-and-Codex review loop on the PS #149 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 308 | `FS` | The PS #149 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #149 PR. Do not merge. If bytes change, rerun Task 308 validation and refresh its evidence package.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 310 — run the independent final quality check on the PS #149 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 309 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #149 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #149 governance, dependency, audit, Husky, exception, scheduled/manual behavior, issue, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 309 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 309. The PR is eligible for the checkpoint decision but remains unmerged.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 309. The PR is eligible for the checkpoint decision but remains unmerged.

## Task 311 — check the PS #149 advisory-deadline state

> **Execution class: Coding agent executable.** The original checkpoint time has passed. Run a truthful late check with current evidence. It is a status check, not implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | Historical calendar checkpoint | Record the missed original time and run the current late check without treating lateness as success or failure. |

The PS P1 advisory decision expires at `2026-10-29T23:59:59Z`. The checkpoint is a coordination gate, not a native issue dependency. An expired advisory is not success.

### Objective

Complete only this leaf action: check the PS #149 advisory-deadline state. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query PS #149 and the P1, P1A, P1B, and P2 handoffs.
2. Record each issue and PR state, reviewed head, checks, dependency state, and merge readiness.
3. If PS #149 is merge-ready, record that its current merge path is available for Task 312.
4. If PS #149 is not merge-ready, prepare the fallback evidence package for Task 312.
5. Record incomplete earlier tasks explicitly. Do not wait beyond the checkpoint and do not create a false PS #149 dependency.

### Validation and evidence

### Stop and escalation conditions

### Exact output

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

### Complete when

One timestamped, verified checkpoint record states whether the PS #149 merge path is ready and contains the evidence needed for the owner decision.

## Task 312 — select and approve the PS #149 deadline path

> **Execution class: Human execution required.** The repository owner must select the deadline path.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 311 | `FS` | Use the truthful current or late-check result. |

### Objective

Complete only this leaf action: select and approve the PS #149 deadline path. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

**Human actions:**

1. If PS #149 is merge-ready, approve or reject its existing merge path.
2. If PS #149 is not merge-ready, approve or reject a separate superseding-decision task.
3. If the superseding task is approved, set its start date to `2026-08-22`.
4. Record the approved scope, owner, canonical approval, and expiry.
5. Approve every repository-settings change in the applicable administrator issue.
6. Do not extend the expiry without a new recorded decision.
7. Do not reuse approval for different bytes or settings.

### Validation and evidence

### Stop and escalation conditions

### Exact output

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 323.

### Complete when

The canonical record approves the current PS #149 merge path or a separate fallback, or explicitly rejects both. If it rejects both, stop the affected sequence and obtain a new owner decision before Task 323.

## Task 313 — create or update the approved advisory-fallback issue

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 312 | `FS` | The owner approved a separate superseding-decision task and its exact scope. |

### Objective

Create or update only the focused advisory-fallback issue authorized by the owner. Do not commence implementation. If no fallback was approved, record `SKIPPED — EXISTING MERGE PATH` and do not create an issue.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the Task-local owner decision, PS #149, its current PR, the advisory evidence, and all repository dependencies.
2. If the owner approved the existing PS #149 merge path and did not approve a fallback, record `SKIPPED — EXISTING MERGE PATH` and stop.
3. If a fallback was approved, search for the separately approved focused issue. Update it when it already represents the exact scope; otherwise create it.
4. Record the canonical approval, owner, start date, expiry, exact fallback scope, affected paths, replacement contract, validator/case requirements, compensating controls, validation, rollback, and real native dependencies.
5. Read the issue and dependencies back. Do not create a branch, edit bytes, update a PR, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The exact approved fallback issue exists without implementation commencement, or the task is recorded `SKIPPED — EXISTING MERGE PATH`.

## Task 314 — commence an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 313 | `FS` | The issue update is complete and provides the starting point. |

### Objective

Explicitly commence only the implementation governed by implement an approved advisory fallback. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 313 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Re-query the approved fallback issue, exact owner authorization, native dependencies, both current main refs, and active implementation PR inventory.
3. Require the approved start date to have arrived, every prerequisite to be complete, and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, approval, actor, timestamp, baseline commit/tree, intended branch, affected-path boundary, and validation plan. Do not edit or update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 315 — implement an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 314 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement an approved advisory fallback without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 314 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Verify that no feature implementation issue or PR is active. If the PS #149 PR is still active, preserve its exact head and tree. Close it only when the Task 312 approval expressly authorizes closure. Otherwise, do not start fallback implementation.
3. Prepare the complete fallback from the approved scope.
4. Include the current audit, every accepted finding, each reason, and each compensating control.
5. Include the canonical approval and expiry, exact replacement contract bytes, validator and case changes, and every dependent version, digest, and name pin.
6. Include validation and rollback instructions.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 316 — create or update the candidate PR for an approved advisory fallback

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 316 PR URL | `{{TASK_316_PR_URL}}` | Produced by this task; replace {{TASK_316_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 315 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement an approved advisory fallback; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 315 recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Create one focused fallback PR under the separately approved task. Run initial local validation and required checks. Record its head SHA and tree.
3. Prove on the candidate that the validator does not enter `advisory-expired`. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback candidate PR exists with all required decision, identity, initial validation, and rollback evidence. If the existing merge path was approved instead, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 317 — run the Copilot-and-Codex review loop on the fallback PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).** Skip this task when Task 316 is skipped.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 316 PR URL | `{{TASK_316_PR_URL}}` | Produced by Task 316; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 316 | `FS` | The approved fallback candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the fallback PR. Do not merge. If bytes change, rerun Task 316 validation and expiry proof.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 318 — run the independent final quality check on the fallback PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session. Skip this task when Task 316 is skipped.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 316 PR URL | `{{TASK_316_PR_URL}}` | Produced by Task 316; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 317 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the fallback PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require exact approval/scope, advisory-expiry, contract, validator, case, pin, rollback, comment, deferral, PR-body, validation, and mergeability coverage. Return to Task 317 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 317.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 317.

## Task 319 — merge the approved fallback PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 316 PR URL | `{{TASK_316_PR_URL}}` | Produced by Task 316; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 318 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If the approved fallback path was not selected and the predecessor review/quality tasks were skipped, record `SKIPPED — EXISTING MERGE PATH` and stop.
2. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
4. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
5. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 320 — publish the landed handoff for the approved fallback PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 316 PR URL | `{{TASK_316_PR_URL}}` | Produced by Task 316; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 319 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If the predecessor recorded `SKIPPED — EXISTING MERGE PATH`, record the same skip result and stop.
2. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
4. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

### Complete when

The approved fallback is landed with all required evidence before advisory expiry. If the fallback was not selected, the task is complete only with `SKIPPED — EXISTING MERGE PATH`.

## Task 321 — check whether the PS #149 candidate requires a post-fallback refresh

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 310 | `FS` | The PS #149 candidate passed review and the independent quality check. |
| Task 312 | `FS` | The owner approved the current merge path or supplied a later applicable approval. |
| Task 320 | `FS` when applicable | The approved fallback is landed. |

### Objective

Decide whether the reviewed PS #149 candidate remains merge-ready after the approved fallback path. Do not edit, update a PR, request review, approve, merge, or publish a handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved task-specific procedure

1. If Task 320 changed an input or overlapping path, rebase or update the PS #149 candidate. Return to Task 309 for the Copilot-and-Codex review loop and Task 310 for a new independent quality check. Obtain a new Task 312 approval when the prior approval does not cover the resulting bytes.

### Procedure

1. Re-query the PS #149 candidate, the fallback branch result, all dependencies, decisions, settings, approvals, reviews, checks, and current `main`.
2. Compare the reviewed candidate head/tree and every input or overlapping path with the state after the fallback branch.
3. If no relevant byte, input, setting, approval, or evidence changed, output `MERGE_READY` for the exact current PS #149 head/tree.
4. If refresh is required, output `REFRESH_REQUIRED` with the exact scope. Repeat Tasks 307 and 265 as new update instances, then repeat Tasks 309 and 267. Repeat Task 312 when the prior approval does not cover the resulting bytes or settings. Then repeat this task.
5. Do not perform any refresh action in this task.

### Candidate-refresh gate

Apply this gate only if Procedure step 2 changes the PS #149 PR:

1. Complete the affected issue scope, local validation, PR-body correction, and initial required checks on the new candidate.
2. Record the new base, head SHA and tree, affected paths, issue links, validation, runtime identities, and changed decision or rollback evidence.
3. Stop the merge action. Return to Task 309 for a new Copilot-and-Codex review loop and Task 310 for a new independent final quality check.
4. Require a new Task 312 approval when the current approval does not cover the new bytes or settings. Do not resume this task until all three records apply to the same current head and tree.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

### Complete when

The current PS #149 candidate has an exact `MERGE_READY` record, or this instance emits `REFRESH_REQUIRED` with the complete repeat path.

## Task 322 — merge the PS #149 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 321 | `FS` | Require `MERGE_READY` for the exact current PS #149 head/tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved task-specific procedure

1. Revalidate the approved candidate head, tree, checks, dependencies, decisions, settings, and approval.
2. Stop if any item still differs from the approved evidence.
3. Apply the task-local merge gate below.
4. Merge one PS #149 PR and close the issue.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 323 — publish the landed handoff for the PS #149 P3 implementation

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #149 URL | `https://github.com/franklesniak/PSStyleGuide/issues/149` | Verified existing GitHub issue |
| Task 308 PR URL | `{{TASK_308_PR_URL}}` | Produced by Task 308; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 322 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved task-specific procedure

1. Post the permanent P3 handoff. Include source and landed identities, paths and blobs, common mechanism choice, package and tool identities, validation, runtime evidence, decision records, review-loop result, final quality-check result, intentional differences, and rollback instructions.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

### Complete when

PS #149 is closed by the approved merge and Terraform #24 can adapt the landed result without redesign.

## Task 324 — update Terraform #24 from the landed PS P3 handoff

> **Execution class: Coding agent executable.** This is issue setup and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 323 | `FS` | Use the final landed PS #149 commit and handoff. |
| Task 294 | `FS` | Terraform #23, the native predecessor of #24, is closed. |

### Objective

Complete only this leaf action: update Terraform #24 from the landed PS P3 handoff. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

**Issue update:**

Replace all placeholders with verified identities and update Terraform #24:

> PSStyleGuide dependency and npm governance landed through `<PS_P3_PR_URL>` at `<PS_P3_LANDED_COMMIT>`, tree `<PS_P3_LANDED_TREE>`. Use permanent handoff `<PS_P3_HANDOFF_URL>` as the implementation source.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until this record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read every relevant PS source blob from that commit. Do not independently select a package-manager policy, audit parser, process runner, exception schema, Husky installer, workflow topology, or evidence format. Adapt repository identity, issue URLs, package findings, generated filenames, check names, and Terraform-specific dependency graph.
>
> Preserve common isolation, exact toolchain selection, install/audit vectors, configuration neutralization, bounded process I/O, termination states, JSON validation, exception governance, live-issue verification, scheduled/manual read-only behavior, and failure truth. Refresh the reciprocal matrix at start and before merge. An unexplained weaker behavior blocks merge.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

### Complete when

Terraform #24 contains the complete pinned starting point, scope, common controls, allowed adaptation surface, and start conditions.

## Task 325 — commence Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 324 | `FS` | The issue update is complete and the implementation slot is free. |

### Objective

Explicitly commence only the implementation governed by implement Terraform #24. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Explicitly commence only Terraform #24 from the landed PS P3 source.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 326 — implement Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 325 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement Terraform #24 without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Adapt the capability; do not independently redesign the named contracts.
2. Preserve all common controls from Task 324 and record every repository-specific adaptation.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 327 — create or update the candidate PR for Terraform #24

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 327 PR URL | `{{TASK_327_PR_URL}}` | Produced by this task; replace {{TASK_327_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 326 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement Terraform #24; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Complete the reciprocal matrix at candidate preparation.
2. Create or update one focused Terraform #24 PR. Run initial local validation and required checks. Record its head SHA and tree. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

### Complete when

The Terraform #24 candidate PR exists with complete source, destination, identity, initial validation, runtime, and intentional-difference evidence.

## Task 328 — run the Copilot-and-Codex review loop on the Terraform #24 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 327 PR URL | `{{TASK_327_PR_URL}}` | Produced by Task 327; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 327 | `FS` | The Terraform #24 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below in TerraformStyleGuide. Do not merge. If bytes change, rerun Task 327 validation and refresh the reciprocal matrix.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 329 — run the independent final quality check on the Terraform #24 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 327 PR URL | `{{TASK_327_PR_URL}}` | Produced by Task 327; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 328 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the Terraform #24 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete Terraform #24 package, audit, exception, Husky, workflow, issue, comment, deferral, PR-body, validation, checks, and mergeability coverage. Return to Task 328 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 328.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 328.

## Task 330 — merge the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 327 PR URL | `{{TASK_327_PR_URL}}` | Produced by Task 327; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 329 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 331 — publish the landed handoff for the Terraform #24 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Terraform issue #24 URL | `https://github.com/franklesniak/TerraformStyleGuide/issues/24` | Verified existing GitHub issue |
| Task 327 PR URL | `{{TASK_327_PR_URL}}` | Produced by Task 327; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 330 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

### Complete when

Terraform #24 is closed by the landed PR and the result is ready for reverse PS comparison.

## Task 332 — reverse-compare T3 in PS — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 331 | `FS` | Use only the landed PS #149 and Terraform #24 commits and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 332 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 332 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 333 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 334 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 336 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 332 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 335 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 332 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 332 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 333 — create or update the focused repair issue for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 333 issue URL | `{{TASK_333_ISSUE_URL}}` | Produced by this task; replace {{TASK_333_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 332 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 332's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 334 — commence the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 333 issue URL | `{{TASK_333_ISSUE_URL}}` | Produced by Task 333; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 333 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 333 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 335 — implement the focused repair for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 333 issue URL | `{{TASK_333_ISSUE_URL}}` | Produced by Task 333; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 334 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 334 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 336 — create or update the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 333 issue URL | `{{TASK_333_ISSUE_URL}}` | Produced by Task 333; resolve globally before execution |
| Task 336 PR URL | `{{TASK_336_PR_URL}}` | Produced by this task; replace {{TASK_336_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 335 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 335 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 337 — run the Copilot-and-Codex review loop on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 336 PR URL | `{{TASK_336_PR_URL}}` | Produced by Task 336; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 336 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 336 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 336's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 337 for the new head before Task 338. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 338 — run the independent final quality check on the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 336 PR URL | `{{TASK_336_PR_URL}}` | Produced by Task 336; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 337 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 337 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 336's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 337 for the new head, and then repeat Task 338 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 339 — merge the focused repair PR for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 336 PR URL | `{{TASK_336_PR_URL}}` | Produced by Task 336; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 338 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 338 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 340 — publish the landed repair handoff for T3 in PS

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 336 PR URL | `{{TASK_336_PR_URL}}` | Produced by Task 336; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 339 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 339 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 341 — perform the final Terraform T3 recheck — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 340 | `FS` | Use the final landed PS state from Task 340. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 341 | Read the final source and destination states only from pinned commits. Record every compared blob and map files by role. |
| Task 341 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 342 | If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle. |
| Task 344 | After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations. |
| Task 351 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 351 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 342 — create or update the focused repair issue for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 342 issue URL | `{{TASK_342_ISSUE_URL}}` | Produced by this task; replace {{TASK_342_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 341 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 341's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 343 — commence the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 342 issue URL | `{{TASK_342_ISSUE_URL}}` | Produced by Task 342; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 342 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 342 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 344 — implement the focused repair for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 342 issue URL | `{{TASK_342_ISSUE_URL}}` | Produced by Task 342; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 343 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 343 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 345 — create or update the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 342 issue URL | `{{TASK_342_ISSUE_URL}}` | Produced by Task 342; resolve globally before execution |
| Task 345 PR URL | `{{TASK_345_PR_URL}}` | Produced by this task; replace {{TASK_345_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 344 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 344 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 346 — run the Copilot-and-Codex review loop on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 345 PR URL | `{{TASK_345_PR_URL}}` | Produced by Task 345; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 345 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 345 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 345's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 346 for the new head before Task 347. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 347 — run the independent final quality check on the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 345 PR URL | `{{TASK_345_PR_URL}}` | Produced by Task 345; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 346 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 346 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 345's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 346 for the new head, and then repeat Task 347 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 348 — merge the focused repair PR for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 345 PR URL | `{{TASK_345_PR_URL}}` | Produced by Task 345; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 347 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 347 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 349 — publish the landed repair handoff for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| Task 345 PR URL | `{{TASK_345_PR_URL}}` | Produced by Task 345; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 348 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 348 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 350 — recheck the reciprocal result for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 341 | `FS` | Use the original pinned comparison and branch result. |
| Task 349 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Final reciprocal-recheck controls

1. Read the final source and destination states only from pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If a common destination difference remains, create one focused destination issue. Start it only after issue setup and a free implementation slot. Complete the task-local conditional repair PR lifecycle.
4. After each repair, compare the landed result in the other repository. Stop if the same row changes direction twice and run a new decision process that evaluates both implementations.
5. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
6. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 335–349 as a new numbered-range instance. After its new handoff, repeat Task 350.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 351 — publish the fixed-point closure for the final Terraform T3 recheck

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 350 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 350 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 352 — commence PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 351 | `FS` | PS #149 and its full reciprocal cycle are closed. |

### Objective

Explicitly commence only the implementation governed by implement PS #151. Do not edit implementation bytes in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Verify the implementation slot is free. Explicitly commence only PS #151.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

### Complete when

The governing issue has an explicit, verified implementation-commencement record and no implementation byte or PR changed, or the task has the authorized skip record.

## Task 353 — implement PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 352 | `FS` | The issue is explicitly commenced and owns the implementation slot. |

### Objective

Implement and locally validate implement PS #151 without creating or updating a pull request.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query PS #151 and all landed policy and P3 contracts.
2. Implement the issue's Node permission-model confinement and duplicate-key preflight controls without silently attributing them to PS #162.

Do not push the branch, create or update a PR, request review, or merge in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

### Complete when

A committed, locally validated implementation candidate exists with exact identities and no PR state changed, or the task has the authorized skip record.

## Task 354 — create or update the candidate PR for PS #151

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 354 PR URL | `{{TASK_354_PR_URL}}` | Produced by this task; replace {{TASK_354_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 353 | `FS` | Use the committed local implementation candidate and validation record. |

### Objective

Create or update only the focused candidate PR for implement PS #151; do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Create or update one focused PS #151 PR. Run initial local validation and required checks. Record its head SHA and tree, affected controls, files and blobs, runtimes, and intentional differences. Stop before merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

### Complete when

The PS #151 candidate PR exists with a complete affected-control map and initial validation.

## Task 355 — run the Copilot-and-Codex review loop on the PS #151 PR

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 354 PR URL | `{{TASK_354_PR_URL}}` | Produced by Task 354; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 354 | `FS` | The PS #151 candidate PR exists and initial checks pass. |

### Objective

Complete only this leaf action: run the Copilot-and-Codex review loop on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local Copilot-and-Codex review-loop prompt below against the PS #151 PR. Do not merge. If bytes change, rerun Task 354 validation.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`:

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

### Complete when

The loop returns `TERMINALLY CLEAN` for a recorded head SHA and tree.

## Task 356 — run the independent final quality check on the PS #151 PR

> **Execution class: Coding agent executable.** Use a fresh coding-agent session.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 354 PR URL | `{{TASK_354_PR_URL}}` | Produced by Task 354; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 355 | `FS` | The Copilot-and-Codex review loop is terminally clean. |

### Objective

Complete only this leaf action: run the independent final quality check on the PS #151 PR. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

Run the task-local independent final PR quality-check prompt below. Require complete PS #151 Node permission-model, duplicate-key preflight, issue, comment, deferral, PR-body, final-diff, validation, checks, and mergeability coverage. Return to Task 355 after any head change.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final PR quality-check prompt

Replace only `<PR_URL_OR_NUMBER>` with the verified pull-request URL or number. Copy the resulting prompt into a fresh coding-agent session after the Copilot-and-Codex review loop is terminally clean:

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

### Stop and escalation conditions

### Exact output

The fresh agent returns `PASS` for the same head SHA and tree as Task 355.

### Complete when

The fresh agent returns `PASS` for the same head SHA and tree as Task 355.

## Task 357 — merge the PS #151 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 354 PR URL | `{{TASK_354_PR_URL}}` | Produced by Task 354; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 356 | `FS` | The independent quality check passed for the current head and tree. |

### Objective

Merge only the current reviewed PR after both final gates apply to the same head and tree. Do not publish the permanent handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. Re-query the PR, all linked and closing issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
2. Verify the terminal-clean and independent `PASS` records identify the current head/tree, no newer feedback exists, all required checks pass, the PR body and dependencies are exact, and every plan-defined dependency and readiness gate is satisfied.
3. Apply the task-local merge gate. Merge with an allowed method. Do not publish the permanent landed handoff in this task.
4. Record the API-returned merge method, actual landed commit/tree, parents, immediate issue state, and post-merge checks.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

### Complete when

The PR is merged and its actual landed identity is recorded, or the conditional fallback merge has the authorized skip record; permanent handoff publication remains for the next task when merge occurred.

## Task 358 — publish the landed handoff for the PS #151 PR

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 354 PR URL | `{{TASK_354_PR_URL}}` | Produced by Task 354; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 357 | `FS` | The PR is merged and its actual landed commit/tree are known. |

### Objective

Publish the permanent implementation handoff using only the actual landed commit and verify the resulting issue state.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Fetch the actual landed commit and tree. Record every affected landed blob and verify post-merge checks.
2. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, versions/digests, validation and runtimes, intentional differences, security/failure effects, rollback, and the exact next comparison input.
3. Re-query and verify the handoff, linked issue state, native dependencies, and all posted identities. Do not use a reviewed head or anticipated merge SHA as a landed identity.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

### Complete when

PS #151 is closed by the landed PR and its affected-control map is ready for Terraform comparison.

## Task 359 — compare PS #151 in Terraform — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 358 | `FS` | Use the final landed PS #151 commit and blobs. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 359 | Compare the landed Node permission-model and duplicate-key controls with the Terraform validator and workflow topology. |
| Task 360 | If an applicable control is absent or weaker, create one focused Terraform issue from the landed PS commit and blobs, then run the four task-local conditional repair PR lifecycle actions. |
| Task 359 | If Terraform is equivalent, post a pinned no-change record. |
| Task 359 | If a control is structurally inapplicable, post a pinned non-applicability record. |
| Task 359 | Do not create a placeholder issue only for symmetry. |
| Task 359 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 359 | Map files by role, not only by filename. |
| Task 359 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 362 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 360 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 361 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 363 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 359 | Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close. |

### Task-local comparison and fixed-point controls

#### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 360 — create or update the focused repair issue for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 359 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 359's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 361 — commence the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 360 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 360 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 362 — implement the focused repair for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 361 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Destination-comparison controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Stop this task at its stated destination result. The dependent reverse-comparison task must compare the landed destination result back against the source before the cycle can close.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 361 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 363 — create or update the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 363 PR URL | `{{TASK_363_PR_URL}}` | Produced by this task; replace {{TASK_363_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 362 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 362 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 364 — run the Copilot-and-Codex review loop on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 363 PR URL | `{{TASK_363_PR_URL}}` | Produced by Task 363; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 363 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 363 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 363's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 364 for the new head before Task 365. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 365 — run the independent final quality check on the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 363 PR URL | `{{TASK_363_PR_URL}}` | Produced by Task 363; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 364 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 364 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 363's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 364 for the new head, and then repeat Task 365 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 366 — merge the focused repair PR for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 363 PR URL | `{{TASK_363_PR_URL}}` | Produced by Task 363; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 365 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 365 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 367 — publish the landed repair handoff for PS #151 in Terraform

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 363 PR URL | `{{TASK_363_PR_URL}}` | Produced by Task 363; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 366 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 366 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 368 — reverse-compare the PS #151 cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 367 | `FS` | Use the final Task 367 Terraform result. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 368 | Read both repositories only from the final pinned commits. Record every compared blob and map files by role. |
| Task 368 | Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`. |
| Task 369 | If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs. |
| Task 370 | Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently. |
| Task 372 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. |
| Task 377 | If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair. |
| Task 371 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 378 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 378 | Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task. |

### Task-local comparison and fixed-point controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 369 — create or update the focused repair issue for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 368 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 368's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 370 — commence the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 369 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 369 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 371 — implement the focused repair for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 370 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 370 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 372 — create or update the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 372 PR URL | `{{TASK_372_PR_URL}}` | Produced by this task; replace {{TASK_372_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 371 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 371 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 373 — run the Copilot-and-Codex review loop on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 372 PR URL | `{{TASK_372_PR_URL}}` | Produced by Task 372; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 372 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 372 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 372's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 373 for the new head before Task 374. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 374 — run the independent final quality check on the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 372 PR URL | `{{TASK_372_PR_URL}}` | Produced by Task 372; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 373 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 373 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 372's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 373 for the new head, and then repeat Task 374 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 375 — merge the focused repair PR for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 372 PR URL | `{{TASK_372_PR_URL}}` | Produced by Task 372; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 374 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 374 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 376 — publish the landed repair handoff for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `franklesniak/PSStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |
| Task 372 PR URL | `{{TASK_372_PR_URL}}` | Produced by Task 372; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 375 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 375 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 377 — recheck the reciprocal result for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 368 | `FS` | Use the original pinned comparison and branch result. |
| Task 376 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reverse-comparison and closure controls

1. Read both repositories only from the final pinned commits. Record every compared blob and map files by role.
2. Classify each applicable matrix row as `same`, `intentional difference`, or `blocker`.
3. If the destination adaptation added or corrected common behavior, create or update one focused source sync-back issue. Pin both landed commits and blobs.
4. Start a source sync-back only after issue setup and a free implementation slot. Adapt the common behavior; do not redesign it independently.
5. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle.
6. If the source changes, compare the new landed source result in the destination again. Use the same issue, slot, lifecycle, and evidence controls for each required destination repair.
7. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
8. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
9. Post one permanent closure record with both repositories' final identities, the matrix digest, validation, review outcomes, and no-change or non-applicability results. If a dependent final-recheck task owns closure, post the current reciprocal record and leave final closure to that task.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 362–376 as a new numbered-range instance. After its new handoff, repeat Task 377.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 378 — publish the fixed-point closure for the PS #151 cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |
| PS issue #151 URL | `https://github.com/franklesniak/PSStyleGuide/issues/151` | Verified existing GitHub issue |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 377 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 377 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 379 — check the independent residual triggers

> **Execution class: Coding agent executable.** This is a read-only trigger check and does not start implementation.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| None | — | No technical predecessor. Run here to avoid interrupting the known capability cycles. Re-run when a listed input changes. |

Keep PS #155 and #156 open and independent. Do not add either issue as a blocker for PS #159, #152, #147, Terraform #21, or Terraform #22 without a new material trigger.

### Objective

Complete only this leaf action: check the independent residual triggers. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

**Trigger check:**

1. Re-query the current issues, repositories, supported hosts, native helpers, candidate files, and external runners.
2. Revisit PS #155 only if the candidate validator permits a competing writer, Windows PowerShell 5.1 support ends and a portable API becomes available, or a later task adopts a suitable native helper.
3. Revisit PS #156 only if the candidate files change or a suitable external runner becomes available.
4. Classify each trigger as `present` or `absent` and link its evidence.
5. If all triggers are absent, post a dated no-trigger record and stop.
6. If a trigger is present, record the affected issue and exact material evidence. Do not start implementation in this task.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 390 must evaluate.

### Complete when

Each trigger has a current, evidence-backed classification and each present trigger identifies the focused residual work that Task 390 must evaluate.

## Task 380 — assess one triggered residual cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 379 | `FS` | A material trigger is documented. |
| Task 378 | `FS` | The preceding known fixed-point cycle is closed. |
| Prior completed instance of Tasks 373–390 | `FS` | Close one residual cycle before starting another. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 380 | Validate the trigger and complete the required finding-specific decision process. |
| Task 381 | Create one focused issue with only its real dependencies. Issue creation does not start implementation. |
| Task 382 | Verify the implementation slot is free. Explicitly start only that residual issue. |
| Task 384 | Prepare one focused PR and run the four task-local conditional repair PR lifecycle actions. |
| Task 380 | Record the complete landed identity, validation, review, and rollback evidence. |
| Task 380 | Do not change repository settings without exact administrator authorization. |
| Task 389 | If the other repository has the same role, compare the landed result there. |
| Task 383 | Create a reciprocal issue before implementation only when a common difference exists. |
| Task 382 | Start the reciprocal issue only after issue setup and a free implementation slot. Run the four task-local conditional repair PR lifecycle actions, then run the reverse comparison. |
| Task 390 | Close the cycle only when each common behavior is `same` or a proved intentional difference with equal security and failure strength. |
| Task 380 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 380 | Map files by role, not only by filename. |
| Task 380 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 383 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 381 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 382 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 384 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 389 | Compare the landed destination result back against the source repository. |
| Task 381 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 384 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 383 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 390 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 390 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 381 — create or update the focused repair issue for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 381 issue URL | `{{TASK_381_ISSUE_URL}}` | Produced by this task; replace {{TASK_381_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 380 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 380's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 382 — commence the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 381 issue URL | `{{TASK_381_ISSUE_URL}}` | Produced by Task 381; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 381 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 381 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 383 — implement the focused repair for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 381 issue URL | `{{TASK_381_ISSUE_URL}}` | Produced by Task 381; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 382 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 382 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 384 — create or update the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 381 issue URL | `{{TASK_381_ISSUE_URL}}` | Produced by Task 381; resolve globally before execution |
| Task 384 PR URL | `{{TASK_384_PR_URL}}` | Produced by this task; replace {{TASK_384_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 383 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 383 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 385 — run the Copilot-and-Codex review loop on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 384 PR URL | `{{TASK_384_PR_URL}}` | Produced by Task 384; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 384 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 384 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 384's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 385 for the new head before Task 386. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 386 — run the independent final quality check on the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 384 PR URL | `{{TASK_384_PR_URL}}` | Produced by Task 384; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 385 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 385 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 384's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 385 for the new head, and then repeat Task 386 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 387 — merge the focused repair PR for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 384 PR URL | `{{TASK_384_PR_URL}}` | Produced by Task 384; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 386 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 386 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 388 — publish the landed repair handoff for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_380_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 380; resolve globally before execution |
| Task 384 PR URL | `{{TASK_384_PR_URL}}` | Produced by Task 384; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 387 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 387 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 389 — recheck the reciprocal result for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 380 | `FS` | Use the original pinned comparison and branch result. |
| Task 388 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 374–388 as a new numbered-range instance. After its new handoff, repeat Task 389.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 390 — publish the fixed-point closure for one triggered residual cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 389 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 389 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 391 — assess one future PS-first paired capability cycle — determine the next disposition

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | This disposition task reads both repositories |
| Selected repair target | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Produced by this task when repair is required; replace globally when known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 378 | `FS` | All currently known capability cycles are closed. |
| Every applicable Task 390 instance | `FS` | Triggered residual work is closed. |
| Prior completed instance of Tasks 384–401 | `FS` | The prior future cycle has a permanent fixed-point record. |

### Objective

Perform only the pinned comparison or trigger evaluation and publish its evidence record. Read repository bytes and settings without changing them. Emit one exact branch result: `NO_REPAIR` or `REPAIR_REQUIRED`. The only permitted write is the task-required evidence comment. Do not create or edit an issue body or dependency, commence implementation, edit repository bytes, create or update a PR, request review, merge, or publish a landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Preserved requirement allocation

| Allocated leaf | Preserved source requirement |
| --- | --- |
| Task 392 | Create or update the PS issue. Classify it as `common capability`, `repository-specific capability`, or `administrative only`. Issue creation does not start implementation. |
| Task 391 | Record landed inputs and blobs, intended paths, real native dependencies, expected differences, and any mapped Terraform issue. |
| Task 393 | Verify the implementation slot is free. Explicitly start only the PS implementation. |
| Task 395 | Prepare one PS PR and run the four task-local conditional repair PR lifecycle actions. Post a permanent handoff with exact landed identities and matrix rows. |
| Task 392 | Create or update the Terraform issue only from that landed PS handoff. Do not replace placeholders before the PS PR lands, and never use a branch, reviewed head, test-merge ref, or anticipated squash SHA as the landed commit. |
| Task 393 | After issue setup and a free implementation slot, explicitly start only the Terraform adaptation. Do not create a shared runtime dependency. |
| Task 391 | Run the four task-local conditional repair PR lifecycle actions for the Terraform PR. |
| Task 392 | Run the reverse PS comparison. Create a focused PS sync-back issue before any correction and start it only after issue setup and a free implementation slot. |
| Task 391 | If PS changes, compare Terraform again. Run the four task-local conditional repair PR lifecycle actions for any required Terraform repair. |
| Task 393 | Apply the row-direction stop rule. Repeat until the cycle reaches a fixed point. Do not start the next PS capability before the permanent closure record exists. |
| Task 391 | Read both repositories only from pinned commits. Record every compared blob. |
| Task 391 | Map files by role, not only by filename. |
| Task 391 | Classify each applicable row as `same`, `intentional difference`, or `blocker`. |
| Task 394 | If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry. |
| Task 392 | If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs. |
| Task 393 | Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently. |
| Task 395 | Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence. |
| Task 400 | Compare the landed destination result back against the source repository. |
| Task 392 | If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot. |
| Task 395 | Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again. |
| Task 394 | Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations. |
| Task 401 | Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains. |
| Task 401 | Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results. |

### Task-local comparison and fixed-point controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Copilot-and-Codex review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query every issue, PR, dependency, main ref, commit, tree, blob, setting, check, and prior handoff needed by this comparison. Use authenticated structured data and paginate every relevant connection.
2. Read both repositories only from recorded landed commits. Map files by role and complete the applicable full or reduced matrix.
3. Validate each possible difference. For each material finding, list exhaustive options, create a finding-specific weighted rubric, score every option, and select the highest-quality action. Keep the decision analysis in `TEMP-*` files.
4. If every applicable common row is `same` or a fully proved intentional difference, post the pinned comparison record and output `NO_REPAIR`.
5. If a blocker remains, output `REPAIR_REQUIRED` with the exact target repository, issue scope, dependency set, pinned inputs, affected roles and paths, required validation, and expected terminal state. Do not create the issue in this task.
6. Apply the row-direction stop rule. If the same row changed direction twice, stop for a new decision process instead of emitting another routine repair.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

### Complete when

The pinned comparison record exists and states exactly one of `NO_REPAIR` or `REPAIR_REQUIRED`, with all identities, matrices, evidence, and stop conditions.

## Task 392 — create or update the focused repair issue for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 392 issue URL | `{{TASK_392_ISSUE_URL}}` | Produced by this task; replace {{TASK_392_ISSUE_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 391 | `FS` | Use its exact `NO_REPAIR` or `REPAIR_REQUIRED` branch result. |

### Objective

If repair is required, create or update exactly one focused issue without commencing implementation. If no repair is required, record `SKIPPED — NO_REPAIR`.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Read Task 391's branch result. If it is `NO_REPAIR`, record `SKIPPED — NO_REPAIR` and stop this task.
2. Re-query the target repository and search for an existing issue with the exact repair scope. Update that issue when it is the correct durable tracker; otherwise create one focused issue. Do not create a symmetry-only issue.
3. State the problem, decision basis, trigger/reopen condition, exact scope, source commits and blobs, intended paths, validation, originating comparison, real native dependencies, security and failure requirements, and terminal outcome.
4. Read the issue and all native dependencies back. Correct false, missing, reversed, or tracker-only dependency edges. Do not create a branch, edit bytes, or occupy the implementation slot.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

### Complete when

One focused, self-contained issue with correct native dependencies exists, or the task has an explicit `SKIPPED — NO_REPAIR` record; implementation has not commenced.

## Task 393 — commence the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 392 issue URL | `{{TASK_392_ISSUE_URL}}` | Produced by Task 392; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 392 | `FS` | A focused issue exists, or the repair branch is skipped. |

### Objective

Explicitly commence only the focused repair issue. Do not edit implementation bytes or create/update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 392 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, native dependencies, target main ref, and all active feature implementation issues and PRs.
3. Require every prerequisite to be complete and the one-at-a-time implementation slot to be free.
4. Record `IMPLEMENTATION COMMENCED` with the issue URL, actor, timestamp, baseline commit/tree, pinned source identities, intended branch, affected-path boundary, and validation plan. Do not edit, push, or open/update a PR.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused issue has an explicit commencement record and owns the implementation slot, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 394 — implement the focused repair for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 392 issue URL | `{{TASK_392_ISSUE_URL}}` | Produced by Task 392; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 393 | `FS` | The focused issue is commenced, or the repair branch is skipped. |

### Objective

Implement and locally validate only the focused repair. Do not create or update a PR.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-specific scope and immutable inputs

### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Copilot-and-Codex review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. If Task 393 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Work in an isolated branch or worktree from the exact commenced target baseline. Read cross-repository inputs only from the pinned landed commits and blobs.
3. Implement the selected repair without independently redesigning common behavior. Limit differences to proved repository-specific needs with equal security and failure strength.
4. Run every issue-specific validation and applicable matrix, negative, mutation, platform, generated-drift, path, policy, Markdown, and security check.
5. Commit the candidate. Record the commit, tree, paths, blobs, versions, digests, runtimes, commands, exit codes, results, intentional differences, and rollback. Do not push or change PR state.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A committed, locally validated repair candidate exists with exact evidence and no PR state changed, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 395 — create or update the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 392 issue URL | `{{TASK_392_ISSUE_URL}}` | Produced by Task 392; resolve globally before execution |
| Task 395 PR URL | `{{TASK_395_PR_URL}}` | Produced by this task; replace {{TASK_395_PR_URL}} globally when the exact URL is known |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 394 | `FS` | Use the committed repair candidate, or the repair branch is skipped. |

### Objective

Create or update only one focused repair PR and bring its initial checks to success. Do not run the formal review loop or merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 394 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the issue, target main ref, remote branch, implementation slot, and current required checks. Push the committed candidate safely.
3. Create or update one focused PR. Link the issue and describe the complete diff, source and target identities, validation, security and failure effects, intentional differences, generated artifacts, deferrals, rollback, and closing behavior accurately.
4. Read the PR back. Record base, head/tree, commits, paths, blobs, body edit state, linked and closing issues, reviews/comments baseline, merge state, and checks.
5. Require all initial checks to pass. Replace every placeholder. Stop before the formal review loop or merge.

### Candidate preparation gate

Before this task is complete:

1. Create or update only the focused PR for the stated issue or approved task.
2. Complete the issue scope, applicable local validation, an accurate PR body, and initial required checks.
3. Record the base, candidate head SHA and tree, affected paths, issue links, and every task-specific source, destination, validation, runtime, and intentional-difference identity.
4. Replace every placeholder and verify every posted identity.
5. Stop before the Copilot-and-Codex review loop, independent final quality check, or merge. Do not perform a later lifecycle action in this task.

### Reviewer-facing body freeze gate

1. Build the reviewer-facing title and body from typed or deterministically recomputed data before review. Bind the reviewed input to the candidate head, tree, complete diff, represented scope, behavior, risk, and rollback statement. Verify the rendered meaning and freeze it after authenticated readback.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify a later change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Only the first two classes invalidate review. Raw body-byte inequality is not the classifier.
4. Treat a successful API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile from the readback and do not repeat the public mutation.
5. Normalize empty, singleton, and multiple API collections. Preserve Markdown backticks and Unicode during construction and readback. Reject disallowed control characters.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is review-ready with an accurate body, exact candidate identities, and successful initial checks, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 396 — run the Copilot-and-Codex review loop on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**
>
> **Required executor: Codex subagent (`gpt-5.6-sol`, `xhigh`).**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 395 PR URL | `{{TASK_395_PR_URL}}` | Produced by Task 395; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 395 | `FS` | The focused repair PR is review-ready, or the branch is skipped. |

### Objective

Run only the Copilot-and-Codex review loop. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 395 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 395's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh`.
4. If reviewable repository bytes change, repeat Task 396 for the new head before Task 397. Do not merge.

### Reviewer-input and mutation-materiality controls

1. Before the first terminal reviewer request for each final code head, generate the reviewer-facing PR body from typed or deterministically recomputed data. Verify every represented commit, tree, blob, byte count, hash, check, parity row, issue relationship, scope, risk, and rollback statement. Verify Markdown transport and reject control characters. Freeze the reviewer-facing body.
2. Keep compact task state, polling state, reviewer requests, review IDs, review results, quality results, audit records, and terminal results outside the frozen reviewer-facing body. Publish mutable results in compact state or a separate comment.
3. Classify each change as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. A code or reviewed-diff change requires a new pair. A material scope, behavior, or risk-description change normally requires a new pair and must record the material reason. Raw body-byte inequality is not the classifier.
4. Request at most one Codex and one GitHub Copilot review for each reviewed input by default. Generate the Copilot REST request from the typed policy specification with exact reviewer login `copilot-pull-request-reviewer[bot]`; the display name `Copilot` is invalid for this REST request. Capture the native status and response body, normalize every collection through the tested helper, and require matching authenticated readback. An empty collection is not success. An accepted request without a match stays `RECONCILING` for at least 120 seconds while other safe work continues; complete negative request-event, requested-reviewer, submitted-review, and review-run evidence then permits one `NO_EFFECT` retry for the same input and channel. A second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger before the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Reject any other same-head request unless a recorded material scope, behavior, or risk reason changes the reviewed input. A non-material fact, result, task state, audit record, or comment-only publication does not request review.
5. Treat a successful review-request or comment API response plus authenticated readback as the mutation boundary. If later local serialization fails, reconcile the confirmed public state and do not repeat the request.
6. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode in construction, transport, and readback. Reject disallowed control characters.
7. Preserve both submitted-review and attributable PR-conversation-comment ingestion for Codex. An exact `@codex review` trigger is neither a finding nor a local instruction.

### Copilot-and-Codex review-loop prompt

~~~text
Use a fresh Codex subagent with model `gpt-5.6-sol` and reasoning effort `xhigh` to take pull request <PR_URL_OR_NUMBER> through the repository's complete automated review-loop process.

Work in the pull request's repository. Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Follow the applicable "Handling code review comments," "Deferring work," "Automated review loop," protected-instruction, validation, and identity-gate requirements exactly. The local Codex subagent is the executor; `chatgpt-codex-connector` is a separate remote reviewer.

Do not merge the pull request.

1. Re-query the pull request. Record the base SHA, current head SHA and tree, draft state, merge state, required checks, linked issues, and existing review/comment baselines.
2. Confirm that the candidate implementation and its initial local validation are complete. If the PR is still a draft, mark it ready for review only when the candidate is ready for the review loop.
4. Record baselines for each bot across review-request events, review runs, review submissions, inline review comments, and PR-level comments. Generate the GitHub Copilot request from the typed policy specification and send the authenticated REST review-request payload with exact reviewer login `copilot-pull-request-reviewer[bot]`. Do not use the display name `Copilot`. Capture the native status and response body. Normalize every returned collection with the tested helper; an empty collection is not success. Confirm the request through matching authenticated request-event, current-requested-reviewer, submitted-review, or Copilot-review-run readback. If an accepted request has no match, record `RECONCILING`, continue other safe work, wait at least 120 seconds, and require complete negative evidence from all four surfaces before recording `NO_EFFECT`. Retry at most once for the same reviewed input and channel; a second proved no-effect attempt is `EXHAUSTED`. Do not send the Codex trigger until the Copilot request is confirmed or is terminally proved non-functional through a persisted `terminalDisposition` whose state is `REPOSITORY_AUTHORIZED_NON_FUNCTIONAL`, whose authority and reason are nonempty, and whose recorded time is not earlier than the Copilot request. Persist Copilot `readyAt` as the authenticated release boundary before a Codex request. For a confirmed request, use the matching authenticated confirmation-readback time. For an unconfirmed terminal request, require `readyAt` to equal `terminalDisposition.recordedAt`. Require the Codex request time to be at or after `readyAt`. Request the remote Codex reviewer every round by posting one ordinary PR conversation comment whose body is exactly `@codex review`. Treat that exact trigger comment as neither a finding nor an instruction to the local Codex subagent. Do not rely on an automatic Codex trigger.
5. Poll authenticated structured data at least every 60 seconds. Paginate every review submission with its body, every inline review comment and thread, and every PR-level comment. Count a submitted-review result for the round only when its commit matches the recorded PR head SHA and it is newer than the applicable baseline. Count a headless Codex PR-conversation result only when the authenticated author, request time, baseline exclusion, reviewed-input key, and serialized predecessor-pair order attribute it to this round. A stale or unattributable result never counts.
6. Inventory all feedback surfaces. Parse every new review body, including every suppressed or advisory section. Verify each declared item count. Reconcile synthetic review-body keys with native inline comments so no finding is duplicated or dropped.
7. Process every actionable inline or review-body finding from both reviewers, humans, and other reviewers one at a time. For each real finding, complete all nine `AGENTS.md` comment-processing steps: validate; list exhaustive options; build a fresh weighted rubric; score the options in a table; select and state the best option in ASD-STE100-compliant language; post the complete evaluation; implement the selected solution; evaluate instruction/style-guide impact; and answer and close the native thread or synthetic key.
8. Ignore comments that begin with @copilot when they are commands addressed to GitHub Copilot, as `AGENTS.md` requires.
9. After each fix, verify that the commit is reachable from the PR head. Run applicable local validation. Search for sibling defects by property and mutation-test new assertions before requesting the next review round.
11. Run up to 80 rounds. If one reviewer cannot review the diff, document the exact failure in a PR comment and continue only as `AGENTS.md` permits.
12. Before declaring the loop clean, run the whole-PR deferred-work sweep across all resolved and unresolved review threads, reviews, inline comments, PR-level comments, and the PR body. Complete illegitimate worker-fact deferrals now. For each legitimate deferral, verify that a self-contained GitHub issue exists, is cited by the PR, and has correct native dependencies. Correct residual/deviation labels that are incorrectly called deferrals.
13. Declare terminal clean only when Copilot and Codex each have a current-head clean result, except a reviewer proved non-functional under `AGENTS.md`; every declared review-body count is reconciled; every native thread and synthetic key is closed with evidence; and no untracked or illegitimate deferral remains. A sentence that says no new comments is not clean when the same review body contains a suppressed or advisory finding.
14. Post and return a terminal review-loop record. Include the PR URL, final head SHA and tree, round count, reviewer review IDs and commit IDs, all processed comment/thread IDs, local validation, deferred-work disposition, non-functional-reviewer evidence if applicable, and the explicit result TERMINALLY CLEAN or NOT CLEAN.

If you reach a blocker, the 80-round cap, or a maintainer decision, stop without merging. State the exact blocker, current head SHA and tree, completed work, open thread IDs, and the next required action.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The repair PR has a `TERMINALLY CLEAN` record for its current head/tree, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 397 — run the independent final quality check on the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 395 PR URL | `{{TASK_395_PR_URL}}` | Produced by Task 395; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 396 | `FS` | A terminal-clean record applies to the current repair head/tree, or the branch is skipped. |

### Objective

Run only the independent final quality check in a fresh coding-agent session. Do not merge.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 396 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Obtain `<PR_URL_OR_NUMBER>` from Task 395's verified output. Replace only that placeholder in the prompt below.
3. Run the prompt in a fresh coding-agent session with no reliance on the Copilot-and-Codex review-loop subagent.
4. If any reviewable repository byte changes, return `FAIL`, repeat Task 396 for the new head, and then repeat Task 397 in another fresh session. If only metadata changes, re-query and repeat affected sections. Do not merge.

### Post-review materiality controls

1. Verify that the reviewed code head, tree, diff, frozen reviewer-facing body semantics, represented scope, and risk remain unchanged before adjudicating findings.
2. Classify each discrepancy as `CODE_OR_DIFF`, `MATERIAL_SCOPE_BEHAVIOR_RISK`, `NON_MATERIAL_FACT`, `RESULT_OR_STATE`, or `COMMENT_ONLY`. Route the first two classes back to implementation or PR preparation, refreeze the corrected reviewer input, and require a new review and quality pair.
3. Correct a verified non-material fact only through compact state or a tightly bounded factual field. Read it back, preserve the frozen reviewed input, do not request reviewers again, and rerun only this quality task. Raw body-byte inequality is not the classifier.
4. Reject a same-head review request without a recorded material scope, behavior, or risk reason. Treat a successful API response plus authenticated readback as the mutation boundary; a later local serialization failure cannot repeat the public mutation.
5. Normalize empty, singleton, and multiple reviewer, review, comment, and thread collections. Preserve Markdown backticks and Unicode and reject disallowed control characters.
6. Preserve and independently reconcile both submitted-review objects and attributable Codex PR-conversation comments.

### Independent final quality-check prompt

~~~text
Perform an independent final quality check of pull request <PR_URL_OR_NUMBER>. Use a fresh coding-agent session. Do not rely on the prior review-loop agent's summary, memory, or context. Re-query and verify all evidence yourself.

Apply the repository-local root `AGENTS.md` that Codex loaded at launch, and use bounded discovery for any more-specific `AGENTS.md`. If the repository has no `AGENTS.md`, read its root `CLAUDE.md` as compatibility workflow instructions; the filename does not change the executor. Apply the applicable code-review-comment process and deferral rules exactly. Do not merge the pull request.

1. Record the repository, PR URL, base SHA, current head SHA and tree, draft state, merge state, required checks, review decisions, and associated issues.
2. Enumerate and paginate every review submission with its complete body, review thread, inline review comment, PR-level comment, commit, check, and PR-body revision available through authenticated structured tooling. Parse suppressed or advisory sections, verify declared counts, and reconcile synthetic review-body keys with native inline findings. Include resolved, unresolved, active, and outdated threads. Record counts and stable IDs so omissions are detectable.
3. Verify that the terminal Copilot-and-Codex review-loop record applies to the current head SHA. Verify that both Copilot and Codex produced current-head clean results, except any reviewer proved non-functional under `AGENTS.md`. A stale result or a head change after the loop is a blocker.
4. Review every code-review comment and thread. Confirm that each real finding was answered and resolved through all required `AGENTS.md` steps. If an unaddressed finding exists, process it one at a time through that complete process. Do not accept an "outdated" label as proof that the finding no longer applies.
5. Sweep all review threads, reviews, PR-level comments, commit messages, and the PR body for unfinished-work or deferral language, including defer, follow-up, future, later, TODO, known gap, left open, being added, will be added, out of scope, context, budget, turns, and similar wording.
6. Re-evaluate every possible deferral. A deferral caused by context-window exhaustion, token or turn limits, time pressure, task size, tedium, or another worker fact is illegitimate. Complete that work in this PR. A legitimate deferral must result from the full `AGENTS.md` decision process on the merits.
7. For each legitimate deferral, verify that a self-contained GitHub issue exists before merge. Confirm that it states the problem, decision basis, trigger or reopen condition, affected scope, and originating PR/thread. Confirm that the PR cites it. Re-query GitHub and verify that its native blocking and blocked-by dependencies are complete and correct. Correct false, missing, reversed, or tracker-only dependency representations before passing the PR.
8. Distinguish deferred work from accepted residuals and intentional deviations. Require accurate labels and bounded evidence. Do not let pending work hide under residual or deviation language.
9. Identify every issue associated with the PR through closing references, development links, explicit PR-body links, and repository evidence. Build a requirement-to-evidence table for every issue requirement and acceptance criterion. Map each requirement to final code, documentation, tests, validation, or an authorized decision.
10. If an issue requirement is incomplete, return FAIL and draft a copy-paste-ready completion prompt for a coding agent. The prompt must name the PR, issue, missing requirement, relevant paths, required validation, review-loop return condition, and prohibition on merge. Do not describe required work as deferred.
12. Inspect the final diff independently for correctness, security, failure truth, unintended scope, debug artifacts, placeholders, secrets, TODO markers, disabled checks, unjustified suppressions, generated-file drift, stale version/digest/name pins, missing negative tests, and documentation inconsistency. Run or verify all applicable repository validation against the recorded head.
13. Verify that all required checks passed on the current head, the PR is mergeable, the base and head identities are current, every fix commit is reachable from the PR head, and no newer comment or review arrived during this check.
14. If you change any reviewable repository byte, commit the fix to the PR head, run applicable validation, return FAIL, and direct the operator to rerun the Copilot-and-Codex review loop followed by this independent check. If you change only issue/PR metadata, re-query it and repeat the affected quality-check sections.
15. Return a final report with PASS or FAIL, the verified head SHA and tree, issue-requirement matrix, comment/thread audit, deferral audit, PR-description disposition, checks and tests, changes made, blocking completion prompts, and the exact next action.

PASS means: no unfinished requirement; no unaddressed reviewer finding; no illegitimate or untracked deferral; accurate issue dependencies; accurate PR title/body; terminal review evidence for the same head; successful required checks; and no unresolved quality blocker. Anything else is FAIL.
~~~

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

A fresh independent `PASS` applies to the same current head/tree as the terminal-clean record, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 398 — merge the focused repair PR for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 395 PR URL | `{{TASK_395_PR_URL}}` | Produced by Task 395; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 397 | `FS` | Both review gates apply to the same current head/tree, or the branch is skipped. |

### Objective

Merge only the focused repair PR after the exact final-head gates pass. Do not publish the landed handoff.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local merge gate

Immediately before the merge:

1. Re-query the PR, its linked issues, reviews, threads, and checks. Fetch both current `origin/main` refs.
2. Verify that the Copilot-and-Codex terminal-clean record and independent `PASS` record identify the current head SHA and tree.
3. Stop if the head changed, new material feedback arrived, a required check is incomplete or failed, the PR is not mergeable, a plan-defined dependency or readiness gate is unmet, or the merge is off-plan. Do not require separate operator approval for an on-plan merge.
4. Use a repository-permitted, non-bypassing merge method that is consistent with the task. Record the merge method, landed commit and tree, closed issues, final path and blob identities, and post-merge checks.
5. Post the permanent handoff only from the landed commit. Do not use a reviewed head or anticipated squash SHA as the landed identity.

### Procedure

1. If Task 397 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Re-query the PR, linked issues, every review submission/body, every review thread/comment, all commits and checks, and both current main refs. Paginate every connection.
3. Verify that terminal-clean and independent `PASS` evidence identify the current head/tree; no new feedback exists; all checks pass; the PR body and dependencies are exact; and the PR is mergeable and the merge remains on-plan.
4. Merge with an allowed method. Record the actual merge method, landed commit/tree, parents, immediate issue state, and post-merge checks. Do not publish the permanent handoff in this task.

### Review-evidence materiality gate

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The focused repair PR is merged and its actual landed identity is recorded, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 399 — publish the landed repair handoff for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | `{{TASK_391_SELECTED_TARGET_REPOSITORY}}` | Selected by Task 391; resolve globally before execution |
| Task 395 PR URL | `{{TASK_395_PR_URL}}` | Produced by Task 395; resolve globally before execution |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 398 | `FS` | The repair PR is merged with an actual landed identity, or the branch is skipped. |

### Objective

Publish only the landed repair handoff from the actual landed commit.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. If Task 398 recorded `SKIPPED — NO_REPAIR`, record the same skip result and stop.
2. Fetch the actual landed commit/tree and every affected blob. Verify landed checks and issue state.
3. Post one permanent handoff with repository, issue and PR URLs, base, reviewed head/tree, review IDs, independent result, merge method, landed commit/tree/blobs, validation, runtimes, intentional differences, security/failure effects, rollback, and the next reciprocal-comparison input.
4. Read the handoff back and verify every identity. Do not start a reciprocal repair in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

### Complete when

The landed repair handoff exists and is verified from the actual landed commit, or the task is recorded `SKIPPED — NO_REPAIR`.

## Task 400 — recheck the reciprocal result for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 391 | `FS` | Use the original pinned comparison and branch result. |
| Task 399 | `FS` | Use the landed repair handoff when repair ran; otherwise use the no-repair record. |

### Objective

Perform one reciprocal recheck and emit `FIXED_POINT_READY` or the exact next `REPAIR_REQUIRED` result. Do not execute a repair or publish closure in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Task-local reciprocal controls

#### Reciprocal fixed-point controls

1. Read both repositories only from pinned commits. Record every compared blob.
2. Map files by role, not only by filename.
3. Classify each applicable row as `same`, `intentional difference`, or `blocker`.
4. If no common difference exists, post a pinned no-change or non-applicability record. Do not open an implementation issue only for symmetry.
5. If a common difference exists, create or update one focused destination issue. Pin the source commit and blobs.
6. Start the destination adaptation only after issue setup and a free implementation slot. Adapt the landed capability; do not redesign it independently.
7. Prepare one destination candidate and complete the task-local conditional repair PR lifecycle. Record the complete landed identity and validation evidence.
8. Compare the landed destination result back against the source repository.
9. If the destination added or corrected common behavior, create a focused source sync-back issue. Start it only after issue setup and a free implementation slot.
10. Prepare the source sync-back candidate and complete the task-local conditional repair PR lifecycle. Then compare the new source result in the destination again.
11. Stop if the same matrix row changes direction twice. Run a new decision process that evaluates both implementations.
12. Close the cycle only when every common row is `same`, every intentional difference has complete evidence, and no blocker remains.
13. Post one permanent closure record with final identities from both repositories, the matrix digest, validation, review outcomes, and no-change or non-applicability results.

#### Intentional-difference evidence

Require byte identity for role-equivalent CI workflows and common materials after substituting only proved repository-specific values. Limit intentional differences to repository identity and canonical URLs; PowerShell or Terraform source, generated, and artifact filenames or payloads; domain examples and documentation; artifact IDs; schema, type, and diagnostic prefixes; stable check and ruleset names; local evidence identifiers; proved platform conditions; and repository-specific live settings or decisions that cannot be represented by the common bytes.

For each proposed intentional difference, name both literals or behaviors and the exact non-common bytes. Explain why the repository requires the difference. Prove equal security and failure strength. Name the owner. State the review or expiry condition. Do not accept convenience, implementation history, separate authorship, or lower effort as a repository-specific reason. If the evidence does not make the exception certain, stop and escalate it to the human operator.

#### Terraform starting-point template

Replace all placeholders with verified landed identities:

> **Starting point — adapt the landed PSStyleGuide implementation**
>
> PSStyleGuide issue `<PS_ISSUE_URL>` landed through PR `<PS_PR_URL>` at commit `<PS_LANDED_COMMIT>`, tree `<PS_LANDED_TREE>`. The permanent implementation handoff is `<PS_HANDOFF_URL>`.
>
> This update records the starting point. It does not commence implementation. Do not create a feature branch or edit implementation files until the issue record is complete, the preceding cycle has a permanent closure record, and no feature implementation issue or PR is active.
>
> Read source files only from `<PS_LANDED_COMMIT>` and record their Git blobs before copying. Do not follow a branch or independently rebuild the capability.
>
> Adapt only repository identity, domain-specific filenames and payloads, schema/type prefixes, check or ruleset names, proved platform applicability, and repository-specific evidence. Preserve all common security, serialization, path, credential, native-status, atomicity, cleanup, error-handling, and failure-truth behavior.
>
> Create no shared runtime dependency. Complete the applicable closed reciprocal catalog at implementation start and before merge. Missing, duplicate, unknown, renamed, empty, unexplained, or weaker behavior blocks merge.
>
> Do not change repository settings without exact administrator authorization. Run the candidate, Copilot-and-Codex review-loop, independent final quality-check, and merge actions as separate steps. Record source and destination blobs, tests, runtime identities, reviewed head/tree, merge method, landed commit/tree, and the next handoff.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Procedure

1. Re-query both repository main refs, relevant issues/PRs, and the Task-range handoff or no-repair record. Pin final commits, trees, and blobs.
2. Repeat the applicable full or reduced matrix in the required direction. Apply every intentional-difference and row-direction control.
3. If a blocker remains and no row-direction stop applies, output `REPAIR_REQUIRED` with the next target and repeat Tasks 385–399 as a new numbered-range instance. After its new handoff, repeat Task 400.
4. If the same row changed direction twice, stop for a new finding-specific decision.
5. When every common row is `same`, every intentional difference is proved, and no blocker remains, output `FIXED_POINT_READY` with both repositories' final pinned identities, matrix digest, validation, reviews, and no-change or non-applicability evidence. Do not publish or close the tracker in this task.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

### Complete when

This recheck instance emits exactly one complete branch record: `REPAIR_REQUIRED` or `FIXED_POINT_READY`.

## Task 401 — publish the fixed-point closure for one future PS-first paired capability cycle

> **Execution class: Coding agent executable.**

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repository | Both: `franklesniak/PSStyleGuide` and `franklesniak/TerraformStyleGuide` | Fixed by the task scope; disposition-dependent targets use a globally unique token |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 400 | `FS` | Require an exact `FIXED_POINT_READY` result for the current final identities. |

### Objective

Publish only the permanent fixed-point or issue-closure record. Do not compare, implement, review, or merge in this task.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

1. Re-query the final identities and the Task 400 result. Stop if the result is not `FIXED_POINT_READY` or if any relevant state changed.
2. Verify that every common row is `same`, every intentional difference has complete owner and review/expiry evidence with equal security and failure strength, and no blocker, open repair, false dependency, or unfinished deferral remains.
3. Publish one permanent closure record with both repositories' final commits, trees and blobs; matrix digest; issues and PRs; validation and runtimes; review and quality results; merge methods; no-change/non-applicability evidence; intentional differences; rollback; and closure state.
4. Close the task-local tracker or implementation issue only when its exact closure condition is satisfied. Read the record and issue state back and verify them.

### Validation and evidence

Apply the shared validation policy and the task-specific validation requirements.

### Stop and escalation conditions

Apply the shared stop policy and the task-specific stop conditions.

### Exact output

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

### Complete when

The permanent fixed-point or issue-closure record exists and every task-local tracker state is correct for the final pinned identities.

## Task 402 — verify final completion

> **Execution class: Coding agent executable.** This is a read-only final audit and evidence-publication task.

### Task variables

Only values enclosed in double braces are variables. Each token has exactly one meaning throughout this document. Before execution, replace every unresolved input token globally, including its braces. Literal values need no replacement. A token produced by this task remains unresolved until the task returns its exact output.

| Item | Value | Source or resolution rule |
| --- | --- | --- |
| Target repositories | `franklesniak/research-misc`, `franklesniak/PSStyleGuide`, and `franklesniak/TerraformStyleGuide` | Research governance uses the recorded stacked base; PS and Terraform use their canonical refs |

### Record inputs

Use predecessor results named in `Task variables` and `Dependencies` from compact state. Do not paste completion records into this plan.

### Dependencies

| Predecessor | Relationship | Requirement |
| --- | --- | --- |
| Task 9 | `FS` | The metadata-policy fixed point is landed and its provenance/failure-truth handoff is exact. |
| Task 18 | `FS` | The corrected review-loop architecture is landed and its nine-scenario handoff is exact. |
| Task 40 | `FS` | The expanded 28-path PR #174 convergence umbrella is closed at a proved three-repository fixed point. |
| Task 40 | `FS` | The reviewed PR #78 three-repository port reached its fixed point and the umbrella closed with exact final identities. |
| Completed history and Tasks 41–378 | `FS` | Every known task and reciprocal cycle is complete. |
| Every applicable Task 390 and Task 401 instance | `FS` | All triggered residual and in-scope future cycles are complete. |
| Task 379 | `FS` | The latest residual-trigger record is current. |

### Objective

Complete only this leaf action: verify final completion. Produce the exact state and evidence in this task's `Complete when` condition; do not perform a successor lifecycle action.

### Corrective fixed-point inputs

The final audit must consume Task 9's clause-proven metadata-policy landing, Task 18's corrected review-loop landing, and Task 40's closed 28-path three-repository fixed point. It must confirm that no template-derived documentation rule was weakened, complete-history metadata enforcement is absent, same-head rereview requires a material reason, dual-channel Codex monitoring remains, and all 28 catalog paths are byte-identical or carry a current pressure-tested smallest necessary difference.

### Task-local common-foundation contract

The final PS canonical implementation and synchronized Terraform implementation must be byte-identical for role-equivalent CI workflows and common materials after only proved repository-specific substitutions, and they must share these behaviors:

- fixed repository-root and destination authority;
- complete-payload, BOM-less UTF-8 and LF serialization;
- fresh same-directory candidate creation, durable flush, byte verification, and single-call publication without direct destination truncation;
- `File.Replace` for an existing ordinary tracked destination;
- non-overwriting same-directory `File.Move` for an absent index-tracked destination;
- a closed result that distinguishes pre-publication failure from `ReplacementStateUncertain`;
- no rollback claim after `File.Replace` or `File.Move` returns;
- reusable raw NUL-delimited Git path and status validation with exact native-status handling;
- no JavaScript action in a job that runs repository-controlled code;
- `permissions: {}` on repository-code jobs;
- a separate action-only job for artifact publication;
- a verified official Node distribution in action-free Node jobs;
- strict offline workflow-policy parsing and fixtures;
- machine-readable authority for roles, inputs, defaults, supply identities, and case allocation;
- read-only supply-freeze recording with a reproducible method;
- the same script-version grammar and PowerShell authoring rules; and
- no workflow writer before P1B/T1B explicitly introduces and proves it.

Use this closed reciprocal catalog for every foundation comparison:

| Stable row | Required comparison |
| --- | --- |
| `GF-PARAMETERS` | Public names, types, defaults, and omission, null, empty, and raw-value rules |
| `GF-DESTINATION` | Trusted root, fixed destinations, provider, wildcard, rooted-path, normalization, comparison, and failure rules |
| `GF-CONTENT` | Source order, wrappers, frontmatter, repository-specific names, and complete payload |
| `GF-SERIALIZATION` | CRLF and lone-CR normalization, LF and final-newline rules, BOM-less UTF-8, and byte checks |
| `GF-WRITE` | Complete-payload candidate creation, durable flush, verification, single-call publication, and prohibited fallbacks |
| `GF-FAILURE` | Phase postconditions, cleanup and uncertainty, bounded diagnostics, and fault cases |
| `GF-HOSTS` | Supported editions and hosts, executable identity, cross-cell equality, and idempotence |
| `GF-VERSION` | Timeless marker grammar, trusted expected version, authoring progression, and independent fixtures |
| `GF-NODE-LOCK` | Exact Node/npm producer and provenance, distribution verification, cache and side effects, dependency graph, and frozen consumers |
| `GF-YAML` | Parser package and API, document and schema strictness, diagnostics, and forbidden features |
| `GF-ACTION-PINS` | Closed action roles, full commit pins, provenance, runtime, and atomic pin updates |
| `GF-ACTION-INPUTS` | Authored security inputs and separately reviewed manifest defaults |
| `GF-GIT` | NUL records, byte allowlists, native statuses, cardinality, refs, ancestry, lease, and refspec |
| `GF-GRAPH` | Production and evidence triggers, permissions, needs, conditions, outputs, side effects, and sole writer |
| `GF-CREDENTIALS` | Job-token availability, authentication projection, cleanup, push-only materialization, and absence proofs |
| `GF-EVIDENCE` | Temporary workflow/ref structural equality, drills, retained identities, cleanup, and final absence |

Each applicable row occurs exactly once. Record both repository URLs and commits, normative and implementation locators, evidence paths and SHA-256 values, observed values or fixture IDs, the status, and a reason. Prove omitted rows inapplicable for a narrow non-foundation capability. Duplicate, missing, unknown, renamed, empty, or unexplained rows block closure.

### Execution controls

Apply the shared compact execution policy. Use the highest applicable risk tier. The task-specific controls below define this task's scope.

### Procedure

**Completion audit:**

- The merged research-misc PR #78 governance files were ported through the separate PS and Terraform lifecycles, every review-created common-byte change completed the serial selected-target repair loop, Task 39 published the final 28-path/84-cell matrix with only pressure-tested legitimate differences, and Task 40 closed only the convergence umbrella.

Verify all of the following:

- Every implementation had a focused issue record and a separate explicit commencement record. No two feature implementation issues or PRs were active at the same time.
- Every cross-repository input was pinned to a landed commit and blob. No work copied from a moving branch or introduced a shared runtime dependency, reusable cross-repository workflow, submodule, package, or third source of truth.
- Every PR completed a discrete Copilot-and-Codex review loop and then a discrete independent fresh-agent quality check against the same final head SHA and tree before merge. Every permanent cycle record contains both gate results and both repositories' issue and PR URLs, bases, reviewed heads/trees, merge methods, landed commits/trees, paths, blobs, validation, runtime identities, and review outcomes.
- Every merged PR passed the whole-PR comment and deferral audit. No unfinished reviewer finding or illegitimate deferral remains. Every legitimate deferral has a cited, self-contained GitHub issue with correct native dependencies.
- Every merged PR title and body accurately describe its final landed diff, issue scope, validation, security effects, intentional differences, deferred-issue links, and rollback information.
- Every required reciprocal matrix contains each applicable row exactly once. Every common row is `same`. Every intentional difference names both behaviors, repository need, equal security and failure strength, owner, and review or expiry condition. No blocker remains.
- Every posted issue body and comment contains verified identities. No placeholder or fabricated SHA, URL, PR number, tree, or blob remains.
- Every native dependency represents a real prerequisite. No tracker falsely blocks implementation, and no setting changed without exact administrator authorization.
- Initial cycles 1 through 5 closed in order with their Terraform comparisons, required repairs, reverse PS comparisons, and permanent fixed-point records.
- PS #159 and its four children are closed with a permanent landed handoff that names both repositories' final commits and blobs.
- PS #163 closed without duplicating volatile protocol text.
- Terraform #31 contains all five initial cycle records and closed without an aggregate foundation implementation PR.
- PR #164 was reviewed and landed through PS #160, and its reciprocal cycle reached a fixed point.
- The generators share the canonical tracked-present/tracked-absent publication and structured-result behavior.
- Both generators use fixed root/destination authority, complete BOM-less UTF-8/LF payloads, same-directory durable candidate verification, one `File.Replace` or non-overwriting `File.Move` publication call, truthful `ReplacementStateUncertain` reporting, no post-publication rollback claim, and reusable raw NUL-delimited Git validation with exact native-status handling.
- Repository-code jobs in both repositories contain no actions and have no repository-token scopes.
- Both repositories use separate action-only publication jobs, verified official Node distributions in action-free Node jobs, strict offline policy parsing and fixtures, machine-readable authority for roles, inputs, defaults, supply identities and case allocation, and the same script-version grammar and PowerShell authoring rules.
- Both repositories have the same common workflow-policy behavior and case coverage, except for recorded repository-specific literals. No workflow writer existed before P1B/T1B introduced and proved it.
- PS #158 is complete, and both repositories have a read-only, reproducible supply-freeze method.
- Terraform #21 closed from landed PS PR #153 after the reverse PS comparison and all sync-back work.
- The global sweep inventoried all common roles, resolved every untracked blocker through focused cycles, and retained final two-repository evidence.
- PS #147 and #152 are complete, Terraform #22 consumed the landed P1B handoff, and the P1B/T1B cycle has no blocker.
- PS #148 and Terraform #23 each have a landed result and the required cross-repository applicability record.
- PS #149 landed after the deadline-path decision. Terraform #24 consumed its handoff after #23. The P3/T3 cycle reached a fixed point.
- PS #151 has an applicable Terraform follow-up or a pinned structural non-applicability record.
- The advisory decision did not expire without completed PS #149 or a separately approved, fully pinned superseding decision.
- PS #155 and #156 remained independent unless a documented trigger created a focused blocker.
- Every later common capability used a landed PS handoff, a Terraform adaptation, a reverse PS comparison, and fixed-point closure before the next PS implementation.
- No shared cross-repository runtime dependency exists.

### Validation and evidence

### Stop and escalation conditions

### Exact output

Post one permanent final audit that identifies the final research governance-base, PS, and Terraform commits and trees; links the PR #78 three-repository fixed point and every later fixed-point record; lists all validation and runtime evidence; records every intentional difference and non-applicability result; and states that no blocker remains.

### Complete when

Post one permanent final audit that identifies the final research governance-base, PS, and Terraform commits and trees; links the PR #78 three-repository fixed point and every later fixed-point record; lists all validation and runtime evidence; records every intentional difference and non-applicability result; and states that no blocker remains.

## References

- [PS issue #145](https://github.com/franklesniak/PSStyleGuide/issues/145) and [PS PR #150](https://github.com/franklesniak/PSStyleGuide/pull/150)
- [PS issue #146](https://github.com/franklesniak/PSStyleGuide/issues/146) and [PS PR #153](https://github.com/franklesniak/PSStyleGuide/pull/153)
- PS issues [#147](https://github.com/franklesniak/PSStyleGuide/issues/147), [#148](https://github.com/franklesniak/PSStyleGuide/issues/148), [#149](https://github.com/franklesniak/PSStyleGuide/issues/149), [#151](https://github.com/franklesniak/PSStyleGuide/issues/151), [#152](https://github.com/franklesniak/PSStyleGuide/issues/152), [#155](https://github.com/franklesniak/PSStyleGuide/issues/155), [#156](https://github.com/franklesniak/PSStyleGuide/issues/156), and [#158](https://github.com/franklesniak/PSStyleGuide/issues/158)
- [PS foundation umbrella #159](https://github.com/franklesniak/PSStyleGuide/issues/159), child issues [#160](https://github.com/franklesniak/PSStyleGuide/issues/160), [#161](https://github.com/franklesniak/PSStyleGuide/issues/161), and [#162](https://github.com/franklesniak/PSStyleGuide/issues/162), and [merged PR #164](https://github.com/franklesniak/PSStyleGuide/pull/164)
- [PS issue #166](https://github.com/franklesniak/PSStyleGuide/issues/166) and [merged PR #165](https://github.com/franklesniak/PSStyleGuide/pull/165)
- [PS Claude-command issue #163](https://github.com/franklesniak/PSStyleGuide/issues/163)
- [PS #161 merged PR #167](https://github.com/franklesniak/PSStyleGuide/pull/167), initial [generator-check failure](https://github.com/franklesniak/PSStyleGuide/actions/runs/31856644635/job/94942553633), initial [policy-check failure](https://github.com/franklesniak/PSStyleGuide/actions/runs/31856644627/job/94942553647), final-head [generator-check success](https://github.com/franklesniak/PSStyleGuide/actions/runs/31869046641/job/94974664264), and final-head [Markdown-check success](https://github.com/franklesniak/PSStyleGuide/actions/runs/31869046647/job/94974664365)
- [Terraform issue #20](https://github.com/franklesniak/TerraformStyleGuide/issues/20), [PR #26](https://github.com/franklesniak/TerraformStyleGuide/pull/26), [PR #27](https://github.com/franklesniak/TerraformStyleGuide/pull/27), and [PR #30](https://github.com/franklesniak/TerraformStyleGuide/pull/30)
- Terraform issues [#21](https://github.com/franklesniak/TerraformStyleGuide/issues/21), [#22](https://github.com/franklesniak/TerraformStyleGuide/issues/22), [#23](https://github.com/franklesniak/TerraformStyleGuide/issues/23), [#24](https://github.com/franklesniak/TerraformStyleGuide/issues/24), and [#31](https://github.com/franklesniak/TerraformStyleGuide/issues/31)
- [Terraform issue #32](https://github.com/franklesniak/TerraformStyleGuide/issues/32) and [merged PR #33](https://github.com/franklesniak/TerraformStyleGuide/pull/33)
- [GitHub secure-use guidance for Actions](https://docs.github.com/en/actions/reference/security/secure-use)
- [GitHub Actions runner authentication design](https://github.com/actions/runner/blob/main/docs/design/auth.md)
- [GitHub Actions `GITHUB_TOKEN`](https://docs.github.com/en/actions/concepts/security/github_token)
- [GitHub workflow permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions)
- [Node.js permission model](https://nodejs.org/api/permissions.html)
- [.NET `File.Replace`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace)
- [.NET `File.Move`](https://learn.microsoft.com/en-us/dotnet/api/system.io.file.move)
- [Git pathname format and `-z`](https://git-scm.com/docs/git-status#_pathname_format_notes_and_z)
- [GitHub issue dependencies](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-issue-dependencies)
- [GitHub Copilot code review](https://docs.github.com/en/copilot/how-tos/copilot-on-github/use-copilot-agents/copilot-code-review)
- [OpenAI Codex review for GitHub pull requests](https://developers.openai.com/codex/integrations/github)
- [OpenAI Codex subagents](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [OpenAI Codex non-interactive mode](https://learn.chatgpt.com/docs/non-interactive-mode)
- [OpenAI Codex `AGENTS.md` instructions](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [Anthropic Claude Code Windows Bash defect #28333](https://github.com/anthropics/claude-code/issues/28333)
- [Anthropic Claude Code setup and update guidance](https://code.claude.com/docs/en/setup)
- [Anthropic Claude Code installation troubleshooting](https://code.claude.com/docs/en/troubleshoot-install)
- [PS #168 — PR-body identity synchronization](https://github.com/franklesniak/PSStyleGuide/issues/168)
- [PS #169 — BSD identity dispatch and candidate-publication binding](https://github.com/franklesniak/PSStyleGuide/issues/169)
- [PS #170 — culture-invariant rationale anchors](https://github.com/franklesniak/PSStyleGuide/issues/170)
- [PS #171 — bounded field-specific generator diagnostics](https://github.com/franklesniak/PSStyleGuide/issues/171)
- [Merged PS PR #167](https://github.com/franklesniak/PSStyleGuide/pull/167) and [landed PS #161 handoff](https://github.com/franklesniak/PSStyleGuide/issues/161#issuecomment-5334317038)
- [Merged Terraform PR #35](https://github.com/franklesniak/TerraformStyleGuide/pull/35) and [landed Terraform #34 handoff](https://github.com/franklesniak/TerraformStyleGuide/issues/34#issuecomment-5336853959)
- [Merged research-misc PR #78](https://github.com/franklesniak/research-misc/pull/78), reviewed head `69bf0aeda1b9bc41a298ce73332fcfc1f3269de3`, merge `51dbeb560dd518b904cf1b571dc7d01fd8045749`, and tree `ec9c46e3770ab79169d5520f2f743cb1d1853c16`
- [research-misc parent PR #21](https://github.com/franklesniak/research-misc/pull/21) and verified governance base branch `agent/bash-pid1-runtime-contract`
- copilot-repo-template follow-up issues [#858](https://github.com/franklesniak/copilot-repo-template/issues/858), [#859](https://github.com/franklesniak/copilot-repo-template/issues/859), [#860](https://github.com/franklesniak/copilot-repo-template/issues/860), [#861](https://github.com/franklesniak/copilot-repo-template/issues/861), [#862](https://github.com/franklesniak/copilot-repo-template/issues/862), and [#863](https://github.com/franklesniak/copilot-repo-template/issues/863)

- [Merged PS PR #174](https://github.com/franklesniak/PSStyleGuide/pull/174), reviewed head `eaf81552ba69c2f610b4963a55320114a306c7a6`, merge `34fb7eb492c46cdf9949a52c357a5dd04f873dab`, and tree `9e7319498954119af7c6a51b4d276ac421544b70`
- [Failed landed run 33309460684](https://github.com/franklesniak/PSStyleGuide/actions/runs/33309460684) and [job 99251756566](https://github.com/franklesniak/PSStyleGuide/actions/runs/33309460684/job/99251756566)
- [PS PR #174 focused issue #173](https://github.com/franklesniak/PSStyleGuide/issues/173), [convergence umbrella #172](https://github.com/franklesniak/PSStyleGuide/issues/172), and [deferred guide issue #175](https://github.com/franklesniak/PSStyleGuide/issues/175)
- [Current PSStyleGuide](https://github.com/franklesniak/PSStyleGuide) and [current copilot-repo-template](https://github.com/franklesniak/copilot-repo-template) policy sources
- PS planning branch `planning-CRT-PR-852` and local controller documents `docs/planning/coding-agent-loop.md`, `docs/planning/coding-agent-loop-without-model-routing.md`, and `docs/planning/coding-agent-loop-performance-plan-2026-08-25.md`
